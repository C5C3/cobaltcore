{
  "timestamp": "2026-03-10T17:13:02.599183+00:00",
  "feature_id": "CC-0013",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0013",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0013\">\n  <title>F001: Implement Keystone reconciler and sub-reconcilers</title>\n  <description>**Size:** 🏗️ large\n**Category:** backend\n**Priority:** critical\n**Feature ID:** CC-0013\n\n---\n\n## Summary\n\nImplement the `KeystoneReconciler` (CC-0013) — the core controller driving the full lifecycle of a `Keystone` custom resource. The reconciler orchestrates 6 sequential sub-reconcilers (secrets, database, Fernet keys, config, deployment, bootstrap), each gating the next via status conditions. It leverages all shared libraries from `internal/common/` (conditions, config, database, deployment, job, secrets, plugins, policy) and wires into the existing `operators/keystone/main.go` with scheme registration and reconciler instantiation.\n\n---\n\n## Scope\n\n**Included:**\n- `KeystoneReconciler` struct with embedded `client.Client`, `Scheme *runtime.Scheme`, `Recorder record.EventRecorder` (per architecture doc pattern in `04-keystone-reconciler.md`)\n- `SetupWithManager` with `For(Keystone)`, `Owns(Deployment, Service, ConfigMap, Job, CronJob)`, `Watches(Secret)` with `handler.EnqueueRequestForOwner` for ESO-managed Secrets\n- RBAC markers for: `keystone.openstack.c5c3.io` (keystones, keystones/status, keystones/finalizers), `apps` (deployments), `\"\"` (services, configmaps, secrets, events), `batch` (jobs, cronjobs), `k8s.mariadb.com` (databases, users, grants), `external-secrets.io` (externalsecrets, pushsecrets)\n- `Reconcile()` main loop: fetch Keystone CR via `client.IgnoreNotFound`, call sub-reconcilers sequentially, aggregate `Ready` condition via `conditions.AllTrue`\n- **`reconcileSecrets`** (`reconcile_secrets.go`): Check ESO Secrets via `secrets.WaitForExternalSecret` + `secrets.IsSecretReady` for `spec.database.secretRef` and `spec.bootstrap.adminPasswordSecretRef`. Set `SecretsReady` condition. Requeue 15s.\n- **`reconcileDatabase`** (`reconcile_database.go`): Managed mode (`spec.database.clusterRef` set) → `database.EnsureDatabase` + `database.EnsureDatabaseUser` (User+Grant together). Brownfield mode (`spec.database.host` set) → skip CRs. Both modes → `database.RunDBSyncJob` with `keystone-manage db_sync`. Set `DatabaseReady` condition. Requeue 30s (CRs) / 60s (Job).\n- **`reconcileFernetKeys`** (`reconcile_fernet.go`): Generate initial Fernet keys in-process via `crypto/rand` (32-byte URL-safe base64), store in K8s Secret with owner reference. Ensure rotation CronJob via `job.EnsureCronJob` on `spec.fernet.rotationSchedule`. Ensure PushSecret via `secrets.EnsurePushSecret` for OpenBao backup. Set `FernetKeysReady` condition. Requeue 30s.\n- **`reconcileConfig`** (`reconcile_config.go`): Build `keystone.conf` from spec fields (database connection, cache servers, fernet paths) + `config.MergeDefaults` + `config.InjectSecrets` for `{{DB_PASSWORD}}` placeholder + `spec.extraConfig` merge. Render plugin config via `plugins.RenderPluginConfig`. Render `api-paste.ini` via `plugins.RenderPastePipelineINI` with `spec.middleware`. Render `policy.yaml` via `policy.LoadPolicyFromConfigMap` + `policy.MergePolicies` + `policy.RenderPolicyYAML` + `config.InjectOsloPolicyConfig`. Create immutable ConfigMap via `config.CreateImmutableConfigMap` → returns actual name with hash suffix. No dedicated status condition (synchronous operation).\n- **`reconcileDeployment`** (`reconcile_deployment.go`): Create/update Deployment via `deployment.EnsureDeployment` with container `keystone-api`, image from `spec.image`, port 5000, liveness/readiness probes on `GET /v3`, volume mounts (ConfigMap, fernet-keys Secret, credential-keys Secret). Create/update Service via `deployment.EnsureService` on port 5000. Set `status.endpoint`. Set `DeploymentReady` condition. Requeue 10s.\n- **`reconcileBootstrap`** (`reconcile_bootstrap.go`): Run `keystone-manage bootstrap` Job via `job.RunJob` with admin credentials from ESO Secret, admin URL, region from `spec.bootstrap`. Uses `job.PodSpecHash` for stale-Job detection on spec changes. Set `BootstrapReady` condition. Requeue 60s.\n- Aggregate `Ready` condition via `conditions.SetCondition` + `conditions.AllTrue(\"SecretsReady\", \"DatabaseReady\", \"FernetKeysReady\", \"DeploymentReady\", \"BootstrapReady\")`\n- **Scheme registration** in `main.go`: Add `mariadbv1alpha1`, `esov1beta1`, `esov1alpha1` to the scheme (required for the reconciler to create/watch MariaDB and ESO CRs)\n- **`main.go` wiring**: Instantiate `KeystoneReconciler` with `mgr.GetClient()`, `mgr.GetScheme()`, `mgr.GetEventRecorderFor(\"keystone-controller\")` at `+kubebuilder:scaffold:builder` marker\n- File organization: one file per sub-reconciler + `keystone_reconciler.go` for struct/Reconcile/Setup, all under `operators/keystone/internal/controller/`\n\n**Excluded:**\n- Integration/E2E tests (CC-0014, separate feature)\n- TLS certificate management via cert-manager (future feature, YAGNI — `tls` package exists but not requested)\n- Horizontal Pod Autoscaler (not requested, YAGNI)\n- Metrics/monitoring instrumentation beyond controller-runtime defaults (future feature)\n- Federation support in `reconcileConfig` (`FederationSpec` exists in CRD but no reconciler logic requested)\n- Changes to existing shared libraries in `internal/common/` (assumed stable and complete)\n- Changes to CRD types or webhooks (CC-0011, already complete)\n- Deployment strategy configuration (future feature)\n- Old immutable ConfigMap pruning (owner-reference GC handles deletion when Keystone CR is deleted; accumulation during rolling updates is acceptable for initial implementation)\n\n---\n\n## Visualization\n\n### Reconcile Flow — Sub-Reconciler Sequence\n\n```mermaid\nflowchart TD\n    START[\"Reconcile()\"] --> FETCH[\"Fetch Keystone CR\"]\n    FETCH --> SECRETS[\"reconcileSecrets\"]\n    SECRETS -->|SecretsReady=True| DB[\"reconcileDatabase\"]\n    SECRETS -->|SecretsReady=False| REQ1[\"Requeue 15s\"]\n    DB -->|DatabaseReady=True| FERNET[\"reconcileFernetKeys\"]\n    DB -->|DatabaseReady=False| REQ2[\"Requeue 30-60s\"]\n    FERNET -->|FernetKeysReady=True| CONFIG[\"reconcileConfig\"]\n    FERNET -->|FernetKeysReady=False| REQ3[\"Requeue 30s\"]\n    CONFIG --> DEPLOY[\"reconcileDeployment\"]\n    DEPLOY -->|DeploymentReady=True| BOOT[\"reconcileBootstrap\"]\n    DEPLOY -->|DeploymentReady=False| REQ4[\"Requeue 10s\"]\n    BOOT -->|BootstrapReady=True| READY[\"Set Ready=True\"]\n    BOOT -->|BootstrapReady=False| REQ5[\"Requeue 60s\"]\n```\n\n### Owned Resources Architecture\n\n```mermaid\nflowchart LR\n    KS[\"Keystone CR\"] -->|owns| DEP[\"Deployment\"]\n    KS -->|owns| SVC[\"Service\"]\n    KS -->|owns| CM[\"ConfigMap\\n#40;immutable#41;\"]\n    KS -->|owns| DBSYNC[\"Job\\n#40;db-sync#41;\"]\n    KS -->|owns| BOOT[\"Job\\n#40;bootstrap#41;\"]\n    KS -->|owns| CRON[\"CronJob\\n#40;fernet-rotate#41;\"]\n    KS -->|owns| FKEY[\"Secret\\n#40;fernet-keys#41;\"]\n    KS -->|owns| PS[\"PushSecret\"]\n    KS -->|creates| DBCR[\"MariaDB Database CR\"]\n    KS -->|creates| USR[\"MariaDB User CR\"]\n    KS -->|creates| GR[\"MariaDB Grant CR\"]\n    KS -->|watches| SEC[\"Secrets\\n#40;ESO-managed#41;\"]\n```\n\n### Sub-Reconciler to Shared Library Mapping\n\n```mermaid\nflowchart LR\n    subgraph reconciler[\"KeystoneReconciler\"]\n        rSec[\"reconcileSecrets\"]\n        rDB[\"reconcileDatabase\"]\n        rFer[\"reconcileFernetKeys\"]\n        rCfg[\"reconcileConfig\"]\n        rDep[\"reconcileDeployment\"]\n        rBoot[\"reconcileBootstrap\"]\n    end\n\n    subgraph common[\"internal/common\"]\n        secrets[\"secrets\"]\n        database[\"database\"]\n        job[\"job\"]\n        config[\"config\"]\n        deployment[\"deployment\"]\n        conditions[\"conditions\"]\n        plugins[\"plugins\"]\n        policy[\"policy\"]\n    end\n\n    rSec --> secrets\n    rDB --> database\n    rFer --> job\n    rFer --> secrets\n    rCfg --> config\n    rCfg --> plugins\n    rCfg --> policy\n    rDep --> deployment\n    rBoot --> job\n    reconciler --> conditions\n```\n\n### Condition State Progression\n\n```mermaid\nstateDiagram-v2\n    [*] --> SecretsReady: reconcileSecrets\n    SecretsReady --> DatabaseReady: reconcileDatabase\n    DatabaseReady --> FernetKeysReady: reconcileFernetKeys\n    FernetKeysReady --> ConfigGenerated: reconcileConfig\n    ConfigGenerated --> DeploymentReady: reconcileDeployment\n    DeploymentReady --> BootstrapReady: reconcileBootstrap\n    BootstrapReady --> Ready: AllTrue\n    Ready --> [*]\n```\n\n---\n\n## Key Components\n\n- **`KeystoneReconciler` struct** (`operators/keystone/internal/controller/keystone_reconciler.go`): Main reconciler with embedded `client.Client`, `Scheme`, `Recorder` fields. Implements `Reconcile()` orchestrating sub-reconcilers sequentially with early-return on `!result.IsZero() || err != nil`. `SetupWithManager` with For/Owns/Watches declarations. All RBAC markers including `\"\"` group events for EventRecorder.\n- **`reconcileSecrets`** (`reconcile_secrets.go`): Calls `secrets.WaitForExternalSecret` for DB and admin credential ExternalSecrets, then `secrets.IsSecretReady` to verify expected keys. Sets `SecretsReady` condition with `conditions.SetCondition`.\n- **`reconcileDatabase`** (`reconcile_database.go`): Branches on `spec.database.clusterRef` (managed → `database.EnsureDatabase` + `database.EnsureDatabaseUser` which handles User+Grant together) vs `spec.database.host` (brownfield → skip CRs). Runs `database.RunDBSyncJob` in both modes. Handles `job.ErrJobFailed` sentinel for permanent failure. Sets `DatabaseReady`.\n- **`reconcileFernetKeys`** (`reconcile_fernet.go`): Generates initial Fernet keys via `crypto/rand` (32-byte URL-safe base64), creates K8s Secret with owner reference. Ensures rotation CronJob via `job.EnsureCronJob`. Ensures PushSecret via `secrets.EnsurePushSecret`. Sets `FernetKeysReady`.\n- **`reconcileConfig`** (`reconcile_config.go`): Config generation pipeline: base sections from spec → `config.MergeDefaults` → `config.InjectSecrets` → merge `spec.extraConfig` → `plugins.RenderPluginConfig` → `plugins.RenderPastePipelineINI` → `policy.LoadPolicyFromConfigMap` + `policy.MergePolicies` + `policy.RenderPolicyYAML` → `config.InjectOsloPolicyConfig` → `config.RenderINI` → `config.CreateImmutableConfigMap`. Returns `configMapName` to caller for Deployment volume mount. No status condition.\n- **`reconcileDeployment`** (`reconcile_deployment.go`): Builds Deployment spec with `spec.image`, `spec.replicas`, port 5000, probes on `/v3`, volume mounts for immutable ConfigMap + fernet-keys Secret. Calls `deployment.EnsureDeployment` + `deployment.EnsureService`. Sets `status.endpoint`. Sets `DeploymentReady`.\n- **`reconcileBootstrap`** (`reconcile_bootstrap.go`): Builds bootstrap Job spec with `keystone-manage bootstrap` command + admin credentials from `secrets.GetSecretValue`. Uses `job.PodSpecHash` annotation for stale-Job detection. Calls `job.RunJob`. Sets `BootstrapReady`.\n- **`main.go` wiring** (`operators/keystone/main.go`): Register `mariadbv1alpha1`, `esov1beta1`, `esov1alpha1` schemes. Instantiate `KeystoneReconciler` with client/scheme/recorder at scaffold marker, after existing webhook registration.</description>\n  <tasks>\n    <task status=\"pending\">1.1 Create KeystoneReconciler struct, Reconcile() method, and SetupWithManager with RBAC markers (REQ-001, REQ-008, REQ-010, REQ-011, REQ-013)</task>\n    <task status=\"pending\">1.2 Implement reconcileSecrets sub-reconciler (REQ-002)</task>\n    <task status=\"pending\">1.3 Implement reconcileDatabase sub-reconciler for managed and brownfield modes (REQ-003)</task>\n    <task status=\"pending\">1.4 Implement reconcileFernetKeys sub-reconciler with key generation, CronJob, and PushSecret (REQ-004)</task>\n    <task status=\"pending\">1.5 Implement reconcileConfig sub-reconciler with config generation pipeline (REQ-005)</task>\n    <task status=\"pending\">2.1 Implement reconcileDeployment sub-reconciler with Deployment and Service creation (REQ-006, REQ-012)</task>\n    <task status=\"pending\">2.2 Implement reconcileBootstrap sub-reconciler with bootstrap Job and stale detection (REQ-007)</task>\n    <task status=\"pending\">3.1 Wire KeystoneReconciler into main.go with external scheme registration (REQ-009)</task>\n    <task status=\"pending\">3.2 Add unit tests for Reconcile() orchestration: sequential sub-reconciler calls, early return, Ready aggregation (REQ-001, REQ-011)</task>\n    <task status=\"pending\">3.3 Add unit tests for sub-reconciler edge cases: error handling, condition messages, requeue delays (REQ-002, REQ-003, REQ-004, REQ-006, REQ-007)</task>\n    <task status=\"pending\">4.1 Write reference documentation for KeystoneReconciler architecture and sub-reconciler contracts</task>\n\n  </tasks>\n  <implementation-notes>Architectural decisions:\n\n1. **File organization**: One file per sub-reconciler under `operators/keystone/internal/controller/`, matching the architecture doc. `keystone_reconciler.go` contains the struct, Reconcile(), SetupWithManager, and RBAC markers. Each `reconcile_*.go` file contains a single private method on the KeystoneReconciler receiver.\n\n2. **Sub-reconciler signature**: Each sub-reconciler is a private method `func (r *KeystoneReconciler) reconcile*(ctx context.Context, keystone *keystonev1alpha1.Keystone) (ctrl.Result, error)`. reconcileConfig additionally returns the configMapName string: `func (r *KeystoneReconciler) reconcileConfig(...) (string, ctrl.Result, error)` — the configMapName is needed by reconcileDeployment for volume mount reference.\n\n3. **Status patching**: Status is patched via `r.Status().Patch(ctx, keystone, patch)` using a merge patch after each reconciliation cycle (not after each sub-reconciler), following controller-runtime conventions. The `defer` pattern is used to ensure status is patched even on early returns.\n\n4. **Shared library functions**: All Kubernetes interactions delegate to `internal/common/*` packages. The reconciler does NOT duplicate Get+Create/Update logic — it constructs the desired resource specs and passes them to the shared Ensure*/RunJob functions. These functions handle owner references, spec comparison, and readiness evaluation per the manual-getcreateupdate-with-setcontrollerreference pattern.\n\n5. **Database connection string**: Assembled in reconcileConfig as `mysql+pymysql://{{DB_USER}}:{{DB_PASSWORD}}@<host>:<port>/<database>` where host comes from clusterRef (resolved from MariaDB CR status, or constructed as `<clusterRef.name>.<namespace>.svc`) or spec.database.host, and credentials are injected via config.InjectSecrets.\n\n6. **Fernet key format**: Keys are 32 bytes from crypto/rand, encoded as URL-safe base64 (base64.URLEncoding). The Secret data map uses string indices '0', '1' as keys, matching the Keystone fernet-keys directory convention.\n\n7. **Existing patterns followed**: operator-entrypoint-via-shared-bootstrap (main.go wiring via bootstrap.Run), manual-getcreateupdate-with-setcontrollerreference (all Ensure* calls), stale-completed-job-detection-via-pod-template-spec-comparison (bootstrap Job), gomega-test-assertions-with-newgomegawitht (test pattern).\n\n8. **Potential pitfalls**: (a) EnsureCronJob always overwrites spec — acceptable per architecture. (b) Old immutable ConfigMaps accumulate — accepted per scope exclusion. (c) PushSecret spec must match the ESO v1alpha1 API exactly. (d) The ESO scheme registration requires separate v1beta1 and v1alpha1 scheme builders (not a single AddToScheme).\n\n9. **Key files**: `internal/common/conditions/conditions.go` (SetCondition, AllTrue), `internal/common/secrets/secrets.go` (WaitForExternalSecret, IsSecretReady, GetSecretValue, EnsurePushSecret), `internal/common/database/database.go` (EnsureDatabase, EnsureDatabaseUser, RunDBSyncJob), `internal/common/job/job.go` (RunJob, EnsureCronJob, PodSpecHash), `internal/common/config/config.go` (RenderINI, MergeDefaults, InjectSecrets, CreateImmutableConfigMap, InjectOsloPolicyConfig), `internal/common/deployment/deployment.go` (EnsureDeployment, EnsureService), `internal/common/plugins/plugins.go` (RenderPluginConfig, RenderPastePipelineINI), `internal/common/policy/policy.go` (LoadPolicyFromConfigMap, MergePolicies, RenderPolicyYAML).</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_reconciler_test.go\" test-function=\"TestReconcile_CRNotFound\" requirement-id=\"REQ-001\">When the Keystone CR does not exist, Reconcile returns no error and zero result</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_reconciler_test.go\" test-function=\"TestReconcile_SecretsNotReady_ReturnsRequeue\" requirement-id=\"REQ-001\">When reconcileSecrets returns a requeue result, Reconcile returns the same result without calling subsequent sub-reconcilers</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_reconciler_test.go\" test-function=\"TestReconcile_AllSubReconcilersSucceed_ReadyTrue\" requirement-id=\"REQ-001\">When all sub-reconcilers succeed, Ready condition is set to True and status is patched</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_reconciler_test.go\" test-function=\"TestReconcile_SubReconcilerError_PropagatesError\" requirement-id=\"REQ-001\">When a sub-reconciler returns an error, Reconcile returns the error and skips subsequent sub-reconcilers</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestReconcileSecrets_DBSecretNotReady\" requirement-id=\"REQ-002\">When DB ExternalSecret is not ready, SecretsReady=False with Reason=WaitingForDBCredentials and RequeueAfter=15s</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestReconcileSecrets_AdminSecretNotReady\" requirement-id=\"REQ-002\">When DB is ready but admin ExternalSecret is not, SecretsReady=False with Reason=WaitingForAdminCredentials and RequeueAfter=15s</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestReconcileSecrets_BothReady\" requirement-id=\"REQ-002\">When both ExternalSecrets are ready, SecretsReady=True with Reason=SecretsAvailable and zero result</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestReconcileSecrets_APIError\" requirement-id=\"REQ-002\">When WaitForExternalSecret returns an API error, the error is propagated</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_ManagedMode_CRsNotReady\" requirement-id=\"REQ-003\">When clusterRef is set and MariaDB CRs are not ready, DatabaseReady=False and returns requeue 30s</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_BrownfieldMode_SkipsCRs\" requirement-id=\"REQ-003\">When host is set, no MariaDB CRs are created, db_sync Job runs directly</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_DBSyncComplete\" requirement-id=\"REQ-003\">When db_sync Job completes, DatabaseReady=True with Reason=DatabaseSynced</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_DBSyncFailed\" requirement-id=\"REQ-003\">When db_sync Job permanently fails, ErrJobFailed error is returned</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestReconcileFernetKeys_InitialGeneration\" requirement-id=\"REQ-004\">When fernet-keys Secret does not exist, keys are generated with correct format and stored in a new Secret</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestReconcileFernetKeys_ExistingNotOverwritten\" requirement-id=\"REQ-004\">When fernet-keys Secret already exists, it is not modified</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestReconcileFernetKeys_CronJobCreated\" requirement-id=\"REQ-004\">CronJob is created with correct schedule from spec.fernet.rotationSchedule</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestReconcileFernetKeys_PushSecretCreated\" requirement-id=\"REQ-004\">PushSecret is created targeting the OpenBao fernet-keys path</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_BaseConfigGeneration\" requirement-id=\"REQ-005\">keystone.conf contains correct [database], [cache], [fernet_tokens], [token] sections from spec fields</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_ExtraConfigMerge\" requirement-id=\"REQ-005\">spec.extraConfig sections are merged into keystone.conf with user values taking precedence</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_PluginAndMiddleware\" requirement-id=\"REQ-005\">Plugin config sections appear in keystone.conf and api-paste.ini includes middleware filters</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_PolicyOverrides\" requirement-id=\"REQ-005\">When policyOverrides is set, policy.yaml is in ConfigMap data and oslo_policy section is in keystone.conf</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_NoPolicyOverrides\" requirement-id=\"REQ-005\">When policyOverrides is nil, no policy.yaml key and no oslo_policy section</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec\" requirement-id=\"REQ-006\">Deployment has correct container name, image, port, probes, and volume mounts</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_ServiceCreated\" requirement-id=\"REQ-006\">Service is created with port 5000 and correct selector</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_NotReady_Requeue\" requirement-id=\"REQ-006\">When Deployment is not ready, returns RequeueAfter 10s and DeploymentReady=False</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_Ready_EndpointSet\" requirement-id=\"REQ-006\">When Deployment is ready, DeploymentReady=True and status.endpoint is set</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_JobCreated\" requirement-id=\"REQ-007\">Job is created with correct keystone-manage bootstrap command and env var from admin Secret</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_JobComplete\" requirement-id=\"REQ-007\">When Job completes, BootstrapReady=True with Reason=BootstrapComplete</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_JobRunning\" requirement-id=\"REQ-007\">When Job is still running, returns RequeueAfter 60s and BootstrapReady=False</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_JobFailed\" requirement-id=\"REQ-007\">When Job permanently fails, ErrJobFailed error is returned wrapped in context</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_reconciler_test.go\" test-function=\"TestReconcile_ReadyAggregation_AllTrue\" requirement-id=\"REQ-011\">When all 5 sub-conditions are True, Ready=True is set via conditions.AllTrue</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_reconciler_test.go\" test-function=\"TestReconcile_ReadyAggregation_MissingCondition\" requirement-id=\"REQ-011\">When any sub-condition is missing, Ready=False is set</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator deploys Keystone CR through complete reconciliation lifecycle\">As a platform operator, I want to create a Keystone CR and have the operator reconcile it through all sub-reconciler phases to a fully running Keystone deployment, so that Keystone Identity Service is deployed and operational without manual intervention</story>\n    <story title=\"Operator waits for ESO-managed secrets before proceeding\">As a platform operator, I want the reconciler to wait for ExternalSecret-provisioned Kubernetes Secrets to become ready before proceeding to database setup, so that the reconciler does not fail when secrets are not yet synced from OpenBao</story>\n    <story title=\"Operator manages MariaDB database resources in managed and brownfield modes\">As a platform operator, I want the reconciler to create MariaDB Database/User/Grant CRs in managed mode or skip them in brownfield mode, and run db_sync in both modes, so that the database is provisioned correctly regardless of whether MariaDB is operator-managed or externally provisioned</story>\n    <story title=\"Operator manages Fernet key generation, rotation, and backup\">As a platform operator, I want the reconciler to generate initial Fernet keys, set up a rotation CronJob, and back up keys via PushSecret, so that Keystone tokens are properly encrypted and keys are rotated on schedule with disaster recovery backup</story>\n    <story title=\"Operator generates Keystone configuration and creates immutable ConfigMap\">As a platform operator, I want the reconciler to generate keystone.conf, api-paste.ini, and optionally policy.yaml, stored in an immutable ConfigMap with content-hash naming, so that configuration changes trigger rolling restarts via new ConfigMap names</story>\n    <story title=\"Operator creates and manages the Keystone Deployment and Service\">As a platform operator, I want the reconciler to create a Deployment with the Keystone API container and a Service exposing port 5000, so that the Keystone API is accessible within the cluster</story>\n    <story title=\"Operator runs the Keystone bootstrap Job\">As a platform operator, I want the reconciler to run keystone-manage bootstrap with admin credentials and region configuration, so that the initial admin user, project, roles, and service catalog entries are created</story>\n    <story title=\"Operator is properly wired into main.go with scheme registration\">As a developer, I want the KeystoneReconciler to be instantiated and registered with the controller manager in main.go, with all required external schemes registered, so that the operator binary starts the reconciler when deployed</story>\n    <story title=\"Operator handles errors and degraded states gracefully\">As a platform operator, I want the reconciler to handle errors at each sub-reconciler phase with appropriate conditions, events, and requeue delays, so that I can diagnose issues from the Keystone CR status and events</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The KeystoneReconciler SHALL implement the Reconcile() method that fetches the Keystone CR and calls sub-reconcilers sequentially, returning early on requeue or error</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The reconcileSecrets sub-reconciler SHALL verify ESO-managed Secrets are ready before allowing downstream sub-reconcilers to proceed</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The reconcileDatabase sub-reconciler SHALL support managed (clusterRef) and brownfield (host) modes for MariaDB, and run db_sync in both modes</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The reconcileFernetKeys sub-reconciler SHALL generate initial Fernet keys, ensure a rotation CronJob, and ensure a PushSecret</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The reconcileConfig sub-reconciler SHALL generate keystone.conf, api-paste.ini, and optionally policy.yaml in an immutable ConfigMap</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The reconcileDeployment sub-reconciler SHALL create a Deployment and Service with correct container spec, probes, and volume mounts</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The reconcileBootstrap sub-reconciler SHALL run a bootstrap Job with keystone-manage bootstrap command and admin credentials</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">SetupWithManager SHALL register For/Owns/Watches declarations for the Keystone CR and all owned resource types</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">main.go SHALL register mariadbv1alpha1, esov1beta1, esov1alpha1 schemes and instantiate KeystoneReconciler</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">RBAC markers SHALL declare complete permissions for all resources the reconciler interacts with</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">Ready condition SHALL be aggregated using conditions.AllTrue over the 5 sub-reconciler conditions</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">All created resources SHALL have controller owner references to enable garbage collection</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 31365,
  "caller": "run_claude_cli"
}