{
  "timestamp": "2026-03-22T19:10:24.985228+00:00",
  "feature_id": "CC-0016",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0016",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<previous-feature feature-id=\"CC-0015\" status=\"completed\">\n  <title>G001: Implement Keystone dependency details</title>\n  <tasks hint=\"showing first 5\">\n    <task>1.1 Add fernet-keys hash annotation to buildKeystoneDeployment() (REQ-001, REQ-002, REQ-008)</task>\n    <task>1.2 Add table-driven unit tests for resolveDatabaseHost() and dbPort() (REQ-003, REQ-004)</task>\n    <task>1.3 Add table-driven unit tests for resolveCacheServers() (REQ-005)</task>\n    <task>2.1 Add envtest integration test for CronJob detailed spec (REQ-006)</task>\n    <task>2.2 Add envtest integration test for bootstrap Job detailed spec (REQ-007)</task>\n  </tasks>\n</previous-feature>\n\n<feature feature-id=\"CC-0016\">\n  <title>G002: Implement Keystone Chainsaw E2E test suite</title>\n  <description>**Size:** 🏗️ large\n**Category:** infrastructure\n**Priority:** medium\n**Feature ID:** CC-0016 (S016)\n\n---\n\n## Open Questions\n\nNo open questions — both decisions resolved by user:\n\n1. **`missing-secret/` isolation** → Use unique `secretRef` names per test CR\n2. **CR name collisions with `parallel: 4`** → Unique CR name per test directory\n\n---\n\n## Summary\n\nCreate 9 Chainsaw E2E test suites under `tests/e2e/keystone/` validating the Keystone operator's full lifecycle on a real cluster. Each suite targets a specific operational scenario: happy-path deployment, secret dependency recovery, Fernet key rotation, replica scaling, garbage collection on deletion, policy override injection, middleware pipeline customization, brownfield (unmanaged) database mode, and rolling image upgrades. Together they cover the operator's condition progression (`SecretsReady` → `DatabaseReady` → `FernetKeysReady` → `DeploymentReady` → `BootstrapReady` → `Ready`), owned resource management, and resilience to operational changes.\n\n---\n\n## Scope\n\n**Included:**\n- **`basic-deployment/`** — Apply valid Keystone CR (managed mode with `clusterRef`), assert all 5 sub-conditions progress to `True` and aggregate `Ready=True` (reason: `AllReady`), verify Deployment `{name}-api` with `availableReplicas > 0`, Service `{name}-api` on port 5000, immutable ConfigMap `{name}-config-{hash}`, CronJob `{name}-fernet-rotate`, fernet Secret `{name}-fernet-keys`, RBAC resources (ServiceAccount/Role/RoleBinding `{name}-fernet-rotate`), PushSecret `{name}-fernet-keys-backup`. Single `script` step with `curl http://{name}-api.openstack.svc:5000/v3` to verify functional API response.\n- **`missing-secret/`** — Apply Keystone CR referencing non-existent Secret names (unique per test, e.g. `missing-secret-keystone-db`), assert `SecretsReady=False` (reason: `WaitingForDBCredentials` or `WaitingForAdminCredentials`), create prerequisite Secrets, assert recovery to `Ready=True`\n- **`fernet-rotation/`** — Verify CronJob `{name}-fernet-rotate` schedule matches `spec.fernet.rotationSchedule`, trigger manual rotation via `kubectl create job --from=cronjob/{name}-fernet-rotate`, assert `{name}-fernet-keys` Secret `.data` changes, assert Deployment pod-template annotation `keystone.c5c3.io/fernet-keys-hash` updates (rolling restart)\n- **`scale/`** — Apply Keystone CR with `replicas: 3`, patch to `replicas: 5`, assert `Deployment.spec.replicas` and `status.availableReplicas` update, patch to `replicas: 2`, assert scale-down\n- **`deletion-cleanup/`** — Deploy Keystone CR, wait for `Ready=True`, delete CR, assert all owned resources return NotFound via `error` checks: Deployment, Service, ConfigMap, CronJob, Jobs (db-sync, bootstrap), Secret (fernet-keys), ServiceAccount, Role, RoleBinding, PushSecret\n- **`policy-overrides/`** — Apply Keystone CR with `policyOverrides.configMapRef`, assert generated ConfigMap contains `policy.yaml` data key, assert `keystone.conf` contains `[oslo_policy]` section with `policy_file = /etc/keystone/policy.yaml`\n- **`middleware-config/`** — Apply Keystone CR with custom `middleware` entries (name, filterFactory, position), assert `api-paste.ini` key in generated ConfigMap contains the modified pipeline filter references\n- **`brownfield-database/`** — Apply Keystone CR with explicit `database.host: openstack-db.openstack.svc.cluster.local` and `database.port: 3306` (no `clusterRef`), assert NO `databases.k8s.mariadb.com` or `users.k8s.mariadb.com` or `grants.k8s.mariadb.com` CRs are created (via `error` checks), assert `keystone.conf` `[database].connection` contains the explicit host\n- **`image-upgrade/`** — Apply Keystone CR, wait for `Ready=True`, patch `spec.image.tag`, assert Deployment container image updates, assert `Ready=True` maintained after rollout completes\n\n**Excluded:**\n- Performance/stress tests (S024 scope)\n- ControlPlane/c5c3-operator integration (S022/S023 scope)\n- TLS certificate validation (not in S016 spec)\n- Federation tests (no federation reconciler yet)\n- Webhook validation tests (already covered by `invalid-cr/` in CC-0012)\n- Cache brownfield testing (not in S016 spec; only database brownfield requested)\n\n---\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph E2E[\"tests/e2e/keystone/\"]\n        BD[\"basic-deployment/\"]\n        MS[\"missing-secret/\"]\n        FR[\"fernet-rotation/\"]\n        SC[\"scale/\"]\n        DC[\"deletion-cleanup/\"]\n        PO[\"policy-overrides/\"]\n        MC[\"middleware-config/\"]\n        BF[\"brownfield-database/\"]\n        IU[\"image-upgrade/\"]\n    end\n\n    subgraph Conditions[\"Condition Coverage\"]\n        SR[\"SecretsReady\"]\n        DR[\"DatabaseReady\"]\n        FKR[\"FernetKeysReady\"]\n        DPR[\"DeploymentReady\"]\n        BR[\"BootstrapReady\"]\n        RDY[\"Ready\"]\n    end\n\n    BD --> SR & DR & FKR & DPR & BR & RDY\n    MS --> SR\n    FR --> FKR\n    SC --> DPR\n    DC --> RDY\n    PO --> DR & DPR\n    MC --> DPR\n    BF --> DR & DPR\n    IU --> DPR & RDY\n```\n\n```mermaid\nsequenceDiagram\n    participant CH as Chainsaw\n    participant K8s as Kubernetes API\n    participant KR as Keystone Reconciler\n    participant OW as Owned Resources\n\n    Note over CH: basic-deployment\n    CH->>K8s: apply Keystone CR + prerequisite Secrets\n    K8s->>KR: reconcile\n    KR->>OW: create fernet Secret, CronJob, RBAC, ConfigMap, MariaDB CRs, db-sync Job, Deployment, Service, bootstrap Job, PushSecret\n    CH->>K8s: assert conditions SecretsReady...Ready = True\n    CH->>K8s: assert Deployment availableReplicas > 0\n    CH->>K8s: script curl /v3\n\n    Note over CH: missing-secret\n    CH->>K8s: apply Keystone CR with unique secretRef names\n    CH->>K8s: assert SecretsReady = False\n    CH->>K8s: create prerequisite Secrets\n    CH->>K8s: assert Ready = True\n\n    Note over CH: fernet-rotation\n    CH->>K8s: apply Keystone CR, wait Ready\n    CH->>K8s: record fernet Secret .data hash\n    CH->>K8s: script kubectl create job --from=cronjob\n    CH->>K8s: assert fernet Secret .data changed\n    CH->>K8s: assert Deployment annotation changed\n\n    Note over CH: deletion-cleanup\n    CH->>K8s: delete Keystone CR\n    CH->>K8s: error assert Deployment not found\n    CH->>K8s: error assert Service not found\n    CH->>K8s: error assert CronJob not found\n```\n\n```mermaid\nstateDiagram-v2\n    [*] --> SecretsReady: ESO Secrets synced\n    SecretsReady --> DatabaseReady: MariaDB CRs + db_sync Job\n    DatabaseReady --> FernetKeysReady: Fernet Secret + CronJob + PushSecret\n    FernetKeysReady --> DeploymentReady: Deployment available + endpoint set\n    DeploymentReady --> BootstrapReady: Bootstrap Job complete\n    BootstrapReady --> Ready: All conditions True\n\n    state \"missing-secret test\" as ms\n    SecretsReady --> ms: SecretsReady=False path\n\n    state \"fernet-rotation test\" as fr\n    FernetKeysReady --> fr: Key rotation path\n\n    state \"scale test\" as sc\n    DeploymentReady --> sc: Replica changes\n```\n\n---\n\n## Key Components\n\n- **`basic-deployment/chainsaw-test.yaml`** + fixture CR + prerequisite Secrets — Happy-path test asserting full condition progression (`SecretsReady` → `DatabaseReady` → `FernetKeysReady` → `DeploymentReady` → `BootstrapReady` → `Ready=True` with reason `AllReady`), resource existence checks (Deployment `{name}-api`, Service, ConfigMap, CronJob, fernet Secret, RBAC, PushSecret), and a `script` step with `curl` to verify HTTP response from `/v3`\n- **`missing-secret/chainsaw-test.yaml`** + fixture CR + late-created Secrets — CR references unique Secret names (e.g. `missing-secret-keystone-db`, `missing-secret-keystone-admin`). Tests `SecretsReady=False` when referenced Secrets don't exist, asserts reason is `WaitingForDBCredentials` or `WaitingForAdminCredentials`, then creates Secrets and asserts recovery to `Ready=True`\n- **`fernet-rotation/chainsaw-test.yaml`** + fixture CR — Verifies CronJob `schedule` field matches spec, triggers rotation via `kubectl create job --from=cronjob/{name}-fernet-rotate` in a `script` step, asserts `{name}-fernet-keys` Secret `.data` changes, asserts Deployment pod-template annotation `keystone.c5c3.io/fernet-keys-hash` value changes (triggering rolling restart)\n- **`scale/chainsaw-test.yaml`** + fixture CR + patch files — Three-step test (3→5→2) using Chainsaw `patch` steps on `spec.replicas`, asserting `Deployment.spec.replicas` and `status.availableReplicas` at each step\n- **`deletion-cleanup/chainsaw-test.yaml`** + fixture CR — Deploys Keystone CR, waits for `Ready=True`, deletes CR, asserts all owned resources return NotFound via Chainsaw `error` checks: Deployment, Service, ConfigMap, CronJob, Jobs (db-sync, bootstrap), Secret (fernet-keys), ServiceAccount, Role, RoleBinding, PushSecret\n- **`policy-overrides/chainsaw-test.yaml`** + fixture CR + policy ConfigMap — Applies Keystone CR with `policyOverrides.configMapRef`, asserts generated ConfigMap contains `policy.yaml` data key and `keystone.conf` contains `[oslo_policy]` section with `policy_file`\n- **`middleware-config/chainsaw-test.yaml`** + fixture CR — Applies Keystone CR with custom `middleware` entries (specifying `name`, `filterFactory`, `position`), asserts `api-paste.ini` in generated ConfigMap contains the modified pipeline filter references\n- **`brownfield-database/chainsaw-test.yaml`** + fixture CR — Applies Keystone CR with `database.host`/`database.port` (no `clusterRef`), asserts NO `databases.k8s.mariadb.com`, `users.k8s.mariadb.com`, or `grants.k8s.mariadb.com` CRs exist via `error` checks, asserts `keystone.conf` `[database].connection` contains the explicit host. Uses the existing `openstack-db` MariaDB instance.\n- **`image-upgrade/chainsaw-test.yaml`** + fixture CR + patch file — Applies Keystone CR, waits for `Ready=True`, patches `spec.image.tag` via Chainsaw `patch` step, asserts Deployment container image updates, asserts `Ready=True` maintained after rollout completes\n- **Shared conventions** — Each test directory contains its own Keystone CR YAML with a unique CR name (e.g., `keystone-basic`, `keystone-scale`). Each test creates its own prerequisite Secrets with test-unique names. Brownfield tests use `host`/`port`; managed tests use `clusterRef` pointing to `openstack-db`/`openstack-memcached`. Assertions follow existing patterns from `infra-stack-health` and `invalid-cr`: CEL expressions with backtick-quoted numerics (`` `0` ``), JMESPath condition filters (`conditions[?type == 'Ready']`), and extended `assert` timeout (5m) for full reconciliation cycles.\n\n---\n\n**Note on condition order:** Research confirms the sub-reconciler execution order is `reconcileSecrets` → `reconcileDatabase` → `reconcileFernetKeys` → `reconcileConfig` → `reconcileDeployment` → `reconcileBootstrap` (per `keystone_controller.go` subConditionTypes). The user's original elaboration had `FernetKeysReady` before `DatabaseReady` — corrected above to match the actual code.</description>\n  <tasks>\n    <task status=\"pending\">1.1 Create basic-deployment E2E test suite — Keystone CR fixture, prerequisite resources, and chainsaw-test.yaml asserting full condition progression, all owned resources, and API accessibility (REQ-001, REQ-002, REQ-003, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">1.2 Create missing-secret E2E test suite — Keystone CR with unique non-existent secretRef names, SecretsReady=False assertion, late ExternalSecret creation, and Ready=True recovery assertion (REQ-004, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">1.3 Create deletion-cleanup E2E test suite — deploy Keystone CR, wait for Ready, delete CR, error-assert all owned resources NotFound (REQ-007, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">2.1 Create fernet-rotation E2E test suite — deploy CR, record fernet hash, trigger manual rotation via kubectl create job, assert Secret data and Deployment annotation changed (REQ-005, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">2.2 Create scale E2E test suite — deploy CR with replicas:3, patch to 5, assert, patch to 2, assert (REQ-006, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">2.3 Create image-upgrade E2E test suite — deploy CR, wait Ready, patch spec.image.tag, assert Deployment image updates and Ready maintained (REQ-011, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">3.1 Create policy-overrides E2E test suite — apply policy ConfigMap + Keystone CR with policyOverrides.configMapRef, assert policy.yaml and oslo_policy in generated ConfigMap (REQ-008, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">3.2 Create middleware-config E2E test suite — apply Keystone CR with custom middleware entries, assert api-paste.ini in generated ConfigMap contains custom filter references (REQ-009, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">3.3 Create brownfield-database E2E test suite — apply Keystone CR with explicit database.host (no clusterRef), error-assert no MariaDB CRs, verify keystone.conf connection (REQ-010, REQ-012, REQ-013) (CC-0016)</task>\n    <task status=\"pending\">4.1 Update E2E testing reference documentation to cover all 9 Keystone test suites (CC-0016)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nAll 9 test suites are independent and can run in parallel (parallel: 4 in chainsaw-config.yaml). Each creates its own Keystone CR with a unique name in the openstack namespace. Tests rely on pre-deployed infrastructure (MariaDB, Memcached, ESO, OpenBao) verified by the infra-stack-health test.\n\n## Corrected Sub-Reconciler Execution Order\n\nThe ACTUAL execution order in keystone_controller.go (lines 70-95) is:\n1. reconcileSecrets → SecretsReady\n2. reconcileFernetKeys → FernetKeysReady\n3. reconcileConfig → (no condition, returns configMapName)\n4. reconcileDatabase → DatabaseReady\n5. reconcileDeployment → DeploymentReady\n6. reconcileBootstrap → BootstrapReady\n7. setReadyCondition → Ready\n\nNote: The elaboration described a different order (DatabaseReady before FernetKeysReady). The tests must match the ACTUAL code order for condition progression assertions.\n\n## Data Flow\n\n```\nKeystone CR → reconcileSecrets (check ExternalSecrets) → reconcileFernetKeys (create Secret, CronJob, RBAC, PushSecret)\n→ reconcileConfig (render keystone.conf + api-paste.ini + policy.yaml → immutable ConfigMap)\n→ reconcileDatabase (create MariaDB CRs if managed, run db-sync Job)\n→ reconcileDeployment (create Deployment + Service, set endpoint)\n→ reconcileBootstrap (run bootstrap Job)\n→ setReadyCondition (aggregate all sub-conditions)\n```\n\n## Key Design Decisions\n\n1. **ConfigMap hash assertion**: ConfigMaps use content-hash suffixes ({name}-config-{hash}). Tests use script steps with kubectl + grep to verify existence since the exact name cannot be predicted.\n\n2. **Secret name alignment**: The reconcileSecrets function calls WaitForExternalSecret with the secretRef.name, then IsSecretReady with the same name. For the E2E tests, the secretRef.name MUST match both the ExternalSecret name AND the materialized Secret name. Verify the deployed ExternalSecret target.name configuration matches what the Keystone CR references.\n\n3. **Missing-secret recovery**: WaitForExternalSecret returns (false, nil) when ExternalSecret is not found (not an error, just not ready). Recovery requires creating ExternalSecrets that the ESO controller can sync, not just raw Secrets.\n\n4. **Fernet rotation verification**: The script-based approach records the Secret data hash BEFORE rotation, triggers rotation via kubectl create job --from=cronjob, then verifies the hash AFTER. This avoids timing issues with polling.\n\n5. **Bootstrap Job TTL**: The bootstrap Job has TTLSecondsAfterFinished: 300. In the deletion-cleanup test, the bootstrap Job may already be TTL-cleaned before CR deletion. The error check should handle both cases (NotFound is expected).\n\n## Potential Pitfalls\n\n- **Namespace**: All resources must be in the openstack namespace where infrastructure (MariaDB, Memcached, ESO) is deployed\n- **Parallel race conditions**: Unique CR names prevent collisions, but shared infrastructure (MariaDB, ESO) is used by all tests concurrently\n- **Timeout calibration**: 5m timeout should accommodate MariaDB CR readiness + db-sync Job + Deployment rollout + bootstrap Job. If flaky, increase per-step timeouts\n- **Image tag for upgrade test**: The patch image tag must be a valid, pullable tag. Consider using the same tag or a known-good alternative tag</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/e2e/keystone/basic-deployment/chainsaw-test.yaml\" test-function=\"assert all sub-conditions True and Ready=True with reason AllReady\" requirement-id=\"REQ-001\">All 5 sub-conditions (SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady) have status True; Ready has status True with reason AllReady</spec>\n    <spec test-file=\"tests/e2e/keystone/basic-deployment/chainsaw-test.yaml\" test-function=\"assert Deployment, Service, CronJob, Secret, RBAC, PushSecret exist\" requirement-id=\"REQ-002\">Deployment keystone-basic-api has availableReplicas > 0; Service keystone-basic-api has port 5000; CronJob, Secret, ServiceAccount, Role, RoleBinding, PushSecret exist with correct names</spec>\n    <spec test-file=\"tests/e2e/keystone/basic-deployment/chainsaw-test.yaml\" test-function=\"assert ConfigMap with name prefix keystone-basic-config- exists\" requirement-id=\"REQ-002\">A ConfigMap starting with keystone-basic-config- exists and contains keystone.conf and api-paste.ini data keys</spec>\n    <spec test-file=\"tests/e2e/keystone/basic-deployment/chainsaw-test.yaml\" test-function=\"script step curl /v3 returns success\" requirement-id=\"REQ-003\">curl http://keystone-basic-api.openstack.svc:5000/v3 exits with code 0</spec>\n    <spec test-file=\"tests/e2e/keystone/missing-secret/chainsaw-test.yaml\" test-function=\"assert SecretsReady=False when Secrets missing\" requirement-id=\"REQ-004\">SecretsReady condition has status False with reason WaitingForDBCredentials or WaitingForAdminCredentials</spec>\n    <spec test-file=\"tests/e2e/keystone/missing-secret/chainsaw-test.yaml\" test-function=\"assert recovery to Ready=True after creating Secrets\" requirement-id=\"REQ-004\">After creating prerequisite ExternalSecrets, Ready condition transitions to True</spec>\n    <spec test-file=\"tests/e2e/keystone/fernet-rotation/chainsaw-test.yaml\" test-function=\"assert CronJob schedule matches spec\" requirement-id=\"REQ-005\">CronJob schedule field matches spec.fernet.rotationSchedule from the Keystone CR</spec>\n    <spec test-file=\"tests/e2e/keystone/fernet-rotation/chainsaw-test.yaml\" test-function=\"assert fernet Secret data changes after manual rotation\" requirement-id=\"REQ-005\">After triggering rotation via kubectl create job --from=cronjob, the fernet Secret data has changed</spec>\n    <spec test-file=\"tests/e2e/keystone/fernet-rotation/chainsaw-test.yaml\" test-function=\"assert Deployment fernet-keys-hash annotation changes\" requirement-id=\"REQ-005\">Deployment pod-template annotation keystone.c5c3.io/fernet-keys-hash has a different value than before rotation</spec>\n    <spec test-file=\"tests/e2e/keystone/scale/chainsaw-test.yaml\" test-function=\"assert scale up from 3 to 5 replicas\" requirement-id=\"REQ-006\">After patching replicas: 5, Deployment spec.replicas == 5 and status.availableReplicas == 5</spec>\n    <spec test-file=\"tests/e2e/keystone/scale/chainsaw-test.yaml\" test-function=\"assert scale down from 5 to 2 replicas\" requirement-id=\"REQ-006\">After patching replicas: 2, Deployment spec.replicas == 2 and status.availableReplicas == 2</spec>\n    <spec test-file=\"tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml\" test-function=\"error assert Deployment and Service not found after CR deletion\" requirement-id=\"REQ-007\">After deleting the Keystone CR, error checks for Deployment and Service return NotFound</spec>\n    <spec test-file=\"tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml\" test-function=\"error assert CronJob, Secret, RBAC, PushSecret, Jobs not found after CR deletion\" requirement-id=\"REQ-007\">After deleting the Keystone CR, error checks for CronJob, Secret, ServiceAccount, Role, RoleBinding, PushSecret, and db-sync Job return NotFound</spec>\n    <spec test-file=\"tests/e2e/keystone/policy-overrides/chainsaw-test.yaml\" test-function=\"assert ConfigMap contains policy.yaml and oslo_policy section\" requirement-id=\"REQ-008\">Generated ConfigMap has policy.yaml data key and keystone.conf contains [oslo_policy] with policy_file</spec>\n    <spec test-file=\"tests/e2e/keystone/policy-overrides/chainsaw-test.yaml\" test-function=\"assert policy.yaml content includes rules from referenced ConfigMap\" requirement-id=\"REQ-008\">The policy.yaml content in the generated ConfigMap contains the rules from the source policy ConfigMap</spec>\n    <spec test-file=\"tests/e2e/keystone/middleware-config/chainsaw-test.yaml\" test-function=\"assert api-paste.ini contains custom filter references\" requirement-id=\"REQ-009\">api-paste.ini in the generated ConfigMap contains the custom filter names and filterFactory entries</spec>\n    <spec test-file=\"tests/e2e/keystone/middleware-config/chainsaw-test.yaml\" test-function=\"assert pipeline includes custom middleware position\" requirement-id=\"REQ-009\">The pipeline definition in api-paste.ini includes the custom filter at the specified position (before/after)</spec>\n    <spec test-file=\"tests/e2e/keystone/brownfield-database/chainsaw-test.yaml\" test-function=\"error assert no MariaDB Database, User, Grant CRs exist\" requirement-id=\"REQ-010\">Error checks verify no databases.k8s.mariadb.com, users.k8s.mariadb.com, or grants.k8s.mariadb.com CRs exist for the CR name</spec>\n    <spec test-file=\"tests/e2e/keystone/brownfield-database/chainsaw-test.yaml\" test-function=\"assert keystone.conf connection uses explicit host\" requirement-id=\"REQ-010\">keystone.conf [database].connection in the generated ConfigMap contains the explicit database host address</spec>\n    <spec test-file=\"tests/e2e/keystone/image-upgrade/chainsaw-test.yaml\" test-function=\"assert Deployment image updates after patch\" requirement-id=\"REQ-011\">After patching spec.image.tag, the Deployment container image reflects the new tag</spec>\n    <spec test-file=\"tests/e2e/keystone/image-upgrade/chainsaw-test.yaml\" test-function=\"assert Ready=True maintained after rollout\" requirement-id=\"REQ-011\">Ready condition remains True with reason AllReady after the image rollout completes</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator developer validates happy-path Keystone deployment\">As a operator developer, I want an E2E test that deploys a Keystone CR in managed mode and verifies the full reconciliation completes, so that regressions in the happy-path reconciliation loop are caught before merge</story>\n    <story title=\"Operator developer validates secret dependency recovery\">As a operator developer, I want an E2E test that verifies the operator waits for missing ESO Secrets and recovers when they appear, so that the SecretsReady requeue behavior is validated end-to-end</story>\n    <story title=\"Operator developer validates fernet key rotation\">As a operator developer, I want an E2E test that triggers fernet key rotation and verifies the Secret changes and Deployment rolls, so that the CronJob-based fernet rotation mechanism is validated end-to-end</story>\n    <story title=\"Operator developer validates replica scaling\">As a operator developer, I want an E2E test that patches replicas up and down and verifies the Deployment scales accordingly, so that spec.replicas changes are properly propagated to the Deployment</story>\n    <story title=\"Operator developer validates garbage collection on CR deletion\">As a operator developer, I want an E2E test that deletes a Keystone CR and verifies all owned resources are garbage-collected, so that owner references and cascading deletion are validated end-to-end</story>\n    <story title=\"Operator developer validates policy override injection\">As a operator developer, I want an E2E test that applies a Keystone CR with policyOverrides and verifies the generated ConfigMap, so that oslo.policy integration is validated end-to-end</story>\n    <story title=\"Operator developer validates middleware pipeline customization\">As a operator developer, I want an E2E test that applies a Keystone CR with custom middleware and verifies api-paste.ini, so that WSGI middleware pipeline customization is validated end-to-end</story>\n    <story title=\"Operator developer validates brownfield database mode\">As a operator developer, I want an E2E test that applies a Keystone CR with explicit database.host (no clusterRef) and verifies no MariaDB CRs are created, so that brownfield database support is validated — no MariaDB operator dependency when using an external DB</story>\n    <story title=\"Operator developer validates rolling image upgrade\">As a operator developer, I want an E2E test that patches spec.image.tag and verifies the Deployment updates without losing Ready status, so that image upgrades are validated as non-disruptive rolling updates</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The basic-deployment test SHALL assert all 5 sub-conditions progress to True and the aggregate Ready condition reaches True with reason AllReady (CC-0016)</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The basic-deployment test SHALL verify existence of all owned resources: Deployment, Service, ConfigMap, CronJob, fernet Secret, RBAC resources, and PushSecret (CC-0016)</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The basic-deployment test SHALL verify the Keystone API is accessible via HTTP on port 5000/v3 (CC-0016)</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The missing-secret test SHALL verify SecretsReady=False when referenced Secrets do not exist, then recovery to Ready=True after creating them (CC-0016)</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The fernet-rotation test SHALL verify CronJob schedule correctness, manual rotation changes the fernet Secret data, and the Deployment pod-template annotation updates (CC-0016)</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The scale test SHALL verify Deployment replicas update on spec.replicas changes: 3→5 (scale up) and 5→2 (scale down) (CC-0016)</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The deletion-cleanup test SHALL verify all owned resources return NotFound after deleting the Keystone CR (CC-0016)</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The policy-overrides test SHALL verify the generated ConfigMap contains policy.yaml and keystone.conf includes the oslo_policy section (CC-0016)</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The middleware-config test SHALL verify the generated ConfigMap's api-paste.ini contains custom middleware filter references (CC-0016)</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The brownfield-database test SHALL verify NO MariaDB CRs are created when using explicit database.host (no clusterRef) (CC-0016)</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The image-upgrade test SHALL verify patching spec.image.tag updates the Deployment container image and Ready=True is maintained (CC-0016)</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">All test suites SHALL use unique Keystone CR names to enable parallel execution with parallel: 4 (CC-0016)</requirement>\n    <requirement id=\"REQ-013\" priority=\"SHALL\">All test files SHALL follow existing project conventions: SPDX headers, Chainsaw v1alpha1 Test kind, CEL/JMESPath assertion patterns, and CC-0016 feature ID references (CC-0016)</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Check if previous feature already implemented any of our pending tasks</rule>\n  <rule>Run: git log --oneline -5 to see what the previous feature committed</rule>\n  <rule>Mark tasks as removed_tasks if they were done by the previous feature</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 30812,
  "caller": "run_claude_cli"
}