{
  "timestamp": "2026-04-11T19:56:41.231363+00:00",
  "feature_id": "CC-0059",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0059",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0059\">\n  <title>Add security and correctness linters to golangci-lint</title>\n  <description># GitHub Issue #146: Extend golangci-lint configuration with security and correctness linters\n\n**Category**: code quality / security | **Scope**: Medium\n\n**Description**: The current golangci-lint configuration only enables 6 linters (`errcheck`, `gocritic`, `govet`, `ineffassign`, `staticcheck`, `unused`). For a Kubernetes operator project with HTTP clients, error wrapping, and concurrent reconciliation logic, several important linters are missing. The `.golangci.yml` configuration should be extended with the following additional linters:\n\n| Linter | Purpose |\n|---|---|\n| `gosec` | Detects security anti-patterns (hardcoded credentials, weak crypto, unsafe operations) |\n| `bodyclose` | Catches unclosed HTTP response bodies that cause memory leaks |\n| `nilerr` | Flags functions that return `nil` instead of the actual error |\n| `errorlint` | Enforces correct `errors.Is` / `errors.As` usage (Go 1.13+ error wrapping) |\n| `exhaustive` | Ensures switch statements over enum types cover all cases |\n| `noctx` | Catches HTTP requests made without a `context.Context` (missing timeout/cancellation) |\n\nProposed `.golangci.yml` linters section:\n\n```yaml\nlinters:\n  default: none\n  enable:\n    # Existing\n    - errcheck\n    - gocritic\n    - govet\n    - ineffassign\n    - staticcheck\n    - unused\n    # New\n    - gosec\n    - bodyclose\n    - nilerr\n    - errorlint\n    - exhaustive\n    - noctx\n```\n\n**Motivation**: The existing linter set covers general correctness but misses security-relevant patterns (`gosec`), resource leaks (`bodyclose`, `noctx`), and error handling correctness (`nilerr`, `errorlint`, `exhaustive`). These are particularly important in operator code where leaked connections degrade cluster health over time, swallowed errors cause silent reconciliation failures, and incomplete switch statements over condition types lead to unhandled states. Adding these linters catches entire categories of bugs at the lint stage rather than in production.\n\n**Affected Areas**:\n- `.golangci.yml`\n- Go source files that produce new lint findings (to be fixed as part of this issue or tracked separately)\n\n**Acceptance Criteria**:\n- [ ] `.golangci.yml` enables `gosec`, `bodyclose`, `nilerr`, `errorlint`, `exhaustive`, and `noctx`\n- [ ] All existing Go source files pass the extended linter configuration (fix any new findings)\n- [ ] CI lint job continues to pass with the updated configuration\n- [ ] No existing linters are removed or disabled\n- [ ] Any `nolint` directives added to suppress false positives include a justification comment\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/146</description>\n  <tasks>\n    <task status=\"pending\">1.1 Update .golangci.yml to enable gosec, bodyclose, nilerr, errorlint, exhaustive, and noctx linters (REQ-001, REQ-002, REQ-004)</task>\n    <task status=\"pending\">2.1 Run extended linters on internal/common module and fix any violations (REQ-003, REQ-005)</task>\n    <task status=\"pending\">2.2 Run extended linters on operators/keystone module and fix any violations (REQ-003, REQ-005)</task>\n    <task status=\"pending\">2.3 Run extended linters on operators/c5c3 module and fix any violations (REQ-003, REQ-005)</task>\n    <task status=\"pending\">3.1 Verify end-to-end make lint passes across all modules (REQ-006, REQ-007)</task>\n    <task status=\"pending\">3.2 Update docs/reference/ci-workflow.md to document the new linters (REQ-008)</task>\n\n  </tasks>\n  <implementation-notes>Architectural decision: Add linters to the existing .golangci.yml without linter-specific settings — use defaults for all 6 new linters. The golangci-lint v2 format groups settings under linters.settings.<name>, but no custom settings are needed for the initial rollout. If false positives emerge, per-linter settings can be tuned in a follow-up.\n\nConfig structure (after change):\n```\n.golangci.yml\n├── version: \"2\"\n├── linters\n│   ├── default: none\n│   ├── enable: [12 linters]\n│   └── exclusions.paths: [zz_generated]\n└── run.timeout: 5m\n```\n\nLint execution flow:\n```\nmake lint\n├── cd internal/common && golangci-lint run ./...\n├── cd operators/keystone && golangci-lint run ./...\n└── cd operators/c5c3 && golangci-lint run ./...\n```\n\nEach module picks up the root .golangci.yml. golangci-lint v2.11.4 (pinned in CI) supports all 6 proposed linters.\n\nPotential pitfalls:\n1. If any new linter produces unexpected findings in generated test helpers or deeply nested code not covered by research, fix the code or add a justified nolint directive.\n2. The exhaustive linter uses default-signifies-exhaustive=false by default — switches with a default case still need all enum values listed. The existing PipelinePosition switch lists both values, so this is fine.\n3. Do not modify .github/workflows/ci.yaml or Makefile — the config change in .golangci.yml is self-contained.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"make lint (CLI verification)\" test-function=\"verify_all_modules_pass_extended_linters [integration]\" requirement-id=\"REQ-006\">make lint exits with code 0 across internal/common, operators/keystone, and operators/c5c3</spec>\n    <spec test-file=\".golangci.yml (config verification)\" test-function=\"verify_new_linters_enabled [unit]\" requirement-id=\"REQ-001\">golangci-lint linters output includes gosec, bodyclose, nilerr, errorlint, exhaustive, noctx</spec>\n    <spec test-file=\".golangci.yml (config verification)\" test-function=\"verify_existing_linters_preserved [unit]\" requirement-id=\"REQ-002\">golangci-lint linters output includes errcheck, gocritic, govet, ineffassign, staticcheck, unused</spec>\n    <spec test-file=\"cd internal/common && golangci-lint run ./... (CLI verification)\" test-function=\"verify_internal_common_passes [integration]\" requirement-id=\"REQ-003\">golangci-lint run exits with code 0 for internal/common module with all 12 linters active</spec>\n    <spec test-file=\"cd operators/keystone && golangci-lint run ./... (CLI verification)\" test-function=\"verify_operators_keystone_passes [integration]\" requirement-id=\"REQ-003\">golangci-lint run exits with code 0 for operators/keystone module; existing nolint:gosec on InsecureSkipVerify is sufficient</spec>\n    <spec test-file=\"cd operators/c5c3 && golangci-lint run ./... (CLI verification)\" test-function=\"verify_operators_c5c3_passes [integration]\" requirement-id=\"REQ-003\">golangci-lint run exits with code 0 for operators/c5c3 module (single main.go file)</spec>\n    <spec test-file=\".golangci.yml (config verification)\" test-function=\"verify_generated_code_excluded [integration]\" requirement-id=\"REQ-004\">Files matching zz_generated.*.go produce no lint findings from any of the 12 linters</spec>\n    <spec test-file=\"grep nolint operators/keystone/internal/testutil/envtest_setup.go (CLI verification)\" test-function=\"verify_nolint_directives_have_justification [unit]\" requirement-id=\"REQ-005\">All nolint directives follow //nolint:linter-name // reason format; no blanket nolint directives exist</spec>\n    <spec test-file=\".github/workflows/ci.yaml (file inspection)\" test-function=\"verify_ci_workflow_unchanged [unit]\" requirement-id=\"REQ-007\">The CI workflow file is not modified; the lint job still runs make lint with golangci-lint v2.11.4</spec>\n    <spec test-file=\"docs/reference/ci-workflow.md (file inspection)\" test-function=\"verify_docs_list_all_linters [unit]\" requirement-id=\"REQ-008\">The lint job section lists all 12 linters (6 existing + 6 new) with descriptions and CC-0059 reference</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Developer gets security anti-pattern feedback during linting\">As a operator developer, I want gosec to flag security anti-patterns (hardcoded credentials, weak crypto, unsafe operations) during local lint runs, so that security issues are caught at development time rather than in code review or production</story>\n    <story title=\"Developer gets error handling correctness feedback during linting\">As a operator developer, I want errorlint and nilerr to enforce correct Go 1.13+ error wrapping patterns and prevent swallowed errors, so that error wrapping with errors.Is/errors.As works correctly and nil-instead-of-error bugs are caught before they cause silent reconciliation failures</story>\n    <story title=\"Developer gets resource leak and exhaustiveness feedback during linting\">As a operator developer, I want bodyclose, noctx, and exhaustive linters to catch HTTP response body leaks, missing context propagation, and incomplete switch statements, so that future code additions that introduce HTTP clients or enum switches are immediately flagged if they have resource leaks or missing cases</story>\n    <story title=\"CI enforces extended lint configuration on all pull requests\">As a reviewer, I want the CI lint job to automatically enforce all 12 linters (6 existing + 6 new) on every pull request, so that no code with security, correctness, or resource leak issues can be merged without explicit justification</story>\n    <story title=\"Nolint suppressions are transparent and auditable\">As a reviewer, I want every nolint directive to include a justification comment explaining why suppression is necessary, so that suppressions are deliberate decisions rather than shortcuts, and can be reviewed for continued validity</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The .golangci.yml configuration SHALL enable gosec, bodyclose, nilerr, errorlint, exhaustive, and noctx linters</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">No existing linters SHALL be removed or disabled from the configuration</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">All Go source files across all modules SHALL pass the extended lint configuration without errors</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">Generated code matching zz_generated.*.go SHALL remain excluded from all lint checks including the new linters</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">Any nolint directives added to suppress false positives SHALL include a justification comment specifying the linter name and reason</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The make lint target SHALL pass with the extended configuration across all modules</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The CI lint workflow job SHALL pass with the updated .golangci.yml without requiring workflow file changes</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The CI workflow reference documentation SHALL be updated to list the new linters</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 12788,
  "caller": "run_claude_cli"
}