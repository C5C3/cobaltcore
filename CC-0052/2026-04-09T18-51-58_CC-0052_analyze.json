{
  "timestamp": "2026-04-09T18:51:58.486538+00:00",
  "feature_id": "CC-0052",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0052",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0052\">\n  <title>Add race condition detection to Go tests</title>\n  <description># GitHub Issue #145: Add race condition detection to Go tests with -race flag\n\n**Category**: testing / reliability | **Scope**: Small\n\n**Description**: Unit and integration tests currently run without the `-race` flag. Kubernetes operator code is inherently concurrent — reconcilers, watches, and informer caches all operate across goroutines. Race conditions are a frequent and hard-to-diagnose source of bugs in this type of codebase. A dedicated CI job should run all Go tests with `-race` enabled, and a corresponding Makefile target should be added for local use.\n\nMakefile extension:\n\n```makefile\ntest-race:\n\tcd internal/common && go test -race ./...\n\tcd operators/keystone && go test -race ./...\n```\n\nProposed CI job (separate from the main test job since `-race` adds ~2-5x overhead):\n\n```yaml\ntest-race:\n  needs: [changes]\n  if: needs.changes.outputs.go == 'true'\n  runs-on: ubuntu-latest\n  timeout-minutes: 20\n  steps:\n    - uses: actions/checkout@v6\n    - uses: actions/setup-go@v6\n      with:\n        go-version-file: go.work\n    - run: |\n        cd internal/common && go test -race -count=1 ./...\n        cd operators/keystone && go test -race -count=1 ./...\n```\n\n**Motivation**: The Go race detector is the standard tool for catching data races at test time. Without it, race conditions can silently corrupt shared state in production — particularly dangerous in operator code where concurrent reconcile loops may read and write the same resources. Running `-race` as a separate job avoids slowing down the primary test feedback loop while still catching races on every PR.\n\n**Affected Areas**:\n- `.github/workflows/ci.yaml`\n- `Makefile`\n\n**Acceptance Criteria**:\n- [ ] New `test-race` Makefile target runs `go test -race ./...` for all Go modules\n- [ ] New `test-race` CI job in `ci.yaml` gated on `needs.changes.outputs.go == 'true'`\n- [ ] CI job uses `-count=1` to disable test caching (race results must not be cached)\n- [ ] CI job has a 20-minute timeout to accommodate the overhead of the race detector\n- [ ] Job fails the pipeline when a data race is detected\n- [ ] Existing test jobs are not modified\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/145</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add `test-race` phony target to Makefile in the Test Targets section (REQ-001)</task>\n    <task status=\"pending\">1.2 Add `test-race` CI job to `.github/workflows/ci.yaml` (REQ-002, REQ-003, REQ-004, REQ-005, REQ-006)</task>\n    <task status=\"pending\">2.1 Update `docs/reference/ci-workflow.md` with test-race job documentation (REQ-002)</task>\n\n  </tasks>\n  <implementation-notes>This is a small, additive feature touching 3 files. No architectural decisions needed.\n\n**Makefile pattern**: Follow the existing `test` target pattern — workspace-relative paths (`./internal/common/...`, `./operators/$$op/...`), iterate over $(OPERATORS), echo module name before each run. Place the target after `test-operator` in the Test Targets section. Do NOT include `-count=1` in the Makefile (that's CI-specific per the acceptance criteria).\n\n**CI job placement**: Insert after the `test-integration` job block (line ~195 in ci.yaml) and before `verify-codegen`. The job is a gate-level job but does NOT block E2E jobs — it runs independently. This is intentional: race detection overhead should not sit on the critical path for E2E execution.\n\n**CI action pins**: All actions must use the exact same SHA pins as other jobs in the file:\n- `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6`\n- `actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6`\n\n**CI run step**: Use workspace-relative paths (not `cd`) to match the `test` / `test-integration` job patterns. Three separate `go test` invocations for clear per-module output:\n```\ngo test -race -count=1 ./internal/common/...\ngo test -race -count=1 ./operators/keystone/...\ngo test -race -count=1 ./operators/c5c3/...\n```\n\n**Why not reuse `make test-race` in CI**: The Makefile target intentionally omits `-count=1` (for developer convenience). CI needs `-count=1` to prevent cached results masking non-deterministic races. Rather than adding complexity (env vars, extra targets), the CI step runs the commands directly.\n\n**Docs update**: The ci-workflow.md doc has a specific format for job sections (description, step table, details). Follow it exactly. Update the ASCII DAG to include test-race as a gate job.\n\n**Pitfalls**:\n- Do NOT use `cd` in the CI run step without subshells — sequential `cd` commands in a bash script change the working directory permanently, causing subsequent relative paths to fail.\n- Do NOT add `test-race` to any existing job's `needs:` array — the issue explicitly requires existing jobs to remain unmodified.\n- The race detector requires CGO; ubuntu-latest runners have GCC pre-installed, so no additional setup is needed.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"Makefile\" test-function=\"make test-race (all modules)\" requirement-id=\"REQ-001\">`make test-race` should execute `go test -race` for internal/common, operators/keystone, and operators/c5c3, exiting 0 when no races are detected</spec>\n    <spec test-file=\"Makefile\" test-function=\"make test-race OPERATOR=keystone (single operator)\" requirement-id=\"REQ-001\">`make test-race OPERATOR=keystone` should run race tests for internal/common and operators/keystone only, skipping c5c3</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"test-race job gating condition\" requirement-id=\"REQ-002\">Job has `needs: [changes]` and `if: needs.changes.outputs.go == 'true'` — skipped when only non-Go files change</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"test-race job action pins\" requirement-id=\"REQ-002\">Job uses same SHA-pinned checkout and setup-go actions as other jobs (de0fac2e..., 4b73464b...)</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"test-race job -count=1 flag\" requirement-id=\"REQ-003\">Run step includes `-count=1` in all `go test` invocations to prevent cached results</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"test-race job timeout\" requirement-id=\"REQ-004\">Job specifies `timeout-minutes: 20`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"test-race job failure propagation\" requirement-id=\"REQ-005\">No `continue-on-error` or `if: always()` on the run step — non-zero exit from go test -race fails the job</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"existing jobs unchanged\" requirement-id=\"REQ-006\">git diff of ci.yaml shows only additions (new test-race job block) — no modifications to existing job blocks</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"no jobs depend on test-race\" requirement-id=\"REQ-006\">grep for 'test-race' in other jobs' `needs:` arrays returns no matches</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Developer detects race conditions locally via Makefile\">As a developer, I want to run race-detected Go tests locally with a single `make test-race` command, so that I can find data races in operator code before pushing to CI</story>\n    <story title=\"CI automatically detects data races on every PR\">As a maintainer, I want a dedicated CI job to run all Go tests with the race detector on every PR that touches Go code, so that race conditions are caught before merging into main</story>\n    <story title=\"Existing CI pipeline is not disrupted by race detection\">As a maintainer, I want the new race detection job to not modify or slow down existing test jobs, so that the primary test feedback loop remains fast and unchanged</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The Makefile SHALL include a `test-race` phony target that runs `go test -race ./...` for internal/common and every operator module</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The CI workflow SHALL include a `test-race` job that is gated on Go code changes via `needs.changes.outputs.go == 'true'`</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The CI `test-race` job SHALL use the `-count=1` flag to disable test result caching</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The CI `test-race` job SHALL have `timeout-minutes: 20`</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The CI workflow SHALL fail when the `test-race` job detects a data race</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">Existing CI test jobs (test, test-integration) SHALL NOT be modified</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 10794,
  "caller": "run_claude_cli"
}