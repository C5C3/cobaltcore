{
  "timestamp": "2026-04-22T13:52:11.448936+00:00",
  "feature_id": "CC-0084",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0084",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0084\">\n  <title>Harden graceful pod termination in Keystone rolling updates</title>\n  <description># GitHub Issue #247: Harden graceful pod termination during Keystone rolling updates\n\n**Category**: improvement | **Scope**: Medium\n\n**Description**: `buildKeystoneDeployment` (operators/keystone/internal/controller/reconcile_deployment.go:138-186) relies on a hardcoded `terminationGracePeriodSeconds: 30` paired with a 5 s `preStop` sleep (`/bin/sh -c \"sleep 5\"`) and uWSGI's `--master` signal handling to drain in-flight requests during rolling updates (e.g. `kubectl patch keystone ... spec.image.tag`). This baseline works for the common case but leaves four production gaps: (1) the 30 s grace period is not user-configurable, leaving only ~25 s for uWSGI to finish in-flight requests — insufficient for workloads with slow LDAP/DB token validation; (2) `uwsgiCommand` (reconcile_deployment.go:278-306) does not set `--harakiri`, so a single hung request can block graceful shutdown until SIGKILL; (3) `--http-keepalive` defaults to `true` (reconcile_deployment.go:281-295) with no keep-alive timeout, letting clients reuse a connection to a pod that has already been removed from the Service endpoints; (4) `Deployment.Spec.Strategy` is never set, so the Kubernetes default `RollingUpdate` with `maxUnavailable: 25%` / `maxSurge: 25%` applies — combined with the `minAvailable: 1` PDB (reconcile_deployment.go:248-271) this permits brief capacity dips rather than zero-downtime rollouts. Expose `spec.terminationGracePeriodSeconds`, `spec.preStopSleepSeconds`, `spec.uwsgi.harakiri`, and `spec.uwsgi.httpKeepAliveTimeout` as optional CRD fields; set an explicit `RollingUpdate` strategy with `maxUnavailable: 0`, `maxSurge: 1` as the new default and make it overridable via `spec.strategy`.\n\n**Motivation**: A `kubectl patch keystone ... spec.image.tag` triggers a pod rotation that must deliver both (a) no new requests routed to a terminating pod and (b) in-flight requests completing cleanly. The current implementation mitigates (a) via the 5 s preStop sleep that covers EndpointSlice propagation, but keep-alive connections bypass this protection because clients keep reusing an already-stale socket. (b) is limited by a fixed 25 s drain window without per-worker hang detection — a single stuck token validation blocks shutdown until SIGKILL truncates every other in-flight request on the same pod. Operators cannot tune any of these values today without forking the operator. At the same time, the default `maxUnavailable: 25%` reduces Keystone capacity during rollout — Keystone is the authentication gate for the entire control plane, so any capacity dip amplifies user-visible latency across all OpenStack services.\n\n**Affected Areas**:\n- `operators/keystone/api/v1alpha1/keystone_types.go`\n- `operators/keystone/api/v1alpha1/keystone_webhook.go`\n- `operators/keystone/internal/controller/reconcile_deployment.go`\n- `operators/keystone/internal/controller/reconcile_deployment_test.go`\n- `operators/keystone/helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml`\n- `operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml`\n- `architecture/docs/04-architecture/04-high-availability.md`\n\n**Acceptance Criteria**:\n- [ ] `KeystoneSpec` gains optional `terminationGracePeriodSeconds *int64` (default 30, min 10) and `preStopSleepSeconds *int64` (default 5, min 0)\n- [ ] `UWSGISpec` gains optional `harakiri *int32` (worker request timeout in seconds, default unset) and `httpKeepAliveTimeout *int32` (idle keep-alive timeout in seconds, default unset)\n- [ ] `buildKeystoneDeployment` sets `.Spec.Strategy` to `RollingUpdate` with `maxUnavailable: 0` and `maxSurge: 1` by default; new optional `spec.strategy *appsv1.DeploymentStrategy` field allows full override\n- [ ] `preStop` sleep duration is derived from `spec.preStopSleepSeconds` instead of the hardcoded `sleep 5`\n- [ ] `uwsgiCommand` appends `--harakiri <n>` when set and `--http-keepalive-timeout <n>` when set\n- [ ] Validating webhook rejects `preStopSleepSeconds >= terminationGracePeriodSeconds` and rejects `harakiri >= (terminationGracePeriodSeconds - preStopSleepSeconds)` so a worker timeout cannot exceed the remaining drain window\n- [ ] Unit tests in `reconcile_deployment_test.go` cover: defaults (grace=30, preStop=5, maxUnavailable=0, maxSurge=1, no harakiri, no keep-alive timeout), all four fields overridden, strategy override replaces defaults, webhook validation paths (preStop >= grace rejected, harakiri too large rejected)\n- [ ] E2E test verifies a rolling image-tag patch produces zero failed requests under a steady load generator running against the Service\n- [ ] CRD manifests in both `helm/.../crds/` and `config/crd/bases/` are regenerated and committed\n- [ ] `architecture/docs/04-architecture/04-high-availability.md` documents the rollout flow (EndpointSlice removal ‖ preStop sleep → SIGTERM → uWSGI graceful shutdown with harakiri fallback → SIGKILL) and the new tunables, including recommended production values and the keep-alive-timeout interaction with the preStop sleep\n\n**Labels:** enhancement\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/247</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add terminationGracePeriodSeconds and preStopSleepSeconds *int64 fields to KeystoneSpec in operators/keystone/api/v1alpha1/keystone_types.go with kubebuilder markers (Minimum=10/default=30 and Minimum=0/default=5). Update zz_generated.deepcopy.go via controller-gen. Unit test TestKeystoneSpecTerminationGracePeriodSecondsField in keystone_types_test.go (REQ-001, REQ-002). Feature: CC-0084.</task>\n    <task status=\"pending\">1.2 Add harakiri and httpKeepAliveTimeout *int32 fields to UWSGISpec in keystone_types.go with kubebuilder Minimum=1 markers (no defaults). Update zz_generated.deepcopy.go. Extend TestUWSGISpecFields to cover the new fields (REQ-003, REQ-004). Feature: CC-0084.</task>\n    <task status=\"pending\">1.3 Add strategy *appsv1.DeploymentStrategy optional field to KeystoneSpec in keystone_types.go with kubebuilder marker allowing full shape. Update imports (k8s.io/api/apps/v1). Regenerate deepcopy. Minimal unit test asserting field is settable (REQ-006). Feature: CC-0084.</task>\n    <task status=\"pending\">2.1 Add webhook range checks for terminationGracePeriodSeconds (>=10) and preStopSleepSeconds (>=0) in operators/keystone/api/v1alpha1/keystone_webhook.go validate(). Add tests TestValidate_TerminationGracePeriodBelowMinRejected and companions in keystone_webhook_test.go (REQ-001, REQ-002). Feature: CC-0084.</task>\n    <task status=\"pending\">2.2 Add webhook cross-field check rejecting preStopSleepSeconds >= terminationGracePeriodSeconds (resolving nil to defaults 5/30) in validate(). Tests TestValidate_PreStop{Equals,Exceeds,StrictlyLess}* in keystone_webhook_test.go (REQ-007). Feature: CC-0084.</task>\n    <task status=\"pending\">2.3 Add webhook range checks for spec.uwsgi.harakiri (>=1) and spec.uwsgi.httpKeepAliveTimeout (>=1) in validate(). Tests TestValidate_UWSGIHarakiriBelowMinRejected and TestValidate_UWSGIKeepAliveTimeoutBelowMinRejected (REQ-003, REQ-004). Feature: CC-0084.</task>\n    <task status=\"pending\">2.4 Add webhook cross-field check rejecting harakiri >= (terminationGracePeriodSeconds - preStopSleepSeconds) using resolved defaults. Tests TestValidate_HarakiriAtDrainBoundaryRejected/AboveDrainRejected/WithinDrainAccepted (REQ-008). Feature: CC-0084.</task>\n    <task status=\"pending\">2.5 Add webhook cross-field check rejecting httpKeepAliveTimeout set when httpKeepAlive=false. Tests TestValidate_KeepAliveTimeoutWithoutKeepAliveRejected and companion accepted case (REQ-012). Feature: CC-0084.</task>\n    <task status=\"pending\">2.6 Add webhook validation rejecting spec.strategy with Type=Recreate combined with non-nil RollingUpdate block. Test TestValidate_StrategyRecreateWithRollingUpdateBlockRejected (REQ-006). Feature: CC-0084.</task>\n    <task status=\"pending\">3.1 Update buildKeystoneDeployment in operators/keystone/internal/controller/reconcile_deployment.go to derive PodSpec.TerminationGracePeriodSeconds from spec.terminationGracePeriodSeconds (fallback 30) and the preStop exec command from spec.preStopSleepSeconds (fallback 5, emit 'sleep <n>' via fmt.Sprintf). Tests TestBuildKeystoneDeployment_TerminationGracePeriod* and PreStopSleep* in reconcile_deployment_test.go (REQ-001, REQ-002, REQ-009, REQ-010). Feature: CC-0084.</task>\n    <task status=\"pending\">3.2 Update uwsgiCommand in reconcile_deployment.go to append '--harakiri <n>' when UWSGISpec.Harakiri is non-nil and '--http-keepalive-timeout <n>' when UWSGISpec.HTTPKeepAliveTimeout is non-nil AND httpKeepAlive is true. Update the doc comment. Tests TestUwsgiCommand_{Harakiri,KeepAliveTimeout}* and TestUwsgiCommand_FlagOrderDeterministic plus KeepAliveTimeoutIgnoredWhenKeepAliveDisabled (REQ-003, REQ-004, REQ-011). Feature: CC-0084.</task>\n    <task status=\"pending\">3.3 Update buildKeystoneDeployment to set Deployment.Spec.Strategy: use spec.strategy verbatim when non-nil, otherwise inject RollingUpdate with MaxUnavailable=IntOrString(0) and MaxSurge=IntOrString(1). Extract helper deploymentStrategy(keystone) for clarity. Tests TestBuildKeystoneDeployment_DefaultRollingUpdateStrategy, StrategyStableAcrossReconciles, StrategyOverrideRollingCustomPercents, StrategyOverrideRecreate (REQ-005, REQ-006). Feature: CC-0084.</task>\n    <task status=\"pending\">3.4 Verify EnsureDeployment in internal/common/deployment does not report drift when spec.Strategy is defaulted on a pre-existing Deployment that lacked one. If drift logic needs adjustment, update it; otherwise add a test note and a TestBuildKeystoneDeployment_StrategyStableAcrossReconciles assertion (REQ-005). Feature: CC-0084.</task>\n    <task status=\"pending\">4.1 Add envtest integration tests TestIntegration_TerminationGracePeriodAppliedToDeployment, TestIntegration_DefaultStrategyAppliedToDeployment, TestIntegration_StrategyOverrideAppliedToDeployment in operators/keystone/internal/controller/integration_test.go (REQ-001, REQ-005, REQ-006). Feature: CC-0084.</task>\n    <task status=\"pending\">4.2 Regenerate CRD manifests via 'make manifests' (or equivalent target). Commit operators/keystone/helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml and operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml. Verify new fields and minimums appear (REQ-013). Feature: CC-0084.</task>\n    <task status=\"pending\">5.1 Create tests/e2e/keystone/rolling-update-zero-downtime/ with 00-keystone-cr.yaml (replicas=3, spec.strategy defaults), 01-patch-image.yaml, and chainsaw-test.yaml. The test: (a) waits for Ready, (b) runs a load-generator Job (curl in a shell loop or vegeta sidecar) at steady rate for 60 s while kubectl patch bumps image tag, (c) asserts zero 5xx/connection-reset from the load generator logs, (d) asserts Deployment.status.availableReplicas stays >= spec.replicas. Keep the test under the chainsaw e2e lane (REQ-014). Feature: CC-0084.</task>\n    <task status=\"pending\">6.1 Update architecture/docs/04-architecture/04-high-availability.md: add a 'Keystone rolling update' subsection with an ASCII sequence diagram (EndpointSlice removal ‖ preStop sleep → SIGTERM → uWSGI graceful shutdown with harakiri fallback → SIGKILL), a tunables table with recommended production values, and the httpKeepAliveTimeout ≤ preStopSleepSeconds interaction note. Reference CC-0084 on every addition (REQ-015). Feature: CC-0084.</task>\n    <task status=\"pending\">6.2 Update docs/reference/keystone-crd.md with entries for spec.terminationGracePeriodSeconds, spec.preStopSleepSeconds, spec.strategy, spec.uwsgi.harakiri, spec.uwsgi.httpKeepAliveTimeout (type, default, minimum, effect). Cross-link to the HA rollout-flow section. Reference CC-0084 on every new entry (REQ-016). Feature: CC-0084.</task>\n\n  </tasks>\n  <implementation-notes>Architectural decisions:\n\n1) Pointer types (*int64/*int32) for all new optional fields so the webhook can distinguish 'unset' from the zero value. terminationGracePeriodSeconds and preStopSleepSeconds are *int64 to match corev1.PodSpec.TerminationGracePeriodSeconds. harakiri and httpKeepAliveTimeout are *int32 to match existing UWSGISpec integers.\n\n2) Defaults live in buildKeystoneDeployment (fallback when nil), not in the defaulting webhook. Reason: keeping defaults out of the stored object means operators who later want 'just use the current default' do not have to chase drift when the operator changes defaults. This matches the existing uwsgi nil-vs-zero pattern (see keystone_webhook.go:93-100 comment on HTTPKeepAlive).\n\n3) Kubebuilder markers set the schema minimum/default. Webhook performs defense-in-depth checks identical to the existing UWSGI/Resources pattern (keystone_webhook.go:276-292).\n\n4) Strategy override is stored verbatim — no merging. When spec.strategy is non-nil, use it exactly; otherwise emit the new default. This mirrors how TopologySpreadConstraints handles user-provided-vs-default (reconcile_deployment.go:324-343).\n\n5) Flag ordering in uwsgiCommand is deterministic: existing flags stay in their current positions; new flags (--harakiri, --http-keepalive-timeout) are appended at the end, in that order. Keeps PodSpec hash stable across reconciles.\n\n6) REQ-011 edge case: if httpKeepAlive=false, neither --http-keepalive nor --http-keepalive-timeout is emitted. The webhook (REQ-012) rejects the combination at admission so the runtime code path is never reached with an invalid CR, but the command-builder still guards against it defensively.\n\n7) CRD regeneration is required because we add fields to two files (helm/ and config/crd/bases/). The project's Makefile has a 'manifests' target via controller-gen; run it locally and commit the diff rather than hand-editing yaml.\n\nData flow (CC-0084):\n  Keystone CR → Default webhook (no new defaults set) → Validate webhook (range + cross-field checks REQ-001..REQ-008, REQ-012) → Reconciler → buildKeystoneDeployment → Deployment\n    ├── PodSpec.TerminationGracePeriodSeconds ← spec.terminationGracePeriodSeconds or 30\n    ├── Container.Lifecycle.PreStop.Exec ← ['/bin/sh','-c','sleep '+spec.preStopSleepSeconds or 5]\n    ├── Container.Command ← uwsgiCommand(spec.uwsgi) with --harakiri/--http-keepalive-timeout appended when set\n    └── Spec.Strategy ← spec.strategy verbatim OR RollingUpdate{MaxUnavailable:0, MaxSurge:1}\n\nArchitecture diagram (ASCII):\n  ┌──────────────┐    ┌──────────────────┐    ┌──────────────────────┐\n  │ Keystone CR  │──▶ │ Validate webhook │──▶ │ buildKeystoneDeploy- │\n  │ (spec)       │    │ (REQ-7/8/12)     │    │ ment (REQ-5/9/10/11) │\n  └──────────────┘    └──────────────────┘    └──────────┬───────────┘\n                                                         ▼\n                                                 appsv1.Deployment\n                                                 ├── Spec.Strategy\n                                                 ├── PodSpec.TGPS\n                                                 ├── Container.Lifecycle.PreStop\n                                                 └── Container.Command (uwsgi)\n\nPotential pitfalls:\n  - Do NOT set spec.terminationGracePeriodSeconds or preStopSleepSeconds in Default(). Keeping pointer nil on stored CRs preserves backwards compatibility and avoids churn for every existing CR on operator upgrade.\n  - Webhook cross-field checks must resolve nil→default BEFORE applying the inequality (REQ-007, REQ-008). Skipping this would make nil values silently bypass the rule.\n  - When spec.strategy is provided, DO NOT merge with defaults. Operators who set maxUnavailable=50% expect exactly 50%, not 50% merged with surge=1.\n  - preStopSleepSeconds=0 still produces a Lifecycle block so EnsureDeployment doesn't detect spurious drift when toggling back and forth.\n\nPatterns to follow:\n  - UWSGI fields: keystone_webhook.go:274-292 (defense-in-depth) and keystone_webhook.go:86-100 (documentation of nil vs zero).\n  - Optional pointer fields: TrustFlushSpec and Autoscaling (keystone_types.go:78, 107).\n  - Defaulting pointer to literal: ptr.To(int64(30)) pattern already used in reconcile_deployment.go:138.\n  - Table-driven tests: see existing reconcile_deployment_test.go style.\n  - E2E chainsaw structure: tests/e2e/keystone/graceful-shutdown/ and tests/e2e/keystone/image-upgrade/ provide the template for the new rolling-update-zero-downtime test.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_types_test.go\" test-function=\"TestKeystoneSpecTerminationGracePeriodSecondsField\" requirement-id=\"REQ-001\">[unit] field is *int64 with kubebuilder marker Minimum=10, default 30; JSON tag omitempty</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_TerminationGracePeriodBelowMinRejected\" requirement-id=\"REQ-001\">[unit] webhook rejects terminationGracePeriodSeconds=5 with Invalid error citing minimum 10</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_TerminationGracePeriodDefault\" requirement-id=\"REQ-001\">[unit] nil terminationGracePeriodSeconds yields PodSpec.TerminationGracePeriodSeconds=30</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_TerminationGracePeriodOverride\" requirement-id=\"REQ-001\">[unit] terminationGracePeriodSeconds=120 propagates to PodSpec.TerminationGracePeriodSeconds=120</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PreStopSleepDefault\" requirement-id=\"REQ-002\">[unit] nil preStopSleepSeconds yields preStop ['/bin/sh','-c','sleep 5']</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PreStopSleepOverride\" requirement-id=\"REQ-002\">[unit] preStopSleepSeconds=15 yields preStop ['/bin/sh','-c','sleep 15']</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PreStopSleepZero\" requirement-id=\"REQ-002\">[unit] preStopSleepSeconds=0 yields preStop ['/bin/sh','-c','sleep 0'] and Lifecycle still emitted</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestUwsgiCommand_HarakiriUnsetOmitsFlag\" requirement-id=\"REQ-003\">[unit] nil harakiri keeps command free of --harakiri</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestUwsgiCommand_HarakiriSetEmitsFlag\" requirement-id=\"REQ-003\">[unit] harakiri=20 emits '--harakiri','20' in the command slice</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_UWSGIHarakiriBelowMinRejected\" requirement-id=\"REQ-003\">[unit] harakiri=0 rejected with minimum 1</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestUwsgiCommand_KeepAliveTimeoutUnsetOmitsFlag\" requirement-id=\"REQ-004\">[unit] nil httpKeepAliveTimeout keeps command free of the flag</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestUwsgiCommand_KeepAliveTimeoutSetEmitsFlag\" requirement-id=\"REQ-004\">[unit] httpKeepAliveTimeout=5 emits '--http-keepalive-timeout','5'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_UWSGIKeepAliveTimeoutBelowMinRejected\" requirement-id=\"REQ-004\">[unit] httpKeepAliveTimeout=0 rejected with minimum 1</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_DefaultRollingUpdateStrategy\" requirement-id=\"REQ-005\">[unit] no spec.strategy yields RollingUpdate with MaxUnavailable=0, MaxSurge=1 (IntOrString Type=Int)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_StrategyStableAcrossReconciles\" requirement-id=\"REQ-005\">[unit] calling buildKeystoneDeployment twice produces deep-equal Spec.Strategy objects (no time-based drift)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_StrategyOverrideRollingCustomPercents\" requirement-id=\"REQ-006\">[unit] spec.strategy with RollingUpdate 50%/0 propagates unchanged, defaults not merged</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_StrategyOverrideRecreate\" requirement-id=\"REQ-006\">[unit] spec.strategy.Type=Recreate yields Deployment Spec.Strategy.Type=Recreate with nil RollingUpdate</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_StrategyRecreateWithRollingUpdateBlockRejected\" requirement-id=\"REQ-006\">[unit] webhook rejects Type=Recreate together with non-nil RollingUpdate block</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_PreStopEqualsGraceRejected\" requirement-id=\"REQ-007\">[unit] preStopSleepSeconds=30 with terminationGracePeriodSeconds=30 rejected with drain-window message</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_PreStopExceedsGraceRejected\" requirement-id=\"REQ-007\">[unit] preStopSleepSeconds=40 with terminationGracePeriodSeconds=30 rejected</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_PreStopStrictlyLessAccepted\" requirement-id=\"REQ-007\">[unit] preStopSleepSeconds=5 with terminationGracePeriodSeconds=30 accepted</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_HarakiriAtDrainBoundaryRejected\" requirement-id=\"REQ-008\">[unit] harakiri=25, grace=30, preStop=5 rejected</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_HarakiriAboveDrainRejected\" requirement-id=\"REQ-008\">[unit] harakiri=60, grace=30, preStop=5 rejected</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_HarakiriWithinDrainAccepted\" requirement-id=\"REQ-008\">[unit] harakiri=20, grace=30, preStop=5 accepted — resolves defaults when nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PreStopDerivedFromSpec\" requirement-id=\"REQ-009\">[unit] table-driven: nil→'sleep 5', 0→'sleep 0', 15→'sleep 15'</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_GracePeriodFromSpec\" requirement-id=\"REQ-010\">[unit] table-driven: nil→30, 60→60, 120→120</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestUwsgiCommand_FlagOrderDeterministic\" requirement-id=\"REQ-011\">[unit] harakiri=20, httpKeepAliveTimeout=5 produces deterministic '--harakiri','20','--http-keepalive-timeout','5' order after existing flags</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestUwsgiCommand_KeepAliveTimeoutIgnoredWhenKeepAliveDisabled\" requirement-id=\"REQ-011\">[unit] httpKeepAlive=false, httpKeepAliveTimeout=5 omits both flags from command</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_KeepAliveTimeoutWithoutKeepAliveRejected\" requirement-id=\"REQ-012\">[unit] httpKeepAlive=false, httpKeepAliveTimeout=5 rejected at field path spec.uwsgi.httpKeepAliveTimeout</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_KeepAliveTimeoutWithKeepAliveAccepted\" requirement-id=\"REQ-012\">[unit] httpKeepAlive=true, httpKeepAliveTimeout=5 accepted</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_TerminationGracePeriodAppliedToDeployment\" requirement-id=\"REQ-001\">[integration] envtest: create Keystone with grace=60, expect Deployment.Spec.Template.Spec.TerminationGracePeriodSeconds=60</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_DefaultStrategyAppliedToDeployment\" requirement-id=\"REQ-005\">[integration] envtest: default Keystone produces Deployment with RollingUpdate 0/1 in cluster</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_StrategyOverrideAppliedToDeployment\" requirement-id=\"REQ-006\">[integration] envtest: spec.strategy=Recreate yields Deployment.Spec.Strategy.Type=Recreate in cluster</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml\" test-function=\"TestCRDRegeneration_HelmContainsNewFields\" requirement-id=\"REQ-013\">[integration] regenerated CRD yaml contains properties for terminationGracePeriodSeconds, preStopSleepSeconds, strategy, uwsgi.harakiri, uwsgi.httpKeepAliveTimeout with correct minimums</spec>\n    <spec test-file=\"operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml\" test-function=\"TestCRDRegeneration_ConfigBaseContainsNewFields\" requirement-id=\"REQ-013\">[integration] config/crd/bases CRD yaml contains the same new fields</spec>\n    <spec test-file=\"tests/e2e/keystone/rolling-update-zero-downtime/chainsaw-test.yaml\" test-function=\"keystone-rolling-update-zero-downtime\" requirement-id=\"REQ-014\">[e2e] chainsaw: load generator sends 200+ requests over 60 s during spec.image.tag patch, asserts 0 5xx and 0 connection resets</spec>\n    <spec test-file=\"tests/e2e/keystone/rolling-update-zero-downtime/chainsaw-test.yaml\" test-function=\"keystone-rolling-update-availability\" requirement-id=\"REQ-014\">[e2e] chainsaw: assert Deployment.status.availableReplicas never drops below spec.replicas during rollout</spec>\n    <spec test-file=\"architecture/docs/04-architecture/04-high-availability.md\" test-function=\"doc-lint-rollout-flow-section\" requirement-id=\"REQ-015\">[integration] doc linter verifies a 'Keystone rolling update' section exists with sequence diagram and CC-0084 references</spec>\n    <spec test-file=\"docs/reference/keystone-crd.md\" test-function=\"doc-lint-new-crd-fields\" requirement-id=\"REQ-016\">[integration] doc linter verifies entries for the five new fields with types/defaults/minimums and CC-0084 references</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator tunes termination grace period for slow LDAP/DB token validation\">As a OpenStack operator, I want to set spec.terminationGracePeriodSeconds on a Keystone CR so I can extend the drain window beyond the hardcoded 30 s, so that Keystone pods with slow upstream token validation finish in-flight requests before SIGKILL during rolling updates (CC-0084)</story>\n    <story title=\"Operator tunes preStop sleep to cover slow EndpointSlice propagation\">As a OpenStack operator, I want to set spec.preStopSleepSeconds independently of the grace period, so that clusters with slow kube-proxy/EndpointSlice propagation get a longer pre-drain window without extending the overall grace period (CC-0084)</story>\n    <story title=\"Operator enables uWSGI harakiri to bound per-request shutdown time\">As a OpenStack operator, I want to set spec.uwsgi.harakiri so a hung request cannot block graceful shutdown beyond a known bound, so that a single stuck LDAP lookup does not prevent other in-flight requests on the same pod from completing cleanly (CC-0084)</story>\n    <story title=\"Operator configures HTTP keep-alive timeout to force reconnection after pod removal\">As a OpenStack operator, I want to set spec.uwsgi.httpKeepAliveTimeout so idle keep-alive connections expire promptly, so that clients reconnect via the Service and are routed to healthy pods instead of reusing a socket to a terminating pod (CC-0084)</story>\n    <story title=\"Operator gets zero-downtime rolling updates out of the box\">As a OpenStack operator, I want buildKeystoneDeployment to set an explicit RollingUpdate strategy with maxUnavailable=0 and maxSurge=1 by default, so that image tag patches to a multi-replica Keystone never dip available capacity below the configured replica count (CC-0084)</story>\n    <story title=\"Operator can fully override the rollout strategy when site policy requires it\">As a OpenStack operator, I want to set spec.strategy to an arbitrary valid appsv1.DeploymentStrategy, so that I can switch to Recreate or customize maxSurge/maxUnavailable percentages for specific clusters without forking the operator (CC-0084)</story>\n    <story title=\"SRE verifies zero failed requests during image-tag rollout\">As a SRE, I want an end-to-end test that runs a steady HTTP load against the Keystone Service during an image-tag rolling update, so that regressions in the graceful-termination chain (strategy, preStop, harakiri, keep-alive) are caught in CI (CC-0084)</story>\n    <story title=\"Operator reads authoritative tunable documentation\">As a OpenStack operator, I want architecture/docs/04-architecture/04-high-availability.md to document the rollout flow and the new tunables, so that I can pick recommended values for terminationGracePeriodSeconds, preStopSleepSeconds, harakiri, and httpKeepAliveTimeout without reading operator source (CC-0084)</story>\n    <story title=\"Developer updates the keystone-crd reference after schema change\">As a developer consuming the CRD, I want docs/reference/keystone-crd.md to describe the new KeystoneSpec and UWSGISpec fields, so that the generated reference documentation stays consistent with the CRD schema (CC-0084)</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The KeystoneSpec SHALL expose terminationGracePeriodSeconds as an optional *int64 field with default 30 and minimum 10</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The KeystoneSpec SHALL expose preStopSleepSeconds as an optional *int64 field with default 5 and minimum 0</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The UWSGISpec SHALL expose harakiri as an optional *int32 field (no default) with minimum 1</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The UWSGISpec SHALL expose httpKeepAliveTimeout as an optional *int32 field (no default) with minimum 1</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">buildKeystoneDeployment SHALL set Spec.Strategy to RollingUpdate with maxUnavailable=0 and maxSurge=1 by default</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The KeystoneSpec SHALL expose strategy as an optional *appsv1.DeploymentStrategy field that fully overrides the defaults</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The validating webhook SHALL reject preStopSleepSeconds >= terminationGracePeriodSeconds</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The validating webhook SHALL reject spec.uwsgi.harakiri >= (terminationGracePeriodSeconds - preStopSleepSeconds)</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">buildKeystoneDeployment SHALL derive the preStop exec command from the effective preStopSleepSeconds value</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">buildKeystoneDeployment SHALL pass the effective terminationGracePeriodSeconds to the PodSpec</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">uwsgiCommand SHALL append --harakiri <n> and --http-keepalive-timeout <n> in a deterministic order after existing flags</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">The validating webhook SHALL reject spec.uwsgi.httpKeepAliveTimeout when spec.uwsgi.httpKeepAlive is explicitly false</requirement>\n    <requirement id=\"REQ-013\" priority=\"SHALL\">CRD manifests in both helm and config/crd/bases SHALL be regenerated so the OpenAPI schema matches the Go types</requirement>\n    <requirement id=\"REQ-014\" priority=\"SHALL\">The E2E chainsaw test SHALL verify zero 5xx/connection-reset responses during a rolling image-tag patch</requirement>\n    <requirement id=\"REQ-015\" priority=\"SHALL\">The high-availability architecture doc SHALL document the Keystone rollout flow and the new tunables</requirement>\n    <requirement id=\"REQ-016\" priority=\"SHALL\">The keystone-crd reference doc SHALL describe every new field with type, default, minimum, and effect</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 35048,
  "caller": "run_claude_cli"
}