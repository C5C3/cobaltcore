{
  "timestamp": "2026-06-04T21:08:44.440666+00:00",
  "feature_id": "CC-0111",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0111",
  "model": "claude-opus-4-8",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0111\">\n  <title>Promote keystone GatewaySpec into commonv1 for shared use</title>\n  <description># GitHub Issue #403: refactor(common): promote the keystone GatewaySpec into commonv1 so both operators share one type (Option B follow-up to #402)\n\n## Context\n\nWhen exposing the ControlPlane-projected Keystone through the shared Gateway\n(#402), the c5c3 **api** package could not reuse the keystone operator's\n`GatewaySpec` because the L1 api package intentionally has **no dependency** on\nthe keystone module (the DECISION recorded on `ServiceKeystoneSpec` in\n`operators/c5c3/api/v1alpha1/controlplane_types.go`). #402 therefore landed\n**Option A**: a curated, c5c3-local copy of the gateway shape\n(`GatewaySpec` + `GatewayParentRefSpec`) that the L2 reconciler maps onto\n`keystonev1alpha1.GatewaySpec`.\n\nThat curated copy is a deliberate, documented duplicate. This issue tracks the\n**Option B** consolidation: promote the shared gateway shape into\n`internal/common/types` (`commonv1`) so both operators reference **one** type and\nthe duplicate disappears.\n\n## Why do this\n\n- **Single source of truth.** Today `GatewaySpec`/`GatewayParentRefSpec` exist\n  twice — `operators/keystone/api/v1alpha1/keystone_types.go` and (since #402)\n  `operators/c5c3/api/v1alpha1/controlplane_types.go`. Field drift between the\n  two (a new `path` default, an added annotation knob, validation markers) would\n  be silent until the L2 mapping in `reconcile_keystone.go` breaks.\n- **Mirrors the existing commonv1 discipline.** `DatabaseSpec`, `CacheSpec`,\n  `ImageSpec`, `PolicySpec`, `SecretRefSpec` already live in `commonv1` and are\n  shared by both the keystone and c5c3 CRDs for exactly this reason. `GatewaySpec`\n  is the odd one out.\n- **Scales to more services.** As `ServicesSpec` grows beyond Keystone, every\n  service that wants external exposure would otherwise re-curate the same shape.\n\n## Why it was deferred (and is its own issue)\n\nIt is heavier than the #402 quick-start goal warranted and touches **two**\noperators' CRDs:\n\n- Moving a type **out** of `keystonev1alpha1` is a breaking change for any\n  external importer of `keystonev1alpha1.GatewaySpec` unless a type alias is left\n  behind.\n- It regenerates and re-syncs CRDs for **both** keystone and c5c3 (CRD drift\n  across two operators — the `check-crd-drift` discipline).\n- It needs `generate-common` (deepcopy for the new `commonv1` types) plus SPDX\n  headers on any new file (the `check-spdx-reuse` discipline).\n\n## Plan\n\n### 1. Move the shape into `commonv1`\n- [ ] Add `GatewaySpec` + `GatewayParentRefSpec` to `internal/common/types/types.go`\n      (the canonical shape: `parentRef`, `hostname`, `path`, `annotations` /\n      `name`, `namespace`, `sectionName`), carrying the `+kubebuilder` validation\n      markers (`hostname` / `parentRef.name` MinLength=1).\n- [ ] `make generate-common` to produce the `commonv1` deepcopy methods (SPDX\n      header via the boilerplate).\n\n### 2. Repoint the keystone operator\n- [ ] Replace `keystonev1alpha1.GatewaySpec` / `GatewayParentRefSpec` with the\n      `commonv1` types in `keystone_types.go` (`KeystoneSpec.Gateway` →\n      `*commonv1.GatewaySpec`), the gateway reconciler / HTTPRoute builder, and the\n      webhook validation.\n- [ ] **Backward-compat decision:** leave a type alias\n      `type GatewaySpec = commonv1.GatewaySpec` (and parentRef) in\n      `keystonev1alpha1` to avoid breaking external importers, **or** do a clean\n      move and document the break. Recommend the alias.\n\n### 3. Repoint the c5c3 operator (remove the Option A duplicate)\n- [ ] Delete the curated `GatewaySpec` / `GatewayParentRefSpec` from\n      `operators/c5c3/api/v1alpha1/controlplane_types.go`; change\n      `ServiceKeystoneSpec.Gateway` to `*commonv1.GatewaySpec`.\n- [ ] Simplify the mapping in `reconcile_keystone.go`: with one shared type the\n      reconciler can assign the gateway through (still distinct from\n      `keystone.Spec.Gateway` only if keystone keeps its alias) — collapse the\n      field-by-field copy.\n\n### 4. Regenerate + sync (both operators)\n- [ ] `make generate manifests sync-crds` and verify `make verify-crd-sync` —\n      the c5c3 and keystone CRD `bases/` + helm `crds/` stay in lockstep. The CRD\n      **schema** for the gateway block should be byte-stable; only the Go source of\n      truth moves.\n\n### 5. Docs + tests\n- [ ] Point both CRD references (`docs/reference/c5c3/controlplane-crd.md`,\n      `docs/reference/keystone/keystone-crd.md`) at the shared `commonv1.GatewaySpec`.\n- [ ] Keystone gateway tests + c5c3 projection tests adjust to the shared type;\n      behaviour must be unchanged.\n\n## Acceptance criteria\n- One `GatewaySpec` definition (in `commonv1`); no curated copy in the c5c3 api\n  package; keystone either aliases or uses it directly.\n- `make verify-crd-sync` green for both operators; gateway CRD schema unchanged.\n- All existing keystone + c5c3 unit/envtest tests pass; the #402 external-exposure\n  behaviour is preserved.\n\n## Out of scope\n- Any change to external-exposure *behaviour* — this is a pure type-consolidation\n  refactor. The user-visible gateway/publicEndpoint semantics shipped in #402.\n\n## Relationship\n- Follow-up to #402 (which implemented Option A — the curated local copy). #402's\n  \"Design decision\" section flagged this as the eventual consolidation.\n\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/403</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add GatewaySpec + GatewayParentRefSpec to internal/common/types/types.go (REQ-001). Copy the canonical shape (parentRef, hostname, path, annotations / name, namespace, sectionName) with operator-neutral godoc and the +kubebuilder:validation:MinLength=1 markers on Hostname and ParentRef.Name. Add to the existing file (no new file) so the SPDX header already covers it.</task>\n    <task status=\"pending\">1.2 Run `make generate-common` and verify GatewaySpec/GatewayParentRefSpec DeepCopy funcs land in internal/common/types/zz_generated.deepcopy.go (REQ-001). Add TestGatewaySpec_DeepCopy + nil-Annotations case to internal/common/types/types_test.go mirroring TestDatabaseTLSSpec_DeepCopy.</task>\n    <task status=\"pending\">2.1 In operators/keystone/api/v1alpha1/keystone_types.go replace the GatewaySpec/GatewayParentRefSpec struct definitions with `type GatewaySpec = commonv1.GatewaySpec` and `type GatewayParentRefSpec = commonv1.GatewayParentRefSpec` (REQ-002). Keep the KeystoneSpec.Gateway field godoc (CC-0065) verbatim on the field so the CRD property description is unchanged.</task>\n    <task status=\"pending\">2.2 Run `make generate` for keystone; confirm keystone zz_generated.deepcopy.go drops the standalone GatewaySpec/GatewayParentRefSpec DeepCopy funcs and KeystoneSpec.DeepCopyInto references types.GatewaySpec (REQ-002). Build the keystone module and run its existing gateway unit/envtest tests unmodified to confirm reconcile_httproute.go, reconcile_networkpolicy.go and keystone_webhook.go are unaffected.</task>\n    <task status=\"pending\">3.1 In operators/c5c3/api/v1alpha1/controlplane_types.go delete the curated GatewaySpec/GatewayParentRefSpec definitions and change ServiceKeystoneSpec.Gateway to `*commonv1.GatewaySpec` (REQ-003). Keep the field godoc; replace the Option A DECISION comment with a one-line CC-0111 note recording the consolidation.</task>\n    <task status=\"pending\">3.2 Collapse the L2 mapping in operators/c5c3/internal/controller/reconcile_keystone.go (REQ-003): replace the field-by-field copy with `keystone.Spec.Gateway = cp.Spec.Services.Keystone.Gateway.DeepCopy()` (nil clears it). Leave a `# DECISION:` style comment justifying DeepCopy over direct pointer share. keystonePublicEndpoint is unchanged.</task>\n    <task status=\"pending\">3.3 Repoint c5c3 tests to commonv1 (REQ-003, REQ-006): in reconcile_keystone_test.go and reconcile_korc_test.go swap c5c3v1alpha1.GatewaySpec/GatewayParentRefSpec for commonv1 equivalents; in controlplane_types_test.go extend TestControlPlaneSpecReusesCommonTypes with the gateway compile-time type assertion. Update any 'curated' wording in test comments to 'shared'.</task>\n    <task status=\"pending\">3.4 Run `make generate` for c5c3; confirm c5c3 zz_generated.deepcopy.go drops the curated GatewaySpec/GatewayParentRefSpec DeepCopy funcs and ServiceKeystoneSpec.DeepCopyInto references types.GatewaySpec (REQ-003). Build the c5c3 module and run its unit tests.</task>\n    <task status=\"pending\">3.5 Run `go mod tidy` in the c5c3 module and confirm the L1 api package (operators/c5c3/api/...) still imports only commonv1/apimachinery/controller-runtime — no operators/keystone import introduced at L1 (REQ-003, REQ-008). Confirm no go.mod require/version changes (check-go-workspace-deps stays green).</task>\n    <task status=\"pending\">4.1 Run `make manifests sync-crds` then `make verify-crd-sync` for both operators (REQ-004). Review the gateway-block diff in both bases/ + helm crds/: assert the validation schema (minLength:1, required arrays, types) is byte-stable and the only change is nested property descriptions converging to the commonv1 godoc. Commit the regenerated + synced CRDs.</task>\n    <task status=\"pending\">4.2 Validate fixtures against the regenerated CRDs (REQ-009): run the existing invalid-cr / e2e gateway fixtures through check-fixture-drift (or kubectl apply --dry-run=server with the regenerated keystone CRD) to confirm tests/e2e/keystone/httproute and gateway-quick-start fixtures still validate.</task>\n    <task status=\"pending\">5.1 Update docs/reference/keystone/keystone-crd.md (REQ-007): present GatewaySpec/GatewayParentRefSpec as shared commonv1 types (fold into or link from the existing 'Shared Types (from internal/common/types)' section), keeping the #gatewayspec / #gatewayparentrefspec anchors intact so c5c3 cross-links resolve. Cite CC-0111.</task>\n    <task status=\"pending\">5.2 Update docs/reference/c5c3/controlplane-crd.md (REQ-007): repoint the GatewaySpec section and the ServiceKeystoneSpec.gateway field row at the shared commonv1.GatewaySpec; remove the 'curated local subset that mirrors keystone field-for-field' framing. Cite CC-0111.</task>\n    <task status=\"pending\">6.1 Full verification sweep (REQ-006, REQ-008): run go test ./... for internal/common, keystone, and c5c3 modules; run check-spdx-reuse, check-crd-drift, check-doc-drift, and check-go-workspace-deps. Confirm all green and that the #402 external-exposure behaviour is preserved end-to-end.</task>\n\n  </tasks>\n  <implementation-notes>PURE TYPE-CONSOLIDATION REFACTOR (CC-0111, GitHub #403) — Option B follow-up to CC-0110/#402. No user-visible behaviour change.\n\nARCHITECTURE / DATA FLOW (3 modules, ASCII):\n\n  internal/common/types (commonv1)  <-- single source of truth\n        ^                 ^\n        | alias           | *commonv1.GatewaySpec (field)\n  keystone/api/v1alpha1   c5c3/api/v1alpha1\n  GatewaySpec=commonv1.*  ServiceKeystoneSpec.Gateway *commonv1.GatewaySpec\n        ^                       |\n        | *commonv1.GatewaySpec | DeepCopy()\n  KeystoneSpec.Gateway  <-------+  (L2 reconcile_keystone.go projects cp.gateway -> keystone.gateway)\n\nData flow at reconcile: ControlPlane.Spec.Services.Keystone.Gateway (*commonv1.GatewaySpec) -> reconcileKeystone DeepCopy -> Keystone.Spec.Gateway (*commonv1.GatewaySpec, via keystone alias) -> keystone-operator buildKeystoneHTTPRoute -> HTTPRoute. With one shared type the field-by-field copy collapses to one DeepCopy assignment.\n\nKEY DECISION 1 (alias for keystone, per #403 recommendation): use `type GatewaySpec = commonv1.GatewaySpec` (and parentRef). An alias is a transparent compile-time identity — every in-package literal (keystone_webhook_test.go uses GatewaySpec{}), every qualified reference (keystonev1alpha1.GatewaySpec in keystone controller tests and historically in c5c3), and every field access (keystone.Spec.Gateway.Hostname in reconcile_httproute.go / reconcile_networkpolicy.go / keystone_webhook.go) keeps compiling with ZERO edits. This is why keystone needs no test or reconciler changes. The alias also preserves backward-compat for any external importer.\n\nKEY DECISION 2 (clean delete for c5c3): the c5c3 curated copy was always a documented temporary duplicate (Option A). Delete it outright; the only in-repo consumers are c5c3's own reconciler + tests, which we update to commonv1. Do NOT add a keystone import to the c5c3 L1 api package — that invariant (CC-0110 plan decision #3) is the whole reason the curated copy existed.\n\nKEY DECISION 3 (CRD descriptions WILL change, schema will NOT): controller-gen reads +kubebuilder markers from imported packages (proven: commonv1.DatabaseTLSSpec.Mode enum already renders into the keystone CRD), so the MinLength markers render identically once moved. BUT the nested gateway property DESCRIPTIONS (hostname/path/parentRef/annotations) currently differ between keystone and c5c3 godocs; after both reference commonv1 they CONVERGE to the single canonical commonv1 godoc. So expect a description-only diff in BOTH CRD bases + helm crds/. The top-level `gateway` property description comes from the FIELD godoc (KeystoneSpec.Gateway / ServiceKeystoneSpec.Gateway), which stays put — keep those field godocs. The acceptance criterion 'gateway CRD schema unchanged' means the VALIDATION schema (types/minLength/required), which is byte-stable; descriptions are non-functional. Write the canonical commonv1 godoc operator-neutrally (no 'Keystone API' / 'projected Keystone' wording) so it reads correctly in both CRDs.\n\nCANONICAL commonv1 GODOC (suggested, operator-neutral): GatewaySpec — 'configures the Gateway API HTTPRoute used to expose a service externally. The operator plays the application-developer role: it manages only the HTTPRoute; the referenced Gateway (and GatewayClass) are platform-team infrastructure provisioned outside the operator.' Fields: ParentRef ('identifies the Gateway the HTTPRoute attaches to'), Hostname ('externally reachable host (SNI / Host header) matched by the HTTPRoute; required', MinLength=1), Path ('URL path prefix matched by the HTTPRoute; the reconciler defaults it to \"/\" when empty'), Annotations ('passed through to the HTTPRoute metadata verbatim for implementation-specific config (rate limits, timeouts, CORS)'). GatewayParentRefSpec: Name (required, MinLength=1), Namespace (optional; empty = the consuming CR's namespace; cross-namespace needs a ReferenceGrant), SectionName (optional listener target).\n\nGENERATION ORDER (Makefile, OPERATORS='keystone c5c3'): make generate-common (commonv1 deepcopy) -> make generate (both operators' deepcopy) -> make manifests (both CRDs + webhook) -> make sync-crds (copy bases/ -> helm crds/ with header) -> make verify-crd-sync (diff ignoring ^#). Run from repo root.\n\nPITFALLS: (1) Don't create a new file in commonv1 for these types unless you add the SPDX boilerplate header — adding to types.go is simpler and header-free. (2) After the keystone alias, DON'T hand-edit zz_generated.deepcopy.go — let make generate remove the now-duplicate funcs. (3) Don't share the Annotations map by pointer-assigning cp.gateway directly without DeepCopy in the mutate closure — DeepCopy keeps the child object's map independent of the parent CR. (4) Remember c5c3 has THREE CRDs in bases/ (controlplanes, credentialrotations, secretaggregates) — only controlplanes changes; verify-crd-sync covers all. (5) CI already runs verify-crd-sync / spdx / drift checks — no new GitHub Actions workflow is needed (no new test tier is introduced); do not add a CI task.\n\nTEST TIERS PRESENT: unit (_test.go alongside packages, incl. commonv1 types_test.go), integration (envtest, keystone integration_test.go), e2e (Chainsaw under tests/e2e/keystone). This refactor adds one unit test (commonv1 deepcopy) + one compile-time assertion (c5c3), repoints c5c3 unit tests, and relies on the unchanged keystone unit/integration + e2e fixtures to prove behaviour is preserved.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"internal/common/types/types_test.go\" test-function=\"TestGatewaySpec_DeepCopy\" requirement-id=\"REQ-005\">[unit] DeepCopy of a populated *GatewaySpec returns a new, value-equal pointer; mutating clone.Annotations / clone.ParentRef does not affect the original; DeepCopy of a nil *GatewaySpec returns nil. Mirrors TestDatabaseTLSSpec_DeepCopy.</spec>\n    <spec test-file=\"internal/common/types/types_test.go\" test-function=\"TestGatewaySpec_DeepCopy_NilAnnotations\" requirement-id=\"REQ-005\">[unit] DeepCopy of a GatewaySpec whose Annotations is nil produces a clone whose Annotations is nil (no spurious empty-map allocation).</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_GatewayValid (existing) + TestValidate_GatewayEmptyHostname / TestValidate_GatewayEmptyParentRefName (existing)\" requirement-id=\"REQ-002\">[unit] These pre-existing tests, which construct GatewaySpec{}/GatewayParentRefSpec{} literals in-package, MUST continue to compile and pass unmodified — proving the type alias is transparent to in-package literals.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"existing HTTPRoute build/lifecycle tests\" requirement-id=\"REQ-006\">[unit/integration] The HTTPRoute built from spec.gateway (parentRef, hostname, path default \"/\", annotations passthrough) is unchanged after the alias swap; all existing assertions pass without edits.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"existing envtest gateway scenarios\" requirement-id=\"REQ-006\">[integration] envtest applies a Keystone CR with spec.gateway against the regenerated CRD and the HTTPRoute reconcile + status.endpoint derivation behave identically to pre-refactor.</spec>\n    <spec test-file=\"operators/c5c3/api/v1alpha1/controlplane_types_test.go\" test-function=\"TestControlPlaneSpecReusesCommonTypes (extend)\" requirement-id=\"REQ-003\">[unit] Add a compile-time assertion `spec.Services.Keystone.Gateway = &commonv1.GatewaySpec{ParentRef: commonv1.GatewayParentRefSpec{Name: \"gw\"}, Hostname: \"h\"}` — fails to compile if a local GatewaySpec is reintroduced.</spec>\n    <spec test-file=\"operators/c5c3/internal/controller/reconcile_keystone_test.go\" test-function=\"TestReconcileKeystone_GatewayProjection (repoint)\" requirement-id=\"REQ-006\">[unit] Construct the source gateway as commonv1.GatewaySpec/GatewayParentRefSpec; assert the projected Keystone CR's gateway fields (parentRef name/namespace/sectionName, hostname, path, annotations) and bootstrap.publicEndpoint match exactly — behaviour unchanged.</spec>\n    <spec test-file=\"operators/c5c3/internal/controller/reconcile_keystone_test.go\" test-function=\"TestReconcileKeystone_GatewayNilStaysInCluster (repoint)\" requirement-id=\"REQ-006\">[unit] With a nil gateway, the projected Keystone CR has Spec.Gateway == nil and an empty bootstrap.publicEndpoint — the collapsed mapping still clears the field.</spec>\n    <spec test-file=\"operators/c5c3/internal/controller/reconcile_keystone_test.go\" test-function=\"TestReconcileKeystone_PublicEndpointDerivedFromHostname (repoint)\" requirement-id=\"REQ-006\">[unit] With gateway set and publicEndpoint empty, the projected keystone bootstrap.publicEndpoint is derived as https://{hostname}/v3 — derivation logic unaffected by the type swap.</spec>\n    <spec test-file=\"operators/c5c3/internal/controller/reconcile_korc_test.go\" test-function=\"K-ORC catalog gateway test (repoint, ~line 814)\" requirement-id=\"REQ-006\">[unit] Repoint c5c3v1alpha1.GatewaySpec/GatewayParentRefSpec to commonv1; assert the K-ORC identity catalog URL still uses the external publicEndpoint when Keystone is exposed via a Gateway.</spec>\n    <spec test-file=\"Makefile target: make verify-crd-sync\" test-function=\"verify-crd-sync (CI gate)\" requirement-id=\"REQ-004\">[integration] After make generate manifests sync-crds, verify-crd-sync exits 0 for keystone and c5c3; the gateway block validation schema (minLength/required/types) is unchanged and only nested descriptions differ.</spec>\n    <spec test-file=\"tests/e2e/keystone/httproute + gateway-quick-start fixtures\" test-function=\"check-fixture-drift / kubectl apply --dry-run=server against regenerated CRD\" requirement-id=\"REQ-009\">[e2e] Existing keystone gateway CR fixtures validate against the regenerated keystone CRD (structural schema byte-stable), confirming no fixture drift.</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"commonv1 owns the single GatewaySpec definition\">As a internal/common maintainer, I want GatewaySpec and GatewayParentRefSpec to live once in internal/common/types alongside DatabaseSpec/CacheSpec/PolicySpec, with deepcopy generated, so that the gateway shape follows the same single-source-of-truth discipline as the other shared CRD shapes and cannot drift between operators</story>\n    <story title=\"Keystone operator references the shared type without behaviour change\">As a keystone operator maintainer, I want keystonev1alpha1.GatewaySpec / GatewayParentRefSpec to become type aliases to the commonv1 types so KeystoneSpec.Gateway is *commonv1.GatewaySpec, so that existing importers (in-repo and external) keep compiling while the duplicate definition is removed</story>\n    <story title=\"c5c3 operator drops the Option A curated copy\">As a c5c3 operator maintainer, I want the curated GatewaySpec/GatewayParentRefSpec deleted from controlplane_types.go and ServiceKeystoneSpec.Gateway changed to *commonv1.GatewaySpec, with the L2 mapping collapsed, so that the documented temporary duplicate disappears and the projection becomes a one-line assignment</story>\n    <story title=\"CRD schema stays valid and in sync across both operators\">As a platform operator applying CRs, I want the gateway validation schema (types, minLength, required) to be byte-stable and both operators' bases/ + helm crds/ to stay in lockstep, so that no Keystone or ControlPlane CR is rejected and clusters do not see schema regressions after the refactor</story>\n    <story title=\"Documentation points at the shared commonv1 type\">As a documentation reader / contributor, I want both CRD reference docs to describe GatewaySpec as a shared commonv1 type rather than a curated/per-operator copy, so that readers find one canonical gateway type description and cross-references resolve correctly</story>\n    <story title=\"External Go importer is not broken by the move\">As a downstream consumer importing keystonev1alpha1, I want keystonev1alpha1.GatewaySpec and keystonev1alpha1.GatewayParentRefSpec to remain referenceable symbols after the move, so that code that imported the keystone gateway types continues to compile without edits</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">commonv1 SHALL define GatewaySpec and GatewayParentRefSpec as the single canonical gateway shape, carrying the existing validation markers, with controller-gen deepcopy generated.</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The keystone operator SHALL reference the commonv1 gateway types via type aliases, preserving all current reconciler/webhook behaviour and external-symbol compatibility.</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The c5c3 operator SHALL delete the Option A curated GatewaySpec/GatewayParentRefSpec, change ServiceKeystoneSpec.Gateway to *commonv1.GatewaySpec, and collapse the L2 projection mapping.</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The gateway CRD validation schema SHALL remain byte-stable for both operators, and both bases/ + helm crds/ SHALL stay in sync.</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The shared commonv1 GatewaySpec SHALL have a deepcopy unit test proving independent (non-aliased) copies, mirroring the existing DatabaseTLSSpec test.</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">All existing keystone and c5c3 unit/envtest tests SHALL pass after the refactor and the #402 external-exposure behaviour SHALL be preserved.</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">Both CRD reference docs SHALL point at the shared commonv1.GatewaySpec instead of describing per-operator copies.</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The Go workspace dependency lockstep and REUSE/SPDX state SHALL be preserved; the refactor introduces no version drift and no uncovered files.</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHOULD\">The fixture set SHALL remain valid against the regenerated CRDs (no fixture references a removed field; gateway fixtures still validate).</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 26782,
  "caller": "run_claude_cli"
}