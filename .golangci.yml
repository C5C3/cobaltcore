version: "2"

linters:
  enable:
    - errcheck
    - gocritic
    - gosimple
    - govet
    - ineffassign
    - staticcheck
    - typecheck
    - unused
  exclusions:
    paths:
      - "zz_generated.*\\.go$"

run:
  timeout: 5m
