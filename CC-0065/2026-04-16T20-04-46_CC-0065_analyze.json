{
  "timestamp": "2026-04-16T20:04:46.192916+00:00",
  "feature_id": "CC-0065",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0065",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0065\">\n  <title>Add optional HTTPRoute sub-reconciler for API exposure</title>\n  <description># GitHub Issue #162: Add optional HTTPRoute sub-reconciler for external Keystone API exposure\n\n**Category**: feature | **Scope**: Medium\n\n**Description**: The Keystone API is currently only exposed via a ClusterIP Service on port 5000. External access (CLI users, Horizon, federation partners) requires manual setup outside the operator. Add a new `reconcileHTTPRoute` sub-reconciler that optionally creates a Gateway API `HTTPRoute` (`gateway.networking.k8s.io/v1`) when `spec.gateway` is configured.\n\nThe Kubernetes Ingress API (`networking.k8s.io/v1`) is frozen and receives no new features. The ingress-nginx controller has been archived as of March 2026. The Gateway API (GA since October 2023, currently v1.5.x) is the recommended successor and supported by all major implementations (Envoy Gateway, Cilium, Istio, Traefik, NGINX Gateway Fabric).\n\nThe operator follows the Gateway API role model: it only creates an **HTTPRoute** (application-developer role). The **GatewayClass** and **Gateway** are infrastructure concerns managed by the platform team.\n\nNew `spec.gateway` section:\n\n```yaml\nspec:\n  gateway:\n    parentRef:\n      name: openstack-gateway        # required, pre-existing Gateway\n      namespace: openstack-infra      # optional, cross-namespace via ReferenceGrant\n      sectionName: https              # optional, target a specific Gateway listener\n    hostname: keystone.example.com    # required, SNI / Host header match\n    path: /                           # optional, default \"/\"\n    annotations: {}                   # optional, pass-through to HTTPRoute metadata\n```\n\nBehavior:\n- When `spec.gateway` is set, create an HTTPRoute targeting the existing `{name}-api` Service on port 5000\n- When `spec.gateway` is removed (nil), delete the HTTPRoute resource\n- Derive `status.endpoint` from the hostname: `https://{hostname}/v3` (assume TLS is terminated at the Gateway)\n- The existing `spec.publicEndpoint` field takes precedence over the gateway-derived URL for `--bootstrap-public-url` if both are set\n- Add an `HTTPRouteReady` condition that reflects whether the route has been accepted by the Gateway controller (check `status.parents[].conditions` for `Accepted=True`)\n\n**Motivation**: Keystone is the central authentication endpoint for all OpenStack services and CLI users. Without operator-managed external exposure, every deployment requires manual creation of routing resources, which drifts from the desired state and is error-prone. The `spec.publicEndpoint` field already exists as a workaround, but the operator cannot ensure that the corresponding network path actually exists. Managing an HTTPRoute declaratively closes this gap, gives the operator a single source of truth for `status.endpoint`, and uses the portable Gateway API standard instead of the frozen Ingress API or controller-specific annotations.\n\n**Affected Areas**:\n- `operators/keystone/api/v1alpha1/keystone_types.go` (new `GatewaySpec` struct with `ParentRef`, `Hostname`, `Path`, `Annotations`)\n- `operators/keystone/api/v1alpha1/keystone_webhook.go` (validation: hostname required, parentRef.name required)\n- `operators/keystone/internal/controller/reconcile_httproute.go` (new file)\n- `operators/keystone/internal/controller/keystone_controller.go` (call `reconcileHTTPRoute`, add to owned resources, aggregate condition)\n- `operators/keystone/internal/controller/reconcile_networkpolicy.go` (extend ingress rules to allow traffic from the Gateway pod/namespace when both `spec.gateway` and `spec.networkPolicy` are set)\n- `operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml` (CRD regeneration)\n- `operators/keystone/go.mod` (add `sigs.k8s.io/gateway-api` dependency)\n\n**Acceptance Criteria**:\n- [ ] HTTPRoute is created when `spec.gateway` is set with `parentRef.name` and `hostname`\n- [ ] HTTPRoute is deleted when `spec.gateway` is removed\n- [ ] HTTPRoute references the existing `{name}-api` Service on port 5000 as `backendRef`\n- [ ] `parentRef` supports `name`, `namespace`, and `sectionName`\n- [ ] `status.endpoint` reflects `https://{hostname}/v3` when gateway is configured\n- [ ] `spec.publicEndpoint` takes precedence over gateway-derived URL for bootstrap\n- [ ] `HTTPRouteReady` condition tracks the `Accepted` condition from `status.parents[].conditions`\n- [ ] `spec.gateway.annotations` are passed through to the HTTPRoute metadata\n- [ ] NetworkPolicy is updated to allow traffic from the Gateway when both `spec.gateway` and `spec.networkPolicy` are set\n- [ ] RBAC markers include `gateway.networking.k8s.io/httproutes` permissions\n- [ ] Webhook validates that `hostname` and `parentRef.name` are non-empty\n- [ ] Unit tests cover creation, deletion, status derivation, and annotation pass-through\n- [ ] Integration test verifies end-to-end HTTPRoute lifecycle\n\n**Supersedes**: #161 (Ingress-based approach)\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/162</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add gateway-api dependency and GatewaySpec types to keystone_types.go (CC-0065, REQ-001, REQ-007)</task>\n    <task status=\"pending\">1.2 Add webhook validation for spec.gateway fields (CC-0065, REQ-007)</task>\n    <task status=\"pending\">2.1 Implement reconcileHTTPRoute sub-reconciler with create/update/delete lifecycle (CC-0065, REQ-001, REQ-002, REQ-003, REQ-005, REQ-006)</task>\n    <task status=\"pending\">2.2 Add buildKeystoneHTTPRoute and lifecycle unit tests (CC-0065, REQ-001, REQ-002, REQ-003, REQ-005, REQ-006)</task>\n    <task status=\"pending\">3.1 Wire reconcileHTTPRoute into main controller, RBAC, and condition aggregation (CC-0065, REQ-009, REQ-010)</task>\n    <task status=\"pending\">3.2 Update status.endpoint derivation for gateway hostname (CC-0065, REQ-004)</task>\n    <task status=\"pending\">3.3 Extend NetworkPolicy to allow Gateway traffic (CC-0065, REQ-008)</task>\n    <task status=\"pending\">4.1 Add gateway-api scheme registration in main.go and integration test setup (CC-0065, REQ-010)</task>\n    <task status=\"pending\">4.2 Add integration tests for HTTPRoute lifecycle (CC-0065, REQ-001, REQ-002, REQ-005)</task>\n    <task status=\"pending\">5.1 Reference documentation for spec.gateway and HTTPRoute sub-reconciler (CC-0065)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\n```\n                     Keystone CR\n                    ┌─────────────────┐\n                    │ spec.gateway    │\n                    │ spec.networkPol.│\n                    └────────┬────────┘\n                             │\n              Reconcile() ─ sequential sub-reconcilers\n                             │\n    ┌────────────────────────┼────────────────────────┐\n    │                        │                        │\n    ▼                        ▼                        ▼\nreconcileNetworkPolicy  reconcileDeployment   reconcileHTTPRoute\n    │                        │                        │\n    ▼                        ▼                        ▼\n NetworkPolicy           Service              HTTPRoute\n (adds gateway           {name}-api:5000       ├─ parentRef → Gateway\n  ingress peer)          + Deployment          ├─ hostname match\n                         + PDB                 ├─ backendRef → {name}-api:5000\n                                               └─ annotations pass-through\n                                                      │\n                                             status.parents[].conditions\n                                                      │\n                                                      ▼\n                                             HTTPRouteReady condition\n```\n\n## Data Flow\n1. User sets spec.gateway on Keystone CR\n2. Webhook validates hostname and parentRef.name are non-empty\n3. reconcileNetworkPolicy adds gateway namespace as ingress peer (if networkPolicy also set)\n4. reconcileDeployment creates Service (always), sets status.endpoint to gateway-derived URL\n5. reconcileHTTPRoute creates HTTPRoute with parentRef, hostname, path, backendRef, annotations\n6. reconcileHTTPRoute reads back HTTPRoute status.parents[].conditions for Accepted\n7. Sets HTTPRouteReady condition (True/Accepted or False/NotAccepted with requeue)\n8. setReadyCondition aggregates all sub-conditions including HTTPRouteReady\n\n## Key Decisions\n- HTTPRoute reconciler runs AFTER reconcileDeployment (Service must exist) and BEFORE reconcileBootstrap (endpoint should be set before bootstrap uses it for public URL fallback)\n- status.endpoint uses gateway-derived URL (https://{hostname}/v3) when spec.gateway is set; reverts to cluster-local when removed\n- spec.publicEndpoint still takes precedence for bootstrap --bootstrap-public-url (no change to existing precedence logic)\n- Gateway namespace ingress peer in NetworkPolicy uses kubernetes.io/metadata.name label selector (standard Kubernetes label) to match the gateway namespace\n- HTTPRoute name follows existing convention: {name}-api (same as Service, Deployment, HPA, NetworkPolicy)\n- ensureHTTPRoute follows ensureNetworkPolicy pattern: additive metadata merge, authoritative spec overwrite, DeepEqual skip guard\n\n## Pitfalls\n- Gateway API CRDs must be pre-installed on the cluster — the operator only creates HTTPRoute, not GatewayClass/Gateway\n- Adding HTTPRouteReady to subConditionTypes affects ALL existing Keystone CRs — first reconcile after upgrade must set the condition\n- Cross-namespace parentRef requires a ReferenceGrant (out of scope for this operator)\n- HTTPRoute acceptance status is populated asynchronously by the Gateway controller — reconciler must handle empty status gracefully</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_GatewayValid\" requirement-id=\"REQ-007\">Validation passes when spec.gateway has valid hostname and parentRef.name</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_GatewayEmptyHostname\" requirement-id=\"REQ-007\">Validation returns field.Required error on spec.gateway.hostname when empty</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_GatewayEmptyParentRefName\" requirement-id=\"REQ-007\">Validation returns field.Required error on spec.gateway.parentRef.name when empty</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_GatewayNil_Accepted\" requirement-id=\"REQ-007\">Validation passes when spec.gateway is nil (feature disabled)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_NameAndNamespace\" requirement-id=\"REQ-001\">HTTPRoute name is {name}-api and namespace matches CR namespace</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_ParentRef\" requirement-id=\"REQ-001\">HTTPRoute parentRef.name, namespace, sectionName match spec.gateway.parentRef fields</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_BackendRef\" requirement-id=\"REQ-003\">BackendRef targets {name}-api Service on port 5000</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_DefaultPath\" requirement-id=\"REQ-003\">Path prefix is '/' when spec.gateway.path is not set</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_CustomPath\" requirement-id=\"REQ-003\">Path prefix matches spec.gateway.path when set to '/identity'</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_Hostname\" requirement-id=\"REQ-001\">HTTPRoute hostname matches spec.gateway.hostname</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_Annotations\" requirement-id=\"REQ-006\">HTTPRoute metadata.annotations includes spec.gateway.annotations</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestBuildKeystoneHTTPRoute_NoAnnotations\" requirement-id=\"REQ-006\">HTTPRoute has no user-specified annotations when spec.gateway.annotations is nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_GatewaySet_CreatesHTTPRoute\" requirement-id=\"REQ-001\">HTTPRoute is created with ownerReference and HTTPRouteReady condition set</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_GatewayNil_NoExisting_SetsNotRequired\" requirement-id=\"REQ-002\">HTTPRouteReady set to True with reason HTTPRouteNotRequired when no HTTPRoute exists</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_GatewayNil_ExistingRoute_DeletesHTTPRoute\" requirement-id=\"REQ-002\">Existing HTTPRoute deleted and condition set to HTTPRouteNotRequired</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_GatewaySet_ConditionObservedGeneration\" requirement-id=\"REQ-005\">HTTPRouteReady condition includes correct ObservedGeneration</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_AcceptedCondition_True\" requirement-id=\"REQ-005\">HTTPRouteReady=True when status.parents[].conditions has Accepted=True</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_AcceptedCondition_False_Requeues\" requirement-id=\"REQ-005\">HTTPRouteReady=False and result.RequeueAfter set when not accepted</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_GatewaySet_HTTPRouteUpdated\" requirement-id=\"REQ-001\">HTTPRoute is updated when spec.gateway changes (e.g., hostname change)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_NoChange_SkipsUpdate\" requirement-id=\"REQ-001\">No API update issued when reconciling with identical spec (idempotent)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_EnsureError_Propagated\" requirement-id=\"REQ-001\">API creation errors are propagated with context message</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_httproute_test.go\" test-function=\"TestReconcileHTTPRoute_DeleteError_Propagated\" requirement-id=\"REQ-002\">API deletion errors are propagated with context message</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_networkpolicy_test.go\" test-function=\"TestBuildKeystoneNetworkPolicy_GatewayIngressPeer\" requirement-id=\"REQ-008\">Additional ingress peer with gateway namespace selector when both gateway and networkPolicy set</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_networkpolicy_test.go\" test-function=\"TestBuildKeystoneNetworkPolicy_GatewayIngressPeer_CrossNamespace\" requirement-id=\"REQ-008\">Gateway peer uses parentRef.namespace when set for cross-namespace routing</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_networkpolicy_test.go\" test-function=\"TestBuildKeystoneNetworkPolicy_NoGateway_NoExtraPeer\" requirement-id=\"REQ-008\">No gateway ingress peer when spec.gateway is nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_HTTPRoute_CreatedWhenGatewaySet\" requirement-id=\"REQ-001\">HTTPRoute created with correct parentRef, hostname, backendRef in envtest</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_HTTPRoute_DeletedWhenGatewayRemoved\" requirement-id=\"REQ-002\">HTTPRoute deleted when spec.gateway is removed via patch</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_HTTPRoute_EndpointDerivedFromGateway\" requirement-id=\"REQ-004\">status.endpoint shows https://{hostname}/v3 when gateway is configured</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Platform operator exposes Keystone API via Gateway API HTTPRoute\">As a platform operator, I want to configure spec.gateway on the Keystone CR so the operator creates an HTTPRoute targeting the existing {name}-api Service on port 5000, so that external clients (CLI users, Horizon, federation partners) can reach Keystone without manually creating routing resources</story>\n    <story title=\"Platform operator removes gateway configuration to delete HTTPRoute\">As a platform operator, I want to remove spec.gateway from the Keystone CR so the operator deletes the HTTPRoute, so that the external route is cleaned up declaratively when no longer needed</story>\n    <story title=\"Platform operator sees gateway-derived endpoint in status\">As a platform operator, I want status.endpoint to reflect the externally reachable URL when spec.gateway is configured, so that I can discover the correct endpoint URL from the CR status without manual calculation</story>\n    <story title=\"Platform operator monitors HTTPRoute acceptance status\">As a platform operator, I want an HTTPRouteReady condition on the Keystone CR that reflects whether the Gateway controller has accepted the route, so that I can detect misconfigurations (wrong Gateway name, missing ReferenceGrant) without inspecting the HTTPRoute directly</story>\n    <story title=\"Platform operator passes custom annotations to HTTPRoute\">As a platform operator, I want to set spec.gateway.annotations so they are passed through to the HTTPRoute metadata, so that I can attach implementation-specific configuration (e.g., rate limits, timeouts) without modifying the operator</story>\n    <story title=\"Platform operator with NetworkPolicy allows Gateway traffic\">As a platform operator, I want the NetworkPolicy to automatically allow ingress from the Gateway when both spec.gateway and spec.networkPolicy are configured, so that external traffic routed through the Gateway can reach Keystone pods without manual NetworkPolicy modifications</story>\n    <story title=\"Webhook rejects invalid gateway configuration\">As a platform operator, I want clear validation errors when spec.gateway is misconfigured, so that I get immediate feedback on missing required fields instead of silent failures</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The operator SHALL create an HTTPRoute when spec.gateway is configured with parentRef.name and hostname</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The operator SHALL delete the HTTPRoute when spec.gateway is removed (nil)</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The HTTPRoute SHALL reference the existing {name}-api Service on port 5000 as backendRef</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The operator SHALL derive status.endpoint from the gateway hostname as https://{hostname}/v3</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The operator SHALL track the HTTPRoute Accepted condition from status.parents[].conditions as HTTPRouteReady</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The operator SHALL pass spec.gateway.annotations through to the HTTPRoute metadata</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The webhook SHALL validate that hostname and parentRef.name are non-empty when spec.gateway is set</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The NetworkPolicy SHALL include an ingress peer for Gateway traffic when both spec.gateway and spec.networkPolicy are set</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The operator SHALL include RBAC permissions for gateway.networking.k8s.io/httproutes</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The operator SHALL register HTTPRoute as an owned resource and include HTTPRouteReady in condition aggregation</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 22727,
  "caller": "run_claude_cli"
}