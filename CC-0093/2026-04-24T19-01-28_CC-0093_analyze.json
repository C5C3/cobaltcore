{
  "timestamp": "2026-04-24T19:01:28.379699+00:00",
  "feature_id": "CC-0093",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0093",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0093\">\n  <title>Parameterize OpenBao RemoteKey per Keystone CR</title>\n  <description># GitHub Issue #279: Parameterize OpenBao RemoteKey per Keystone CR to eliminate cross-CR KV path collisions\n\n**Category**: bug | **Scope**: Medium\n\n**Description**: The Fernet and credential `PushSecret` builders hardcode the OpenBao `RemoteKey` to the same two literal paths for every Keystone CR in a cluster:\n\n- `operators/keystone/internal/controller/reconcile_fernet.go:423` — `RemoteKey: \"openstack/keystone/fernet-keys\"`\n- `operators/keystone/internal/controller/reconcile_credential.go:429` — `RemoteKey: \"openstack/keystone/credential-keys\"`\n\nEvery `PushSecret` CR — `{keystone.Name}-fernet-keys-backup` / `{keystone.Name}-credential-keys-backup` — is itself name-scoped, and the local `Secret` it selects is name-scoped too (`reconcile_fernet.go:417`, `reconcile_credential.go:423`). Only the remote KV-v2 path is flat-namespaced across CRs. Two Keystone CRs in the same Kubernetes namespace therefore share exactly two backing OpenBao objects — whichever CR's ESO push lands last wins, and either CR's deletion can race with the other CR's next push.\n\nIn production this is dormant (the conventional deployment runs a single Keystone per namespace), but it is load-bearing in the E2E suite, where `tests/e2e/chainsaw-config.yaml:40` sets `parallel: 4` and every `tests/e2e/keystone/*/chainsaw-test.yaml` file lives in the same `openstack` namespace. Step 6 of `tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml:255-256` asserts that both KV-v2 paths are `not-found` after the `keystone-cleanup` CR is deleted — an assertion that cannot hold whenever a concurrent test's CR is mid-reconcile, because that CR's `PushSecret` is writing back to the shared path the cleanup test just purged.\n\nThe operator's own OpenBao policy (`deploy/openbao/policies/push-keystone-keys.hcl:14-20`) already rejects a wildcard over the Keystone subtree — for the orthogonal reason of protecting the read-only `openstack/keystone/db` credentials consumed by Keystone — so the flat two-path scheme is not even a policy simplification we are preserving.\n\n**Motivation**: Reproduced on `feature/CC-0088` in CI run [24903037791/job 72927386373](https://github.com/C5C3/forge/actions/runs/24903037791/job/72927386373) at `2026-04-24T17:53:33Z`. The `keystone-deletion-cleanup` step-6 retry loop polled for 30 s and observed both KV-v2 paths continuously present with `created_time=2026-04-24T17:53:11.602847833Z` (Fernet) — created 10 s after the cleanup test's `kubectl delete` at `17:53:01` and exactly matching the `keystone-brownfield-database` step-2 apply at `17:53:11`. The adjacent `keystone-credential-rotation` and `keystone-concurrent-cr-conflicts` CRs running in parallel contribute additional overlapping writers. Step-4's `PushSecret NotFound` assertion passes cleanly on the cleanup test's own backup objects — the leak is purely the shared remote path being re-created by a neighbour.\n\nPreviously observed on `main` in CI run [24839732116](https://github.com/C5C3/forge/actions/runs/24839732116) at `14:31:30` (recorded in auto-memory `project_shared_openbao_kv_path.md`), 16 s after `keystone-cleanup` deletion, same failure mode. Both occurrences match the same timestamp-evidenced race; this is a reliably reproducible flake rather than a one-off. PR #271 (CC-0091) closed the independent adoption-race leak in the operator itself and added the bounded-retry loop step-6 depends on, but the loop cannot tolerate an external, legitimate writer regenerating the path — only this issue can.\n\nSpin-off from analysing the red `feature/CC-0088` E2E run; CC-0088 itself is unrelated (Envoy Gateway / HTTPRoute assertions).\n\n**Proposed approach**:\n\n1. Switch both builders to a per-CR remote key layout, e.g. `openstack/keystone/{keystone.Name}/fernet-keys` and `openstack/keystone/{keystone.Name}/credential-keys`. The CR name is already in scope in both functions (`fernetKeysPushSecret(keystone *keystonev1alpha1.Keystone)` at `reconcile_fernet.go:403`, symmetric at `reconcile_credential.go:409`), so the change is a one-line `fmt.Sprintf` in each. A nested subpath keeps the convention of the sibling `openstack/keystone/db` path used by `deploy/eso/externalsecrets/keystone-db.yaml:27` and preserves the existing wildcard reads granted by `deploy/openbao/policies/eso-management.hcl:34` (`kv-v2/data/openstack/keystone/*`).\n2. Broaden the `push-keystone-keys.hcl` policy from two literal paths to two per-name patterns while still excluding `/db`:\n   - `kv-v2/{data,metadata}/openstack/keystone/+/fernet-keys`\n   - `kv-v2/{data,metadata}/openstack/keystone/+/credential-keys`\n\n   The `+` single-segment glob (OpenBao/Vault capability template syntax) keeps the policy tight enough that a compromised ESO still cannot reach `openstack/keystone/db`, matching the `push-keystone-keys.hcl:14-20` rationale. Verify the `eso-management` wildcard (`kv-v2/data/openstack/keystone/*`) still covers the new read paths so the redundant-`read` capability remains redundant rather than load-bearing.\n3. Update the step-6 chainsaw script in `tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml:255-256` to target the CR-specific paths (`kv-v2/openstack/keystone/keystone-cleanup/fernet-keys` etc.), so the retry loop asserts exactly the path the cleanup CR owned and cannot be perturbed by parallel tests.\n4. Update both builder unit tests (`reconcile_fernet_test.go:412`, `reconcile_credential_test.go:416`) to the new expected `RemoteKey` string.\n5. Update reconciler docs: `docs/reference/keystone-reconciler.md:763-764`, `:803`, `:1341`, `:1376`, `:1510`, `:1550`, and `architecture/docs/09-implementation/04-keystone-reconciler.md:279` to reference `{keystone.Name}` in the path.\n\n**Acceptance Criteria**:\n- [ ] `fernetKeysPushSecret` and `credentialKeysPushSecret` emit per-CR `RemoteKey` values that embed `keystone.Name`.\n- [ ] `push-keystone-keys.hcl` grants `create/update/read/delete` on the per-name patterns only, still denies access to `openstack/keystone/db`, and does so under a policy comment that captures the rationale for including the name segment.\n- [ ] Two Keystone CRs named `keystone-a` and `keystone-b` in the same namespace produce four distinct populated KV-v2 paths (`.../keystone-a/{fernet,credential}-keys`, `.../keystone-b/{fernet,credential}-keys`) — verifiable by hand with `bao kv list kv-v2/openstack/keystone/` or by extending `tests/e2e/keystone/concurrent-cr-conflicts` with a positive KV assertion.\n- [ ] `chainsaw/keystone-deletion-cleanup` step-6 asserts the `keystone-cleanup`-scoped paths and passes while any of `keystone-autoscaling`, `keystone-credential-rotation`, `keystone-brownfield-database`, or `keystone-concurrent-cr-conflicts` run in parallel (`parallel: 4`).\n- [ ] No regression in `chainsaw/keystone-fernet-rotation` or `chainsaw/keystone-credential-rotation` — PushSecret still reaches `Ready=True` on the new path.\n- [ ] Deletion-cleanup flake does not recur across 10 consecutive CI runs (manual `gh workflow run e2e.yaml` trigger) to confirm the race is closed, matching the validation bar used for #271.\n- [ ] Docs: the `{keystone.Name}` segment appears in every reference-doc location that previously listed the flat path, including the PushSecret table at `docs/reference/keystone-reconciler.md:763-764` and the rotation sections at `:1341/:1376/:1510/:1550`.\n- [ ] Migration note added to `docs/reference/keystone-reconciler.md`: pre-existing clusters carrying data at `kv-v2/openstack/keystone/{fernet,credential}-keys` will see the operator write a new path on next reconcile; the old paths are orphaned but harmless (read-only from Keystone's perspective — the live Secret is driven from the local Kubernetes `Secret`, not the OpenBao backup) and can be hard-deleted manually. Call this out explicitly so upgraders don't interpret the stale path as a leak.\n\n**Refs**: CC-0079 (openbao-finalizer), CC-0083 (PushSecret write policy), CC-0091 (adoption race), #271, #273. Auto-memory: `project_shared_openbao_kv_path.md`.\n\n**Labels:** bug\n\n---\nSource: https://github.com/C5C3/forge/issues/279</description>\n  <tasks>\n    <task status=\"pending\">1.1 Parameterize RemoteKey in fernetKeysPushSecret (operators/keystone/internal/controller/reconcile_fernet.go:423) using fmt.Sprintf(\"openstack/keystone/%s/fernet-keys\", keystone.Name); update the godoc at :396-:402 so the kv-v2/openstack/keystone/fernet-keys mention becomes kv-v2/openstack/keystone/{name}/fernet-keys and references feature id CC-0093 (REQ-001, REQ-010)</task>\n    <task status=\"pending\">1.2 Parameterize RemoteKey in credentialKeysPushSecret (operators/keystone/internal/controller/reconcile_credential.go:429) using fmt.Sprintf(\"openstack/keystone/%s/credential-keys\", keystone.Name); update the godoc at :402-:408 symmetrically and reference CC-0093 (REQ-002, REQ-010)</task>\n    <task status=\"pending\">1.3 Update reconcile_fernet_test.go:412 to Equal(\"openstack/keystone/test-keystone/fernet-keys\") and add TestFernetKeysPushSecret_RemoteKeyIsCRScoped (table-driven over names [keystone-a, keystone-b, keystone-cleanup]) and TestFernetKeysPushSecret_PreservesDeletionPolicyAndStoreRef in the same file — all must pass go test ./operators/keystone/... (REQ-001, REQ-006)</task>\n    <task status=\"pending\">1.4 Update reconcile_credential_test.go:416 to Equal(\"openstack/keystone/test-keystone/credential-keys\") and add TestCredentialKeysPushSecret_RemoteKeyIsCRScoped and TestCredentialKeysPushSecret_PreservesDeletionPolicyAndStoreRef with symmetric assertions (REQ-002, REQ-006)</task>\n    <task status=\"pending\">2.1 Rewrite deploy/openbao/policies/push-keystone-keys.hcl: replace the four literal path blocks (lines 44-58) with four `+`-glob blocks — kv-v2/{data,metadata}/openstack/keystone/+/fernet-keys and kv-v2/{data,metadata}/openstack/keystone/+/credential-keys — each with capabilities = [\"create\",\"update\",\"read\",\"delete\"]. Extend the header comment to state: (a) why the `+` single-segment glob is now used (per-CR layout, CC-0093), (b) why /db exclusion still holds (`+` requires the literal /fernet-keys or /credential-keys suffix — a literal `db` leaf has no such child), (c) that eso-management.hcl's kv-v2/data/openstack/keystone/* already covers the new read paths so the redundant-read capability remains redundant. Preserve existing CC-0079 and CC-0083 rationale paragraphs verbatim (REQ-003, REQ-009)</task>\n    <task status=\"pending\">2.2 Verify the OpenBao/Vault KV-v2 glob semantics claim from 2.1 by consulting the upstream policy docs (ACL templating / path glob rules) and record the citation in the new header comment of push-keystone-keys.hcl. If the upstream semantics do NOT let `+` work as a single-segment glob in the current OpenBao release pinned by deploy/openbao, fall back to enumerating the production CR names — and raise that as a blocker back to the planning context rather than silently diverging (REQ-003, REQ-009)</task>\n    <task status=\"pending\">3.1 Update tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml step-6 (lines 205-285 region): change the shell for-loop path list (lines 255-256) from the two shared strings to \"kv-v2/openstack/keystone/keystone-cleanup/fernet-keys\" and \"kv-v2/openstack/keystone/keystone-cleanup/credential-keys\". Keep the 30 s / 3 s bounded retry (CC-0091) and the stderr per-path error line unchanged. Update the step-6 header comment (lines 205-226) to note per-CR isolation as the reason cross-test interference is no longer possible and reference CC-0093 (REQ-004)</task>\n    <task status=\"pending\">3.2 Extend tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml with a new step after step-2 (Ready=True assertion) that execs bao kv get against all four CR-scoped paths under a 60 s / 3 s bounded retry: kv-v2/openstack/keystone/keystone-concurrent-{a,b}/{fernet-keys,credential-keys}. Script skeleton: reuse the openbao-system / openbao-0 / init-output root-token extraction from deletion-cleanup step-6. Also assert the legacy flat paths (openstack/keystone/fernet-keys, openstack/keystone/credential-keys) return not-found. Provide a catch block dumping openbao pod logs on failure. Reference CC-0093 in the step header comment (REQ-005)</task>\n    <task status=\"pending\">3.3 Add TestIntegrationKeystone_PushSecretRemoteKeyIsPerCR and TestIntegrationKeystone_CredentialPushSecretRemoteKeyIsPerCR to operators/keystone/internal/controller/integration_test.go. Each test: create two Keystones (kA, kB) in a shared namespace via the existing setupEnvTestWithController helper, drive a reconcile, Eventually.Get the two PushSecrets, assert their Spec.Data[0].Match.RemoteRef.RemoteKey values are distinct and each contains its CR's Name (REQ-001, REQ-002)</task>\n    <task status=\"pending\">4.1 Update docs/reference/keystone-reconciler.md table rows at lines 763-764: change the KV-v2 Path column to kv-v2/data/openstack/keystone/{keystone.Name}/fernet-keys and kv-v2/data/openstack/keystone/{keystone.Name}/credential-keys. No other change in this table (REQ-007)</task>\n    <task status=\"pending\">4.2 Rewrite the `> **Known limitation:**` block in docs/reference/keystone-reconciler.md (lines 779-784) to remove the stale flat-path claim. Replace with a two-sentence normative statement: paths are per-CR-scoped via `openstack/keystone/{keystone.Name}/<leaf>`, safe under multi-Keystone-per-namespace. Cross-link the new migration-note subsection. Reference CC-0093 (REQ-007)</task>\n    <task status=\"pending\">4.3 Update the Go code fragment at docs/reference/keystone-reconciler.md:803 so RemoteKey reads `fmt.Sprintf(\"openstack/keystone/%s/fernet-keys\", keystone.Name)` and a preceding sentence names the per-CR layout. The fragment must still compile as a copy-paste example (REQ-007)</task>\n    <task status=\"pending\">4.4 Update the CC-0091 motivating-race paragraph at docs/reference/keystone-reconciler.md:~1007 so the observed stuck path is described as `kv-v2/openstack/keystone/{name}/fernet-keys` (with the note that CI run 24842115250 predated CC-0093 and observed the now-legacy flat path). Keep the CC-0091 narrative intact — only the path form changes (REQ-007)</task>\n    <task status=\"pending\">4.5 Update the Fernet sub-reconciler section at docs/reference/keystone-reconciler.md:1340-1376: the bullet at :1341 and the PushSecret Remote Key row at :1376 both become kv-v2/data/openstack/keystone/{name}/fernet-keys (REQ-007)</task>\n    <task status=\"pending\">4.6 Update the credential sub-reconciler section at docs/reference/keystone-reconciler.md:1509-1550: the bullet at :1510 and the PushSecret Remote Key row at :1550 both become kv-v2/data/openstack/keystone/{name}/credential-keys (REQ-007)</task>\n    <task status=\"pending\">4.7 Add a new migration-note subsection in docs/reference/keystone-reconciler.md near the PushSecret overview (section containing lines 755-810). The note states: (a) on upgrade to CC-0093 the operator will write to the new `{name}`-scoped paths on next reconcile, (b) the old flat paths `kv-v2/openstack/keystone/{fernet,credential}-keys` are orphaned but harmless (live Keystone state is driven from the local Kubernetes Secret, not the OpenBao backup), (c) the recommended cleanup commands are `bao kv metadata delete kv-v2/openstack/keystone/fernet-keys` and `bao kv metadata delete kv-v2/openstack/keystone/credential-keys`. Reference feature id CC-0093 (REQ-007)</task>\n    <task status=\"pending\">5.1 Run `rg 'openstack/keystone/(fernet|credential)-keys' docs/ deploy/ operators/` after 1.1-4.7 and confirm every remaining match is either (a) inside the new migration note, or (b) a comment explicitly documenting the legacy form. Record the verified grep output in the PR description body as the CC-0093 completeness proof (REQ-007)</task>\n    <task status=\"pending\">5.2 Document the architecture-submodule follow-up: in the PR description, flag that architecture/docs/09-implementation/04-keystone-reconciler.md:279 still references the flat path and must be corrected via a separate upstream PR to github.com/C5C3/C5C3 (the submodule URL in .gitmodules). Do NOT modify the submodule content from this worktree (REQ-008)</task>\n    <task status=\"pending\">5.3 Full verification pass: run go vet ./..., go test ./operators/keystone/..., and golangci-lint run --config .golangci.yml ./operators/keystone/... from /workspace (or the closest go.work project root). Run chainsaw lint against tests/e2e/keystone/{deletion-cleanup,concurrent-cr-conflicts}/chainsaw-test.yaml if the binary is available. Record outcomes (pass / blocked-by-sandbox) in the PR description. Do not mark completion if any go test or vet failure is unresolved (REQ-001, REQ-002, REQ-006, REQ-010)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture summary\n\nThe fix is a two-site string-format change in the operator (reconcile_fernet.go:423, reconcile_credential.go:429), a one-file policy rewrite (push-keystone-keys.hcl), a two-file E2E update (deletion-cleanup + concurrent-cr-conflicts), and a documentation-consistency sweep (docs/reference/keystone-reconciler.md at nine pre-existing locations plus a new migration-note subsection). The CR name is already in scope in both builders — no new plumbing, no new CRD field, no new flag (REQ-010).\n\n## Data flow (unchanged except the highlighted segment)\n\n```\nKeystone CR (name=N)\n  └─ reconcile_fernet.go / reconcile_credential.go\n       └─ fernetKeysPushSecret(ks) / credentialKeysPushSecret(ks)\n            └─ builds PushSecret { ObjectMeta.Name = \"<N>-{fernet,credential}-keys-backup\",\n                                    Spec.Selector.Secret.Name = \"<N>-{fernet,credential}-keys\",\n     <<< CHANGE >>>          Spec.Data[0].Match.RemoteRef.RemoteKey = \"openstack/keystone/<N>/{fernet,credential}-keys\"\n                                    Spec.DeletionPolicy = Delete }  # CC-0079 wiring intact\n```\n\nOpenBao authorization of the new path is governed by push-keystone-keys.hcl bound (alongside eso-management.hcl) to the management-cluster ESO Kubernetes auth role. The eso-management.hcl wildcard `kv-v2/data/openstack/keystone/*` is the read-side grant and (per upstream KV-v2 glob semantics) covers any nested leaf — a tight reading of the `*` operator in ACL path rules. Task 2.2 makes that semantics verification load-bearing, not assumed.\n\n## Architecture patterns referenced\n\n- Builder-returns-*PushSecret: identical shape in reconcile_fernet.go:403 and reconcile_credential.go:409 (symmetric pair pattern used repeatedly in the sub-reconcilers — see also openBaoBackupPushSecretNames at reconcile_secrets.go:156).\n- Per-CR name-scoping: already in use for PushSecret.metadata.Name and Spec.Selector.Secret.Name — the RemoteKey is the last hold-out and REQ-001/REQ-002 close the gap.\n- Chainsaw bounded-retry shell pattern from CC-0091: reused verbatim in step-6 of deletion-cleanup and in the new concurrent-cr-conflicts step (see tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml:250-284).\n- OpenBao policy header-comment convention: push-keystone-keys.hcl:5-43 — every rationale paragraph is cross-referenced with its feature id (CC-0079, CC-0083). CC-0093 must be appended in the same style.\n\n## Risks and how to avoid them\n\n1. **OpenBao glob semantics regression.** If the deployed OpenBao version does NOT honour `+` as a single-segment glob in ACL policies, PushSecret writes will 403 at runtime and PushSecret.Status never reaches Ready. Task 2.2 specifically blocks on documenting the upstream reference before merging. Fallback: enumerate known CR names (production runs a single `keystone` CR, but we intentionally support arbitrary names — this would regress REQ-001 acceptance).\n2. **Upgrade orphan confusion.** A reader sees stale data at `kv-v2/openstack/keystone/{fernet,credential}-keys` and opens a bug against the operator for leaking. The migration-note subsection (Task 4.7) explicitly labels these as harmless and gives the cleanup command — this was also called out as an acceptance criterion in the source issue.\n3. **Submodule divergence.** architecture/ is a git submodule; any write from this worktree crosses independent git contexts. Per repo rule 12 and REQ-008, the plan strictly forbids modifying the submodule and delivers the required update as a deferred upstream PR.\n4. **Test symmetry drift.** The two builders have symmetric fates — the table-driven distinct-name tests added by 1.3 and 1.4 lock that in, preventing a future refactor from silently re-unifying the paths.\n5. **Unit-only coverage gap.** A pure builder-level test would pass even if the Ensure/Patch layer regressed back to a literal string. Integration-level tests at 3.3 guard against that by exercising the full reconcile with envtest.\n\n## Key files\n\n- operators/keystone/internal/controller/reconcile_fernet.go (builder — CHANGE)\n- operators/keystone/internal/controller/reconcile_credential.go (builder — CHANGE)\n- operators/keystone/internal/controller/reconcile_fernet_test.go (unit tests — CHANGE)\n- operators/keystone/internal/controller/reconcile_credential_test.go (unit tests — CHANGE)\n- operators/keystone/internal/controller/integration_test.go (envtest — ADD)\n- deploy/openbao/policies/push-keystone-keys.hcl (policy — CHANGE)\n- deploy/openbao/policies/eso-management.hcl (READ-ONLY reference — verify wildcard covers the new read path)\n- deploy/eso/externalsecrets/keystone-db.yaml (READ-ONLY reference — path-convention sibling)\n- tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml (step-6 — CHANGE)\n- tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml (new positive-assertion step — CHANGE)\n- docs/reference/keystone-reconciler.md (nine pre-existing locations + new migration note — CHANGE)\n- architecture/docs/09-implementation/04-keystone-reconciler.md (SUBMODULE, READ-ONLY — follow-up PR)\n\n## Deferred items\n\n- **Upstream architecture-submodule doc fix.** architecture/docs/09-implementation/04-keystone-reconciler.md:279 still references `kv-v2/openstack/keystone/fernet-keys` as the backup path. Per repo rule 12 and REQ-008 this worktree cannot modify submodule content. Rationale: crossing git contexts from a worktree produces divergent submodule state. Timing: open the follow-up PR against github.com/C5C3/C5C3 immediately after CC-0093 merges, so the parent-repo documentation does not carry a dangling submodule reference.\n- **Automated OpenBao policy test-harness.** The plan verifies policy HCL changes by reviewer checklist because no in-repo test framework executes policies against synthetic request paths today. Timing: consider adding a `go test` harness that instantiates an OpenBao dev server and exercises the policy against `keystone/+/{fernet,credential}-keys` plus `keystone/db` as a cross-cutting CC-0083 hardening follow-up.\n- **Hard-delete of orphaned flat-path data on upgrade.** The migration note documents the manual `bao kv metadata delete` commands. A first-reconcile-time automation that performs this purge is explicitly out of scope — it would require the operator to hold a write grant on the legacy path, and granting that for a one-time migration is worse than leaving the paths orphaned (REQ-009 invariant).\n- **Multi-Keystone-per-namespace E2E extension.** Concurrent-cr-conflicts already creates two CRs in the same namespace; after CC-0093 lands, we could extend it to run a Fernet rotation on both CRs in parallel and assert both PushSecrets reach Ready=True on their distinct paths. Timing: optional follow-up — REQ-005's positive KV-path assertion is the minimum necessary guard.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestReconcileFernetKeys_PushSecretExists\" requirement-id=\"REQ-001\">[unit] Update assertion at line 412 to Equal(\"openstack/keystone/test-keystone/fernet-keys\") — pins REQ-001 at builder level</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_PushSecretExists\" requirement-id=\"REQ-002\">[unit] Update assertion at line 416 to Equal(\"openstack/keystone/test-keystone/credential-keys\") — pins REQ-002 at builder level</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetKeysPushSecret_RemoteKeyIsCRScoped\" requirement-id=\"REQ-001\">[unit] New table-driven test over names [keystone-a, keystone-b, keystone-cleanup] asserts each fernetKeysPushSecret(&Keystone{Name:n}).Spec.Data[0].Match.RemoteRef.RemoteKey == fmt.Sprintf(\"openstack/keystone/%s/fernet-keys\", n) and that the three results are pairwise distinct</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialKeysPushSecret_RemoteKeyIsCRScoped\" requirement-id=\"REQ-002\">[unit] Symmetric table-driven test for credentialKeysPushSecret; asserts the CR-scoped RemoteKey and pairwise distinctness</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetKeysPushSecret_PreservesDeletionPolicyAndStoreRef\" requirement-id=\"REQ-001\">[unit] Assert Spec.DeletionPolicy == PushSecretDeletionPolicyDelete and SecretStoreRefs == [{Kind:ClusterSecretStore, Name:openbao-cluster-store}] — regression pin that the CC-0079 wiring is untouched by the RemoteKey change</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialKeysPushSecret_PreservesDeletionPolicyAndStoreRef\" requirement-id=\"REQ-002\">[unit] Symmetric DeletionPolicy/StoreRef regression pin for credentialKeysPushSecret</spec>\n    <spec test-file=\"tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml\" test-function=\"step-6 bao kv get loop\" requirement-id=\"REQ-004\">[e2e] Script asserts not-found on kv-v2/openstack/keystone/keystone-cleanup/fernet-keys and kv-v2/openstack/keystone/keystone-cleanup/credential-keys within the existing 30s / 3s bounded retry; stderr errors name the still-present CR-scoped path</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"new step — assert four per-CR KV paths present\" requirement-id=\"REQ-005\">[e2e] After both CRs reach Ready=True, exec bao kv get on kv-v2/openstack/keystone/{keystone-concurrent-a,keystone-concurrent-b}/{fernet-keys,credential-keys} with a 60s bounded retry; assert all four exit 0 and the legacy flat paths are not-found</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegrationKeystone_PushSecretRemoteKeyIsPerCR\" requirement-id=\"REQ-001\">[integration] envtest: create two Keystones named kA and kB in the same namespace, run the reconciler, assert PushSecret kA-fernet-keys-backup has RemoteKey openstack/keystone/kA/fernet-keys and PushSecret kB-fernet-keys-backup has openstack/keystone/kB/fernet-keys — distinct strings</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegrationKeystone_CredentialPushSecretRemoteKeyIsPerCR\" requirement-id=\"REQ-002\">[integration] Symmetric envtest assertion for credential-keys-backup PushSecret RemoteKey — two CRs produce two distinct strings</spec>\n    <spec test-file=\"deploy/openbao/policies/push-keystone-keys.hcl\" test-function=\"manual / reviewer check (textual)\" requirement-id=\"REQ-003\">[unit] File contains exactly four path blocks matching kv-v2/{data,metadata}/openstack/keystone/+/{fernet,credential}-keys; each has capabilities = [\"create\",\"update\",\"read\",\"delete\"]; file header comment references CC-0093 and explains why + is used and why /db exclusion holds. Verified via the review checklist at PR-review time — no automated policy unit test framework exists in-repo for HCL today</spec>\n    <spec test-file=\"docs/reference/keystone-reconciler.md\" test-function=\"grep-check after edit\" requirement-id=\"REQ-007\">[unit] After the doc edit, `rg 'openstack/keystone/(fernet|credential)-keys' docs/reference/keystone-reconciler.md` only returns hits inside the migration-note block. All nine enumerated locations (763, 764, 780, 803, 1007, 1341, 1376, 1510, 1550 pre-change) show `{keystone.Name}` as a segment</spec>\n    <spec test-file=\"docs/reference/keystone-reconciler.md\" test-function=\"migration-note presence check\" requirement-id=\"REQ-007\">[unit] A migration-note subsection exists, references CC-0093, states the stale paths are harmless, and gives the `bao kv metadata delete kv-v2/openstack/keystone/fernet-keys` and `kv-v2/openstack/keystone/credential-keys` cleanup commands</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator engineer isolates OpenBao KV paths per Keystone CR\">As a operator engineer, I want the Fernet and credential backup PushSecrets of each Keystone CR to write to a CR-scoped OpenBao KV-v2 path, so that two Keystone CRs in the same Kubernetes namespace never overwrite each other's backups and a deletion of one CR cannot race a sibling CR's next push</story>\n    <story title=\"Security reviewer keeps least-privilege on the ESO write path\">As a security reviewer, I want the push-keystone-keys.hcl policy to grant create/update/read/delete only on the per-name Fernet and credential backup paths and still deny any access to openstack/keystone/db, so that broadening the path scheme does not accidentally widen the blast radius of a compromised ESO token (CC-0083 rationale)</story>\n    <story title=\"E2E test author runs deletion-cleanup under parallel:4 without flakes\">As a E2E test author, I want the deletion-cleanup chainsaw test step-6 bao-cli loop to assert absence of the keystone-cleanup-scoped KV paths, not the shared flat paths, so that a concurrent keystone-autoscaling, keystone-credential-rotation, keystone-brownfield-database, or keystone-concurrent-cr-conflicts test cannot repopulate the asserted paths and mask the cleanup test's own behaviour</story>\n    <story title=\"Concurrent CR test positively asserts per-CR KV isolation\">As a E2E test author, I want tests/e2e/keystone/concurrent-cr-conflicts to include a positive assertion that both keystone-concurrent-a and keystone-concurrent-b populate four distinct kv-v2 paths, so that cross-CR path collision is guarded by CI, not only by manual `bao kv list` verification</story>\n    <story title=\"Operator reading the reconciler reference understands the per-CR layout\">As a operations engineer, I want docs/reference/keystone-reconciler.md to consistently reference `{keystone.Name}` in every mention of the Fernet and credential KV-v2 backup paths, so that a reader building a multi-Keystone-per-namespace deployment sees no residual mention of the flat two-path scheme and the known-limitation call-out is removed</story>\n    <story title=\"Cluster operator upgrading an existing deployment understands migration impact\">As a cluster operator, I want a migration note in the reconciler reference explaining that pre-existing clusters will orphan their flat-path backups on first reconcile after upgrade, so that I do not interpret the stale kv-v2/openstack/keystone/{fernet,credential}-keys paths as an operator leak and I know the safe manual cleanup procedure</story>\n    <story title=\"Reviewer verifies that the eso-management read wildcard still covers the new paths\">As a security reviewer, I want the planning and implementation steps to explicitly check that kv-v2/data/openstack/keystone/* in deploy/openbao/policies/eso-management.hcl:34 still covers kv-v2/data/openstack/keystone/{name}/{fernet,credential}-keys, so that the redundant-read capability on push-keystone-keys.hcl remains genuinely redundant (policy-portability only) and the management-cluster ESO role keeps read on the new path without a second grant</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The fernetKeysPushSecret builder SHALL emit RemoteKey equal to `openstack/keystone/{keystone.Name}/fernet-keys` for any non-empty keystone.Name</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The credentialKeysPushSecret builder SHALL emit RemoteKey equal to `openstack/keystone/{keystone.Name}/credential-keys` for any non-empty keystone.Name</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The push-keystone-keys.hcl OpenBao policy SHALL grant [create, update, read, delete] on both the data and metadata sub-paths for the CR-name-scoped fernet-keys and credential-keys leaves only</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The deletion-cleanup chainsaw step-6 retry loop SHALL assert absence of kv-v2/openstack/keystone/keystone-cleanup/fernet-keys and kv-v2/openstack/keystone/keystone-cleanup/credential-keys, not the shared flat paths</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHOULD\">The concurrent-cr-conflicts chainsaw test SHOULD include a positive assertion that all four per-CR kv-v2 paths exist after both CRs are Ready=True</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The unit tests TestReconcileFernetKeys_PushSecretRemoteKey and TestReconcileCredentialKeys_PushSecretRemoteKey SHALL assert the new nested RemoteKey layout</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The reconciler reference documentation SHALL reference `{keystone.Name}` in every location that currently shows the flat KV-v2 path</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The architecture submodule mention of the flat path (architecture/docs/09-implementation/04-keystone-reconciler.md:279) SHALL NOT be modified from this worktree</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHOULD\">The push-keystone-keys.hcl policy SHOULD NOT widen read/write access to kv-v2/openstack/keystone/db under any CR name</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The implementation SHALL NOT introduce a new operator config field, new API version, or new CRD version</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 36087,
  "caller": "run_claude_cli"
}