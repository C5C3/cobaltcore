{
  "timestamp": "2026-03-12T16:45:19.041657+00:00",
  "feature_id": "CC-0010",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0010",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<previous-feature feature-id=\"CC-0009\" status=\"completed\">\n  <title>D002: Implement OpenBao deployment and secret management</title>\n  <tasks hint=\"showing first 5\">\n    <task>1.1 Add openbao-system namespace, create OpenBao HelmRelease, and wire into base kustomization (REQ-001, REQ-006)</task>\n    <task>1.2 Create cert-manager Certificate for OpenBao TLS and wire into infrastructure kustomization (REQ-002, REQ-006)</task>\n    <task>2.1 Create 4 ESO HCL policies: eso-control-plane.hcl, eso-hypervisor.hcl, eso-storage.hcl, eso-management.hcl in deploy/openbao/policies/ (REQ-004, REQ-006)</task>\n    <task>2.2 Create 4 operational HCL policies: push-app-credentials.hcl, push-ceph-keys.hcl, ci-cd-provisioner.hcl, pki-issuer.hcl in deploy/openbao/policies/ (REQ-004, REQ-006)</task>\n    <task>3.1 Create deploy/openbao/bootstrap/init-unseal.sh — idempotent init (5-share, 3-threshold Shamir), store unseal keys as K8s Secret, unseal all 3 replicas via kubectl exec (REQ-003, REQ-006, REQ-007)</task>\n  </tasks>\n</previous-feature>\n\n<feature feature-id=\"CC-0010\">\n  <title>D003: Implement infrastructure deployment automation E2E</title>\n  <description>**Size:** 🏗️ large\n**Category:** infrastructure\n**Priority:** high\n**Feature ID:** S010 (CC-0014)\n\n---\n\n## Summary\n\nImplement `make deploy-infra` and `make teardown-infra` Makefile targets that deploy the full infrastructure stack (cert-manager, OpenBao, ESO, MariaDB Operator, Memcached Operator, infrastructure CRs, ExternalSecrets) to a kind cluster using FluxCD with kustomize overlays for kind-specific resource sizing. Bootstrap OpenBao end-to-end (init-unseal, secret engines, auth, policies, bootstrap secrets) to validate the full secret chain through ESO. Create a Chainsaw E2E test at `tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml` validating all components reach healthy state. Extend `.github/workflows/ci.yaml` with an `e2e-infra` job running the full stack test on every PR and push to main.\n\n---\n\n## Scope\n\n**Included:**\n- `make deploy-infra` target replacing stub S008 — installs FluxCD in kind, applies existing manifests via kustomize overlays in dependency order\n- `make teardown-infra` target (new) — deletes the kind cluster\n- `make install-test-deps` target replacing stub S002 — installs chainsaw, flux CLI, kind, helm prerequisites\n- `make e2e` target replacing stub S002 — runs Chainsaw tests\n- Orchestration script `hack/deploy-infra.sh` — creates kind cluster, runs `flux install`, applies kustomize overlays in two phases (base → infrastructure), runs OpenBao bootstrap, waits for health\n- Cleanup script `hack/teardown-infra.sh` — `kind delete cluster`\n- Kind cluster config `hack/kind-config.yaml` (single control-plane node)\n- Kustomize overlays `deploy/kind/base/` and `deploy/kind/infrastructure/` — patches HelmReleases and CRs for reduced replicas and `standard` storage class\n- Full OpenBao bootstrap invocation (init-unseal → setup-secret-engines → setup-auth → setup-policies → write-bootstrap-secrets)\n- Chainsaw E2E test `tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml` — asserts readiness of all operators, CRs, ClusterIssuer, and ExternalSecret sync status\n- GitHub Actions `e2e-infra` job in `.github/workflows/ci.yaml` using `helm/kind-action`, `fluxcd/flux2/action`, SHA-pinned actions per existing CI conventions\n\n**Excluded:**\n- Operator deployment (keystone-operator, c5c3-operator) — separate features S017/S022, YAGNI\n- Modification of existing `deploy/flux-system/` manifests — read-only reference, kind-specific differences handled via kustomize overlays in `deploy/kind/`\n- Production HA validation (3-replica Galera, multi-node Raft) — belongs to S024 stress tests, YAGNI\n- Image build or push — no operator images needed for infrastructure-only stack\n- Separate workflow file — single `ci.yaml` with new job, path-filtered optimization deferred\n\n---\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph MakeTargets[\"Makefile Targets\"]\n        ITD[\"make install-test-deps\"]\n        DI[\"make deploy-infra\"]\n        TI[\"make teardown-infra\"]\n        E2E[\"make e2e\"]\n    end\n\n    subgraph DeployScript[\"hack/deploy-infra.sh\"]\n        K[\"1. kind create cluster\"]\n        FI[\"2. flux install\"]\n        NS[\"3. kubectl apply -k deploy/kind/base\"]\n        WAIT1[\"4. Wait: HelmReleases Ready\"]\n        INF[\"5. kubectl apply -k deploy/kind/infrastructure\"]\n        WAIT2[\"6. Wait: OpenBao pods Ready\"]\n        BOOT[\"7. OpenBao bootstrap scripts\"]\n        WAIT3[\"8. Wait: ExternalSecrets Synced\"]\n    end\n\n    subgraph FluxReconciliation[\"FluxCD Reconciles HelmReleases\"]\n        CM[\"cert-manager\"]\n        OB[\"OpenBao\"]\n        MO[\"MariaDB Operator\"]\n        ESO[\"ESO\"]\n        MCO[\"Memcached Operator\"]\n    end\n\n    subgraph InfraCRs[\"Infrastructure CRs via Kustomize\"]\n        CI2[\"ClusterIssuer\"]\n        TLS[\"OpenBao TLS Certificate\"]\n        MDB[\"MariaDB CR 1 replica\"]\n        MC[\"Memcached CR 1 replica\"]\n        CSS[\"ClusterSecretStore\"]\n        ES[\"ExternalSecrets x3\"]\n    end\n\n    subgraph KindOverlays[\"deploy/kind/ Kustomize Overlays\"]\n        OVB[\"base/ — patches HelmRelease replicas and storage\"]\n        OVI[\"infrastructure/ — patches CR replicas and storage class\"]\n    end\n\n    DI --> K --> FI --> NS\n    NS --> OVB\n    OVB -->|\"FluxCD\"| FluxReconciliation\n    CM --> OB & MO & ESO & MCO\n    FluxReconciliation --> WAIT1\n    WAIT1 --> INF\n    INF --> OVI\n    OVI --> InfraCRs\n    InfraCRs --> WAIT2 --> BOOT --> WAIT3\n```\n\n```mermaid\nsequenceDiagram\n    participant CI as GitHub Actions\n    participant Kind as kind cluster\n    participant Flux as FluxCD\n    participant K8s as Kubernetes API\n    participant OB as OpenBao\n    participant CS as Chainsaw\n\n    CI->>Kind: kind create cluster\n    CI->>Flux: flux install\n    CI->>K8s: kubectl apply -k deploy/kind/base\n    Flux->>K8s: Reconcile cert-manager HelmRelease\n    Flux->>K8s: Reconcile openbao, mariadb-op, eso, memcached-op\n    CI->>K8s: Wait HelmReleases Ready\n    CI->>K8s: kubectl apply -k deploy/kind/infrastructure\n    K8s-->>K8s: cert-manager issues OpenBao TLS cert\n    CI->>K8s: Wait OpenBao pods Ready\n    CI->>OB: init-unseal.sh\n    CI->>OB: setup-secret-engines, auth, policies, secrets\n    CI->>K8s: Wait ExternalSecrets Synced\n    CI->>CS: chainsaw test infra-stack-health\n    CS->>K8s: Assert all components healthy\n    CS-->>CI: JUnit XML report\n```\n\n---\n\n## Key Components\n\n- **`Makefile` targets**: Replace stubs `deploy-infra` (S008), `install-test-deps` (S002), `e2e` (S002); add new `teardown-infra`; each delegates to scripts in `hack/`\n- **`hack/deploy-infra.sh`**: Orchestration script — creates kind cluster, runs `flux install`, applies `deploy/kind/base/` kustomization (FluxCD reconciles HelmReleases), applies `deploy/kind/infrastructure/` kustomization (CRs + ESO resources), runs OpenBao bootstrap scripts from `deploy/openbao/bootstrap/`, includes `wait_for_ready()` helpers with configurable timeouts; follows existing shell conventions (`set -euo pipefail`, SPDX header)\n- **`hack/teardown-infra.sh`**: Cleanup script — `kind delete cluster --name <cluster-name>`\n- **`hack/kind-config.yaml`**: Single control-plane node, sufficient for CI runners (~7GB RAM, 2 vCPUs)\n- **`deploy/kind/base/kustomization.yaml`**: Kustomize overlay referencing `../../flux-system/`, patches OpenBao HelmRelease to standalone mode (1 replica, HA disabled); other operators unchanged (single-replica by default or stateless)\n- **`deploy/kind/infrastructure/kustomization.yaml`**: Kustomize overlay referencing `../../flux-system/infrastructure/`, patches MariaDB CR (1 replica, Galera disabled, MaxScale disabled, `standard` storage class), patches Memcached CR (1 replica)\n- **`tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml`**: Chainsaw v1alpha2 test asserting: cert-manager Deployment Ready, OpenBao StatefulSet Ready, ESO Deployment Ready, MariaDB Operator Deployment Ready, Memcached Operator Deployment Ready, ClusterIssuer Ready condition, MariaDB CR Ready condition, Memcached CR Ready condition, ClusterSecretStore Valid condition, ExternalSecrets SecretSynced condition; uses extended assert timeout (~5min) for operator startup\n- **`.github/workflows/ci.yaml` — `e2e-infra` job**: New job alongside existing `lint` and `test`; uses SHA-pinned `helm/kind-action` to create cluster, installs FluxCD, runs `make deploy-infra`, executes `chainsaw test --config tests/e2e/chainsaw-config.yaml tests/e2e/infrastructure/`, uploads JUnit report as artifact; `timeout-minutes: 20`; follows existing CI conventions (SHA-pinned actions, `permissions: contents: read`, `concurrency` with cancel-in-progress on PRs)</description>\n  <tasks>\n    <task status=\"pending\">1.1 Create kind cluster config `hack/kind-config.yaml` — single control-plane node with standard settings for CI runners (REQ-010, REQ-011)</task>\n    <task status=\"pending\">1.2 Create kustomize overlay `deploy/kind/base/kustomization.yaml` — references `../../flux-system/`, adds strategic merge patch for OpenBao HelmRelease (standalone mode: 1 replica, HA disabled, standalone Raft config without retry_join peers, `standard` storage class). Validate with `kustomize build deploy/kind/base/` (REQ-003)</task>\n    <task status=\"pending\">1.3 Create kustomize overlay `deploy/kind/infrastructure/kustomization.yaml` — references `../../flux-system/infrastructure/`, adds strategic merge patches for MariaDB CR (1 replica, Galera disabled, MaxScale disabled, `standard` storage class) and Memcached CR (1 replica). Validate with `kustomize build deploy/kind/infrastructure/` (REQ-003)</task>\n    <task status=\"pending\">2.1 Create orchestration script `hack/deploy-infra.sh` — implements the full 8-step deployment sequence: kind create cluster (using hack/kind-config.yaml), flux install, kubectl apply -k deploy/kind/base, wait for HelmReleases Ready, kubectl apply -k deploy/kind/infrastructure, wait for OpenBao pods Ready, run OpenBao bootstrap, wait for ExternalSecrets synced. Includes `log()`, `wait_for_helmreleases()`, `wait_for_pods()`, `wait_for_externalsecrets()` helper functions with configurable timeouts via environment variables. SPDX header, `set -euo pipefail`, CC-0010 reference. Handles pre-flight checks (docker running, no existing cluster). (REQ-001, REQ-004, REQ-005, REQ-011, REQ-012)</task>\n    <task status=\"pending\">2.2 Create teardown script `hack/teardown-infra.sh` — runs `kind delete cluster --name <cluster-name>`, idempotent (succeeds even if cluster does not exist). SPDX header, `set -euo pipefail`, CC-0010 reference. (REQ-002, REQ-011)</task>\n    <task status=\"pending\">2.3 Create install-test-deps script `hack/install-test-deps.sh` — installs pinned versions of chainsaw, flux CLI, kind, and kubectl. Skips download if tool already exists at correct version. SPDX header, `set -euo pipefail`, CC-0010 reference. (REQ-006, REQ-011)</task>\n    <task status=\"pending\">3.1 Update Makefile: replace `deploy-infra` stub (S008) with `hack/deploy-infra.sh`, add `teardown-infra` target delegating to `hack/teardown-infra.sh`, replace `install-test-deps` stub (S002) with `hack/install-test-deps.sh`, replace `e2e` stub (S002) with `chainsaw test --config tests/e2e/chainsaw-config.yaml tests/e2e/`. Keep existing .PHONY declarations and comment style. (REQ-001, REQ-002, REQ-006, REQ-007)</task>\n    <task status=\"pending\">4.1 Create Chainsaw E2E test `tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml` — v1alpha2 Test with steps asserting: (1) cert-manager Deployment readiness in cert-manager namespace, (2) OpenBao StatefulSet readiness in openbao-system, (3) ESO Deployment readiness in external-secrets, (4) MariaDB Operator Deployment readiness in mariadb-system, (5) Memcached Operator Deployment readiness in memcached-system, (6) ClusterIssuer Ready condition on selfsigned-cluster-issuer, (7) MariaDB CR Ready condition on openstack-db, (8) Memcached CR Ready condition on openstack-memcached, (9) ClusterSecretStore Valid condition on openbao-cluster-store, (10) ExternalSecret SecretSynced for keystone-admin, keystone-db, mariadb-root-password. Uses extended assert timeout (~5min). SPDX header, CC-0010 reference. (REQ-008)</task>\n    <task status=\"pending\">5.1 Add `e2e-infra` job to `.github/workflows/ci.yaml` — new job alongside existing `lint` and `test` (no `needs:` dependency). Steps: checkout (SHA-pinned actions/checkout), setup-go (SHA-pinned actions/setup-go with go-version-file: go.work), create kind cluster (SHA-pinned helm/kind-action with config: hack/kind-config.yaml), install flux CLI, run `make deploy-infra`, run `chainsaw test --config tests/e2e/chainsaw-config.yaml tests/e2e/infrastructure/`, upload JUnit report as artifact (SHA-pinned actions/upload-artifact). `timeout-minutes: 20`, follows existing `permissions: contents: read` and concurrency conventions. (REQ-009)</task>\n    <task status=\"pending\">6.1 Write reference documentation `docs/reference/infrastructure/e2e-deployment.md` — documents `make deploy-infra`, `make teardown-infra`, `make install-test-deps`, `make e2e` targets; deployment sequence diagram; kustomize overlay structure in `deploy/kind/`; environment variables for timeout configuration; prerequisites (docker, kubectl); CI job description. (REQ-001 through REQ-012)</task>\n\n  </tasks>\n  <implementation-notes>**Architecture**: This feature creates a shell-based deployment orchestration layer on top of the existing FluxCD manifests (CC-0008) and OpenBao bootstrap scripts (CC-0009). The key design choice is using kustomize overlays in `deploy/kind/` to patch production manifests rather than creating separate kind-specific manifests — this ensures kind testing always validates the same manifests that ship to production, with only resource sizing differences.\n\n**Kustomize overlay strategy**: `deploy/kind/base/kustomization.yaml` uses the existing `deploy/flux-system/` as its base and adds a strategic merge patch for the OpenBao HelmRelease. The patch must set `server.ha.enabled: false`, `server.ha.replicas: 1`, change the Raft config to standalone mode (removing retry_join peers), and set `dataStorage.storageClass: standard`. Similarly, `deploy/kind/infrastructure/kustomization.yaml` patches MariaDB (1 replica, no Galera, no MaxScale, `standard` storage class) and Memcached (1 replica).\n\n**OpenBao bootstrap adaptation**: The existing bootstrap scripts (CC-0009) expect 3-replica HA. The deploy-infra.sh script must adapt by setting `PODS=(openbao-0)` to skip pods 1 and 2 during init-unseal. The BAO_TOKEN extraction from the init-unseal output must be passed to subsequent scripts. Since the bootstrap scripts use `kubectl exec` with the namespace from common.sh, no modifications to the CC-0009 scripts are needed.\n\n**Deployment sequence**: The 8-step sequence is: (1) kind create cluster, (2) flux install, (3) kubectl apply -k deploy/kind/base, (4) wait HelmReleases Ready, (5) kubectl apply -k deploy/kind/infrastructure, (6) wait OpenBao pods Ready, (7) OpenBao bootstrap (5 scripts), (8) wait ExternalSecrets synced. The wait functions use `kubectl wait` with configurable timeouts defaulting to 300s.\n\n**CI job**: The e2e-infra job runs independently of lint and test (no `needs:` dependency). It uses `helm/kind-action` to create the cluster (this action handles docker setup in GitHub runners), installs FluxCD via the flux CLI, then delegates to `make deploy-infra` and `chainsaw test`. The JUnit report is uploaded via `actions/upload-artifact`.\n\n**Key files to reference**: `deploy/flux-system/kustomization.yaml` (base resources), `deploy/flux-system/infrastructure/kustomization.yaml` (infrastructure CRs), `deploy/openbao/bootstrap/*.sh` (bootstrap scripts from CC-0009), `deploy/flux-system/releases/openbao.yaml` (OpenBao HelmRelease to patch), `deploy/flux-system/infrastructure/mariadb.yaml` (MariaDB CR to patch), `deploy/flux-system/infrastructure/memcached.yaml` (Memcached CR to patch), `tests/e2e/keystone/invalid-cr/chainsaw-test.yaml` (Chainsaw test pattern reference), `.github/workflows/ci.yaml` (CI conventions reference).\n\n**Potential pitfalls**: (1) FluxCD reconciliation timing — HelmReleases may take 2-3 minutes to become Ready after apply; the wait timeout must account for this. (2) OpenBao TLS — the cert-manager Certificate for OpenBao TLS must be issued before OpenBao pods can start; this is handled by the phase ordering. (3) OpenBao standalone Raft — standalone mode requires the Raft config to omit retry_join peers, otherwise the single node will fail to bootstrap. (4) kind networking — kind uses a custom Docker network; FluxCD's source-controller must be able to reach external Helm registries from within the cluster.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"deploy/kind/base/kustomization.yaml\" test-function=\"kustomize_build_base_produces_valid_yaml\" requirement-id=\"REQ-003\">`kustomize build deploy/kind/base/` produces valid YAML with OpenBao HelmRelease patched to standalone mode (1 replica)</spec>\n    <spec test-file=\"deploy/kind/infrastructure/kustomization.yaml\" test-function=\"kustomize_build_infrastructure_produces_valid_yaml\" requirement-id=\"REQ-003\">`kustomize build deploy/kind/infrastructure/` produces valid YAML with MariaDB CR at 1 replica and Memcached CR at 1 replica</spec>\n    <spec test-file=\"deploy/kind/base/kustomization.yaml\" test-function=\"kustomize_build_base_openbao_standalone_no_ha\" requirement-id=\"REQ-003\">The rendered OpenBao HelmRelease has `server.ha.enabled: false`, `server.ha.replicas: 1`, and no retry_join peers in the Raft config</spec>\n    <spec test-file=\"deploy/kind/infrastructure/kustomization.yaml\" test-function=\"kustomize_build_infrastructure_mariadb_no_galera\" requirement-id=\"REQ-003\">The rendered MariaDB CR has `replicas: 1`, `galera.enabled: false`, `maxScale.enabled: false`, `storage.storageClassName: standard`</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_creates_kind_cluster\" requirement-id=\"REQ-001\">The script creates a kind cluster using hack/kind-config.yaml, the cluster is accessible via kubectl</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_installs_flux\" requirement-id=\"REQ-001\">After kind cluster creation, `flux install` is run and flux-system pods are running</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_waits_for_helmreleases\" requirement-id=\"REQ-004\">After applying base kustomization, the script waits until all 5 HelmReleases (cert-manager, openbao, mariadb-operator, external-secrets, memcached-operator) show Ready=True</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_runs_openbao_bootstrap\" requirement-id=\"REQ-005\">After OpenBao pods are Ready, the script invokes all 5 bootstrap scripts (init-unseal, setup-secret-engines, setup-auth, setup-policies, write-bootstrap-secrets) in sequence</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_waits_for_externalsecrets_synced\" requirement-id=\"REQ-004\">After OpenBao bootstrap, the script waits for all 3 ExternalSecrets to reach SecretSynced condition</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_fails_if_cluster_exists\" requirement-id=\"REQ-001\">If a kind cluster with the same name already exists, the script exits non-zero with a descriptive error</spec>\n    <spec test-file=\"hack/teardown-infra.sh\" test-function=\"teardown_deletes_cluster\" requirement-id=\"REQ-002\">Running the teardown script deletes the kind cluster; `kind get clusters` no longer lists it</spec>\n    <spec test-file=\"hack/teardown-infra.sh\" test-function=\"teardown_idempotent_no_cluster\" requirement-id=\"REQ-002\">Running teardown when no cluster exists exits 0 without error</spec>\n    <spec test-file=\"hack/install-test-deps.sh\" test-function=\"install_test_deps_installs_tools\" requirement-id=\"REQ-006\">After running the script, chainsaw, flux, kind, and kubectl are available on PATH with correct pinned versions</spec>\n    <spec test-file=\"hack/install-test-deps.sh\" test-function=\"install_test_deps_idempotent\" requirement-id=\"REQ-006\">Running the script twice does not fail and does not re-download already installed tools</spec>\n    <spec test-file=\"tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml\" test-function=\"assert_operator_deployments_ready\" requirement-id=\"REQ-008\">Chainsaw asserts cert-manager, ESO, MariaDB Operator, and Memcached Operator Deployments have available replicas > 0</spec>\n    <spec test-file=\"tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml\" test-function=\"assert_openbao_statefulset_ready\" requirement-id=\"REQ-008\">Chainsaw asserts OpenBao StatefulSet has readyReplicas >= 1</spec>\n    <spec test-file=\"tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml\" test-function=\"assert_infrastructure_crs_ready\" requirement-id=\"REQ-008\">Chainsaw asserts ClusterIssuer Ready, MariaDB CR Ready, Memcached CR Ready conditions</spec>\n    <spec test-file=\"tests/e2e/infrastructure/infra-stack-health/chainsaw-test.yaml\" test-function=\"assert_eso_resources_functional\" requirement-id=\"REQ-008\">Chainsaw asserts ClusterSecretStore Valid and all 3 ExternalSecrets SecretSynced</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"e2e_infra_job_exists_with_correct_config\" requirement-id=\"REQ-009\">The e2e-infra job uses SHA-pinned actions, timeout-minutes: 20, uploads JUnit artifact, and runs make deploy-infra plus chainsaw test</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"e2e_infra_job_uses_sha_pinned_actions\" requirement-id=\"REQ-009\">All `uses:` references in the e2e-infra job use SHA-pinned versions (SHA hash comments), not floating tags</spec>\n    <spec test-file=\"hack/kind-config.yaml\" test-function=\"kind_config_single_control_plane\" requirement-id=\"REQ-010\">The kind config defines a single control-plane node (no workers)</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_has_spdx_header_and_strict_mode\" requirement-id=\"REQ-011\">Script has SPDX Apache-2.0 header, `set -euo pipefail`, and CC-0010 feature reference</spec>\n    <spec test-file=\"hack/deploy-infra.sh\" test-function=\"deploy_script_timeout_overridable\" requirement-id=\"REQ-012\">Setting HELMRELEASE_TIMEOUT=600 before running changes the timeout from default to 600s</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Platform engineer deploys the full infrastructure stack to a kind cluster with a single command\">As a platform engineer, I want to run `make deploy-infra` and have the full infrastructure stack (cert-manager, OpenBao, ESO, MariaDB Operator, Memcached Operator, infrastructure CRs, ExternalSecrets) deployed and healthy in a local kind cluster, so that I can validate the complete infrastructure chain locally before pushing changes</story>\n    <story title=\"Platform engineer tears down the infrastructure stack\">As a platform engineer, I want to run `make teardown-infra` and have the kind cluster fully deleted, so that I can clean up resources and free local machine resources after testing</story>\n    <story title=\"Platform engineer installs E2E test dependencies\">As a platform engineer, I want to run `make install-test-deps` to install chainsaw, flux CLI, kind, and helm prerequisites, so that I can set up my development environment for running E2E tests without manual tool installation</story>\n    <story title=\"Platform engineer runs E2E tests against the deployed infrastructure\">As a platform engineer, I want to run `make e2e` and execute all Chainsaw E2E tests against the running kind cluster, so that I can validate that the infrastructure stack is healthy before merging changes</story>\n    <story title=\"CI pipeline validates infrastructure stack health on every PR\">As a CI system, I want to run the full infrastructure deployment and Chainsaw E2E test automatically on every PR and push to main, so that infrastructure regressions are caught before merge</story>\n    <story title=\"Kustomize overlays reduce resource requirements for kind\">As a platform engineer, I want kind-specific kustomize overlays that patch production manifests to use single-replica, reduced-resource configurations suitable for a single-node kind cluster, so that the infrastructure stack can run on a developer laptop or CI runner with limited resources (~7GB RAM, 2 vCPUs)</story>\n    <story title=\"Chainsaw E2E test validates all infrastructure components reach healthy state\">As a CI system, I want a comprehensive Chainsaw test that asserts readiness of all deployed components, so that any infrastructure regression is immediately detectable</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The system SHALL provide a `make deploy-infra` target that deploys the full infrastructure stack to a kind cluster in dependency order</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The system SHALL provide a `make teardown-infra` target that deletes the kind cluster</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The system SHALL provide kustomize overlays in `deploy/kind/` that patch production manifests for single-node kind cluster resource constraints</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The deployment script SHALL apply manifests in two phases with health waits between phases</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The deployment script SHALL invoke existing OpenBao bootstrap scripts from `deploy/openbao/bootstrap/` for full end-to-end secret chain validation</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The system SHALL provide a `make install-test-deps` target that installs E2E test dependencies</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The system SHALL provide a `make e2e` target that runs Chainsaw E2E tests</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The Chainsaw E2E test SHALL assert readiness of all deployed infrastructure components</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The GitHub Actions CI workflow SHALL include an `e2e-infra` job that runs the infrastructure E2E test on every PR and push to main</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The kind cluster configuration SHALL define a single control-plane node sufficient for CI runners</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">All new shell scripts SHALL follow existing project conventions: `set -euo pipefail`, SPDX Apache-2.0 header, feature ID reference</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHOULD\">The deployment script SHOULD include configurable timeouts with wait_for_ready helper functions</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Check if previous feature already implemented any of our pending tasks</rule>\n  <rule>Run: git log --oneline -5 to see what the previous feature committed</rule>\n  <rule>Mark tasks as removed_tasks if they were done by the previous feature</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 29102,
  "caller": "run_claude_cli"
}