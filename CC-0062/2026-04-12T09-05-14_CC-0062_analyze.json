{
  "timestamp": "2026-04-12T09:05:14.688576+00:00",
  "feature_id": "CC-0062",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0062",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0062\">\n  <title>Separate liveness and readiness probes for DB health</title>\n  <description># GitHub Issue #163: Separate liveness and readiness probes to prevent DB-induced CrashLoop\n\n**Category**: improvement | **Scope**: Small\n\n**Description**: Both the liveness and readiness probes on the `keystone-api` container currently use the same check: `HTTPGet /v3:5000`. This endpoint requires a working database connection — if MariaDB is temporarily unavailable, `/v3` returns HTTP 500, the liveness probe fails, and kubelet kills the container. With multiple replicas this creates a thundering-herd restart: all pods crash-loop simultaneously and bombard the recovering database with connection storms, delaying recovery further.\n\nSeparate the probes by intent:\n\n- **Liveness probe** → `TCPSocket` on port 5000. This verifies that the uWSGI process is alive and accepting connections. A TCP check does not exercise the database path, so a transient DB outage will not trigger unnecessary restarts.\n- **Readiness probe** → `HTTPGet /v3` on port 5000 (unchanged). This verifies full API functionality including database connectivity. When the DB is down, the pod becomes not-ready and is removed from the Service endpoints — no traffic is routed to it, but the process stays alive and can recover in-place once the DB returns.\n\n```go\nLivenessProbe: &corev1.Probe{\n    ProbeHandler: corev1.ProbeHandler{\n        TCPSocket: &corev1.TCPSocketAction{\n            Port: intstr.FromInt32(5000),\n        },\n    },\n    InitialDelaySeconds: 15,\n    PeriodSeconds:       20,\n},\nReadinessProbe: &corev1.Probe{\n    ProbeHandler: corev1.ProbeHandler{\n        HTTPGet: &corev1.HTTPGetAction{\n            Path: \"/v3\",\n            Port: intstr.FromInt32(5000),\n        },\n    },\n    InitialDelaySeconds: 5,\n    PeriodSeconds:       10,\n},\n```\n\n**Motivation**: A shared database is a common-mode failure — restarting the application pod cannot fix it. The established pattern (proven by operators like Keycloak/Quarkus) is: liveness checks the process, readiness checks the function. When the DB recovers, pods that remained alive immediately pass their readiness probe and re-enter the Service endpoints without a cold start. This reduces mean-time-to-recovery from minutes (CrashLoop backoff) to seconds (next readiness probe cycle).\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/reconcile_deployment.go` (`buildKeystoneDeployment`, lines 170-189)\n- `operators/keystone/internal/controller/reconcile_deployment_test.go`\n\n**Acceptance Criteria**:\n- [ ] Liveness probe uses `TCPSocket` on port 5000\n- [ ] Readiness probe uses `HTTPGet /v3` on port 5000 (unchanged)\n- [ ] Pods remain running (no restart) when MariaDB is temporarily unavailable\n- [ ] Pods are removed from Service endpoints when MariaDB is unavailable (readiness fails)\n- [ ] Pods re-enter Service endpoints within one readiness probe period after DB recovery\n- [ ] Unit tests verify probe types and configuration values\n- [ ] Existing liveness/readiness timing parameters (initialDelaySeconds, periodSeconds) remain unchanged\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/163</description>\n  <tasks>\n    <task status=\"pending\">1.1 Change liveness probe from HTTPGet to TCPSocket in buildKeystoneDeployment and update unit tests (REQ-001, REQ-002, REQ-003)</task>\n    <task status=\"pending\">2.1 Update keystone-reconciler reference documentation to reflect separated probe design (REQ-004)</task>\n\n  </tasks>\n  <implementation-notes>Minimal change: replace HTTPGet with TCPSocket in the liveness probe handler within buildKeystoneDeployment. The corev1.TCPSocketAction type is already available via the corev1 import. intstr.FromInt32 is already used for the readiness probe port. No new dependencies or imports required.\n\nThe change affects exactly 2 production lines (the ProbeHandler block for liveness) and ~6 test assertion lines. Documentation update is a table row change plus a rationale note.\n\nPattern followed: existing probe configuration pattern in the same function, just switching the handler type.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (liveness probe assertions)\" requirement-id=\"REQ-001\">LivenessProbe.TCPSocket is not nil, TCPSocket.Port equals 5000, LivenessProbe.HTTPGet is nil, InitialDelaySeconds=15, PeriodSeconds=20</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (readiness probe assertions)\" requirement-id=\"REQ-002\">ReadinessProbe.HTTPGet is not nil, HTTPGet.Path equals /v3, HTTPGet.Port equals 5000, InitialDelaySeconds=5, PeriodSeconds=10 — all unchanged</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (timing parameters)\" requirement-id=\"REQ-003\">Liveness InitialDelaySeconds=15 and PeriodSeconds=20; Readiness InitialDelaySeconds=5 and PeriodSeconds=10</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (liveness HTTPGet is nil)\" requirement-id=\"REQ-001\">LivenessProbe.HTTPGet is nil — verifies no HTTP handler is configured on liveness</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_FullReconcile_Brownfield (existing — no changes needed)\" requirement-id=\"REQ-001\">Full reconciliation flow succeeds with the updated probe configuration — integration test does not assert probe types directly but validates end-to-end reconciliation</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Keystone pods survive transient DB outages without restarting\">As a cluster operator, I want Keystone API pods to remain running when MariaDB is temporarily unavailable, so that the pods can recover in-place once the database returns, avoiding CrashLoop backoff delays and thundering-herd restarts</story>\n    <story title=\"Keystone pods are removed from Service endpoints when DB is down\">As a cluster operator, I want Keystone API pods to be removed from Service endpoints when they cannot serve requests due to DB unavailability, so that no user traffic is routed to pods that would return HTTP 500 errors</story>\n    <story title=\"Liveness probe verifies uWSGI process health without DB dependency\">As a platform engineer, I want the liveness probe to only check that the uWSGI process is alive and accepting TCP connections, so that a common-mode DB failure does not cascade into unnecessary pod restarts across all replicas</story>\n    <story title=\"Unit tests verify separated probe configuration\">As a developer, I want unit tests to assert the correct probe types and timing parameters, so that regressions to the old shared-probe behavior are caught immediately in CI</story>\n    <story title=\"Documentation reflects separated probe design\">As a developer, I want the keystone-reconciler reference documentation to accurately describe the separated probe configuration, so that other engineers understand the intentional probe separation and its rationale</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The keystone-api container liveness probe SHALL use TCPSocket on port 5000</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The keystone-api container readiness probe SHALL continue using HTTPGet /v3 on port 5000</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The liveness probe timing parameters SHALL remain unchanged (initialDelaySeconds=15, periodSeconds=20)</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The keystone-reconciler reference documentation SHALL reflect the updated probe configuration</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 10051,
  "caller": "run_claude_cli"
}