{
  "timestamp": "2026-04-05T13:37:37.130773+00:00",
  "feature_id": "CC-0042",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0042",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0042\">\n  <title>Add spec.resources field to Keystone CRD</title>\n  <description>**Size:** 🔧 small\n**Category:** backend\n**Priority:** critical\n**Source:** GitHub Issue [#120](https://github.com/C5C3/forge/issues/120)\n\nAdd an optional `spec.resources` field of type `*corev1.ResourceRequirements` to `KeystoneSpec` in `keystone_types.go` with `+optional` marker and `omitempty` JSON tag. In the defaulting webhook (`Default()` in `keystone_webhook.go`), inject sensible defaults (256Mi/512Mi memory, 100m/500m CPU) when `spec.Resources == nil`. In the validation webhook (`validateKeystone()`), reject specs where any resource request exceeds its corresponding limit via `specPath.Child(\"resources\")` accumulated into `field.ErrorList`. Wire `spec.Resources` into the `keystone-api` container in `buildKeystoneDeployment()` in `reconcile_deployment.go`. Regenerate the CRD manifest via `controller-gen`. Add unit tests for webhook defaulting (nil → defaults, explicit preserved), validation (valid, request > limit rejected), and deployment builder (resources in container spec). Update CRD reference docs at `docs/reference/keystone-crd.md`.\n\n**Rationale:** Without resource requests, pods run as BestEffort QoS and the existing HPA (CC-0038) cannot compute utilization ratios since it targets CPU/memory utilization percentages against undefined baselines. This is a correctness bug — HPA is effectively non-functional without defined resource requests to compute percentages against.\n\n**Affected Areas:**\n- operators/keystone/api/v1alpha1/keystone_types.go\n- operators/keystone/api/v1alpha1/keystone_webhook.go\n- operators/keystone/api/v1alpha1/keystone_webhook_test.go\n- operators/keystone/internal/controller/reconcile_deployment.go\n- operators/keystone/internal/controller/reconcile_deployment_test.go\n- helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml\n- docs/reference/keystone-crd.md</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add Resources field to KeystoneSpec and regenerate DeepCopy (CC-0042, REQ-004)</task>\n    <task status=\"pending\">2.1 Add resource defaulting in Default() webhook with unit tests (CC-0042, REQ-001)</task>\n    <task status=\"pending\">2.2 Add resource validation in validate() webhook with unit tests (CC-0042, REQ-002)</task>\n    <task status=\"pending\">2.3 Wire resources into buildKeystoneDeployment with unit tests (CC-0042, REQ-003)</task>\n    <task status=\"pending\">2.4 Regenerate CRD manifest and sync to Helm chart (CC-0042, REQ-004)</task>\n    <task status=\"pending\">3.1 Add integration tests for resources field admission pipeline (CC-0042, REQ-001, REQ-002)</task>\n    <task status=\"pending\">3.2 Update CRD reference documentation for resources field (CC-0042, REQ-005)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\n```\n┌───────────────────────┐     ┌──────────────────────────────┐     ┌───────────────────────────────┐\n│  KeystoneSpec         │     │  Defaulting Webhook          │     │  Validating Webhook           │\n│  (keystone_types.go)  │────▶│  Default() in webhook.go     │────▶│  validate() in webhook.go     │\n│                       │     │                              │     │                               │\n│  Resources            │     │  if Resources == nil:         │     │  for each request in Requests: │\n│  *corev1.Resource     │     │    inject defaults            │     │    if request > limit:         │\n│   Requirements        │     │    (256Mi/512Mi, 100m/500m)  │     │      append field.Invalid      │\n└───────────────────────┘     └──────────────────────────────┘     └───────────────────────────────┘\n                                                                              │\n                                                                              ▼\n                              ┌──────────────────────────────┐     ┌───────────────────────────────┐\n                              │  Deployment Reconciler       │     │  Kubernetes                   │\n                              │  buildKeystoneDeployment()   │     │                               │\n                              │  in reconcile_deployment.go  │     │  Scheduler: QoS-aware         │\n                              │                              │────▶│  HPA: utilization %           │\n                              │  Container.Resources =       │     │    = usage / request           │\n                              │    *spec.Resources           │     │                               │\n                              └──────────────────────────────┘     └───────────────────────────────┘\n```\n\n## Data Flow\n\n1. User creates/updates Keystone CR (spec.resources may be nil or explicit)\n2. Mutating webhook: Default() checks if Resources == nil → injects defaults\n3. CRD schema validation: Kubernetes validates the ResourceRequirements structure\n4. Validating webhook: validate() checks each request vs corresponding limit\n5. Reconciler: buildKeystoneDeployment() reads spec.Resources and sets it on the container\n6. Kubernetes scheduler uses resource requests for bin-packing decisions\n7. HPA computes utilization ratios: current_usage / resource_request\n\n## Key Design Decisions\n\n1. **Pointer field pattern**: Resources is `*corev1.ResourceRequirements` (pointer) with `+optional` and `omitempty`, matching the established pattern used by Autoscaling, NetworkPolicy, and Federation fields on KeystoneSpec. Nil means 'not specified by user' which triggers defaulting.\n\n2. **Nil-check defaulting**: The webhook uses `obj.Spec.Resources == nil` (not zero-value check) because ResourceRequirements has no natural zero value that distinguishes 'unset' from 'intentionally empty'. This differs from scalar defaults (replicas == 0) but matches the pointer-field convention.\n\n3. **Generic resource iteration**: Validation iterates over `Requests` map keys rather than hardcoding CPU/memory, making it automatically handle any future resource types (e.g. ephemeral-storage, hugepages). The field path uses `string(resourceName)` for accurate error reporting.\n\n4. **Safe nil handling in builder**: buildKeystoneDeployment handles nil Resources gracefully with a zero-value fallback, even though the webhook guarantees non-nil. This prevents panics if the reconciler processes a CR that bypassed the webhook (e.g. pre-existing CRs during operator upgrade).\n\n## Import Requirements\n\n- **keystone_types.go**: Add `corev1 \"k8s.io/api/core/v1\"`\n- **keystone_webhook.go**: Add `corev1 \"k8s.io/api/core/v1\"` and `\"k8s.io/apimachinery/pkg/api/resource\"` (for MustParse in defaults)\n- **keystone_webhook_test.go**: Already imports `corev1` — add `\"k8s.io/apimachinery/pkg/api/resource\"` for test assertions\n- **reconcile_deployment_test.go**: Add `\"k8s.io/apimachinery/pkg/api/resource\"` for test helper\n\n## Pitfalls\n\n1. **DeepCopy generation**: Must run `make generate` after adding the pointer field to ensure zz_generated.deepcopy.go handles nil-check correctly. Forgetting this causes compilation errors.\n\n2. **Test helper updates**: `deployTestKeystone()` in reconcile_deployment_test.go must include Resources to match what the webhook would produce. The `validKeystone()` helper in webhook tests does NOT need Resources (nil is valid for validation since the check is only triggered when Resources is non-nil).\n\n3. **CRD sync**: Must run both `make manifests` AND `make sync-crds` — the first generates the CRD, the second copies it to the Helm chart directory. `make verify-crd-sync` in CI catches desync.\n\n4. **Resource quantity comparison**: Use `request.Cmp(limit) > 0` (not `request > limit`) because resource.Quantity does not support Go comparison operators.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestDefault_ResourcesSetWhenNil [unit]\" requirement-id=\"REQ-001\">Default() on Keystone with nil spec.resources should set Resources to {Requests: {memory: 256Mi, cpu: 100m}, Limits: {memory: 512Mi, cpu: 500m}}</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestDefault_ResourcesPreservedWhenExplicit [unit]\" requirement-id=\"REQ-001\">Default() on Keystone with explicit Resources (1Gi/2Gi memory, 200m/1 CPU) should not modify the values</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_ResourcesValidAccepted [unit]\" requirement-id=\"REQ-002\">Keystone with CPU request=100m/limit=500m and memory request=256Mi/limit=512Mi should pass validation without error</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_ResourcesCPURequestExceedsLimitRejected [unit]\" requirement-id=\"REQ-002\">Keystone with CPU request=1000m and limit=500m should fail validation with error containing 'resources' and 'cpu' and 'must not exceed limit'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_ResourcesMemoryRequestExceedsLimitRejected [unit]\" requirement-id=\"REQ-002\">Keystone with memory request=1Gi and limit=256Mi should fail validation with error containing 'resources' and 'memory' and 'must not exceed limit'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_ResourcesBothExceedReportsBothErrors [unit]\" requirement-id=\"REQ-002\">Keystone with both CPU (1000m > 500m) and memory (1Gi > 256Mi) request > limit should report both errors; error contains both 'cpu' and 'memory'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_ResourcesNilAccepted [unit]\" requirement-id=\"REQ-002\">Keystone with nil spec.resources should pass validation without any resource-related errors</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_ResourcesRequestsOnlyAccepted [unit]\" requirement-id=\"REQ-002\">Keystone with only Requests set (cpu: 100m, memory: 256Mi, no Limits) should pass validation</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_ContainerResources [unit]\" requirement-id=\"REQ-003\">The keystone-api container in the built Deployment should have Resources with Requests {memory: 256Mi, cpu: 100m} and Limits {memory: 512Mi, cpu: 500m}</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_CustomResources [unit]\" requirement-id=\"REQ-003\">The keystone-api container should have Resources matching custom spec values (e.g. 1Gi request, 2Gi limit memory) when set on the Keystone CR</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/integration_test.go\" test-function=\"TestIntegration_ResourcesDefaultedWhenNil [integration]\" requirement-id=\"REQ-001\">CR created without spec.resources should have defaults (256Mi/512Mi, 100m/500m) applied after passing through the full admission pipeline (mutating webhook → schema → validating webhook)</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/integration_test.go\" test-function=\"TestIntegration_ResourcesPreservedWhenExplicit [integration]\" requirement-id=\"REQ-001\">CR created with explicit resources (1Gi/2Gi, 200m/1) should preserve those values unchanged after admission</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/integration_test.go\" test-function=\"TestIntegration_ResourcesRequestExceedsLimitRejected [integration]\" requirement-id=\"REQ-002\">CR with CPU request=1000m > limit=500m should be rejected by the validating webhook with IsInvalid or IsForbidden error containing 'resources'</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Keystone pods get sensible resource defaults\">As a platform operator, I want Keystone pods to have resource requests and limits by default without me specifying them, so that pods run as Burstable QoS class and HPA (CC-0038) can compute utilization ratios against defined baselines</story>\n    <story title=\"Platform operator sets custom resource limits\">As a platform operator, I want to specify custom CPU and memory requests/limits for Keystone pods, so that I can right-size Keystone for my cluster's capacity and workload characteristics</story>\n    <story title=\"Invalid resource specs are rejected with clear errors\">As a platform operator, I want to receive clear, field-specific error messages when I submit invalid resource configurations, so that I can correct my CR spec without guessing what went wrong</story>\n    <story title=\"HPA computes utilization ratios correctly with resource baselines\">As a platform operator, I want HPA to function correctly by computing CPU/memory utilization percentages against defined resource requests, so that autoscaling responds to actual load rather than being non-functional due to undefined baselines</story>\n    <story title=\"CRD reference docs cover resources field comprehensively\">As a platform engineer, I want the CRD reference documentation to describe the resources field, its defaults, and validation rules, so that I can understand how to configure resources without reading source code</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The defaulting webhook SHALL inject resource defaults (256Mi request / 512Mi limit memory, 100m request / 500m limit CPU) when spec.resources is nil</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The validation webhook SHALL reject specs where any resource request exceeds its corresponding limit, accumulating all errors into field.ErrorList</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The buildKeystoneDeployment function SHALL include spec.resources in the keystone-api container specification</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The CRD manifest SHALL be regenerated to include the resources field with embedded corev1.ResourceRequirements schema, and synced to the Helm chart</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The CRD reference documentation SHALL include the resources field in the KeystoneSpec table, defaulting webhook table, and validation rules table</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 16393,
  "caller": "run_claude_cli"
}