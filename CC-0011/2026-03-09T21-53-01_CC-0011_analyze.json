{
  "timestamp": "2026-03-09T21:53:01.370021+00:00",
  "feature_id": "CC-0011",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0011",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0011\">\n  <title>E001: Add Keystone API types, webhooks & CRD generation</title>\n  <description>**Size:** 🏗️ large\n**Category:** backend\n**Priority:** critical\n\n## Decisions (from open questions)\n\n1. **Cache.Backend defaulting** → Yes, `Default()` sets `Cache.Backend = \"dogpile.cache.pymemcache\"` when empty (per architecture doc line 318-319).\n2. **`groupversion_info.go`** → Yes, included in scope (mandatory prerequisite — types and webhook reference `SchemeBuilder` and `GroupVersion`).\n3. **`Fernet.RotationSchedule` defaulting** → Rely on Kubebuilder marker only (`+kubebuilder:default=\"0 0 * * 0\"`); `Default()` does not set it.\n\n## Summary\n\nDefine the Keystone CRD types (`CC-0011`) as the reference implementation for all CobaltCore service operators. This includes the root `Keystone` type with Kubebuilder markers, the full `KeystoneSpec` with validated sub-specs (Database, Cache, Fernet, Federation, Bootstrap, Middleware, Plugins, PolicyOverrides, ExtraConfig), a `KeystoneStatus` with conditions and endpoint, defaulting and validation webhooks, CRD YAML generation via controller-gen, and comprehensive unit tests for every webhook rule.\n\n## Scope\n\n**Included:**\n- `operators/keystone/api/v1alpha1/keystone_types.go` — Keystone, KeystoneList, KeystoneSpec, KeystoneStatus, FernetSpec, FederationSpec, BootstrapSpec with all Kubebuilder markers (`+object:root`, `+subresource:status`, `+printcolumn`, `+validation:Minimum`, `+default`, `+validation:XValidation`)\n- `operators/keystone/api/v1alpha1/groupversion_info.go` — SchemeBuilder, GroupVersion (`keystone.openstack.c5c3.io/v1alpha1`), AddToScheme\n- `operators/keystone/api/v1alpha1/keystone_webhook.go` — `Default()` (Replicas→3, Fernet.MaxActiveKeys→3, Cache.Backend→\"dogpile.cache.pymemcache\", Bootstrap.AdminUser→\"admin\", Bootstrap.Region→\"RegionOne\"), `ValidateCreate()`/`ValidateUpdate()` delegating to shared `validate()` with: cron expression validation, duplicate plugin ConfigSection detection, PolicyOverrides source requirement, empty policy rule name detection\n- `operators/keystone/api/v1alpha1/zz_generated.deepcopy.go` — generated by `controller-gen object`\n- `operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml` — generated by `controller-gen crd`\n- `operators/keystone/api/v1alpha1/keystone_webhook_test.go` — table-driven unit tests with Gomega for every defaulting and validation rule\n- `operators/keystone/main.go` update — add `keystonev1alpha1.AddToScheme(scheme)` in `init()`\n- `/workspace/Makefile` update — wire `generate` and `manifests` targets to controller-gen for keystone operator\n- `operators/keystone/go.mod` update — add `github.com/robfig/cron/v3` as direct dependency\n\n**Excluded:**\n- Reconciler implementation (CC-0012+, separate feature)\n- Webhook certificate management / cert-manager integration (infrastructure concern, separate)\n- `ValidateDelete()` webhook (no delete validation required per architecture docs)\n- Conversion webhooks for future API versions (YAGNI — only v1alpha1 exists)\n- E2E/integration tests with envtest (separate task, this covers unit tests only)\n- Status condition logic / condition helpers (already implemented in `internal/common/conditions/`)\n- Shared types modifications (`DatabaseSpec`, `CacheSpec`, etc. already exist in `internal/common/types/`)\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph api[\"operators/keystone/api/v1alpha1/\"]\n        GV[\"groupversion_info.go\\nSchemeBuilder, GroupVersion\"]\n        Types[\"keystone_types.go\\nKeystone, KeystoneSpec, KeystoneStatus\\nFernetSpec, FederationSpec, BootstrapSpec\"]\n        WH[\"keystone_webhook.go\\nDefault, ValidateCreate, ValidateUpdate\"]\n        DC[\"zz_generated.deepcopy.go\\n#40;generated#41;\"]\n        Tests[\"keystone_webhook_test.go\\nTable-driven unit tests\"]\n    end\n\n    subgraph common[\"internal/common/types/\"]\n        ST[\"types.go\\nImageSpec, DatabaseSpec, CacheSpec\\nSecretRefSpec, PolicySpec\\nPluginSpec, MiddlewareSpec\"]\n    end\n\n    subgraph gen[\"Generation\"]\n        CG[\"controller-gen object\\n+ controller-gen crd\"]\n        CRD[\"config/crd/bases/\\nkeystone.openstack.c5c3.io_keystones.yaml\"]\n    end\n\n    subgraph entry[\"operators/keystone/\"]\n        Main[\"main.go\\nScheme registration\"]\n    end\n\n    Types -->|\"imports commonv1\"| ST\n    WH -->|\"validates\"| Types\n    GV -->|\"SchemeBuilder\"| Types\n    CG -->|\"generates\"| DC\n    CG -->|\"generates\"| CRD\n    Types -->|\"input\"| CG\n    Main -->|\"AddToScheme\"| GV\n    Tests -->|\"tests\"| WH\n```\n\n```mermaid\nflowchart LR\n    subgraph defaulting[\"Default webhook - 5 fields\"]\n        D1[\"Replicas: 0 -> 3\"]\n        D2[\"Fernet.MaxActiveKeys: 0 -> 3\"]\n        D3[\"Cache.Backend: empty -> dogpile.cache.pymemcache\"]\n        D4[\"Bootstrap.AdminUser: empty -> admin\"]\n        D5[\"Bootstrap.Region: empty -> RegionOne\"]\n    end\n\n    subgraph validation[\"Validation webhook - 5 rules\"]\n        V1[\"Replicas >= 1\"]\n        V2[\"Fernet.RotationSchedule valid cron\"]\n        V3[\"No duplicate Plugin.ConfigSection\"]\n        V4[\"PolicyOverrides: rules or configMapRef set\"]\n        V5[\"PolicyOverrides: no empty rule names\"]\n    end\n\n    subgraph xvalidation[\"XValidation CEL - 2 rules\"]\n        X1[\"Database: clusterRef XOR host\"]\n        X2[\"PolicyOverrides: rules OR configMapRef\"]\n    end\n```\n\n## Key Components\n\n- **`keystone_types.go`**: Root `Keystone` type with 3 printcolumns (Ready/Endpoint/Age), `KeystoneSpec` with 11 fields importing shared types from `commonv1`, sub-specs `FernetSpec` (cron schedule + max keys), `FederationSpec` (enabled bool), `BootstrapSpec` (admin user + secret ref + region), `KeystoneStatus` with conditions array + endpoint string, `KeystoneList` for list operations, `init()` scheme registration\n- **`groupversion_info.go`**: `GroupVersion` constant (`keystone.openstack.c5c3.io/v1alpha1`), `SchemeBuilder` and `AddToScheme` var (~15 lines of boilerplate)\n- **`keystone_webhook.go`**: `Default()` method (5 field defaults), `ValidateCreate()`/`ValidateUpdate()` delegating to shared `validate()` with 5 validation rules using `field.ErrorList` + `apierrors.NewInvalid()`, imports `robfig/cron/v3` for cron parsing\n- **`keystone_webhook_test.go`**: Table-driven tests covering: each default applied when zero-value, defaults not overriding explicit values, valid cron accepted, invalid cron rejected, duplicate plugin sections rejected, PolicyOverrides without source rejected, empty policy rule names rejected, valid specs passing, replicas < 1 rejected\n- **`zz_generated.deepcopy.go`**: Generated by `controller-gen object` — DeepCopyObject/DeepCopyInto for all types\n- **CRD YAML**: Generated by `controller-gen crd` with full OpenAPI v3 schema, XValidation rules, printer columns, status subresource\n- **`main.go` update**: Add `keystonev1alpha1.AddToScheme(scheme)` import and registration in `init()`\n- **`Makefile` update**: Wire `generate` and `manifests` targets from current no-ops to `controller-gen` commands scoped to `operators/keystone/api/...`\n- **`go.mod` update**: Add `github.com/robfig/cron/v3` as direct dependency (already an indirect dep via `internal/common`)The elaboration is complete. All sections delivered:\n\n- **Decisions**: 3 user-answered questions incorporated (Cache.Backend defaulting, groupversion_info.go inclusion, Fernet.RotationSchedule marker-only)\n- **Summary**: CC-0011 reference implementation scoped\n- **Scope**: 9 in-scope items, 7 excluded with YAGNI rationale\n- **Visualization**: 2 Mermaid diagrams (component architecture + webhook rule breakdown)\n- **Key Components**: 9 components mapped to actual codebase paths\n\nEvery aspect of the original feature request verified against the architecture doc (`03-crd-implementation.md`) and existing codebase: root type with 3 printcolumns, all 11 KeystoneSpec fields (Replicas, Image, Database, Cache, Fernet, Federation, Bootstrap, Middleware, Plugins, PolicyOverrides, ExtraConfig), XValidation CEL rules, KeystoneStatus, defaulting webhook (5 defaults), validation webhook (5 rules), CRD YAML generation, and unit tests.\n\n<promise>IMPLEMENTATION_COMPLETE</promise></description>\n  <tasks>\n    <task status=\"pending\">1.1 Create groupversion_info.go with GroupVersion, SchemeBuilder, and AddToScheme (CC-0011, REQ-010)</task>\n    <task status=\"pending\">1.2 Create keystone_types.go with all CRD types, Kubebuilder markers, and init() scheme registration (CC-0011, REQ-007, REQ-008)</task>\n    <task status=\"pending\">2.1 Create keystone_webhook.go with Default() and validate() functions; add robfig/cron/v3 dependency (CC-0011, REQ-001 through REQ-006)</task>\n    <task status=\"pending\">2.2 Create defaulting webhook unit tests in keystone_webhook_test.go (CC-0011, REQ-001)</task>\n    <task status=\"pending\">2.3 Add validation webhook unit tests to keystone_webhook_test.go (CC-0011, REQ-002 through REQ-006)</task>\n    <task status=\"pending\">3.1 Update main.go with keystonev1alpha1 scheme registration (CC-0011, REQ-010)</task>\n    <task status=\"pending\">3.2 Wire Makefile generate and manifests targets to controller-gen; run generation (CC-0011, REQ-009)</task>\n    <task status=\"pending\">4.1 Write reference documentation for Keystone CRD API types and webhooks (CC-0011)</task>\n\n  </tasks>\n  <implementation-notes>**Architecture**: Keystone CRD is the reference implementation for all CobaltCore service operators. The pattern established here (groupversion_info.go → types.go → webhook.go → tests → main.go scheme registration → Makefile generation) will be replicated for Nova, Neutron, Glance, etc.\n\n**Shared types**: All reusable specs (ImageSpec, DatabaseSpec, CacheSpec, SecretRefSpec, PolicySpec, PluginSpec, MiddlewareSpec) already exist in `internal/common/types/types.go` (CC-0004). KeystoneSpec imports these as `commonv1`. Only Keystone-specific sub-specs (FernetSpec, FederationSpec, BootstrapSpec) are defined in keystone_types.go.\n\n**Webhook pattern**: Default() uses conditional checks (==0 / ==\"\") rather than always-set to allow Kubebuilder +default markers to also provide schema-level defaults. validate() accumulates all errors in field.ErrorList and returns a single apierrors.NewInvalid — it does NOT short-circuit on the first error. ValidateCreate/ValidateUpdate both delegate to validate() since there are no create-specific or update-specific rules.\n\n**Cron validation**: Uses `github.com/robfig/cron/v3` ParseStandard (5-field cron, no seconds). This package is already an indirect dependency in internal/common/go.mod; adding it as a direct dep in keystone/go.mod.\n\n**Test pattern**: Table-driven tests with `testing.T` + `gomega.NewGomegaWithT(t)`, matching the convention in internal/common/conditions/conditions_test.go and internal/common/plugins/plugins_test.go. In-package tests (package v1alpha1, not v1alpha1_test) for access to unexported validate() method.\n\n**Generation**: controller-gen is used for two purposes: (1) `object` generates DeepCopy methods in zz_generated.deepcopy.go, (2) `crd` generates the full CRD YAML. The Makefile targets are parameterized to scope generation to operators/keystone/api/... paths. golangci-lint already excludes zz_generated.*.go via .golangci.yml.\n\n**Potential pitfalls**: (1) Ensure `api/v1alpha1/` directory is created before writing files. (2) The XValidation CEL expressions must use the exact syntax: `has(self.clusterRef) != has(self.host)` not `self.clusterRef != nil`. (3) PolicyOverrides is a pointer (*PolicySpec) so the validate() function must nil-check before accessing fields. (4) Federation is also a pointer (*FederationSpec) for the optional pattern. (5) The SchemeBuilder.Register call in init() of keystone_types.go must use &Keystone{} and &KeystoneList{}.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestDefault_setsAllDefaultsWhenZeroValued\" requirement-id=\"REQ-001\">Default() should set all 5 fields (replicas, maxActiveKeys, cache.backend, adminUser, region) to their default values when the spec has zero/empty values</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestDefault_doesNotOverrideExplicitValues\" requirement-id=\"REQ-001\">Default() should not modify any field that already has a non-zero/non-empty value</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestDefault_setsIndividualDefaults\" requirement-id=\"REQ-001\">Default() should set each individual default independently (table-driven test per field)</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_rejectsReplicasLessThan1\" requirement-id=\"REQ-002\">validate() should return field.Invalid error for spec.replicas when replicas=0 or negative</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_acceptsReplicasEqualOrGreaterThan1\" requirement-id=\"REQ-002\">validate() should not return errors for spec.replicas when replicas=1 or greater</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_acceptsValidCronExpression\" requirement-id=\"REQ-003\">validate() should not return errors for standard cron expressions like '0 0 * * 0'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_rejectsInvalidCronExpression\" requirement-id=\"REQ-003\">validate() should return field.Invalid for spec.fernet.rotationSchedule with 'not-a-cron' and include parse error details</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_rejectsEmptyCronExpression\" requirement-id=\"REQ-003\">validate() should return field.Invalid for spec.fernet.rotationSchedule when empty string</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_rejectsDuplicatePluginConfigSection\" requirement-id=\"REQ-004\">validate() should return field.Duplicate for spec.plugins[i].configSection when two plugins share the same section name</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_acceptsUniquePluginConfigSections\" requirement-id=\"REQ-004\">validate() should not return errors when plugins have distinct configSection values</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_rejectsPolicyOverridesWithoutSource\" requirement-id=\"REQ-005\">validate() should return field.Required for spec.policyOverrides when both rules and configMapRef are nil</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_acceptsPolicyOverridesWithRulesOnly\" requirement-id=\"REQ-005\">validate() should not return errors when policyOverrides has rules but no configMapRef</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_acceptsPolicyOverridesWithConfigMapRefOnly\" requirement-id=\"REQ-005\">validate() should not return errors when policyOverrides has configMapRef but no rules</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_rejectsEmptyPolicyRuleName\" requirement-id=\"REQ-006\">validate() should return field.Invalid for spec.policyOverrides.rules when a rule key is empty string</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_accumulatesMultipleErrors\" requirement-id=\"REQ-006\">validate() should return all validation errors (e.g., invalid replicas AND invalid cron) in a single ErrorList, not short-circuit on first error</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_validSpecPasses\" requirement-id=\"REQ-002\">validate() should return no errors for a fully valid Keystone spec with all required fields set correctly</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidateCreate_delegatesToValidate\" requirement-id=\"REQ-002\">ValidateCreate() should call validate() and return its results (no additional create-specific validation)</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidateUpdate_delegatesToValidate\" requirement-id=\"REQ-002\">ValidateUpdate() should call validate() and return its results (no additional update-specific validation)</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Platform operator creates a Keystone CR with minimal spec and gets sensible defaults\">As a platform operator, I want to create a Keystone CR with only required fields and have optional fields defaulted to sensible values, so that I can deploy Keystone quickly without specifying every configuration option</story>\n    <story title=\"Platform operator gets immediate validation feedback for invalid Keystone specs\">As a platform operator, I want to receive clear, field-specific error messages when I submit an invalid Keystone CR, so that I can correct my configuration before the operator attempts reconciliation</story>\n    <story title=\"API server rejects structurally invalid Keystone CRs via OpenAPI schema and CEL rules\">As a platform operator, I want the API server to reject CRs that violate structural constraints before they reach the webhook, so that I get the fastest possible feedback on schema violations</story>\n    <story title=\"Developer creates a new service operator CRD following the Keystone reference pattern\">As a CRD developer, I want to use Keystone's types, webhook, and generation setup as a reference implementation, so that all CobaltCore operators follow consistent CRD patterns</story>\n    <story title=\"CI pipeline generates and validates CRD artifacts from Go types\">As a CI system, I want make generate and make manifests to produce deepcopy code and CRD YAML from Go types, so that CRD manifests are always in sync with the Go type definitions</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The defaulting webhook SHALL set Replicas to 3, Fernet.MaxActiveKeys to 3, Cache.Backend to 'dogpile.cache.pymemcache', Bootstrap.AdminUser to 'admin', and Bootstrap.Region to 'RegionOne' when their values are zero or empty</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The validation webhook SHALL reject Keystone CRs with replicas less than 1</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The validation webhook SHALL reject Keystone CRs with an invalid cron expression in fernet.rotationSchedule</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The validation webhook SHALL reject Keystone CRs with duplicate plugin configSection values</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The validation webhook SHALL reject PolicyOverrides when neither rules nor configMapRef is set</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The validation webhook SHALL reject PolicyOverrides containing empty rule names</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The KeystoneSpec SHALL include XValidation CEL rules for Database (clusterRef XOR host) and PolicyOverrides (rules OR configMapRef)</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The Keystone CRD types SHALL define all 11 KeystoneSpec fields with correct Go types, JSON tags, and Kubebuilder markers</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The controller-gen generation SHALL produce valid deepcopy code and CRD YAML from the Go types</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The Keystone operator main.go SHALL register the Keystone API types with the runtime scheme</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 21990,
  "caller": "run_claude_cli"
}