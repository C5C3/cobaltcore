{
  "timestamp": "2026-04-25T08:28:30.200936+00:00",
  "feature_id": "CC-0094",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0094",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0094\">\n  <title>test(e2e): Expand webhook/CEL rejection matrix for Keystone</title>\n  <description># GitHub Issue #281: test(e2e): Expand webhook/CEL rejection matrix for Keystone CR (#277 Phase 1)\n\n## Context\n\nParent: #277, Phase 1 → *Webhook / CEL validation matrix*.\n\nToday `tests/e2e/keystone/invalid-cr/` covers exactly two rejection cases: invalid cron in `spec.fernet.rotationSchedule` and duplicate `plugins[*].configSection` values (see `tests/e2e/keystone/invalid-cr/chainsaw-test.yaml`, REQ-008/009 under CC-0012).\n\nThe validation rules in `operators/keystone/api/v1alpha1/keystone_types.go` (CEL via `+kubebuilder:validation:XValidation`) and `operators/keystone/api/v1alpha1/keystone_webhook.go` (defense-in-depth) cover considerably more misconfigurations than that. This sub-issue closes the E2E gap for every rejection path that is already implemented.\n\n## Scope — new rejection cases\n\nThe existing test is extended with eleven input files and matching `apply` steps. Each row is taken 1:1 from the Phase 1 checklist in #277:\n\n| # | Scenario | Enforced by | Expected `fieldPath` / key string |\n|---|----------|-------------|------------------------------------|\n| 1 | `spec.database` with **both** `clusterRef` and `host` set | CEL on `DatabaseSpec` (keystone_types.go:61) + webhook (keystone_webhook.go:195) | `spec.database` / `exactly one of clusterRef or host must be set` |\n| 2 | `spec.cache` with **both** `clusterRef` and `servers` set | CEL on `CacheSpec` (types.go:66) + webhook (webhook.go:185) | `spec.cache` / `exactly one of clusterRef or servers must be set` |\n| 3 | `spec.autoscaling` with neither `targetCPUUtilization` nor `targetMemoryUtilization` | CEL on `AutoscalingSpec` (types.go:207) + webhook | `spec.autoscaling` / `at least one of targetCPUUtilization or targetMemoryUtilization` |\n| 4 | `spec.policyOverrides` with neither `rules` nor `configMapRef` | CEL (types.go:100) + webhook | `spec.policyOverrides` / `at least one of rules or configMapRef must be set` |\n| 5 | `spec.policyOverrides.rules` with an empty key | CEL (types.go:101) + webhook | `spec.policyOverrides.rules` / `policy rule name must not be empty` |\n| 6 | `spec.networkPolicy.ingress: []` (empty) | CEL on `NetworkPolicySpec` (types.go:236) + webhook | `spec.networkPolicy.ingress` / `at least one ingress source must be specified` |\n| 7 | `spec.replicas: 0` **and** `spec.replicas: -1` (two variants) | `+kubebuilder:validation:Minimum=1` + webhook | `spec.replicas` / `replicas must be at least 1` |\n| 8 | `spec.autoscaling.minReplicas > spec.autoscaling.maxReplicas` | webhook (webhook.go ~428) | `spec.autoscaling.minReplicas` / `must not exceed maxReplicas` |\n| 9 | `spec.fernet.maxActiveKeys: 2` **and** `spec.credentialKeys.maxActiveKeys: 2` | `+kubebuilder:validation:Minimum=3` + webhook | `spec.fernet.maxActiveKeys` / `must be at least 3` (same for `credentialKeys`) |\n\n> Note: the parent checklist says `maxActiveKeys < 2`, but the code enforces `>= 3`. The test follows the code; the parent checklist will be corrected when the box is ticked.\n\n## Explicitly out of scope (prerequisites, separate tickets)\n\nThree checkboxes from Phase 1 / \"Webhook matrix\" require webhook features that do **not** exist today. They need feature tickets first; the E2E tests follow once the feature lands (see #277, *Out of scope*):\n\n- Empty / malformed `spec.image.tag` — `commonv1.ImageSpec.Tag` (`internal/common/types/types.go:14`) has neither `MinLength` nor a regex pattern.\n- `topologySpreadConstraints[*].maxSkew: 0` — upstream `corev1.TopologySpreadConstraint.MaxSkew` carries no CRD-level minimum; no defense-in-depth in the Keystone webhook today.\n- Mutation of immutable fields (`spec.database.clusterRef`, analogously `spec.cache.clusterRef`) — `ValidateUpdate` does not currently compare old vs. new.\n\nThose three points will be split out of the Phase 1 list into their own `feat(CC-xxxx): harden <field> validation` issues, each followed by a dedicated test.\n\n## Test design\n\nLocation: `tests/e2e/keystone/invalid-cr/` (extend the existing test — **no** new directory; every case shares the scope \"webhook rejects an otherwise minimal Keystone CR\").\n\nFile layout after the change:\n\n```\ntests/e2e/keystone/invalid-cr/\n├── 00-invalid-cron.yaml                             (existing)\n├── 01-duplicate-plugins.yaml                        (existing)\n├── 02-database-both-modes.yaml                      (new)\n├── 03-cache-both-modes.yaml                         (new)\n├── 04-autoscaling-no-target.yaml                    (new)\n├── 05-policy-overrides-no-source.yaml               (new)\n├── 06-policy-overrides-empty-rule-key.yaml          (new)\n├── 07-networkpolicy-empty-ingress.yaml              (new)\n├── 08-replicas-zero.yaml                            (new)\n├── 09-replicas-negative.yaml                        (new)\n├── 10-hpa-min-greater-than-max.yaml                 (new)\n├── 11-fernet-maxactivekeys-below-minimum.yaml       (new)\n├── 12-credentialkeys-maxactivekeys-below-minimum.yaml (new)\n└── chainsaw-test.yaml                               (extended)\n```\n\nAssertion pattern per step (identical to `00-invalid-cron.yaml`, so CI reports stay homogeneous):\n\n```yaml\n- try:\n  - apply:\n      file: 02-database-both-modes.yaml\n      expect:\n      - check:\n          ($error != null): true\n          (contains($error, 'spec.database')): true\n          (contains($error, 'exactly one of clusterRef or host must be set')): true\n```\n\nInput YAMLs: each file carries a **complete, otherwise valid** minimal CR where exactly **one** field violates the rule under test. That keeps the error-message assertion sharp and makes follow-on regressions (e.g. a new required field) fail loudly. Template: the existing `00-invalid-cron.yaml` / `01-duplicate-plugins.yaml`.\n\nAll CRs remain **unapplied** in cluster state (admission reject), so no cleanup steps and no `delete` blocks are needed. The test fits inside the 30 s `apply` timeout from `tests/e2e/chainsaw-config.yaml` and does not affect `parallel: 4` execution.\n\n## Definition of Done\n\n- [ ] `chainsaw test tests/e2e/keystone/invalid-cr` green locally against `make kind-up`\n- [ ] `make e2e` green (rest of the Keystone suite unchanged, no new flakes)\n- [ ] Parallel-4 CI job `e2e` green in at least two consecutive runs (anti-flake)\n- [ ] All eleven new steps use the `(contains($error, …))` pattern from the existing test; no dependency on an exact HTTP-code string\n- [ ] No new infra components in `deploy/kind/`, no deviations from `tests/e2e/chainsaw-config.yaml`\n- [ ] Parent #277: the nine covered checkboxes ticked, the three prerequisite points linked as follow-up issues\n- [ ] Commit prefix `test(CC-xxxx)` in line with the repo style (feature code to be assigned by a maintainer)\n\n## Risks / open questions\n\n- **Error-message drift**: the CEL and webhook layers can produce slightly different strings (e.g. for `replicas: 0` the CRD layer says `\"should be greater than or equal to 1\"`, the webhook says `\"replicas must be at least 1\"`). Mitigation: match on the common substring (`\"replicas\"`) plus `($error != null): true`. Please confirm in review.\n- **Validator ordering**: CRD schema → CEL → validating webhook. If CEL rejects first, the request never reaches the webhook and the webhook-specific string is absent from `$error`. The test therefore matches the semantic core message, not the producing layer.\n- Feature code (`CC-xxxx`) to be assigned by a maintainer; suggest extending the sequence past CC-0093.\n\n---\nSource: https://github.com/C5C3/forge/issues/281</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add tests/e2e/keystone/invalid-cr/02-database-both-modes.yaml (otherwise-valid Keystone CR setting BOTH spec.database.clusterRef.name AND spec.database.host) and append its chainsaw step to chainsaw-test.yaml — assertions: ($error != null), contains 'spec.database', contains 'exactly one of clusterRef or host must be set' (REQ-001, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.2 Add tests/e2e/keystone/invalid-cr/03-cache-both-modes.yaml (otherwise-valid Keystone CR setting BOTH spec.cache.clusterRef.name AND a non-empty spec.cache.servers list) and append its chainsaw step — assertions: ($error != null), contains 'spec.cache', contains 'exactly one of clusterRef or servers must be set' (REQ-002, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.3 Add tests/e2e/keystone/invalid-cr/04-autoscaling-no-target.yaml (spec.autoscaling.maxReplicas=10, no targetCPUUtilization, no targetMemoryUtilization; spec.replicas kept <= maxReplicas) and append its chainsaw step — assertions: ($error != null), contains 'spec.autoscaling', contains 'at least one of targetCPUUtilization or targetMemoryUtilization' (REQ-003, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.4 Add tests/e2e/keystone/invalid-cr/05-policy-overrides-no-source.yaml (spec.policyOverrides: {} — present but empty) and append its chainsaw step — assertions: ($error != null), contains 'spec.policyOverrides', contains 'at least one of rules or configMapRef must be set' (REQ-004, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.5 Add tests/e2e/keystone/invalid-cr/06-policy-overrides-empty-rule-key.yaml (spec.policyOverrides.rules with a single entry whose key is the empty string '') and append its chainsaw step — assertions: ($error != null), contains 'spec.policyOverrides.rules', contains 'policy rule name must not be empty' (REQ-005, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.6 Add tests/e2e/keystone/invalid-cr/07-networkpolicy-empty-ingress.yaml (spec.networkPolicy.ingress: []) and append its chainsaw step — assertions: ($error != null), contains 'spec.networkPolicy.ingress', contains 'at least one ingress source' (REQ-006, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.7 Add tests/e2e/keystone/invalid-cr/08-replicas-zero.yaml (spec.replicas: 0) and append its chainsaw step — assertions: ($error != null), contains 'replicas' (loose match for CRD 'greater than or equal to 1' OR webhook 'replicas must be at least 1'); add a comment in the step noting the loose match rationale and that the reviewer must confirm defaulting does not mask it (REQ-007, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.8 Add tests/e2e/keystone/invalid-cr/09-replicas-negative.yaml (spec.replicas: -1) and append its chainsaw step — assertions: ($error != null), contains 'replicas' (CRD Minimum=1 rejects deterministically because defaulter only triggers on ==0) (REQ-008, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.9 Add tests/e2e/keystone/invalid-cr/10-hpa-min-greater-than-max.yaml (spec.autoscaling.minReplicas=5, maxReplicas=3, AND targetCPUUtilization=70 to avoid confounding with REQ-003; spec.replicas kept at 3 or below so the implicit-min>max rule at keystone_webhook.go:438 does not fire) and append its chainsaw step — assertions: ($error != null), contains 'spec.autoscaling.minReplicas', contains 'must not exceed maxReplicas' (REQ-009, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.10 Add tests/e2e/keystone/invalid-cr/11-fernet-maxactivekeys-below-minimum.yaml (spec.fernet.maxActiveKeys: 2; rotationSchedule omitted so CRD default '0 0 * * 0' applies) and append its chainsaw step — assertions: ($error != null), contains 'maxActiveKeys' (loose match because CRD Minimum=3 wording differs from webhook wording) (REQ-010, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">1.11 Add tests/e2e/keystone/invalid-cr/12-credentialkeys-maxactivekeys-below-minimum.yaml (spec.credentialKeys.maxActiveKeys: 2; fernet.maxActiveKeys defaulted to 3) and append its chainsaw step — assertions: ($error != null), contains 'maxActiveKeys' (REQ-011, REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">2.1 Audit the final tests/e2e/keystone/invalid-cr/chainsaw-test.yaml: verify (a) every new `- try:` block is preceded by a `# REQ-NNN (CC-0094):` comment identifying the rule under test, (b) all steps appear in numeric filename order 02..12 after the pre-existing 00/01 steps, (c) no `delete`/`assert`/`wait` blocks were introduced, (d) the top-level SPDX header remains unchanged (REQ-013) — CC-0094</task>\n    <task status=\"pending\">2.2 Cross-review all eleven new manifests against the baseline template from 00-invalid-cron.yaml: (a) identical SPDX header, (b) `apiVersion: keystone.openstack.c5c3.io/v1alpha1`, `kind: Keystone`, (c) every required spec path (image, database, cache, fernet, bootstrap) present with the canonical minimal-valid values, (d) exactly one field violates the rule under test, (e) metadata.name distinct across all eleven files (REQ-012) — CC-0094</task>\n    <task status=\"pending\">3.1 Extend docs/reference/keystone-crd.md §'invalid-cr Suite' table with 11 new rows (Step name, Manifest filename, Requirement one-line, Expected Error substrings) in numeric filename order; add CC-0094 to the front-matter `feature:` list; extend the file-layout ASCII tree at the end of the doc to include 02-...yaml through 12-...yaml (REQ-014) — CC-0094</task>\n    <task status=\"pending\">3.2 Add a one-paragraph aside to docs/reference/keystone-crd.md (after the expanded table) documenting the 'layer-ordering' rationale for loose-substring assertions (CRD schema → CEL → webhook); cite why REQ-007/REQ-010/REQ-011 match only 'replicas' or 'maxActiveKeys' rather than the full webhook wording — CC-0094</task>\n    <task status=\"pending\">4.1 Static verification (agent-local, sandbox): run `yamllint` or equivalent schema parse over the 11 new manifests AND `chainsaw-test.yaml`; verify each new manifest is schema-valid per the generated CRD at operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml EXCEPT for the one deliberately-violating field. Report any diagnostic findings without attempting kind-up (sandbox constraint — documented in memory) (REQ-012, REQ-013) — CC-0094</task>\n    <task status=\"pending\">4.2 Live-cluster verification (HUMAN/CI, cannot run in sandbox): `make kind-up && chainsaw test tests/e2e/keystone/invalid-cr` — confirm all 13 steps (2 existing + 11 new) pass on the first run and on a second consecutive run (anti-flake). If any step flakes or if spec.replicas:0 is silently defaulted, follow the fallback in REQ-007 scenario 3 and the test spec for 08-replicas-zero.yaml — CC-0094</task>\n    <task status=\"pending\">4.3 Parent-issue maintenance: on PR merge, tick the nine in-scope checkboxes under #277 Phase 1 and link the three deferred prerequisite items (spec.image.tag MinLength, topologySpreadConstraints maxSkew Minimum, clusterRef immutability on ValidateUpdate) to follow-up feat-tickets as described in deferred_items — CC-0094</task>\n\n  </tasks>\n  <implementation-notes>ARCHITECTURE / SCOPE MODE (Standard): pure E2E test expansion. No changes to operators/** or internal/**. The only production-adjacent change is the addition of CC-0094 to the `feature:` front-matter of docs/reference/keystone-crd.md and new rows in the invalid-cr Suite table.\n\nDATA FLOW (admission path exercised per step):\n  kubectl apply (chainsaw)\n    → kube-apiserver: OpenAPI/structural schema check (includes +kubebuilder:validation:Minimum markers)\n      → CRD CEL XValidation (keystone_types.go: DatabaseSpec, CacheSpec, AutoscalingSpec, PolicySpec, NetworkPolicySpec)\n        → Mutating webhook: Default() (keystone_webhook.go:79-131 — only rewrites zero/nil fields)\n          → Validating webhook: validate() (keystone_webhook.go:151-573 — defense-in-depth)\n            → Rejection flows back to chainsaw as $error (string body of the API machinery response)\n\nLAYER-ORDERING: the CRD schema layer runs BEFORE CEL, which runs BEFORE the mutating webhook, which runs BEFORE the validating webhook. Consequences:\n  - spec.replicas: 0 → defaulting at keystone_webhook.go:80 rewrites to 3 IF the mutating webhook runs before Minimum=1 structural validation. In Kubernetes 1.25+ the structural schema check runs BEFORE mutating webhooks for known typed fields, so Minimum=1 should reject first. If this ordering has changed in the target cluster version, the fallback recorded in REQ-007 scenario 3 kicks in.\n  - spec.replicas: -1 → never defaulted (defaulter only triggers on == 0); Minimum=1 rejects deterministically.\n  - maxActiveKeys: 2 → never defaulted (defaulter only triggers on == 0); Minimum=3 rejects deterministically.\n  - database/cache mutual exclusivity: CEL rejects first with the identical message string (the XValidation `message:` field carries the same text the webhook uses), so the loose substring match works for both layers.\n\nOTHERWISE-VALID BASELINE (target template, copied from 00-invalid-cron.yaml with the offending field swapped):\n  apiVersion: keystone.openstack.c5c3.io/v1alpha1\n  kind: Keystone\n  metadata:\n    name: <case-specific>\n  spec:\n    replicas: 3\n    image:\n      repository: ghcr.io/c5c3/keystone\n      tag: \"2025.2\"\n    database:\n      host: db.example.com\n      port: 3306\n      database: keystone\n      secretRef:\n        name: keystone-db\n    cache:\n      backend: dogpile.cache.pymemcache\n      servers: [\"mc:11211\"]\n    fernet:\n      rotationSchedule: \"0 0 * * 0\"\n      maxActiveKeys: 3\n    bootstrap:\n      adminUser: admin\n      adminPasswordSecretRef:\n        name: keystone-admin\n      region: RegionOne\n\nThe Secret referenced in spec.bootstrap.adminPasswordSecretRef does NOT need to exist in the test namespace — the CR is rejected at admission and never reaches the reconciler that would look the Secret up.\n\nASSERTION ROBUSTNESS STRATEGY:\n  - Two (contains($error, ...)) predicates per step is the target, matching step 00-invalid-cron's shape.\n  - For webhook-only rules (REQ-009 min>max) both predicates match the webhook wording — a regression that moves the rule to CEL with a different message is loud by design.\n  - For dual-layer rules (REQ-001..REQ-006) the predicates use the shared message string that both layers emit.\n  - For Minimum-marker rules (REQ-007, REQ-008, REQ-010, REQ-011) ONE predicate matches only the field-name substring; the second predicate matches ($error != null) — because the CRD Minimum wording 'should be greater than or equal to N' is not under operator control and could change between Kubernetes versions.\n\nPITFALL REGISTRY:\n  (a) Empty key in a YAML map — use single-quoted empty string ''. Some YAML parsers are lenient; verifying via the CRD generation path is the only reliable check.\n  (b) spec.cache.servers empty list — the CEL rule only fires when size > 0, so the dual-mode manifest must use a non-empty servers list.\n  (c) Defaulter mutation — maxActiveKeys=0 and replicas=0 are rewritten by Default(); tests must use 2 and -1 respectively to avoid being silently repaired.\n  (d) HPA implicit-min rule — if minReplicas is absent, the reconciler defaults it to spec.replicas, and keystone_webhook.go:438 rejects when that implicit default exceeds maxReplicas. The REQ-009 manifest therefore sets minReplicas explicitly AND keeps spec.replicas below maxReplicas.\n  (e) PolicyOverrides empty object — `spec.policyOverrides: {}` with both rules and configMapRef absent triggers both CEL and webhook. An absent pointer would skip both.\n  (f) Sandbox limitation — per memory, the local environment has no docker/cluster. Task 4.1 performs the static-render verification; Task 4.2 is marked as a human/CI step.\n\nPATTERN: the chainsaw-test.yaml shape is already the repo canon (see tests/e2e/keystone/invalid-cr/chainsaw-test.yaml pre-CC-0094). No new pattern file needs to be created — this PR is pure pattern replication.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/02-database-both-modes.yaml\" test-function=\"chainsaw step: database-both-modes-rejected\" requirement-id=\"REQ-001\">[e2e] apply of a CR with both spec.database.clusterRef and spec.database.host populated returns $error containing 'spec.database' and 'exactly one of clusterRef or host must be set'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/02-database-both-modes.yaml\" test-function=\"baseline-isolation (manifest-level invariant)\" requirement-id=\"REQ-001\">[e2e] every other required spec field is valid; no second rule-violation appears in $error</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/03-cache-both-modes.yaml\" test-function=\"chainsaw step: cache-both-modes-rejected\" requirement-id=\"REQ-002\">[e2e] apply of a CR with both spec.cache.clusterRef and a non-empty spec.cache.servers list returns $error containing 'spec.cache' and 'exactly one of clusterRef or servers must be set'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/03-cache-both-modes.yaml\" test-function=\"servers-non-empty (manifest invariant)\" requirement-id=\"REQ-002\">[e2e] spec.cache.servers MUST be non-empty in the manifest — an empty servers list would NOT trigger the CEL rule `size(self.servers) > 0`</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/04-autoscaling-no-target.yaml\" test-function=\"chainsaw step: autoscaling-no-target-rejected\" requirement-id=\"REQ-003\">[e2e] apply of a CR with spec.autoscaling set but both target*Utilization absent returns $error containing 'spec.autoscaling' and 'at least one of targetCPUUtilization or targetMemoryUtilization'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/04-autoscaling-no-target.yaml\" test-function=\"maxReplicas-valid (manifest invariant)\" requirement-id=\"REQ-003\">[e2e] spec.autoscaling.maxReplicas >= spec.replicas so the maxReplicas<replicas guard (keystone_webhook.go:438) does not fire</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/05-policy-overrides-no-source.yaml\" test-function=\"chainsaw step: policy-overrides-no-source-rejected\" requirement-id=\"REQ-004\">[e2e] apply of a CR with spec.policyOverrides: {} returns $error containing 'spec.policyOverrides' and 'at least one of rules or configMapRef must be set'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/05-policy-overrides-no-source.yaml\" test-function=\"empty-object-marker (manifest invariant)\" requirement-id=\"REQ-004\">[e2e] spec.policyOverrides is present as an explicit empty object so that both CEL and webhook evaluate the rule (absent field would skip both guards)</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/06-policy-overrides-empty-rule-key.yaml\" test-function=\"chainsaw step: policy-overrides-empty-rule-key-rejected\" requirement-id=\"REQ-005\">[e2e] apply of a CR with spec.policyOverrides.rules: {'': 'role:admin'} returns $error containing 'spec.policyOverrides.rules' and 'policy rule name must not be empty'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/06-policy-overrides-empty-rule-key.yaml\" test-function=\"yaml-empty-key-serialization (manifest invariant)\" requirement-id=\"REQ-005\">[e2e] the empty key is expressed as `'':` (quoted empty string) so the YAML parser preserves the entry</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/07-networkpolicy-empty-ingress.yaml\" test-function=\"chainsaw step: networkpolicy-empty-ingress-rejected\" requirement-id=\"REQ-006\">[e2e] apply of a CR with spec.networkPolicy.ingress: [] returns $error containing 'spec.networkPolicy.ingress' and 'at least one ingress source'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/07-networkpolicy-empty-ingress.yaml\" test-function=\"networkPolicy-present (manifest invariant)\" requirement-id=\"REQ-006\">[e2e] spec.networkPolicy is an explicit non-nil object so the inner NetworkPolicySpec CEL rule is reached; a nil pointer would skip the check entirely</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/08-replicas-zero.yaml\" test-function=\"chainsaw step: replicas-zero-rejected\" requirement-id=\"REQ-007\">[e2e] apply of a CR with spec.replicas: 0 returns $error containing 'replicas' (loose match to tolerate CRD-layer 'should be greater than or equal to 1' and webhook 'replicas must be at least 1')</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/08-replicas-zero.yaml\" test-function=\"defaulting-verification (manifest + local run)\" requirement-id=\"REQ-007\">[e2e] local `chainsaw test` run must confirm that spec.replicas: 0 is NOT silently defaulted-away — if it is, flip this case to -2 and keep REQ-008 as the sole explicit test; note the outcome in the PR description</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/09-replicas-negative.yaml\" test-function=\"chainsaw step: replicas-negative-rejected\" requirement-id=\"REQ-008\">[e2e] apply of a CR with spec.replicas: -1 returns $error containing 'replicas'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/09-replicas-negative.yaml\" test-function=\"defaulting-bypassed (manifest invariant)\" requirement-id=\"REQ-008\">[e2e] -1 is not == 0 so the defaulter at keystone_webhook.go:80 does not fire; CRD Minimum=1 rejects deterministically</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/10-hpa-min-greater-than-max.yaml\" test-function=\"chainsaw step: hpa-min-gt-max-rejected\" requirement-id=\"REQ-009\">[e2e] apply of a CR with spec.autoscaling.minReplicas=5, spec.autoscaling.maxReplicas=3, and spec.autoscaling.targetCPUUtilization=70 returns $error containing 'spec.autoscaling.minReplicas' and 'must not exceed maxReplicas'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/10-hpa-min-greater-than-max.yaml\" test-function=\"no-target-rule-not-triggered (manifest invariant)\" requirement-id=\"REQ-009\">[e2e] targetCPUUtilization is set so the REQ-003 rule does not fire; min>max is the sole rejection cause</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/11-fernet-maxactivekeys-below-minimum.yaml\" test-function=\"chainsaw step: fernet-keys-below-min-rejected\" requirement-id=\"REQ-010\">[e2e] apply of a CR with spec.fernet.maxActiveKeys: 2 returns $error containing 'maxActiveKeys'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/11-fernet-maxactivekeys-below-minimum.yaml\" test-function=\"defaulting-bypassed (manifest invariant)\" requirement-id=\"REQ-010\">[e2e] 2 != 0 so the defaulter at keystone_webhook.go:83 does not fire; CRD Minimum=3 rejects deterministically</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/12-credentialkeys-maxactivekeys-below-minimum.yaml\" test-function=\"chainsaw step: credkeys-below-min-rejected\" requirement-id=\"REQ-011\">[e2e] apply of a CR with spec.credentialKeys.maxActiveKeys: 2 returns $error containing 'maxActiveKeys'</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/12-credentialkeys-maxactivekeys-below-minimum.yaml\" test-function=\"sibling-fernet-valid (manifest invariant)\" requirement-id=\"REQ-011\">[e2e] spec.fernet.maxActiveKeys defaulted to 3 (or omitted) so only credentialKeys triggers the rule</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/02..12-*.yaml (structural review)\" test-function=\"otherwise-valid-baseline\" requirement-id=\"REQ-012\">[e2e] every file declares spec.image, spec.database (valid mode), spec.cache (valid mode), spec.fernet.rotationSchedule, spec.bootstrap.adminPasswordSecretRef — only the field-under-test violates a rule</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/02..12-*.yaml (metadata review)\" test-function=\"unique-metadata-name\" requirement-id=\"REQ-012\">[e2e] metadata.name values: db-both-modes, cache-both-modes, autoscaling-no-target, policy-no-source, policy-empty-key, networkpolicy-empty-ingress, replicas-zero, replicas-negative, hpa-min-gt-max, fernet-keys-below-min, credkeys-below-min — all distinct</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/chainsaw-test.yaml\" test-function=\"assertion-shape-uniformity\" requirement-id=\"REQ-013\">[e2e] every `- try:` block contains exactly one `apply` action with `expect[0].check` of `($error != null): true` plus two `(contains($error, ...))` predicates — diffable against the pre-CC-0094 template</spec>\n    <spec test-file=\"tests/e2e/keystone/invalid-cr/chainsaw-test.yaml\" test-function=\"step-comments-cite-req-and-feature\" requirement-id=\"REQ-013\">[e2e] each new `- try:` block is preceded by a `# REQ-NNN (CC-0094):` comment</spec>\n    <spec test-file=\"docs/reference/keystone-crd.md\" test-function=\"invalid-cr-suite-table-extended\" requirement-id=\"REQ-014\">markdownlint/cspell clean; §'invalid-cr Suite' table has 13 rows in numeric filename order (2 existing + 11 new)</spec>\n    <spec test-file=\"docs/reference/keystone-crd.md\" test-function=\"front-matter-feature-list-includes-CC-0094\" requirement-id=\"REQ-014\">the `feature:` front-matter value lists CC-0094 alongside the existing CC-xxxx entries</spec>\n    <spec test-file=\"docs/reference/keystone-crd.md\" test-function=\"file-layout-tree-updated\" requirement-id=\"REQ-014\">the ASCII tree at the end of the CRD doc lists 02-...yaml through 12-...yaml under tests/e2e/keystone/invalid-cr/</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator developer verifies all implemented webhook/CEL rules are exercised by E2E tests (CC-0094)\">As a operator developer, I want every CEL XValidation and webhook.validate() rejection path in operators/keystone/api/v1alpha1/ to have a corresponding Chainsaw E2E case under tests/e2e/keystone/invalid-cr/, so that a silent regression (e.g. someone removing a CEL marker or a webhook guard during a refactor) fails a deterministic E2E step instead of shipping to production</story>\n    <story title=\"SRE/maintainer detects rejection-path regressions before merge (CC-0094)\">As a SRE / release maintainer, I want `make e2e` and the GitHub Actions `e2e` job to fail loudly when any covered rejection rule stops rejecting, so that a PR that accidentally weakens validation (deleted CEL rule, short-circuited webhook branch) cannot land on main</story>\n    <story title=\"Reviewer trusts error-message assertions against layer drift (CC-0094)\">As a code reviewer, I want assertion substrings that match the semantic core message independent of which validation layer (CRD schema, CEL, webhook) produced the rejection, so that a rule move between layers (e.g. tightening a CEL rule that today is only enforced in the webhook) does not silently turn a test into a test of the producing layer</story>\n    <story title=\"Parallel-4 CI run stays green and fast (CC-0094)\">As a CI pipeline owner, I want the expanded invalid-cr suite to run inside the same job slot and namespace as today without increasing wall time or flakiness, so that `make e2e` remains under its existing budget and the job does not contend for shared infrastructure</story>\n    <story title=\"Documentation reader finds the expanded rejection coverage (CC-0094)\">As a API consumer / new contributor, I want the invalid-cr Suite table in docs/reference/keystone-crd.md to list all eleven new steps with their trigger and expected error substrings, so that when a cluster admin hits a validation rejection in the wild, the reference doc explains which E2E step proves the rule is intentional</story>\n    <story title=\"Parent issue maintainer closes the Phase 1 checklist cleanly (CC-0094)\">As a issue triage maintainer, I want the three out-of-scope prerequisite items (image.tag MinLength, topologySpreadConstraints maxSkew Minimum, clusterRef immutability on update) to be explicitly deferred with follow-up ticket stubs rather than silently dropped, so that the Phase 1 checklist in #277 is fully resolved — either ticked or linked to a follow-up — with no ambiguity about what CC-0094 delivers</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR that sets both spec.database.clusterRef and spec.database.host (CC-0094)</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR that sets both spec.cache.clusterRef and spec.cache.servers (CC-0094)</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.autoscaling sets neither targetCPUUtilization nor targetMemoryUtilization (CC-0094)</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.policyOverrides sets neither rules nor configMapRef (CC-0094)</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.policyOverrides.rules contains an empty map key (CC-0094)</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.networkPolicy.ingress is an empty array (CC-0094)</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.replicas is 0 (CC-0094)</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.replicas is negative (CC-0094)</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.autoscaling.minReplicas exceeds spec.autoscaling.maxReplicas (CC-0094)</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.fernet.maxActiveKeys is below the minimum of 3 (CC-0094)</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The invalid-cr E2E suite SHALL reject a Keystone CR whose spec.credentialKeys.maxActiveKeys is below the minimum of 3 (CC-0094)</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">Each new invalid-cr manifest SHALL carry a complete, otherwise-valid minimal Keystone CR differing from the valid baseline by exactly one rule-violating field (CC-0094)</requirement>\n    <requirement id=\"REQ-013\" priority=\"SHALL\">The chainsaw-test.yaml SHALL use the identical assertion shape (($error != null): true plus two (contains($error, ...)) predicates) for every new case (CC-0094)</requirement>\n    <requirement id=\"REQ-014\" priority=\"SHALL\">docs/reference/keystone-crd.md §'invalid-cr Suite' SHALL list all eleven new steps with Step, Manifest, Requirement, Expected Error columns (CC-0094)</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 36203,
  "caller": "run_claude_cli"
}