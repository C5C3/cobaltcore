{
  "timestamp": "2026-04-12T18:28:40.562030+00:00",
  "feature_id": "CC-0067",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0067",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0067\">\n  <title>Add post-deployment Keystone API health check</title>\n  <description># GitHub Issue #183: Add post-deployment Keystone API health verification\n\n**Category**: feature | **Scope**: Medium\n\n**Description**: After reconcileDeployment confirms all replicas are available, add a health check sub-reconciler that performs an HTTP GET to the Keystone /v3 endpoint from within the reconciler. This verifies that the Keystone API is actually serving requests, not just that pods are running. Set a KeystoneAPIReady condition based on the probe result.\n\n**Motivation**: A Deployment can report all replicas as ready while uWSGI serves 500s due to misconfigured keystone.conf, database connectivity issues, or other runtime failures. Deployment readiness only confirms the container started and passed its readiness probe — it does not verify end-to-end API functionality. This is a gap between infrastructure readiness and service readiness.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/reconcile_deployment.go`\n- `operators/keystone/internal/controller/keystone_controller.go`\n\n**Acceptance Criteria**:\n- [ ] New sub-reconciler or post-check performs HTTP GET to Keystone /v3 endpoint\n- [ ] KeystoneAPIReady condition reflects actual API availability\n- [ ] Configurable timeout and retry for the health check\n- [ ] Reconciler handles cases where Service is not yet created or endpoints are not ready\n- [ ] Unit test with mock HTTP server validates health check logic\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/183</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add HTTPDoer interface and HTTPClient field to KeystoneReconciler (REQ-006)</task>\n    <task status=\"pending\">1.2 Add health check constants to requeue_intervals.go (REQ-002)</task>\n    <task status=\"pending\">1.3 Add 'KeystoneAPIReady' to subConditionTypes (REQ-005)</task>\n    <task status=\"pending\">2.1 Implement reconcileHealthCheck happy path — HTTP 2xx sets True, non-2xx sets False (REQ-001, REQ-004, REQ-008)</task>\n    <task status=\"pending\">2.2 Implement reconcileHealthCheck — empty endpoint handling with test (REQ-004)</task>\n    <task status=\"pending\">2.3 Implement reconcileHealthCheck — timeout error classification with test (REQ-002, REQ-003)</task>\n    <task status=\"pending\">2.4 Implement reconcileHealthCheck — connection refused and DNS error tests (REQ-003)</task>\n    <task status=\"pending\">3.1 Wire reconcileHealthCheck into Reconcile() chain after reconcileDeployment (REQ-007)</task>\n    <task status=\"pending\">3.2 Add response body close verification test and health check chain ordering test (REQ-007, REQ-008)</task>\n    <task status=\"pending\">4.1 Update keystone-reconciler.md with KeystoneAPIReady condition and reconcileHealthCheck sub-reconciler (REQ-001, REQ-005)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThe health check is implemented as a new sub-reconciler `reconcileHealthCheck` following the exact pattern of existing sub-reconcilers (reconcileSecrets, reconcileDeployment, etc.). It slots into the reconciliation chain between `reconcileDeployment` and `reconcileHPA`.\n\n## Data Flow\n\n```\nreconcileDeployment (sets Status.Endpoint, DeploymentReady=True)\n        │\n        ▼\nreconcileHealthCheck\n  1. Read keystone.Status.Endpoint (set by prior step)\n  2. Create HTTP GET request with HealthCheckTimeout context\n  3. Call httpClient().Do(req)\n  4. Classify result → set KeystoneAPIReady condition\n  5. Return ctrl.Result{} or ctrl.Result{RequeueAfter: RequeueHealthCheck}\n        │\n        ▼\nreconcileHPA (only runs if health check passes)\n```\n\n## Key Design Decisions\n\n1. **HTTPDoer interface over concrete http.Client**: Enables unit testing with mock HTTP servers (httptest.NewServer) and mock error injection. The interface has a single method matching http.Client.Do, so no adapter is needed.\n\n2. **Network errors → condition False + requeue, not returned errors**: Connection refused, DNS failures, and timeouts are expected transient states during deployment convergence. Returning them as errors would trigger controller-runtime's exponential backoff, delaying recovery. Instead, we set a descriptive condition and requeue at a fixed interval.\n\n3. **Using Status.Endpoint**: The endpoint URL is already computed by reconcileDeployment (http://{name}-api.{namespace}.svc.cluster.local:5000/v3). Reusing it avoids URL construction duplication and ensures the health check targets the same endpoint that consumers will use.\n\n4. **10-second timeout**: Matches the Deployment polling interval (RequeueDeploymentPolling = 10s). Long enough for a cold start but short enough to not block the reconcile loop.\n\n## Files Modified\n\n- `operators/keystone/internal/controller/reconcile_healthcheck.go` (NEW) — sub-reconciler implementation + HTTPDoer interface\n- `operators/keystone/internal/controller/reconcile_healthcheck_test.go` (NEW) — unit tests\n- `operators/keystone/internal/controller/keystone_controller.go` — add HTTPClient field, wire reconcileHealthCheck into chain, add to subConditionTypes\n- `operators/keystone/internal/controller/requeue_intervals.go` — add RequeueHealthCheck and HealthCheckTimeout constants\n- `docs/reference/keystone-reconciler.md` — add condition type and sub-reconciler documentation\n- `docs/reference/keystone-crd.md` — add KeystoneAPIReady to conditions example\n\n## Patterns to Follow\n\n- Sub-reconciler pattern: see reconcile_secrets.go (condition setting, requeue, error handling)\n- Test pattern: see reconcile_deployment_test.go (scheme setup, fake client, Gomega assertions)\n- Condition pattern: see internal/common/conditions/conditions.go (SetCondition with metav1.Condition)\n- Requeue constants: see requeue_intervals.go (centralized, documented)\n\n## Pitfalls\n\n- **Do not return HTTP errors as reconcile errors**: This would trigger exponential backoff instead of fixed-interval requeue\n- **Always close response bodies**: Even on non-2xx responses, Go net/http returns a non-nil Body\n- **Handle nil HTTPClient in production**: The SetupWithManager path does not set HTTPClient, so httpClient() must fall back to http.DefaultClient</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_Healthy200_SetsConditionTrue\" requirement-id=\"REQ-001\">HTTP 200 response sets KeystoneAPIReady=True with Reason 'APIHealthy' and no requeue [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_Unhealthy500_SetsConditionFalse\" requirement-id=\"REQ-001\">HTTP 500 response sets KeystoneAPIReady=False with Reason 'APIUnhealthy', message includes '500', and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_Unhealthy503_SetsConditionFalse\" requirement-id=\"REQ-001\">HTTP 503 response sets KeystoneAPIReady=False with Reason 'APIUnhealthy' and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_ObservedGenerationSet\" requirement-id=\"REQ-001\">KeystoneAPIReady condition ObservedGeneration matches keystone.Generation for both True and False states [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_Timeout_SetsConditionFalse\" requirement-id=\"REQ-002\">Context deadline exceeded error sets KeystoneAPIReady=False with Reason 'HealthCheckTimeout' and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_TimeoutConstantDefined\" requirement-id=\"REQ-002\">HealthCheckTimeout constant equals 10 * time.Second and is defined in requeue_intervals.go [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_ConnectionRefused_SetsConditionFalse\" requirement-id=\"REQ-003\">Connection refused error sets KeystoneAPIReady=False with Reason 'ConnectionFailed' and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_DNSError_SetsConditionFalse\" requirement-id=\"REQ-003\">DNS resolution failure sets KeystoneAPIReady=False with Reason 'EndpointNotReady' and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_GenericNetworkError_SetsConditionFalse\" requirement-id=\"REQ-003\">Generic network error sets KeystoneAPIReady=False with Reason 'HealthCheckFailed' and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_EmptyEndpoint_SetsConditionFalse\" requirement-id=\"REQ-004\">Empty keystone.Status.Endpoint sets KeystoneAPIReady=False with Reason 'EndpointNotReady' and requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_UsesStatusEndpoint\" requirement-id=\"REQ-004\">Health check HTTP request targets exactly keystone.Status.Endpoint URL [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSubConditionTypes_IncludesKeystoneAPIReady\" requirement-id=\"REQ-005\">subConditionTypes slice contains 'KeystoneAPIReady' after 'DeploymentReady' and before 'HPAReady' [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestAggregateReady_FalseWhenKeystoneAPIReadyMissing\" requirement-id=\"REQ-005\">aggregateReady returns false when KeystoneAPIReady is not set even if all other conditions are True [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestHttpClient_ReturnsInjectedClient\" requirement-id=\"REQ-006\">httpClient() returns the injected HTTPDoer when HTTPClient is non-nil [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestHttpClient_FallsBackToDefaultClient\" requirement-id=\"REQ-006\">httpClient() returns http.DefaultClient when HTTPClient is nil [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_HealthCheckCalledAfterDeployment\" requirement-id=\"REQ-007\">Full Reconcile() calls reconcileHealthCheck after reconcileDeployment succeeds, and health check requeue prevents HPA from running [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_HealthCheckSkippedWhenDeploymentNotReady\" requirement-id=\"REQ-007\">When reconcileDeployment requeues, reconcileHealthCheck is NOT called and KeystoneAPIReady condition is not set [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_ResponseBodyClosed_Success\" requirement-id=\"REQ-008\">HTTP response body is closed after processing a 200 response, verified via a custom ReadCloser that tracks Close() calls [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_healthcheck_test.go\" test-function=\"TestReconcileHealthCheck_ResponseBodyClosed_Error\" requirement-id=\"REQ-008\">HTTP response body is closed after processing a 500 response, verified via a custom ReadCloser that tracks Close() calls [unit]</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator verifies Keystone API is serving requests after deployment\">As a cluster operator, I want the reconciler to perform an HTTP GET to the Keystone /v3 endpoint after the Deployment reports ready, so that I can trust that KeystoneAPIReady=True means the API is actually responding, not just that pods passed their readiness probe</story>\n    <story title=\"Operator requeues when health check fails transiently\">As a cluster operator, I want the reconciler to retry the health check on transient failures instead of permanently marking the API as unhealthy, so that brief startup hiccups don't prevent the reconciler from eventually reaching Ready=True</story>\n    <story title=\"Operator handles missing Service gracefully during health check\">As a cluster operator, I want the reconciler to handle cases where the Kubernetes Service or Endpoints are not yet resolvable, so that the reconciler does not crash or return hard errors when DNS resolution fails for the in-cluster service</story>\n    <story title=\"Operator uses configurable timeout for health check HTTP requests\">As a cluster operator, I want the health check HTTP request to use a bounded timeout, so that a hanging Keystone API does not block the reconciler indefinitely</story>\n    <story title=\"KeystoneAPIReady participates in aggregate Ready condition\">As a cluster operator, I want the KeystoneAPIReady condition to be included in the aggregate Ready evaluation, so that Ready=True is only set when the API is verified healthy end-to-end, not just infrastructure-ready</story>\n    <story title=\"Health check sub-reconciler is testable with mock HTTP server\">As a developer, I want the health check logic to be unit-testable with an injectable HTTP client interface, so that tests can simulate healthy, unhealthy, timeout, and connection-refused scenarios without real network calls</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The reconciler SHALL perform an HTTP GET to the Keystone /v3 endpoint after DeploymentReady=True and set KeystoneAPIReady based on the response</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The reconciler SHALL use a bounded timeout for health check HTTP requests to prevent blocking the reconcile loop</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The reconciler SHALL handle connection-level failures (DNS, TCP) gracefully without returning hard errors</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The reconciler SHALL use keystone.Status.Endpoint as the health check target URL</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The KeystoneAPIReady condition SHALL participate in the aggregate Ready condition evaluation</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The reconciler SHALL use an injectable HTTP client interface for testability</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The health check sub-reconciler SHALL execute after reconcileDeployment and before reconcileHPA in the reconciliation chain</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The health check SHALL close HTTP response bodies to prevent resource leaks</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17148,
  "caller": "run_claude_cli"
}