{
  "timestamp": "2026-04-13T18:51:18.899865+00:00",
  "feature_id": "CC-0071",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0071",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0071\">\n  <title>Parallelize independent sub-reconcilers</title>\n  <description># GitHub Issue #188: Parallelize independent sub-reconcilers for reduced reconcile latency\n\n**Category**: performance | **Scope**: Large\n\n**Description**: Refactor the reconcile loop in keystone_controller.go to identify and run independent sub-reconcilers concurrently using errgroup. After reconcileConfig (which produces configMapName needed by downstream reconcilers), run reconcileFernetKeys, reconcileCredentialKeys, and reconcileNetworkPolicy in parallel since they have no data dependency on each other. Merge results and continue with dependent reconcilers sequentially.\n\n**Motivation**: The 10 sub-reconcilers currently execute fully sequentially (keystone_controller.go:84-131). Some are provably independent and could run concurrently, reducing end-to-end reconcile latency. This matters most during initial CR creation when all sub-resources must be created, and during upgrades when multiple phases execute.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/keystone_controller.go`\n\n**Acceptance Criteria**:\n- [ ] Independent sub-reconcilers run concurrently via errgroup\n- [ ] Dependency graph between sub-reconcilers is documented\n- [ ] Error from any parallel sub-reconciler cancels the group and is returned\n- [ ] Condition updates from parallel sub-reconcilers do not race (use mutex or sequential condition writes)\n- [ ] Reconcile latency reduced by at least 30% for initial CR creation in benchmarks\n- [ ] No behavioral change in reconcile outcomes compared to sequential execution\n\n---\nSource: https://github.com/C5C3/forge/issues/188</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add shortestRequeue and mergeParallelConditions helper functions to keystone_controller.go with unit tests (CC-0071, REQ-003, REQ-004)</task>\n    <task status=\"pending\">2.1 Implement reconcileParallelGroup method with errgroup.WithContext and per-goroutine DeepCopy in keystone_controller.go (CC-0071, REQ-001, REQ-002, REQ-005)</task>\n    <task status=\"pending\">2.2 Add unit tests for parallel Reconcile: success path, error cancellation, and partial condition merge (CC-0071, REQ-001, REQ-002, REQ-008)</task>\n    <task status=\"pending\">3.1 Verify all existing unit and integration tests pass with go test -race flag (CC-0071, REQ-003, REQ-008)</task>\n    <task status=\"pending\">3.2 Add BenchmarkReconcile measuring end-to-end reconcile latency with fake client (CC-0071, REQ-007)</task>\n    <task status=\"pending\">4.1 Update docs/reference/keystone-reconciler.md with parallel flow diagram, dependency graph, and condition merge pattern (CC-0071, REQ-006)</task>\n\n  </tasks>\n  <implementation-notes>## Architectural Decision: DeepCopy per goroutine vs Mutex\n\nThe core design decision is how to prevent data races on keystone.Status.Conditions when parallel sub-reconcilers call conditions.SetCondition concurrently.\n\n**Chosen: DeepCopy per goroutine with sequential merge.** Each parallel goroutine receives its own DeepCopy of the Keystone object. After errgroup.Wait(), conditions are extracted from each copy and set on the original sequentially. This eliminates shared mutable state by construction.\n\n**Alternative considered: sync.Mutex on condition writes.** Would require adding a conditionMu field to KeystoneReconciler and modifying all 3 parallel sub-reconcilers to use a locked SetCondition variant. Rejected because it increases blast radius (3 additional files changed) and introduces a new synchronization primitive that could be misused.\n\n## Architecture Diagram\n\n```\nReconcile(ctx, req)\n├── r.Get(Keystone CR)                    [sequential]\n├── reconcileSecrets(ctx, &ks)             [sequential] → SecretsReady\n├── reconcileConfig(ctx, &ks)              [sequential] → configMapName\n├── reconcileParallelGroup(ctx, &ks, cfgMap)  [NEW - CC-0071]\n│   ├── DeepCopy × 3\n│   ├── errgroup.WithContext(ctx)\n│   │   ├── go: reconcileFernetKeys(gctx, copy1, cfgMap)    → FernetKeysReady\n│   │   ├── go: reconcileCredentialKeys(gctx, copy2, cfgMap) → CredentialKeysReady\n│   │   └── go: reconcileNetworkPolicy(gctx, copy3)          → NetworkPolicyReady\n│   ├── g.Wait()\n│   ├── mergeParallelConditions(ks, copy1, copy2, copy3)\n│   └── shortestRequeue(result1, result2, result3)\n├── reconcileDatabase(ctx, &ks, cfgMap)    [sequential] → DatabaseReady\n├── reconcileDeployment(ctx, &ks, cfgMap)  [sequential] → DeploymentReady\n├── reconcileHPA(ctx, &ks)                 [sequential] → HPAReady\n├── reconcileBootstrap(ctx, &ks, cfgMap)   [sequential] → BootstrapReady\n├── reconcileTrustFlush(ctx, &ks, cfgMap)  [sequential] → TrustFlushReady\n├── setReadyCondition(&ks)                 [sequential] → Ready\n└── updateStatus(ctx, &ks, result, err)    [sequential]\n```\n\n## Data Flow for Parallel Group\n\n```\nKeystone CR (original, with SecretsReady set)\n    │\n    ├─ DeepCopy → ksCopyFernet ──[goroutine 1]──→ FernetKeysReady on copy\n    ├─ DeepCopy → ksCopyCred   ──[goroutine 2]──→ CredentialKeysReady on copy\n    └─ DeepCopy → ksCopyNetpol ──[goroutine 3]──→ NetworkPolicyReady on copy\n                                                     │\n                                          errgroup.Wait()\n                                                     │\n                                    mergeParallelConditions:\n                                      copy1 → FernetKeysReady → original\n                                      copy2 → CredentialKeysReady → original\n                                      copy3 → NetworkPolicyReady → original\n```\n\n## Sub-Reconciler Dependency Graph\n\n| Sub-Reconciler | Inputs | Condition | Dependencies | Parallel |\n|---|---|---|---|---|\n| reconcileSecrets | CR spec | SecretsReady | none | no (must be first) |\n| reconcileConfig | CR spec, DB secret | (returns configMapName) | Secrets | no (produces configMapName) |\n| reconcileFernetKeys | configMapName | FernetKeysReady | Config | **yes** |\n| reconcileCredentialKeys | configMapName | CredentialKeysReady | Config | **yes** |\n| reconcileNetworkPolicy | CR spec | NetworkPolicyReady | none | **yes** |\n| reconcileDatabase | configMapName | DatabaseReady | Config | no (complex state machine) |\n| reconcileDeployment | configMapName | DeploymentReady | Config, Database (implicit) | no |\n| reconcileHPA | CR spec | HPAReady | Deployment (naming) | no |\n| reconcileBootstrap | configMapName | BootstrapReady | Deployment (API must be running) | no |\n| reconcileTrustFlush | configMapName | TrustFlushReady | Config | no |\n\n## Potential Pitfalls\n\n1. **Do NOT pass the original keystone pointer to goroutines** — conditions.SetCondition on the shared slice will race. Always use DeepCopy.\n2. **Always merge conditions after g.Wait()** — even on error, partial conditions from successful goroutines should be merged so the status reflects actual state.\n3. **Use errgroup.WithContext, not plain errgroup.Group** — context cancellation is needed so remaining goroutines exit promptly on first error.\n4. **The result variables (fernetResult, credResult, netpolResult) are safe to read after g.Wait()** — errgroup.Wait provides the happens-before guarantee.\n5. **Do NOT include reconcileDatabase in the parallel group** — it has a complex state machine with requeue logic (30s waits for MariaDB CRs, db_sync Job, schema-check Job) and reconcileDeployment depends on it implicitly.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestShortestRequeue_AllZero\" requirement-id=\"REQ-004\">shortestRequeue with all zero Results returns ctrl.Result{} (zero value) [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestShortestRequeue_SingleNonZero\" requirement-id=\"REQ-004\">shortestRequeue with one non-zero RequeueAfter returns that Result [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestShortestRequeue_PicksMinimum\" requirement-id=\"REQ-004\">shortestRequeue with RequeueAfter 15s and 30s returns ctrl.Result{RequeueAfter: 15s} [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestMergeParallelConditions_MergesCorrectly\" requirement-id=\"REQ-003\">mergeParallelConditions extracts FernetKeysReady from source copy and sets it on destination [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestMergeParallelConditions_SkipsMissingCondition\" requirement-id=\"REQ-003\">mergeParallelConditions does not modify destination when source copy lacks the expected condition type [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestMergeParallelConditions_PreservesExistingConditions\" requirement-id=\"REQ-003\">mergeParallelConditions does not overwrite pre-existing conditions (e.g. SecretsReady) on the destination [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_ParallelGroupSetsAllConditions\" requirement-id=\"REQ-001\">Full reconcile with all sub-resources ready sets FernetKeysReady, CredentialKeysReady, NetworkPolicyReady all to True, identical to sequential execution [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcileParallelGroup_ErrorCancelsAndReturns\" requirement-id=\"REQ-002\">When one parallel reconciler's API call fails (injected via interceptor), Reconcile returns a non-nil error [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcileParallelGroup_PartialConditionsMergedOnError\" requirement-id=\"REQ-002\">When FernetKeys errors, conditions from successful parallel reconcilers (e.g. NetworkPolicyReady) are still present in status [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_SequentialOrderPreserved\" requirement-id=\"REQ-005\">After parallel group, reconcileDatabase is called next; if it requeues, subsequent reconcilers are skipped [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_SetsAllSubConditionsTrue\" requirement-id=\"REQ-008\">Existing test: all 9 sub-conditions set to True on full successful reconcile (validates no behavioral change) [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_Idempotent\" requirement-id=\"REQ-008\">Existing test: second Reconcile call produces same result as first (validates idempotency preserved) [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_AggregatesReadyCondition\" requirement-id=\"REQ-008\">Existing test: Ready condition aggregation works correctly with parallel-set sub-conditions [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"BenchmarkReconcile_FullReconcile\" requirement-id=\"REQ-007\">Benchmark measures ns/op for full reconcile cycle with fake client; documents baseline for comparison [benchmark]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"all integration tests with -race flag\" requirement-id=\"REQ-003\">All existing integration tests pass under go test -race with no data races detected [integration]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"all unit tests with -race flag\" requirement-id=\"REQ-003\">All unit tests (existing + new) pass under go test -race with no data races detected [unit]</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator runs independent sub-reconcilers in parallel for faster reconciliation\">As a cluster operator, I want the Keystone controller to reconcile independent sub-resources concurrently via errgroup, so that initial CR creation and upgrades complete faster with reduced end-to-end reconcile latency</story>\n    <story title=\"Operator propagates errors from parallel sub-reconcilers correctly\">As a cluster operator, I want errors from any parallel sub-reconciler to cancel the errgroup context and surface as the Reconcile error, so that I can diagnose failures without ambiguity about which sub-reconciler failed</story>\n    <story title=\"Operator maintains thread-safe condition updates during parallel execution\">As a developer, I want parallel sub-reconcilers to update status conditions without data races, so that the operator passes go test -race and condition data is never corrupted</story>\n    <story title=\"Developer understands the sub-reconciler dependency graph from documentation\">As a developer, I want the dependency graph between all sub-reconcilers to be documented in the reconciler reference, so that I can safely identify future parallelization opportunities and understand execution ordering</story>\n    <story title=\"Operator achieves measurable latency reduction for initial CR creation\">As a cluster operator, I want the reconcile loop to complete at least 30% faster for initial CR creation, so that the Keystone Identity Service is available sooner after CR creation</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The reconcile loop SHALL run reconcileFernetKeys, reconcileCredentialKeys, and reconcileNetworkPolicy concurrently via errgroup.WithContext after reconcileConfig completes and before reconcileDatabase starts</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">An error from any parallel sub-reconciler SHALL cancel the errgroup context and the first error SHALL be returned as the Reconcile error</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">Condition updates from parallel sub-reconcilers SHALL NOT race on the shared Status.Conditions slice; each parallel goroutine SHALL operate on a DeepCopy and conditions SHALL be merged sequentially after errgroup.Wait returns</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">If any parallel sub-reconciler returns a non-zero ctrl.Result (RequeueAfter or Requeue), the parallel group SHALL return the shortest non-zero RequeueAfter</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">Sequential sub-reconcilers SHALL continue to execute in the order: Secrets → Config → [parallel group] → Database → Deployment → HPA → Bootstrap → TrustFlush → setReadyCondition</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The dependency graph between all sub-reconcilers SHALL be documented in docs/reference/keystone-reconciler.md with inputs, outputs, and parallelization status for each</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHOULD\">Reconcile latency SHOULD be reduced by at least 30% for initial CR creation as measured by BenchmarkReconcile</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">There SHALL be no behavioral change in reconcile outcomes compared to sequential execution: same resources created, same conditions set, same errors returned</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17467,
  "caller": "run_claude_cli"
}