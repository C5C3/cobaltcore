{
  "timestamp": "2026-04-09T21:26:34.721568+00:00",
  "feature_id": "CC-0055",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0055",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0055\">\n  <title>Refactor build-images workflow into reusable components</title>\n  <description># GitHub Issue #166: Refactor build-images workflow: extract repeated steps into reusable scripts and composite actions\n\n**Category**: improvement | **Scope**: Medium\n\n**Description**: `.github/workflows/build-images.yaml` has grown to ~1295 lines and contains large duplicated step sequences across jobs. This makes the workflow hard to read, test locally, and maintain as new services and releases are added. The repeated blocks should be extracted into standalone shell scripts under `hack/` and composite actions under `.github/actions/`, following the pattern established by CC-0050 for `ci.yaml`.\n\nKey areas of duplication and complexity:\n\n1. **Supply chain security sequence** — The SBOM generation → Grype vulnerability scan → SARIF upload → attestation → build provenance → cosign signing pipeline is copy-pasted for python-base (lines 265–330), venv-builder (lines 355–418), tempest (lines 649–693), and service images (lines 1032–1077). That is ~65 lines repeated 4 times (~260 lines). Extract into a composite action (e.g. `.github/actions/supply-chain-attest`) parameterised by image name and digest.\n\n2. **Docker registry setup** — The `docker/login-action` + `docker/setup-buildx-action` + `sigstore/cosign-installer` block is repeated in build-base-images, merge-base-images, build-tempest, merge-tempest-image, build-service-images, merge-service-images, verify-base-images, test-service-images, and verify-service-images (~9 jobs). Extract into a composite action (e.g. `.github/actions/setup-docker-registry`).\n\n3. **Digest export + artifact upload** — The pattern of `mkdir -p /tmp/digests && touch \"/tmp/digests/${digest#sha256:}\"` followed by `actions/upload-artifact` is repeated for python-base, venv-builder, tempest, and service images. Extract into a composite action or script.\n\n4. **Manifest merge logic** — The `docker buildx imagetools create` + `imagetools inspect` pattern is similar across merge-base-images (python-base, venv-builder), merge-tempest-image, and merge-service-images. Extract into `hack/ci-merge-manifest.sh` with env var interface.\n\n5. **Source ref resolution + upstream checkout + patches + constraint overrides** — Duplicated between `build-service-images` (lines 788–819) and `test-service-images` (lines 1130–1163) with explicit `NOTE: This step MUST stay in sync` comments acknowledging the drift risk. Extract into a composite action (e.g. `.github/actions/checkout-service-source`).\n\n6. **Normalize image owner** — The one-liner `echo \"owner=${GITHUB_REPOSITORY_OWNER,,}\" >> \"$GITHUB_OUTPUT\"` is repeated in 6+ jobs. Absorb into a composite action or set as a workflow-level env.\n\n7. **test-service-images inline docker run** — The test execution step (lines 1204–1252) is a ~50-line inline script with volume mounts, environment variables, and a nested `bash -c` block. Extract into `hack/ci-run-unit-tests.sh` for local testing and shellcheck coverage.\n\n**Motivation**: As more OpenStack services are added (glance, nova, neutron, etc.), each new service multiplies the copy-paste burden in the supply chain and manifest merge sections. The `NOTE: This step MUST stay in sync` comments on lines 1129, 1151, and 1159 are a maintenance anti-pattern — a single composite action eliminates the synchronisation problem. Standalone scripts can be tested locally with shellcheck and run outside of CI for debugging. The workflow file should be a declarative orchestration layer, not a container for business logic.\n\n**Affected Areas**:\n- `.github/workflows/build-images.yaml`\n- `.github/actions/` (new composite actions)\n- `hack/` (new scripts)\n- `tests/ci/` (script validation tests)\n\n**Acceptance Criteria**:\n- [ ] Supply chain security sequence (SBOM, Grype, SARIF, attestation, provenance, cosign) is a single composite action called for each image\n- [ ] Docker registry setup (login, buildx, cosign) is a composite action shared across jobs\n- [ ] Digest export + artifact upload is consolidated\n- [ ] Manifest merge logic is extracted into a reusable script with env var interface\n- [ ] Service source checkout (source-ref, upstream clone, patches, constraint overrides) is a composite action eliminating \"MUST stay in sync\" duplication\n- [ ] Unit test execution block is extracted into a standalone script\n- [ ] `build-images.yaml` is reduced to < 600 lines (orchestration only, no multi-line inline scripts beyond ~5 lines)\n- [ ] All extracted scripts pass shellcheck\n- [ ] No functional change to workflow behavior — all existing jobs produce identical results\n- [ ] Extracted scripts are usable for local debugging\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/166</description>\n  <tasks>\n    <task status=\"pending\">1.1 Create setup-docker-registry composite action (REQ-002, REQ-010)</task>\n    <task status=\"pending\">1.2 Create export-digest composite action (REQ-007, REQ-010)</task>\n    <task status=\"pending\">1.3 Create hack/ci-merge-manifest.sh script (REQ-004, REQ-008)</task>\n    <task status=\"pending\">1.4 Create hack/ci-run-unit-tests.sh script (REQ-005, REQ-008)</task>\n    <task status=\"pending\">1.5 Create checkout-service-source composite action (REQ-003, REQ-010)</task>\n    <task status=\"pending\">1.6 Create supply-chain-attest composite action (REQ-001, REQ-010)</task>\n    <task status=\"pending\">2.1 Refactor build-base-images job to use new composite actions (REQ-002, REQ-006, REQ-007, REQ-009)</task>\n    <task status=\"pending\">2.2 Refactor merge-base-images job to use new composite actions and script (REQ-001, REQ-002, REQ-004, REQ-006, REQ-009)</task>\n    <task status=\"pending\">2.3 Refactor verify-base-images and generate-matrix jobs (REQ-002, REQ-009)</task>\n    <task status=\"pending\">2.4 Refactor build-tempest and merge-tempest-image jobs (REQ-001, REQ-002, REQ-006, REQ-004, REQ-007, REQ-009)</task>\n    <task status=\"pending\">2.5 Refactor build-service-images job (REQ-001, REQ-002, REQ-003, REQ-007, REQ-009)</task>\n    <task status=\"pending\">2.6 Refactor merge-service-images job (REQ-001, REQ-002, REQ-004, REQ-006, REQ-009)</task>\n    <task status=\"pending\">2.7 Refactor test-service-images job (REQ-002, REQ-003, REQ-005, REQ-009)</task>\n    <task status=\"pending\">2.8 Refactor verify-service-images job (REQ-002, REQ-009)</task>\n    <task status=\"pending\">3.1 Create tests/ci/verify_composite_actions.sh test script (REQ-010)</task>\n    <task status=\"pending\">3.2 Create tests/ci/verify_build_images_refactor.sh test script (REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-009)</task>\n    <task status=\"pending\">4.1 Update docs/reference/build-images-workflow.md for CC-0055 refactoring (REQ-009)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThis is a pure refactoring of `.github/workflows/build-images.yaml` (1295 lines → <600 lines). No behavioral changes.\n\n### Component Diagram\n\n```\nbuild-images.yaml (orchestration)\n  ├── .github/actions/setup-docker-registry/     (login + buildx + cosign)\n  ├── .github/actions/supply-chain-attest/        (SBOM + Grype + SARIF + attest + provenance + cosign)\n  ├── .github/actions/checkout-service-source/    (source-ref + checkout + patches + constraints)\n  ├── .github/actions/export-digest/              (mkdir + touch + upload-artifact)\n  ├── .github/actions/platform-pair/              (existing)\n  ├── .github/actions/derive-service-tags/        (existing)\n  ├── hack/ci-merge-manifest.sh                   (imagetools create + inspect)\n  └── hack/ci-run-unit-tests.sh                   (docker run test execution)\n```\n\n### Normalize Image Owner Strategy\n\nThe `echo \"owner=${GITHUB_REPOSITORY_OWNER,,}\"` one-liner appears in 5 jobs. Options:\n1. **Workflow-level env** — Cannot use `${{ github.repository_owner }}` in env because bash lowercasing `,,` is not available in expression context.\n2. **Dedicated step in composite actions** — Each composite action that needs the owner could accept it as an input, pushing resolution to the caller.\n3. **Single job output** — A lightweight job (or folded into lint-dockerfiles) resolves the owner once; downstream jobs consume via `needs.*.outputs`.\n\nRecommended: Option 2 (accept as input). Each composite action that needs the image owner takes it as an input parameter. The workflow still needs one normalize step per job that uses the owner directly, but composite actions abstract it away. This matches how derive-service-tags already accepts `repository-owner` as input.\n\nAlternatively, move to a top-level env using the GitHub expression (which already lowercases): `env: IMAGE_OWNER: ${{ github.repository_owner }}` — but GHCR requires lowercase and `github.repository_owner` preserves original casing. The safest approach is a single normalize job whose output is consumed by all downstream jobs via `needs`.\n\n### Key Design Decisions\n\n1. **supply-chain-attest scan-mode input**: PR scans use image-mode (scan locally-loaded image), push scans use sbom-mode (generate SBOM first). The composite action handles both via a single `scan-mode` input rather than separate actions.\n\n2. **ci-merge-manifest.sh TAGS interface**: Tags are passed as space-separated env var rather than positional args, following the established CC-0050 env var pattern. The script splits TAGS and constructs -t flags.\n\n3. **checkout-service-source installs yq internally**: This avoids the manual yq installation in test-service-images (lines 1119-1127) which used a different installation method than the mikefarah/yq action used elsewhere.\n\n### Potential Pitfalls\n\n1. **Composite action output propagation**: Composite actions cannot set job-level outputs directly. The workflow must map step outputs from the composite action to job outputs explicitly.\n2. **Conditional steps in composite actions**: GitHub Actions composite actions support `if:` conditions on steps, but the condition syntax must use `inputs.*` not `github.*` for action-level conditionals.\n3. **hashFiles in composite actions**: The `hashFiles()` function works differently in composite actions — the path is relative to the repository root, which should be correct for the patches check.\n4. **secrets in composite actions**: Composite actions cannot access secrets directly. The workflow must pass secrets as inputs (e.g., GITHUB_TOKEN for docker login).</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_set_euo_pipefail (covers ci-merge-manifest.sh)\" requirement-id=\"REQ-008\">ci-merge-manifest.sh contains 'set -euo pipefail'</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_set_euo_pipefail (covers ci-run-unit-tests.sh)\" requirement-id=\"REQ-008\">ci-run-unit-tests.sh contains 'set -euo pipefail'</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_shellcheck (covers ci-merge-manifest.sh)\" requirement-id=\"REQ-008\">ci-merge-manifest.sh passes shellcheck --severity=warning</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_shellcheck (covers ci-run-unit-tests.sh)\" requirement-id=\"REQ-008\">ci-run-unit-tests.sh passes shellcheck --severity=warning</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_spdx_headers (covers ci-merge-manifest.sh)\" requirement-id=\"REQ-008\">ci-merge-manifest.sh has SPDX header at lines 2 and 4</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_spdx_headers (covers ci-run-unit-tests.sh)\" requirement-id=\"REQ-008\">ci-run-unit-tests.sh has SPDX header at lines 2 and 4</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_error_annotations (covers ci-merge-manifest.sh)\" requirement-id=\"REQ-008\">ci-merge-manifest.sh has ::error:: annotations for required env vars</spec>\n    <spec test-file=\"tests/ci/verify_ci_scripts.sh\" test-function=\"test_error_annotations (covers ci-run-unit-tests.sh)\" requirement-id=\"REQ-008\">ci-run-unit-tests.sh has ::error:: annotations for required env vars</spec>\n    <spec test-file=\"tests/ci/verify_composite_actions.sh\" test-function=\"test_spdx_headers\" requirement-id=\"REQ-010\">All new composite actions (supply-chain-attest, setup-docker-registry, checkout-service-source, export-digest) have SPDX headers</spec>\n    <spec test-file=\"tests/ci/verify_composite_actions.sh\" test-function=\"test_feature_id_comment\" requirement-id=\"REQ-010\">All new composite actions have a # CC-0055 traceability comment</spec>\n    <spec test-file=\"tests/ci/verify_composite_actions.sh\" test-function=\"test_shell_explicit\" requirement-id=\"REQ-010\">All composite action run: steps have explicit shell: bash</spec>\n    <spec test-file=\"tests/ci/verify_composite_actions.sh\" test-function=\"test_sha_pinned_actions\" requirement-id=\"REQ-010\">All external action references in composite actions use SHA+comment pin format</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_workflow_line_count\" requirement-id=\"REQ-009\">build-images.yaml is under 600 lines</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_no_inline_scripts_over_5_lines\" requirement-id=\"REQ-009\">No inline run: block in build-images.yaml exceeds 5 lines</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_no_must_stay_in_sync_comments\" requirement-id=\"REQ-003\">build-images.yaml contains no 'MUST stay in sync' comments</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_normalize_owner_count\" requirement-id=\"REQ-006\">The normalize image owner one-liner appears at most once in build-images.yaml (in a dedicated job or not at all if using workflow env)</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_supply_chain_attest_usage\" requirement-id=\"REQ-001\">build-images.yaml references ./.github/actions/supply-chain-attest at least 4 times (python-base, venv-builder, tempest, service)</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_setup_docker_registry_usage\" requirement-id=\"REQ-002\">build-images.yaml references ./.github/actions/setup-docker-registry in all jobs that need docker access</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_checkout_service_source_usage\" requirement-id=\"REQ-003\">build-images.yaml references ./.github/actions/checkout-service-source in both build-service-images and test-service-images</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_export_digest_usage\" requirement-id=\"REQ-007\">build-images.yaml references ./.github/actions/export-digest for all digest export operations</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_ci_merge_manifest_usage\" requirement-id=\"REQ-004\">build-images.yaml calls hack/ci-merge-manifest.sh in merge-base-images, merge-tempest-image, and merge-service-images</spec>\n    <spec test-file=\"tests/ci/verify_build_images_refactor.sh\" test-function=\"test_ci_run_unit_tests_usage\" requirement-id=\"REQ-005\">build-images.yaml calls hack/ci-run-unit-tests.sh in test-service-images</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"CI maintainer uses supply-chain-attest composite action for all image types\">As a CI maintainer, I want the SBOM generation, Grype vulnerability scan, SARIF upload, attestation, build provenance, and cosign signing pipeline to be a single parameterised composite action, so that adding a new image type requires one action call instead of copy-pasting ~65 lines, and security pipeline changes propagate to all images automatically</story>\n    <story title=\"CI maintainer uses setup-docker-registry composite action across all jobs\">As a CI maintainer, I want the docker/login-action + docker/setup-buildx-action + sigstore/cosign-installer block to be a single composite action, so that updating action versions or adding a new registry only requires changing one file instead of ~9 jobs</story>\n    <story title=\"CI maintainer uses checkout-service-source composite action eliminating sync comments\">As a CI maintainer, I want the source-ref resolution, upstream checkout, patch application, and constraint override steps to be a single composite action, so that the 'NOTE: This step MUST stay in sync' comments are eliminated and build-service-images and test-service-images always use identical checkout logic</story>\n    <story title=\"CI maintainer uses ci-merge-manifest.sh for all manifest merge operations\">As a CI maintainer, I want the docker buildx imagetools create + imagetools inspect pattern to be a reusable script with env var interface, so that manifest merge logic is tested via shellcheck, runnable locally, and changes apply to all image types</story>\n    <story title=\"CI maintainer uses ci-run-unit-tests.sh for service test execution\">As a CI maintainer, I want the ~50-line inline docker run block in test-service-images to be an extracted shell script, so that the test execution logic is shellcheck-clean, locally runnable for debugging, and not buried in workflow YAML</story>\n    <story title=\"CI maintainer sees normalize-image-owner handled as workflow-level env\">As a CI maintainer, I want the repeated one-liner `echo owner=${GITHUB_REPOSITORY_OWNER,,}` to appear only once, so that adding a new job does not require remembering to add the normalize step</story>\n    <story title=\"CI maintainer uses digest-export composite action for all images\">As a CI maintainer, I want the mkdir + touch + upload-artifact pattern for digest export to be consolidated, so that the digest export pattern is consistent and adding a new image type does not require copy-pasting the pattern</story>\n    <story title=\"CI maintainer confirms all extracted scripts pass verification tests\">As a CI maintainer, I want all new hack/ci-*.sh scripts to be automatically validated by the existing tests/ci/verify_ci_scripts.sh test suite, so that script quality standards (set -euo pipefail, shellcheck, SPDX headers, error annotations) are enforced</story>\n    <story title=\"CI maintainer confirms no functional change to workflow behavior\">As a CI maintainer, I want all existing jobs to produce identical results after the refactoring, so that the refactoring is purely structural with no behavioral regression</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The system SHALL extract the supply chain security sequence (SBOM, Grype, SARIF, attestation, provenance, cosign) into a composite action `.github/actions/supply-chain-attest/action.yaml`</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The system SHALL extract Docker registry setup (login, buildx, cosign) into a composite action `.github/actions/setup-docker-registry/action.yaml`</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The system SHALL extract the checkout-service-source sequence into a composite action `.github/actions/checkout-service-source/action.yaml`</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The system SHALL extract manifest merge logic into `hack/ci-merge-manifest.sh` with env var interface</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The system SHALL extract the unit test execution block into `hack/ci-run-unit-tests.sh`</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The system SHALL consolidate normalize-image-owner into a single resolution point</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The system SHALL extract digest export + artifact upload into a composite action `.github/actions/export-digest/action.yaml`</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">All extracted scripts SHALL pass shellcheck and follow CC-0050 conventions</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The system SHALL preserve all existing workflow behavior with no functional changes</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">All composite actions SHALL follow established conventions (SPDX header, CC-ID comment, SHA-pinned actions, shell: bash explicit)</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 22333,
  "caller": "run_claude_cli"
}