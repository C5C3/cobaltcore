{
  "timestamp": "2026-04-12T13:52:40.600974+00:00",
  "feature_id": "CC-0064",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0064",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0064\">\n  <title>Add schema drift detection before DatabaseReady</title>\n  <description># GitHub Issue #164: Add schema drift detection before setting DatabaseReady condition\n\n**Category**: improvement | **Scope**: Medium\n\n**Description**: The `DatabaseReady` condition is currently set to True as soon as the `db_sync` Job completes successfully. There is no verification that the database schema actually matches what the running Keystone version expects. Add a pre-flight schema version check that queries the `alembic_version` table and compares the result against the expected migration head before setting `DatabaseReady=True`.\n\nImplement this as a lightweight init container or a separate validation Job that runs after `db_sync` completes:\n\n1. Connect to the database using the existing connection string from the ConfigMap\n2. Execute `SELECT version_num FROM alembic_version` (single row, single column)\n3. Compare against the expected Alembic head revision for the deployed Keystone version\n4. If the version matches → exit 0 (schema is consistent)\n5. If the version does not match → exit 1 with a descriptive error message including both expected and actual versions\n\nAdditionally, surface the detected schema version in the `DatabaseReady` condition message so operators can see it via `kubectl get keystone -o yaml` without needing database access.\n\nThe expected Alembic head can be obtained by running `keystone-manage db_version` in the Keystone image. This value should be captured during the `db_sync` Job (or a follow-up check) and compared against the live database.\n\n**Motivation**: Schema drift can occur in several scenarios: a manual `ALTER TABLE` was run against the database, a partial migration failed silently (Alembic marked it complete but a DDL statement was rolled back by the storage engine), or the database was restored from a backup that predates the last migration. In all cases, the operator currently reports `DatabaseReady=True` based on a stale Job completion status, while Keystone encounters runtime errors (missing columns, type mismatches, integrity constraint violations). Detecting drift at the operator level enables early alerting and prevents rollout of incompatible API versions.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/reconcile_database.go` (add schema verification step after db_sync completion)\n- `operators/keystone/internal/controller/reconcile_database_test.go`\n\n**Acceptance Criteria**:\n- [ ] After `db_sync` Job succeeds, schema version is verified against the live database\n- [ ] `DatabaseReady` condition message includes the detected Alembic version (e.g. `DatabaseSynced at revision abc123`)\n- [ ] Schema mismatch sets `DatabaseReady=False` with reason `SchemaDriftDetected` and a message showing expected vs. actual version\n- [ ] Schema check is idempotent and read-only (SELECT only, no DDL)\n- [ ] Schema check uses the same image, SecurityContext, and config mounts as the db_sync Job\n- [ ] Schema check Job has `backoffLimit: 2` and `ttlSecondsAfterFinished: 300`\n- [ ] Requeue with `RequeueDatabaseWait` interval while schema check Job is running\n- [ ] Unit tests cover: matching version passes, mismatched version fails with descriptive reason, missing alembic_version table fails gracefully\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/164</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add buildSchemaCheckJob function to reconcile_database.go (REQ-003, REQ-004)</task>\n    <task status=\"pending\">2.1 Integrate schema check into reconcileDatabase flow after db_sync success (REQ-001, REQ-005, REQ-006, REQ-007, REQ-008)</task>\n    <task status=\"pending\">2.2 Add unit tests for schema check integration in reconcileDatabase (REQ-001, REQ-005, REQ-006, REQ-007)</task>\n    <task status=\"pending\">2.3 Update existing tests to include schema-check Job in test fixtures (REQ-001, REQ-005)</task>\n    <task status=\"pending\">3.1 Add reference documentation for schema drift detection (REQ-001 through REQ-008)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThe schema-check Job is inserted into the reconcileDatabase flow as a second stage after db_sync, before setting DatabaseReady=True. This is a separate Job (not an init container) because:\n1. It uses the existing job.RunJob infrastructure with pod-spec hash comparison for stale detection\n2. It allows independent backoffLimit (2 vs 4 for db_sync)\n3. It enables ttlSecondsAfterFinished for automatic cleanup\n4. It separates the \"write\" (db_sync) from \"read\" (schema-check) operations\n\n## Data Flow\n\n```\nreconcileDatabase()\n  ├─ MariaDB CRs ready? (managed mode only)\n  ├─ Upgrade path? → delegate to reconcileUpgrade\n  ├─ db_sync Job (existing)\n  │   ├─ failed → DatabaseReady=False/DBSyncFailed, return error\n  │   ├─ running → DatabaseReady=False/DBSyncInProgress, requeue 30s\n  │   └─ done → proceed to schema check\n  ├─ schema-check Job (NEW)\n  │   ├─ failed → DatabaseReady=False/SchemaDriftDetected, return error\n  │   ├─ running → DatabaseReady=False/SchemaCheckInProgress, requeue 30s\n  │   └─ done → proceed to set DatabaseReady=True\n  └─ DatabaseReady=True/DatabaseSynced (message includes 'revision')\n```\n\n## Schema Check Script Pattern\n\nFollow the embedded Python script pattern from reconcile_bootstrap.go (line 79-95). The script:\n1. Reads keystone.conf from /etc/keystone/keystone.conf.d/ using configparser\n2. Parses the database connection URL\n3. Connects via PyMySQL\n4. Executes SELECT version_num FROM alembic_version\n5. Runs keystone-manage db_version (subprocess) to get expected head\n6. Compares and exits 0 (match) or 1 (mismatch/error)\n\n## Key Files\n\n- `operators/keystone/internal/controller/reconcile_database.go` - Add buildSchemaCheckJob() and modify reconcileDatabase() flow\n- `operators/keystone/internal/controller/reconcile_database_test.go` - Add test helpers and test cases\n- `operators/keystone/internal/controller/reconcile_bootstrap.go` - Reference pattern for embedded Python scripts and TTL Job config\n- `internal/common/job/job.go` - RunJob helper (reused as-is)\n- `operators/keystone/internal/controller/security_context.go` - restrictedSecurityContext() helper (reused as-is)\n- `docs/reference/keystone-reconciler.md` - Update documentation\n\n## Pitfalls\n\n1. **Cannot read Job logs from controller**: The controller cannot access pod logs to extract the actual Alembic version string. The condition message uses a static 'verified' marker rather than the actual version. To get the actual version, an operator must check the schema-check Job logs.\n2. **Script must handle both SQLAlchemy-style and raw MySQL connection strings**: The configparser approach from reconcile_bootstrap.go handles this correctly.\n3. **keystone-manage db_version output format**: This command outputs the Alembic revision hash followed by additional text. The script must parse only the revision string.\n4. **Upgrade flow**: The schema check is only added to the non-upgrade (simple db_sync) path. The upgrade flow (expand/migrate/contract) has its own validation via Alembic's _validate_upgrade_order check. Adding schema checks to the upgrade flow is deferred.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_Name\" requirement-id=\"REQ-003\">Job name should be {keystone.Name}-schema-check, namespace matches keystone.Namespace [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_Image\" requirement-id=\"REQ-003\">Container image should be {spec.image.repository}:{spec.image.tag} [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_SecurityContext\" requirement-id=\"REQ-003\">Container should satisfy PSS Restricted profile via expectRestrictedSecurityContext helper [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_ConfigVolume\" requirement-id=\"REQ-003\">Container should mount config ConfigMap at /etc/keystone/keystone.conf.d/ read-only [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_BackoffLimit\" requirement-id=\"REQ-004\">backoffLimit should be 2 (not the default 4 used by db_sync) [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_TTL\" requirement-id=\"REQ-004\">ttlSecondsAfterFinished should be 300 [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_RestartPolicy\" requirement-id=\"REQ-004\">RestartPolicy should be Never [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_Command\" requirement-id=\"REQ-002\">Command should use /bin/sh -eu -c with the schema check Python script [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckRunning_Requeues\" requirement-id=\"REQ-007\">When db_sync is complete but schema-check is running, should requeue with RequeueDatabaseWait and set condition SchemaCheckInProgress [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckComplete_DatabaseSynced\" requirement-id=\"REQ-005\">When both db_sync and schema-check complete, DatabaseReady=True/DatabaseSynced with message containing 'revision' [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckFailed_SchemaDriftDetected\" requirement-id=\"REQ-006\">When schema-check fails, DatabaseReady=False/SchemaDriftDetected, returns error [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_Managed_AllReady_WithSchemaCheck\" requirement-id=\"REQ-001\">Managed mode with all MariaDB CRs ready + db_sync + schema-check complete → DatabaseReady=True [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckStale_Recreated\" requirement-id=\"REQ-008\">Completed schema-check with stale pod-spec hash triggers deletion and recreation, requeues [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckNotCreatedWhenDBSyncRunning\" requirement-id=\"REQ-001\">When db_sync is still running, no schema-check Job should exist, condition remains DBSyncInProgress [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckNotCreatedWhenDBSyncFails\" requirement-id=\"REQ-001\">When db_sync fails, no schema-check Job should exist, condition remains DBSyncFailed [unit]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_SchemaCheckFailed_InstalledReleaseNotUpdated\" requirement-id=\"REQ-006\">When schema-check fails, InstalledRelease should NOT be updated to the new tag [unit]</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator verifies schema version after db_sync before marking DatabaseReady\">As a OpenStack platform operator, I want the operator to verify that the database schema matches the expected Alembic migration head after db_sync completes, so that schema drift caused by manual DDL changes, partial migration failures, or database restores from stale backups is detected before Keystone pods serve traffic with an incompatible schema</story>\n    <story title=\"Operator receives descriptive error when schema drift is detected\">As a OpenStack platform operator, I want to see a clear error message in the DatabaseReady condition showing both expected and actual schema versions when drift is detected, so that I can diagnose the cause of schema drift without needing direct database access or inspecting Job logs</story>\n    <story title=\"Operator surfaces detected schema version in DatabaseReady condition on success\">As a OpenStack platform operator, I want the DatabaseReady condition message to include the detected Alembic revision when the schema check passes, so that I can verify which migration version the database is at by running kubectl get keystone -o yaml without needing database access</story>\n    <story title=\"Schema check handles missing alembic_version table gracefully\">As a OpenStack platform operator, I want the schema check to fail with a descriptive error when the alembic_version table does not exist, so that I receive a clear indication that the database has never been migrated rather than a cryptic SQL error</story>\n    <story title=\"Schema check is idempotent and read-only\">As a OpenStack platform operator, I want the schema check to be a read-only SELECT query with no side effects, so that running the schema check repeatedly or concurrently cannot corrupt the database or interfere with migrations</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The system SHALL run a schema-check Job after db_sync Job completes successfully to verify the database schema version matches the expected Alembic migration head</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The schema-check Job SHALL execute a Python script that queries SELECT version_num FROM alembic_version and compares the result against the expected Alembic head obtained from keystone-manage db_version</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The schema-check Job SHALL use the same image, SecurityContext (restrictedSecurityContext), config volume mounts, and RestartPolicy as the db_sync Job</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The schema-check Job SHALL have backoffLimit=2 and ttlSecondsAfterFinished=300</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The system SHALL set DatabaseReady=True with reason DatabaseSynced and a message containing the detected Alembic revision when the schema check passes</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The system SHALL set DatabaseReady=False with reason SchemaDriftDetected when the schema-check Job fails</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The system SHALL requeue with RequeueDatabaseWait interval while the schema-check Job is running</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The schema-check Job SHALL be managed by job.RunJob with pod-spec hash comparison for stale Job detection</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17100,
  "caller": "run_claude_cli"
}