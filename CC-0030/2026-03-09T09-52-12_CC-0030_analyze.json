{
  "timestamp": "2026-03-09T09:52:12.980596+00:00",
  "feature_id": "CC-0030",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0030",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0030\">\n  <title>Add cosign keyless signing to container images</title>\n  <description>**Size:** 🔧 small\n**Category:** infrastructure\n**Priority:** high\n**Source:** Proposed for 'Now that SBOM should be created and attested'\n\nAdd cosign-based container image signing to `.github/workflows/build-images.yaml` completing the supply chain security chain (build → push → SBOM → attest → sign).\n\n**Workflow changes (build-images.yaml):**\n1. Add `sigstore/cosign-installer` action (SHA-pinned with `# v<N>` comment) as an early step in both `build-base-images` and `build-service-images` jobs\n2. Add `cosign sign --yes` step after SBOM attestation in `build-base-images` for python-base (`@${{ steps.build-python-base.outputs.digest }}`) and venv-builder (`@${{ steps.build-venv-builder.outputs.digest }}`)\n3. Add `cosign sign --yes` step after SBOM attestation in `build-service-images` for service images (`${{ steps.tags.outputs.image }}@${{ steps.build-service.outputs.digest }}`)\n4. Guard all signing steps with `if: github.event_name != 'pull_request'` (matching existing attestation guards)\n5. No permission changes needed — existing `id-token: write` + `packages: write` are sufficient for keyless Fulcio/Rekor signing\n\n**Test updates (tests/container-images/verify_build_images_workflow.sh):**\n- Add yq-based assertions for `sigstore/cosign-installer` presence in both build jobs\n- Assert `cosign sign` steps exist with correct `if:` guards\n- Assert signing steps reference image digests\n- Follow existing PASS/FAIL counter pattern\n\n**Doc updates (docs/reference/build-images-workflow.md):**\n- Add `cosign verify` command examples with `--certificate-identity-regexp` and `--certificate-oidc-issuer` parameters\n- Place alongside existing `cosign verify-attestation` and `gh attestation verify` commands\n\n**Rationale:** Images currently receive CycloneDX SBOMs and Sigstore attestations (CC-0029) but lack independent identity signing. Image signing adds an identity assertion ('this image was produced by this workflow') on top of existing property assertions ('this image has this SBOM'). This is a standard supply chain security practice that completes the trust chain and enables downstream consumers to verify image provenance via `cosign verify`. Keyless signing via GitHub OIDC avoids key management overhead.\n\n**Affected Areas:**\n- .github/workflows/build-images.yaml\n- tests/container-images/verify_build_images_workflow.sh\n- docs/reference/build-images-workflow.md</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add cosign-installer and cosign sign steps to build-base-images job in build-images.yaml (REQ-018, REQ-019, REQ-021, REQ-022)</task>\n    <task status=\"pending\">1.2 Add cosign-installer and cosign sign step to build-service-images job in build-images.yaml (REQ-018, REQ-020, REQ-021, REQ-022)</task>\n    <task status=\"pending\">2.1 Add verification tests for cosign-installer presence in both build jobs (REQ-018, REQ-024)</task>\n    <task status=\"pending\">2.2 Add verification tests for cosign sign steps: count, PR guard, and digest references (REQ-019, REQ-020, REQ-021, REQ-022, REQ-024)</task>\n    <task status=\"pending\">3.1 Update reference documentation with cosign signing section and verify commands (REQ-025)</task>\n\n  </tasks>\n  <implementation-notes>This feature adds cosign keyless signing as the final step in the supply chain security chain (build → push → SBOM → attest → sign). The implementation follows the exact same patterns established by CC-0029 for SBOM/attestation:\n\n**Action pinning:** `sigstore/cosign-installer@faadad0cce49287aee09b3a48701e75088a2c6ad # v4` — matches the SHA-pinned with version comment convention used by all other actions in the workflow.\n\n**Step ordering in build-base-images:** cosign-installer (early, after login) → ... → Build python-base → Generate SBOM → Attest SBOM → **Sign python-base** → Build venv-builder → Generate SBOM → Attest SBOM → **Sign venv-builder**. The cosign-installer only needs to run once per job.\n\n**Step ordering in build-service-images:** cosign-installer (early, after login) → ... → Build service → Generate SBOM → Attest SBOM → **Sign service image** → Verify (PR only).\n\n**Signing command pattern:** `cosign sign --yes ghcr.io/<owner>/<image>@<digest>`. The `--yes` flag enables non-interactive keyless mode (Fulcio/Rekor). The `COSIGN_EXPERIMENTAL` env var is NOT needed for cosign v2+ — keyless is the default.\n\n**Permissions:** No changes needed. cosign keyless signing requires: (1) `id-token: write` for OIDC token request → already present for CC-0029, (2) `packages: write` for pushing the signature to GHCR OCI registry → already present for image push. The id-token permission comment should be updated to reference both CC-0029 and CC-0030.\n\n**PR guard:** `if: github.event_name != 'pull_request'` — same guard used by all SBOM/attestation steps.\n\n**Risks:** (1) cosign-installer action version may have breaking changes — mitigated by SHA pinning. (2) Fulcio/Rekor service availability — transient failures would fail the workflow; this is acceptable as signing is mandatory for production builds. (3) Race condition between push and sign is not a concern because both use digest references.\n\n**Key files:**\n- `.github/workflows/build-images.yaml` — main workflow file\n- `tests/container-images/verify_build_images_workflow.sh` — workflow structure tests\n- `tests/lib/assertions.sh` — shared assertion helpers (read-only, not modified)\n- `docs/reference/build-images-workflow.md` — reference documentation</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_installer_in_build_base_images\" requirement-id=\"REQ-018\">Should detect sigstore/cosign-installer action in build-base-images job steps, SHA-pinned with version comment</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_installer_in_build_service_images\" requirement-id=\"REQ-018\">Should detect sigstore/cosign-installer action in build-service-images job steps, SHA-pinned with version comment</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_sign_steps_in_build_base_images\" requirement-id=\"REQ-019\">Should find exactly 2 cosign sign steps in build-base-images (one for python-base, one for venv-builder)</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_sign_step_in_build_service_images\" requirement-id=\"REQ-020\">Should find exactly 1 cosign sign step in build-service-images</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_sign_steps_pr_skip_guard\" requirement-id=\"REQ-021\">All cosign sign steps in both build jobs should have if: github.event_name != 'pull_request' guard</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_sign_references_digest_base_images\" requirement-id=\"REQ-022\">Base image signing steps should reference build-python-base.outputs.digest and build-venv-builder.outputs.digest respectively</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_cosign_sign_references_digest_service_images\" requirement-id=\"REQ-022\">Service image signing step should reference steps.tags.outputs.image and steps.build-service.outputs.digest</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_permissions_unchanged_after_signing\" requirement-id=\"REQ-023\">Existing id-token: write and packages: write permissions on build jobs remain unchanged; no new permissions added. Note: this is already covered by existing test_sbom_permissions tests and test_verify_jobs_no_sbom_permissions — no new test needed, just verify existing tests still pass.</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Image consumer can verify image provenance via cosign\">As a image consumer, I want container images pushed to GHCR to be signed with a Sigstore keyless identity, so that I can verify that images were produced by the expected GitHub Actions workflow before deploying them</story>\n    <story title=\"CI pipeline signs images after SBOM attestation\">As a CI pipeline, I want to sign every pushed container image with cosign keyless signing after SBOM attestation completes, so that the supply chain security chain is complete: build → push → SBOM → attest → sign</story>\n    <story title=\"PR builds skip image signing\">As a contributor, I want pull request builds to skip image signing entirely, so that PR CI time is not increased and no signing artifacts are created for ephemeral PR builds</story>\n    <story title=\"Security auditor can verify signing configuration in workflow tests\">As a security auditor, I want automated tests to verify that cosign signing is correctly configured in the workflow, so that signing cannot be accidentally removed or misconfigured without test failures</story>\n    <story title=\"Operator can follow documentation to verify image signatures\">As a operator, I want reference documentation with cosign verify command examples, so that I can verify image signatures without reading the workflow source code</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-018\" priority=\"SHALL\">The workflow SHALL install the sigstore/cosign-installer action (SHA-pinned with version comment) in both build-base-images and build-service-images jobs</requirement>\n    <requirement id=\"REQ-019\" priority=\"SHALL\">The workflow SHALL sign python-base and venv-builder images with cosign after SBOM attestation in build-base-images job</requirement>\n    <requirement id=\"REQ-020\" priority=\"SHALL\">The workflow SHALL sign service images with cosign after SBOM attestation in build-service-images job</requirement>\n    <requirement id=\"REQ-021\" priority=\"SHALL\">All cosign signing steps SHALL be guarded with if: github.event_name != 'pull_request'</requirement>\n    <requirement id=\"REQ-022\" priority=\"SHALL\">Cosign signing SHALL use digest references, not mutable tags, to identify images</requirement>\n    <requirement id=\"REQ-023\" priority=\"SHALL\">No additional workflow permissions SHALL be required for cosign keyless signing</requirement>\n    <requirement id=\"REQ-024\" priority=\"SHALL\">Verification tests SHALL assert cosign-installer and signing step configuration in the workflow</requirement>\n    <requirement id=\"REQ-025\" priority=\"SHALL\">Reference documentation SHALL include cosign verify command examples for verifying image signatures</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 12768,
  "caller": "run_claude_cli"
}