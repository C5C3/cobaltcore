{
  "timestamp": "2026-04-14T17:56:42.582804+00:00",
  "feature_id": "CC-0066",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0066",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0066\">\n  <title>Add E2E tests for concurrent CR conflicts and failover</title>\n  <description># GitHub Issue #184: Add E2E tests for concurrent CR conflicts and operator pod failover\n\n**Category**: testing | **Scope**: Medium\n\n**Description**: Create E2E tests for two untested scenarios: (1) two Keystone CRs in the same namespace sharing database or secret names to verify the secretToKeystoneMapper and reconciler behavior under resource contention, and (2) killing the operator pod during active reconciliation to validate leader election failover and incomplete status update recovery.\n\n**Motivation**: The secretToKeystoneMapper returns reconcile requests for all matching CRs but reconcilers assume exclusive ownership of sub-resources by name. This conflict is untested. Similarly, chaos tests kill dependency pods but never the operator itself, leaving leader election failover and partial-reconcile recovery unvalidated.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/keystone_controller.go`\n- `operators/keystone/test/e2e/`\n\n**Acceptance Criteria**:\n- [ ] E2E test deploys two Keystone CRs in the same namespace and verifies no resource conflicts or data corruption\n- [ ] E2E test kills operator pod during reconciliation and verifies the new leader resumes correctly\n- [ ] Status conditions are consistent after operator pod failover\n- [ ] Test verifies no orphaned or duplicate sub-resources after concurrent CR operations\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/184</description>\n  <tasks>\n    <task status=\"pending\">1.1 Create concurrent CR conflict E2E test suite: CR fixtures and chainsaw-test with deploy, Ready assertions, sub-resource isolation, deletion, and cleanup verification (REQ-001, REQ-002, REQ-003, REQ-004, REQ-008)</task>\n    <task status=\"pending\">2.1 Create operator pod kill chaos E2E test suite: CR fixture, PodChaos manifest, patch file, and chainsaw-test with baseline, kill, recovery, and post-failover reconciliation (REQ-005, REQ-006, REQ-007, REQ-008)</task>\n    <task status=\"pending\">3.1 Update docs/reference/keystone-e2e-tests.md with concurrent-cr-conflicts scenario documentation (REQ-008)</task>\n    <task status=\"pending\">3.2 Update docs/reference/chaos-e2e-tests.md with operator-pod-kill scenario documentation (REQ-008)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThis feature adds two E2E test suites with no production code changes:\n\n```\ntests/e2e/keystone/concurrent-cr-conflicts/    (happy-path, runs in e2e-operator CI job)\n├── 00-keystone-cr-a.yaml\n├── 01-keystone-cr-b.yaml\n└── chainsaw-test.yaml\n\ntests/e2e-chaos/operator-pod-kill/              (chaos, runs in e2e-chaos CI job)\n├── 00-keystone-cr.yaml\n├── 01-podchaos.yaml\n├── 02-patch-replicas.yaml\n└── chainsaw-test.yaml\n```\n\n## Data Flow: Concurrent CR Test\n\n```\nApply CR-A + CR-B (shared secretRef: keystone-db, keystone-admin)\n  │\n  ├─ secretToKeystoneMapper enqueues BOTH CRs on shared Secret change\n  │     (keystone_controller.go:211-233)\n  │\n  ├─ Reconciler for CR-A creates: keystone-concurrent-a-api (Deployment),\n  │   keystone-concurrent-a-api (Service), keystone-concurrent-a-fernet-rotate\n  │   (CronJob), keystone-concurrent-a-config-{hash} (ConfigMap),\n  │   keystone-concurrent-a (Database/User/Grant)\n  │\n  ├─ Reconciler for CR-B creates: keystone-concurrent-b-api (Deployment),\n  │   keystone-concurrent-b-api (Service), etc. — all uniquely named\n  │\n  ├─ Both CRs reach Ready=True independently\n  │\n  └─ Delete CR-A → owner ref GC cleans CR-A resources → CR-B unaffected\n```\n\n## Data Flow: Operator Pod Kill Test\n\n```\nBaseline: Keystone CR Ready=True, operator running 2 replicas\n  │\n  ├─ PodChaos mode:all kills BOTH operator pods\n  │     (targets default namespace, label: app.kubernetes.io/name=keystone-operator)\n  │\n  ├─ Deployment controller creates 2 new pods\n  │\n  ├─ New pods start → compete for leader lease (keystone.openstack.c5c3.io)\n  │\n  ├─ Winner: starts controller manager → re-registers watches → processes queue\n  │\n  ├─ Keystone CR re-evaluated → all conditions still True → Ready=True\n  │\n  └─ Patch replicas 1→2 → new leader reconciles → Deployment scaled → Ready=True\n```\n\n## Key Design Decisions\n\n1. **mode: all for PodChaos**: Guarantees the leader is killed (not just the standby). Forces actual leader re-election.\n\n2. **Shared secrets, different database names**: Both CRs share secretRef (keystone-db) and adminPasswordSecretRef (keystone-admin) to test mapper behavior, but use different database.database names (keystone_concurrent_a, keystone_concurrent_b) to avoid MariaDB-level conflicts.\n\n3. **replicas: 1 for all test CRs**: Minimizes resource usage in CI kind cluster while still testing core reconciliation.\n\n4. **Operator namespace targeting**: The operator is deployed to the default namespace via `hack/ci-deploy-operator.sh` (no -n flag). PodChaos targets `default` namespace. If deployment namespace changes, update 01-podchaos.yaml selector.namespaces.\n\n5. **No CI workflow changes needed**: The concurrent-cr-conflicts test is picked up automatically by the e2e-operator job (tests/e2e/keystone/ path). The operator-pod-kill test is picked up by the e2e-chaos job (tests/e2e-chaos/ path).\n\n## Potential Pitfalls\n\n- **Operator namespace**: If the operator is deployed to a namespace other than `default`, the PodChaos selector.namespaces must be updated. The implementation should verify this.\n- **Leader election timeout**: The default lease duration is 15s with renewal every 10s. After killing all pods, the new leader may take up to 15s + pod startup time to acquire the lease. The 5m assert timeout is generous.\n- **Resource contention in CI**: The concurrent CR test creates 2 full Keystone stacks (2 Deployments, 2 db_sync Jobs, 2 bootstrap Jobs). CI kind cluster must have sufficient resources.\n- **Database name uniqueness**: Each test suite uses unique database names (keystone_concurrent_a, keystone_concurrent_b, keystone_chaos_op) to avoid conflicts with existing tests.\n\n## Deferred Items\n\n1. **Concurrent CRs with same database name**: Testing two CRs with identical database.database values could expose MariaDB-level conflicts. Deferred because it may require production code changes to handle gracefully. Revisit after validating the basic shared-secret scenario.\n\n2. **Kill operator during initial reconciliation**: The current test kills the operator after Ready=True baseline. Killing during initial reconciliation (before Ready) would test partial-status recovery. Deferred because reliable timing in Chainsaw is impractical. Could be tested via envtest integration test instead.\n\n3. **envtest integration test for secretToKeystoneMapper with concurrent CRs**: A unit/integration test verifying the mapper returns correct reconcile.Request sets for multiple CRs. Deferred as a separate enhancement.\n\n4. **Webhook availability during operator failover**: Testing that admission webhooks remain available (or gracefully fail) during operator pod kill. Deferred as a separate scope.\n\n5. **Continuous chaos (duration-based pod kill)**: Testing with continuous PodChaos (duration: 60s) rather than one-shot. Deferred as a more advanced chaos scenario.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 2: Assert both CRs Ready=True\" requirement-id=\"REQ-001\">Both keystone-concurrent-a and keystone-concurrent-b have Ready=True with reason AllReady</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 2: Assert all sub-conditions True on both CRs\" requirement-id=\"REQ-001\">SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady all True on both CRs</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 3: Assert unique Deployments per CR\" requirement-id=\"REQ-002\">Deployment keystone-concurrent-a-api and keystone-concurrent-b-api both exist with correct labels</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 3: Assert unique Services per CR\" requirement-id=\"REQ-002\">Service keystone-concurrent-a-api and keystone-concurrent-b-api both exist with port 5000</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 4: Assert unique Fernet CronJobs per CR\" requirement-id=\"REQ-002\">CronJob keystone-concurrent-a-fernet-rotate and keystone-concurrent-b-fernet-rotate both exist</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 6: Assert CR-B Ready after CR-A deletion\" requirement-id=\"REQ-003\">keystone-concurrent-b remains Ready=True with AllReady after keystone-concurrent-a is deleted</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 6: Assert CR-B resources intact after CR-A deletion\" requirement-id=\"REQ-003\">Deployment keystone-concurrent-b-api exists with availableReplicas > 0 after CR-A deletion</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 5: Assert CR-A resources cleaned up\" requirement-id=\"REQ-004\">Deployment keystone-concurrent-a-api, Service keystone-concurrent-a-api, CronJob keystone-concurrent-a-fernet-rotate return NotFound</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Step 5: Assert no duplicate Deployments\" requirement-id=\"REQ-004\">Script step counts Deployments with app.kubernetes.io/name=keystone and asserts exactly 1 remains</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Step 4: Assert operator pod recovery\" requirement-id=\"REQ-005\">Operator Deployment has at least 1 available replica after PodChaos deleted</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Step 3: PodChaos kills all operator pods\" requirement-id=\"REQ-005\">PodChaos with mode: all and selector app.kubernetes.io/name: keystone-operator kills both operator pods</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Step 5: Assert Ready=True maintained after failover\" requirement-id=\"REQ-006\">Keystone CR keystone-chaos-op has Ready=True with reason AllReady after operator recovery</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Step 5: Assert all sub-conditions True after failover\" requirement-id=\"REQ-006\">SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady all True after recovery</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Step 7: Assert replica patch applied by new leader\" requirement-id=\"REQ-007\">After patching replicas from 1 to 2, Deployment keystone-chaos-op-api has replicas: 2 and availableReplicas >= 2</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Step 7: Assert Ready=True maintained after replica patch\" requirement-id=\"REQ-007\">Ready=True with reason AllReady after the replica scaling operation completes</spec>\n    <spec test-file=\"tests/e2e/keystone/concurrent-cr-conflicts/chainsaw-test.yaml\" test-function=\"Catch blocks on all assert steps\" requirement-id=\"REQ-008\">Every assert step has a catch block that dumps both CR statuses, pod logs, and events</spec>\n    <spec test-file=\"tests/e2e-chaos/operator-pod-kill/chainsaw-test.yaml\" test-function=\"Catch blocks on all assert steps\" requirement-id=\"REQ-008\">Every assert step has a catch block using diagnostics.sh with operator pod status, logs, and Chaos Mesh status</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Platform engineer deploys concurrent Keystone CRs with shared dependencies\">As a platform engineer, I want to deploy two Keystone CRs in the same namespace that share the same secretRef and adminPasswordSecretRef names, so that I can verify the secretToKeystoneMapper and reconciler handle resource contention without conflicts or data corruption</story>\n    <story title=\"Platform engineer verifies resource isolation after deleting one concurrent CR\">As a platform engineer, I want to delete one of two coexisting Keystone CRs and confirm the other is unaffected, so that I can safely manage independent Keystone instances sharing infrastructure in the same namespace</story>\n    <story title=\"SRE verifies operator resilience to pod failure via leader election failover\">As a SRE, I want to kill all operator pods and confirm leader election failover restores reconciliation capability, so that I can trust the HA operator deployment (2 replicas with --leader-elect) survives pod failures</story>\n    <story title=\"SRE verifies new leader reconciles spec changes after failover\">As a SRE, I want to patch a Keystone CR after operator pod failover and verify the new leader processes the change, so that I can confirm the operator is fully functional after leader election, not just running but actively reconciling</story>\n    <story title=\"CI engineer diagnoses E2E test failures via catch block diagnostics\">As a CI engineer, I want diagnostic output collected automatically when any assert step fails, so that I can troubleshoot E2E test failures from CI logs without reproducing the failure locally</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">Two Keystone CRs sharing the same secretRef and adminPasswordSecretRef names in the same namespace SHALL both reach Ready=True</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">Each Keystone CR SHALL create uniquely-named sub-resources using the CR name as prefix, with no shared sub-resources between concurrent CRs</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">Deleting one Keystone CR SHALL NOT affect the status or sub-resources of the other CR in the same namespace</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">After deleting one concurrent CR, its sub-resources SHALL be garbage-collected with no orphaned or duplicate resources remaining</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">Killing all operator pods via PodChaos SHALL trigger leader election failover with the Deployment controller restarting pods</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">Status conditions on the Keystone CR SHALL remain consistent (Ready=True) after operator pod failover</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The new leader SHALL reconcile spec changes (e.g., replica count patch) applied after operator pod failover</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">Every assert step in both test suites SHALL include a catch block collecting diagnostic output on failure</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17061,
  "caller": "run_claude_cli"
}