{
  "timestamp": "2026-03-12T06:53:03.805026+00:00",
  "feature_id": "CC-0009",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0009",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<previous-feature feature-id=\"CC-0008\" status=\"completed\">\n  <title>D001: Implement FluxCD infrastructure manifests</title>\n  <tasks hint=\"showing first 5\">\n    <task>1.1 Create deploy/flux-system/sources/ with 5 HelmRepository CRs (CC-0008, REQ-001, REQ-008)</task>\n    <task>2.1 Create cert-manager HelmRelease with self-signed ClusterIssuer (CC-0008, REQ-002, REQ-003, REQ-004, REQ-008)</task>\n    <task>2.2 Create MariaDB Operator HelmRelease (CC-0008, REQ-002, REQ-003, REQ-008)</task>\n    <task>2.3 Create External Secrets Operator HelmRelease (CC-0008, REQ-002, REQ-003, REQ-008)</task>\n    <task>2.4 Create Memcached Operator HelmRelease (CC-0008, REQ-002, REQ-003, REQ-008)</task>\n  </tasks>\n</previous-feature>\n\n<feature feature-id=\"CC-0009\">\n  <title>D002: Implement OpenBao deployment and secret management</title>\n  <description>**Size:** 🏗️ large\n**Category:** infrastructure\n**Priority:** high\n**Feature:** CC-0009\n\n---\n\n## Open Questions\n\nNo remaining open questions — all design decisions resolved.\n\n---\n\n## Summary\n\nCC-0009 deploys OpenBao as a 3-replica HA Raft cluster with TLS on the management cluster, creates idempotent bootstrap scripts for initialization (unseal, secret engines, auth methods, policies, and initial secret seeding), defines 8 least-privilege HCL policies for ESO and CI/CD consumers, and wires ESO integration via a `ClusterSecretStore` and `ExternalSecret` resources for Keystone and MariaDB credentials. This establishes the centralized secret management backbone referenced by all subsequent OpenStack service deployments.\n\n---\n\n## Scope\n\n**Included:**\n- **HelmRelease** at `deploy/flux-system/releases/openbao.yaml` — HA 3-replica Raft, TLS via cert-manager `openbao-tls` Secret, `injector.enabled: false`, `ui = false`, 10Gi PVCs (`storageClass: local-path`), resources 256Mi/250m request → 512Mi limit, `dependsOn: cert-manager`. Follows CC-0008 conventions (interval `30m`, `install.crds: CreateReplace`, `upgrade.crds: CreateReplace`, `upgrade.remediation.retries: 3`). Canonical spec from `architecture/docs/09-implementation/09-openbao-deployment.md`\n- **cert-manager Certificate** at `deploy/flux-system/infrastructure/openbao-tls-cert.yaml` — references `selfsigned-cluster-issuer` (ClusterIssuer), produces `openbao-tls` Secret in `openbao-system` namespace, SANs: `openbao-0.openbao-internal`, `openbao-1.openbao-internal`, `openbao-2.openbao-internal`, `openbao.openbao-system.svc`. Placed in infrastructure kustomization per two-phase CRD-dependent pattern\n- **`deploy/openbao/bootstrap/init-unseal.sh`** — idempotent init (5-share, 3-threshold Shamir) + unseal all 3 replicas; stores unseal keys/root token as K8s Secret `openbao-unseal-keys`; checks `bao status` before init\n- **`deploy/openbao/bootstrap/setup-secret-engines.sh`** — enable KV v2 at `kv-v2/` and PKI at `pki/`; idempotent via `bao secrets list` guard\n- **`deploy/openbao/bootstrap/setup-auth.sh`** — Kubernetes auth at `kubernetes/{management,control-plane,hypervisor,storage}` with per-cluster SA/role bindings + AppRole at `approle/` for CI/CD; idempotent via `bao auth list` guard\n- **`deploy/openbao/bootstrap/setup-policies.sh`** — apply all 8 HCL policies via `bao policy write` (upsert = inherently idempotent)\n- **`deploy/openbao/bootstrap/write-bootstrap-secrets.sh`** — generate random passwords and write to `kv-v2/bootstrap/keystone-admin`, `kv-v2/infrastructure/mariadb`, `kv-v2/openstack/keystone/db`; idempotent via `bao kv get` check (skip if key exists)\n- **8 HCL policy files** in `deploy/openbao/policies/`: `eso-control-plane.hcl`, `eso-hypervisor.hcl`, `eso-storage.hcl`, `eso-management.hcl`, `push-app-credentials.hcl`, `push-ceph-keys.hcl`, `ci-cd-provisioner.hcl`, `pki-issuer.hcl`. Policy bodies defined in `architecture/docs/09-implementation/09-openbao-deployment.md`\n- **`deploy/eso/clustersecretstore.yaml`** — name `openbao-cluster-store`, `vault` provider type, server `https://openbao.openbao-system.svc:8200`, path `kv-v2`, version `v2`, `kubernetes/management` auth mount, role `eso-management`, ServiceAccount `external-secrets` in `external-secrets` namespace\n- **`deploy/eso/externalsecrets/keystone-db.yaml`** — ExternalSecret in `openstack` namespace pulling from `kv-v2/openstack/keystone/db`, creates `keystone-db-credentials` Secret\n- **`deploy/eso/externalsecrets/keystone-admin.yaml`** — ExternalSecret in `openstack` namespace pulling from `kv-v2/bootstrap/keystone-admin`, creates `keystone-admin-credentials` Secret\n- **`deploy/eso/externalsecrets/mariadb-root-password.yaml`** — ExternalSecret in **`openstack`** namespace pulling from `kv-v2/infrastructure/mariadb`, creates `mariadb-root-password` Secret (resolves dangling `rootPasswordSecretKeyRef` in `deploy/flux-system/infrastructure/mariadb.yaml` which is namespace `openstack`)\n- **`deploy/eso/kustomization.yaml`** — Kustomize entrypoint for ESO resources\n- **Wiring updates**: add `openbao-system` namespace to `deploy/flux-system/namespaces.yaml`; add `releases/openbao.yaml` to `deploy/flux-system/kustomization.yaml` resources list; add `openbao-tls-cert.yaml` to `deploy/flux-system/infrastructure/kustomization.yaml` resources list. Existing `sources/openbao.yaml` HelmRepository already present and listed from CC-0008\n- **SPDX license headers** and `# Feature: CC-0009` annotations on all files\n\n**Excluded:**\n- `make deploy-infra` / `make teardown-infra` targets (CC-0010)\n- Chainsaw E2E tests (CC-0010)\n- Database secret engine (`database/mariadb/` — not requested; only KV v2 and PKI)\n- PushSecret manifests (architecture doc defines them, but not in CC-0009 scope)\n- Transit auto-unseal (production hardening, YAGNI)\n- Remote cluster ClusterSecretStore instances (created when those clusters are bootstrapped)\n- UI enablement (architecture doc sets `ui = false`)\n- OpenBao PKI-backed ClusterIssuer (separate concern, architecture doc Phase 5+)\n- ServiceMonitor for OpenBao metrics (not in CC-0009 feature request)\n- Full secret inventory seeding beyond keystone-admin, mariadb, keystone/db (rabbitmq, valkey, service-passwords, ceph — seeded by later features)\n\n---\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph mgmt[\"Management Cluster\"]\n        subgraph obns[\"openbao-system namespace\"]\n            OB0[\"openbao-0\"]\n            OB1[\"openbao-1\"]\n            OB2[\"openbao-2\"]\n            OB0 <-->|Raft| OB1\n            OB1 <-->|Raft| OB2\n            OB0 <-->|Raft| OB2\n            TLS[\"openbao-tls Secret\"]\n            CERT[\"cert-manager Certificate\"]\n            CERT -->|creates| TLS\n            TLS -->|mounts| OB0\n            TLS -->|mounts| OB1\n            TLS -->|mounts| OB2\n        end\n        subgraph esons[\"external-secrets namespace\"]\n            ESO[\"ESO Controller\"]\n            CSS[\"ClusterSecretStore: openbao-cluster-store\"]\n            CSS -->|vault provider| OB0\n        end\n        subgraph osns[\"openstack namespace\"]\n            ESDB[\"ExternalSecret: keystone-db\"]\n            ESADM[\"ExternalSecret: keystone-admin\"]\n            ESMDB[\"ExternalSecret: mariadb-root-password\"]\n            ESDB -->|pulls from| CSS\n            ESADM -->|pulls from| CSS\n            ESMDB -->|pulls from| CSS\n            KSDB[\"Secret: keystone-db-credentials\"]\n            KSADM[\"Secret: keystone-admin-credentials\"]\n            MDBSEC[\"Secret: mariadb-root-password\"]\n            ESDB -->|creates| KSDB\n            ESADM -->|creates| KSADM\n            ESMDB -->|creates| MDBSEC\n            MDB[\"MariaDB Galera CR: openstack-db\"]\n            MDB -->|rootPasswordSecretKeyRef| MDBSEC\n        end\n    end\n    subgraph bootstrap[\"Bootstrap Scripts - deploy/openbao/bootstrap/\"]\n        INIT[\"init-unseal.sh\"]\n        ENGINES[\"setup-secret-engines.sh\"]\n        AUTH[\"setup-auth.sh\"]\n        POLICIES[\"setup-policies.sh\"]\n        SECRETS[\"write-bootstrap-secrets.sh\"]\n        INIT --> ENGINES --> AUTH --> POLICIES --> SECRETS\n    end\n    subgraph policies[\"HCL Policies - deploy/openbao/policies/\"]\n        P1[\"eso-control-plane\"]\n        P2[\"eso-hypervisor\"]\n        P3[\"eso-storage\"]\n        P4[\"eso-management\"]\n        P5[\"push-app-credentials\"]\n        P6[\"push-ceph-keys\"]\n        P7[\"ci-cd-provisioner\"]\n        P8[\"pki-issuer\"]\n    end\n    POLICIES -->|applies| policies\n    AUTH -->|configures K8s auth| OB0\n    ENGINES -->|enables KV v2 + PKI| OB0\n    INIT -->|init + unseal| OB0\n    SECRETS -->|seeds initial data| OB0\n```\n\n```mermaid\nsequenceDiagram\n    participant OP as Operator\n    participant BS as Bootstrap Scripts\n    participant OB as OpenBao HA Cluster\n    participant CM as cert-manager\n    participant ESO as External Secrets Operator\n    participant K8S as Kubernetes API\n\n    CM->>K8S: Create openbao-tls Secret\n    Note over OB: HelmRelease deploys 3 replicas\n    OP->>BS: Run init-unseal.sh\n    BS->>OB: bao operator init\n    OB-->>BS: Unseal keys + root token\n    BS->>K8S: Store unseal keys as Secret\n    BS->>OB: bao operator unseal x3 replicas\n    OP->>BS: Run setup-secret-engines.sh\n    BS->>OB: Enable KV v2 + PKI\n    OP->>BS: Run setup-auth.sh\n    BS->>OB: Enable K8s auth x4 clusters + AppRole\n    OP->>BS: Run setup-policies.sh\n    BS->>OB: Write 8 HCL policies\n    OP->>BS: Run write-bootstrap-secrets.sh\n    BS->>OB: Seed kv-v2 with Keystone + MariaDB secrets\n    Note over ESO: ClusterSecretStore connects\n    ESO->>OB: Authenticate via kubernetes/management\n    ESO->>OB: Read kv-v2/openstack/keystone/db\n    ESO->>K8S: Create keystone-db-credentials Secret\n    ESO->>OB: Read kv-v2/bootstrap/keystone-admin\n    ESO->>K8S: Create keystone-admin-credentials Secret\n    ESO->>OB: Read kv-v2/infrastructure/mariadb\n    ESO->>K8S: Create mariadb-root-password Secret\n```\n\n---\n\n## Key Components\n\n- **HelmRelease** (`deploy/flux-system/releases/openbao.yaml`): FluxCD-managed OpenBao — 3 StatefulSet replicas, Raft consensus (`retry_join` for all 3 peers), TLS listener on `:8200`, cluster port `:8201`, `injector.enabled: false`, `ui = false`, 10Gi PVCs (`storageClass: local-path`), resources 256Mi/250m request → 512Mi limit, `dependsOn: [{name: cert-manager, namespace: cert-manager}]`, chart version `\">=0.5.0 <1.0.0\"`, sourceRef `openbao` in `flux-system`\n- **Certificate** (`deploy/flux-system/infrastructure/openbao-tls-cert.yaml`): cert-manager `Certificate` in `openbao-system` namespace, `issuerRef: {name: selfsigned-cluster-issuer, kind: ClusterIssuer}`, `secretName: openbao-tls`, dnsNames: `openbao-{0,1,2}.openbao-internal`, `openbao.openbao-system.svc`\n- **init-unseal.sh**: Checks `bao status` before running `bao operator init -key-shares=5 -key-threshold=3 -format=json`; stores unseal keys/root token as K8s Secret `openbao-unseal-keys` in `openbao-system`; iterates `openbao-{0,1,2}.openbao-internal:8200` to unseal all replicas with threshold keys\n- **setup-secret-engines.sh**: Enables `kv-v2` (version=2) and `pki` (max-lease-ttl=87600h) engines; guards with `bao secrets list | grep` to skip if already enabled\n- **setup-auth.sh**: Enables `kubernetes` auth at 4 mount paths (`kubernetes/{management,control-plane,hypervisor,storage}`) with per-cluster SA/role bindings (role `eso-<cluster>`, bound SA `external-secrets` in `external-secrets` namespace, TTL 1h); enables `approle` at `approle/` for CI/CD; guards with `bao auth list`\n- **setup-policies.sh**: Applies all 8 HCL policies from `deploy/openbao/policies/` via `bao policy write <name> <file>` (write = upsert, inherently idempotent)\n- **write-bootstrap-secrets.sh**: Generates random passwords (`openssl rand -base64 32`) and writes to `kv-v2/bootstrap/keystone-admin`, `kv-v2/infrastructure/mariadb`, `kv-v2/openstack/keystone/db`; idempotent via `bao kv get` check (skip if key exists)\n- **8 HCL Policies**: Least-privilege policies per consumer — `eso-management` (read `bootstrap/*`, `infrastructure/*`), `eso-control-plane` (read `bootstrap/*`, `openstack/*`, `infrastructure/*`, `ceph/*`), `eso-hypervisor` (read `ceph/client-nova`, `openstack/nova/compute-*`), `eso-storage` (read/create/update `ceph/*`), `push-app-credentials` (create/update `openstack/*/app-credential`), `push-ceph-keys` (create/update `ceph/*`), `ci-cd-provisioner` (create/update/read `kv-v2/data/*`, read/list `kv-v2/metadata/*`), `pki-issuer` (create/update `pki/issue/*`, `pki/sign/*`)\n- **ClusterSecretStore** (`deploy/eso/clustersecretstore.yaml`): name `openbao-cluster-store`, `vault` provider, server `https://openbao.openbao-system.svc:8200`, path `kv-v2`, version `v2`, auth mount `kubernetes/management`, role `eso-management`, serviceAccountRef `external-secrets`\n- **ExternalSecrets** (3 total, all in `openstack` namespace): `keystone-db-credentials` (from `kv-v2/openstack/keystone/db`), `keystone-admin-credentials` (from `kv-v2/bootstrap/keystone-admin`), `mariadb-root-password` (from `kv-v2/infrastructure/mariadb` — resolves dangling `rootPasswordSecretKeyRef` in `deploy/flux-system/infrastructure/mariadb.yaml`)\n- **Wiring**: `openbao-system` namespace added to `namespaces.yaml`; `releases/openbao.yaml` added to `deploy/flux-system/kustomization.yaml` resources; `openbao-tls-cert.yaml` added to `deploy/flux-system/infrastructure/kustomization.yaml` resources\n\n---\n\n**Correction from user's prior elaboration:** The `mariadb-root-password` ExternalSecret belongs in namespace **`openstack`**, not `mariadb-system`. The MariaDB Galera CR (`deploy/flux-system/infrastructure/mariadb.yaml`) is deployed in `openstack` namespace and its `rootPasswordSecretKeyRef` references a Secret in the same namespace.\n\n---\n\n| Request Element | Coverage |\n|---|---|\n| HelmRelease (HA Raft, TLS, no injector) | Scope, Key Components |\n| Bootstrap `init-unseal` | Scope, Key Components, Sequence Diagram |\n| Bootstrap `secret engines` (KV v2, PKI) | Scope, Key Components |\n| Bootstrap `auth methods` (K8s per cluster, AppRole) | Scope, Key Components |\n| Bootstrap `policy application` | Scope, Key Components |\n| 8 HCL policies (all named) | Scope (all 8 listed), Key Components |\n| `deploy/eso/clustersecretstore.yaml` | Scope, Key Components |\n| `deploy/eso/externalsecrets/` (Keystone DB + admin) | Scope, Key Components, Sequence Diagram |\n| MariaDB root password ExternalSecret | Scope, Key Components, Correction note |\n| write-bootstrap-secrets.sh | Scope, Key Components, Sequence Diagram |\n| Certificate resource (CRD-dependent, infrastructure phase) | Scope, Key Components |\n| CC-0008 pattern compliance (releases/, infrastructure/) | Scope (wiring updates), Key Components |</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add openbao-system namespace, create OpenBao HelmRelease, and wire into base kustomization (REQ-001, REQ-006)</task>\n    <task status=\"pending\">1.2 Create cert-manager Certificate for OpenBao TLS and wire into infrastructure kustomization (REQ-002, REQ-006)</task>\n    <task status=\"pending\">2.1 Create 4 ESO HCL policies: eso-control-plane.hcl, eso-hypervisor.hcl, eso-storage.hcl, eso-management.hcl in deploy/openbao/policies/ (REQ-004, REQ-006)</task>\n    <task status=\"pending\">2.2 Create 4 operational HCL policies: push-app-credentials.hcl, push-ceph-keys.hcl, ci-cd-provisioner.hcl, pki-issuer.hcl in deploy/openbao/policies/ (REQ-004, REQ-006)</task>\n    <task status=\"pending\">3.1 Create deploy/openbao/bootstrap/init-unseal.sh — idempotent init (5-share, 3-threshold Shamir), store unseal keys as K8s Secret, unseal all 3 replicas via kubectl exec (REQ-003, REQ-006, REQ-007)</task>\n    <task status=\"pending\">3.2 Create deploy/openbao/bootstrap/setup-secret-engines.sh — enable KV v2 at kv-v2/ (version=2) and PKI at pki/ (max-lease-ttl=87600h), idempotent via bao secrets list guard (REQ-003, REQ-006, REQ-007)</task>\n    <task status=\"pending\">3.3 Create deploy/openbao/bootstrap/setup-auth.sh — enable Kubernetes auth at kubernetes/{management,control-plane,hypervisor,storage} with ESO roles, enable AppRole at approle/ with provisioner role, idempotent via bao auth list guard (REQ-003, REQ-006, REQ-007)</task>\n    <task status=\"pending\">3.4 Create deploy/openbao/bootstrap/setup-policies.sh — apply all 8 HCL policies from deploy/openbao/policies/ via bao policy write (upsert, inherently idempotent) (REQ-003, REQ-006, REQ-007)</task>\n    <task status=\"pending\">3.5 Create deploy/openbao/bootstrap/write-bootstrap-secrets.sh — generate passwords with openssl rand -base64 32, write to kv-v2/bootstrap/keystone-admin, kv-v2/infrastructure/mariadb (key: root-password), kv-v2/openstack/keystone/db (keys: username, password), idempotent via bao kv get guard (REQ-003, REQ-006, REQ-007, REQ-009)</task>\n    <task status=\"pending\">4.1 Create deploy/eso/clustersecretstore.yaml — ClusterSecretStore openbao-cluster-store with vault provider (server, path kv-v2, version v2), kubernetes/management auth (role eso-management, SA external-secrets), caProvider for TLS (REQ-005, REQ-006, REQ-008)</task>\n    <task status=\"pending\">4.2 Create 3 ExternalSecrets in deploy/eso/externalsecrets/ (keystone-db.yaml, keystone-admin.yaml, mariadb-root-password.yaml) in openstack namespace with correct key mappings, plus deploy/eso/kustomization.yaml entrypoint (REQ-005, REQ-006)</task>\n    <task status=\"pending\">5.1 Write reference documentation for OpenBao bootstrap procedure at docs/reference/infrastructure/openbao-bootstrap.md — covers prerequisites, script execution order, environment setup, idempotency guarantees, and secret paths (REQ-006)</task>\n\n  </tasks>\n  <implementation-notes>This is a pure infrastructure-as-code feature with no application code (no backend API, no frontend, no hooks). All artifacts are Kubernetes manifests (YAML), HCL policy files, and shell scripts.\n\nKey architectural decisions:\n1. HA Raft consensus with 3 replicas for data reliability and automatic leader election\n2. TLS via cert-manager self-signed Certificate for secure Raft and API communication\n3. Shamir secret sharing (5 shares, 3 threshold) for initialization security\n4. Per-cluster Kubernetes auth mounts for multi-cluster isolation\n5. 8 least-privilege HCL policies for defense-in-depth access control\n6. Idempotent bootstrap scripts using guard checks (bao status, secrets list, auth list, kv get)\n7. kubectl exec pattern for running bao CLI commands inside pods (no direct network access needed)\n8. Separate deploy/eso/ directory for ESO resources, applied independently from FluxCD base infrastructure\n\nCritical pitfalls to avoid:\n1. KV v2 HCL policy paths MUST include the data/ prefix (e.g., kv-v2/data/bootstrap/* not kv-v2/bootstrap/*) — this is an OpenBao/Vault API quirk where the actual API path includes data/ for read/write operations\n2. ExternalSecret remoteRef key is the path UNDER the store's mount path (e.g., infrastructure/mariadb not kv-v2/infrastructure/mariadb) since the ClusterSecretStore already sets path: kv-v2\n3. mariadb-root-password key mapping: OpenBao stores as root-password but MariaDB CR expects key password — the ExternalSecret must remap via secretKey/property\n4. Certificate SANs must exactly match StatefulSet pod DNS names (openbao-{0,1,2}.openbao-internal) and the Service DNS (openbao.openbao-system.svc)\n5. Bootstrap execution order is critical: init-unseal → setup-secret-engines → setup-auth → setup-policies → write-bootstrap-secrets\n6. The openbao HelmRepository source already exists at deploy/flux-system/sources/openbao.yaml from CC-0008 — do NOT create a duplicate\n7. Remote cluster Kubernetes auth mounts (control-plane, hypervisor, storage) are created with roles but actual host/CA config is deferred to when those clusters are provisioned\n8. ClusterSecretStore needs caProvider to trust the self-signed TLS certificate — reference the openbao-tls Secret's ca.crt key</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"deploy/flux-system/releases/openbao.yaml\" test-function=\"validate_helmrelease_cc0008_conventions\" requirement-id=\"REQ-001\">HelmRelease has interval 30m, install.crds CreateReplace, upgrade.crds CreateReplace, upgrade.remediation.retries 3, createNamespace true</spec>\n    <spec test-file=\"deploy/flux-system/releases/openbao.yaml\" test-function=\"validate_helmrelease_ha_raft_config\" requirement-id=\"REQ-001\">HelmRelease values configure 3 replicas, Raft storage with retry_join for 3 peers, TLS listener on :8200, injector disabled, ui false, 10Gi PVC local-path, resources 256Mi/250m to 512Mi</spec>\n    <spec test-file=\"deploy/flux-system/releases/openbao.yaml\" test-function=\"validate_helmrelease_depends_on_cert_manager\" requirement-id=\"REQ-001\">HelmRelease dependsOn includes cert-manager in cert-manager namespace</spec>\n    <spec test-file=\"deploy/flux-system/infrastructure/openbao-tls-cert.yaml\" test-function=\"validate_certificate_issuer_ref\" requirement-id=\"REQ-002\">Certificate references selfsigned-cluster-issuer ClusterIssuer and creates openbao-tls Secret in openbao-system namespace</spec>\n    <spec test-file=\"deploy/flux-system/infrastructure/openbao-tls-cert.yaml\" test-function=\"validate_certificate_sans\" requirement-id=\"REQ-002\">Certificate dnsNames include openbao-0.openbao-internal, openbao-1.openbao-internal, openbao-2.openbao-internal, openbao.openbao-system.svc</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/init-unseal.sh\" test-function=\"shellcheck_passes_and_idempotent_init_guard\" requirement-id=\"REQ-003\">Script passes shellcheck, checks bao status exit code before init, exits cleanly if already initialized, stores keys as K8s Secret</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/setup-secret-engines.sh\" test-function=\"shellcheck_passes_and_idempotent_engine_guard\" requirement-id=\"REQ-003\">Script passes shellcheck, checks bao secrets list before enabling engines, skips if already enabled</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/write-bootstrap-secrets.sh\" test-function=\"shellcheck_passes_and_idempotent_secret_guard\" requirement-id=\"REQ-003\">Script passes shellcheck, checks bao kv get before writing each secret, skips existing secrets without overwriting</spec>\n    <spec test-file=\"deploy/openbao/policies/eso-management.hcl\" test-function=\"validate_eso_management_read_only\" requirement-id=\"REQ-004\">Policy grants only read capability on kv-v2/data/bootstrap/* and kv-v2/data/infrastructure/*, no other capabilities</spec>\n    <spec test-file=\"deploy/openbao/policies/eso-hypervisor.hcl\" test-function=\"validate_eso_hypervisor_narrow_scope\" requirement-id=\"REQ-004\">Policy grants read only on kv-v2/data/ceph/client-nova and kv-v2/data/openstack/nova/compute-*, no broader access</spec>\n    <spec test-file=\"deploy/openbao/policies/ci-cd-provisioner.hcl\" test-function=\"validate_ci_cd_no_delete\" requirement-id=\"REQ-004\">Policy grants create/update/read on kv-v2/data/* and read/list on kv-v2/metadata/*, delete capability is absent</spec>\n    <spec test-file=\"deploy/eso/externalsecrets/mariadb-root-password.yaml\" test-function=\"validate_mariadb_key_mapping\" requirement-id=\"REQ-005\">ExternalSecret in openstack namespace maps remoteRef property root-password to secretKey password, target name mariadb-root-password</spec>\n    <spec test-file=\"deploy/eso/externalsecrets/keystone-db.yaml\" test-function=\"validate_keystone_db_keys\" requirement-id=\"REQ-005\">ExternalSecret in openstack namespace creates keystone-db-credentials Secret with username and password keys from openstack/keystone/db</spec>\n    <spec test-file=\"deploy/eso/externalsecrets/keystone-admin.yaml\" test-function=\"validate_keystone_admin_store_ref\" requirement-id=\"REQ-005\">ExternalSecret references openbao-cluster-store ClusterSecretStore with refreshInterval 1h, creates keystone-admin-credentials in openstack namespace</spec>\n    <spec test-file=\"deploy/flux-system/releases/openbao.yaml\" test-function=\"validate_yaml_spdx_and_feature_annotation\" requirement-id=\"REQ-006\">File starts with SPDX-FileCopyrightText and SPDX-License-Identifier headers, includes Feature: CC-0009 comment</spec>\n    <spec test-file=\"deploy/openbao/policies/eso-control-plane.hcl\" test-function=\"validate_hcl_spdx_headers\" requirement-id=\"REQ-006\">HCL policy files have SPDX-FileCopyrightText and SPDX-License-Identifier headers with Feature: CC-0009 annotation</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/init-unseal.sh\" test-function=\"validate_strict_error_handling\" requirement-id=\"REQ-007\">Script begins with #!/bin/bash and set -euo pipefail immediately after SPDX headers</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/setup-auth.sh\" test-function=\"validate_kubectl_exec_pattern\" requirement-id=\"REQ-007\">All bao commands executed via kubectl exec -n openbao-system, not direct network connection</spec>\n    <spec test-file=\"deploy/eso/clustersecretstore.yaml\" test-function=\"validate_kubernetes_auth_config\" requirement-id=\"REQ-008\">ClusterSecretStore authenticates via kubernetes/management mount with role eso-management and serviceAccountRef external-secrets in external-secrets namespace</spec>\n    <spec test-file=\"deploy/eso/clustersecretstore.yaml\" test-function=\"validate_vault_provider_config\" requirement-id=\"REQ-008\">ClusterSecretStore uses vault provider with server https://openbao.openbao-system.svc:8200, path kv-v2, version v2, caProvider referencing openbao-tls Secret</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/write-bootstrap-secrets.sh\" test-function=\"validate_secure_password_generation\" requirement-id=\"REQ-009\">Script uses openssl rand -base64 32 for all password generation, each invocation independent</spec>\n    <spec test-file=\"deploy/openbao/bootstrap/write-bootstrap-secrets.sh\" test-function=\"validate_passwords_not_logged\" requirement-id=\"REQ-009\">Generated passwords are not echoed to stdout/stderr; only status messages like 'Writing kv-v2/bootstrap/keystone-admin...' or 'Skipping (already exists)' are output</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Cluster operator deploys OpenBao HA cluster via FluxCD\">As a cluster operator, I want to deploy OpenBao as a 3-replica HA Raft cluster using FluxCD HelmRelease with TLS enabled, so that I have a centralized, highly-available secret management backbone for all CobaltCore services</story>\n    <story title=\"Cluster operator initializes and unseals OpenBao\">As a cluster operator, I want idempotent bootstrap scripts to initialize OpenBao with Shamir secret sharing and unseal all replicas, so that I can safely run the initialization process multiple times without side effects or data loss</story>\n    <story title=\"Cluster operator configures OpenBao secret engines and auth methods\">As a cluster operator, I want bootstrap scripts to configure KV v2 and PKI secret engines plus Kubernetes auth for 4 clusters and AppRole for CI/CD, so that OpenBao is ready for ESO integration and CI/CD pipeline access across the multi-cluster topology</story>\n    <story title=\"Security administrator enforces least-privilege secret access\">As a security administrator, I want 8 HCL policies enforcing least-privilege access for each consumer type, so that each component can only access the specific secrets it needs, following defense-in-depth principles</story>\n    <story title=\"Cluster operator seeds initial bootstrap secrets\">As a cluster operator, I want a bootstrap script to generate and seed initial passwords for keystone-admin, mariadb root, and keystone DB credentials, so that downstream services have credentials available from day one without manual password management</story>\n    <story title=\"ESO syncs OpenBao secrets to Kubernetes\">As a External Secrets Operator, I want a ClusterSecretStore connected to OpenBao and ExternalSecret resources for keystone-db, keystone-admin, and mariadb-root-password, so that Kubernetes Secrets are automatically created and kept in sync with OpenBao, resolving the dangling mariadb-root-password reference</story>\n    <story title=\"All files follow project conventions\">As a project maintainer, I want all new files to include SPDX license headers, CC-0009 feature annotations, and follow established formatting patterns, so that provenance tracking, license compliance, and code consistency are maintained across the codebase</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The OpenBao HelmRelease SHALL follow CC-0008 FluxCD conventions and match the canonical architecture doc specification</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The cert-manager Certificate SHALL use selfsigned-cluster-issuer and generate a TLS Secret with correct SANs for all OpenBao pods</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">All bootstrap scripts SHALL be idempotent — safe to run multiple times without side effects</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">HCL policies SHALL enforce least-privilege access, granting each consumer only the capabilities it needs on specific paths</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">ExternalSecrets SHALL create Kubernetes Secrets in the openstack namespace with correct key mappings matching consumer expectations</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">All new files SHALL include SPDX license headers and Feature: CC-0009 annotations following CC-0008 conventions</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">Bootstrap scripts SHALL use strict error handling (set -euo pipefail) and execute bao commands via kubectl exec</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The ClusterSecretStore SHALL authenticate to OpenBao via the kubernetes/management auth mount with TLS verification</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">Bootstrap secrets SHALL be generated with cryptographically secure randomness and not logged to stdout</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Check if previous feature already implemented any of our pending tasks</rule>\n  <rule>Run: git log --oneline -5 to see what the previous feature committed</rule>\n  <rule>Mark tasks as removed_tasks if they were done by the previous feature</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 31994,
  "caller": "run_claude_cli"
}