{
  "timestamp": "2026-03-06T23:39:57.472481+00:00",
  "feature_id": "CC-0027",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0027",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0027\">\n  <title>Wire extra-packages.yaml as Docker build source of truth</title>\n  <description>**Size:** 📦 medium\n**Category:** infrastructure\n**Priority:** high\n**Source:** Proposed for 'There is releases/2025.2/extra-packages.yaml. JEdoc'\n\nRestructure releases/2025.2/extra-packages.yaml into pip_extras (bare extras: ldap, memcache_pool, oauth1), pip_packages (standalone packages, initially empty), and apt_packages (runtime: libapache2-mod-wsgi-py3, libldap2, libsasl2-2, libxml2) — fixing the incorrect libldap-2.5-0 to libldap2. Add a 'Resolve extra packages' step in .github/workflows/build-images.yaml (build-service-images job, after the existing 'Resolve source ref' step) that uses yq to extract all three fields per service from extra-packages.yaml and writes them to $GITHUB_OUTPUT. Pass these as --build-arg PIP_EXTRAS, PIP_PACKAGES, and EXTRA_APT_PACKAGES to docker/build-push-action. In images/keystone/Dockerfile, declare the three ARGs and replace the hardcoded [ldap,memcache_pool,oauth1] with [${PIP_EXTRAS}], add ${PIP_PACKAGES} to uv pip install, and replace the hardcoded apt package list with ${EXTRA_APT_PACKAGES}. Update tests/container-images/verify_release_config.sh: remove the now-obsolete test_extra_packages_match_dockerfile function and update test_extra_packages_valid_yaml_structure to validate the new field names (pip_extras, pip_packages, apt_packages), non-empty pip_extras and apt_packages, and valid package name patterns.\n\n**Rationale:** extra-packages.yaml exists but is completely ignored by the build pipeline — packages are independently hardcoded in the Dockerfile, creating silent drift risk and a false sense of configuration. The file also contains libldap-2.5-0 which does not exist as an Ubuntu Noble package (correct name: libldap2). Making the YAML the single source of truth eliminates duplication, prevents drift, and ensures release-specific package declarations actually reach the built images.\n\n**Affected Areas:**\n- releases/2025.2/extra-packages.yaml\n- .github/workflows/build-images.yaml\n- images/keystone/Dockerfile\n- tests/container-images/verify_release_config.sh</description>\n  <tasks>\n    <task status=\"pending\">1.1 Restructure releases/2025.2/extra-packages.yaml: rename pip_packages to pip_extras with bare names (ldap, memcache_pool, oauth1), add empty pip_packages list, fix libldap-2.5-0 to libldap2, replace WARNING comment with single-source-of-truth header (REQ-001, REQ-002)</task>\n    <task status=\"pending\">1.2 Update images/keystone/Dockerfile: declare ARG PIP_EXTRAS and ARG PIP_PACKAGES in build stage, ARG EXTRA_APT_PACKAGES in runtime stage; replace hardcoded [ldap,memcache_pool,oauth1] with [${PIP_EXTRAS}], add ${PIP_PACKAGES} to uv pip install, replace hardcoded apt package list with ${EXTRA_APT_PACKAGES} (REQ-006)</task>\n    <task status=\"pending\">2.1 Add 'Resolve extra packages' step in .github/workflows/build-images.yaml build-service-images job after 'Resolve source ref': use yq to extract pip_extras (join with comma), pip_packages (join with space), apt_packages (join with space) from extra-packages.yaml, write to $GITHUB_OUTPUT with null/empty checks and ::error:: messages (REQ-003, REQ-004)</task>\n    <task status=\"pending\">2.2 Add build-args (PIP_EXTRAS, PIP_PACKAGES, EXTRA_APT_PACKAGES) to the docker/build-push-action step in build-service-images job, referencing steps.extra-packages.outputs (REQ-005)</task>\n    <task status=\"pending\">3.1 Update test_extra_packages_valid_yaml_structure in tests/container-images/verify_release_config.sh: validate new field names (pip_extras, pip_packages, apt_packages), assert pip_extras count >= 1 and apt_packages count >= 1, add pattern validation for pip_extras entries (^[a-z][a-z0-9_]*$) and apt_packages entries (^[a-z0-9][a-z0-9.+-]+$) (REQ-007)</task>\n    <task status=\"pending\">3.2 Remove test_extra_packages_match_dockerfile function and its invocation from tests/container-images/verify_release_config.sh (REQ-008)</task>\n    <task status=\"pending\">4.1 Update docs/reference/container-images.md: replace extra-packages.yaml section with new pip_extras/pip_packages/apt_packages schema, update example YAML, fix libldap-2.5-0 to libldap2, update keystone Dockerfile example to show ARG-based usage, update field description table (REQ-009)</task>\n    <task status=\"pending\">4.2 Update docs/reference/build-images-workflow.md: add 'Resolve extra packages' step to build-service-images steps table (step 3, after 'Resolve source ref'), document build-args (PIP_EXTRAS, PIP_PACKAGES, EXTRA_APT_PACKAGES) in the Build Contexts or a new Build Arguments section, update step numbering (REQ-009)</task>\n\n  </tasks>\n  <implementation-notes>Architectural decisions and patterns:\n\n1. **yq extraction pattern**: Follow the established `source-ref` resolution pattern in build-images.yaml (env vars for matrix values, yq query, null check, $GITHUB_OUTPUT). The new step should use the same structure including the descriptive echo at the end.\n\n2. **Join strategy for yq output**: pip_extras use comma-join (`.pip_extras | join(\",\")`) because they become a single [extra1,extra2] bracket expression. pip_packages and apt_packages use space-join (`.pip_packages | join(\" \")` / `.apt_packages | join(\" \")`) because they are separate shell arguments.\n\n3. **Dockerfile ARG placement**: PIP_EXTRAS and PIP_PACKAGES must be declared in Stage 1 (build) before the RUN instruction that uses them. EXTRA_APT_PACKAGES must be declared in Stage 2 (runtime) before the apt-get RUN instruction. ARGs are stage-scoped in multi-stage builds.\n\n4. **Empty PIP_PACKAGES handling**: When PIP_PACKAGES is empty, the uv pip install command becomes `uv pip install ... \"/tmp/keystone-build[${PIP_EXTRAS}]\" ` — the trailing empty string is harmless because shell word splitting on an unquoted empty variable produces no additional arguments.\n\n5. **EXTRA_APT_PACKAGES must be unquoted**: The apt-get install command uses `${EXTRA_APT_PACKAGES}` (unquoted) to allow shell word splitting into separate package arguments. This is intentional — quoting would pass the entire space-separated string as a single argument to apt.\n\n6. **Header comment update**: Replace the WARNING block in extra-packages.yaml with a comment stating this file is the consumed source of truth for per-service packages in the Docker build.\n\n7. **Architecture doc divergence**: architecture/docs/08-container-images/01-build-pipeline.md still references libldap-2.5-0 in its example — this is read-only reference material and is NOT modified by this feature.\n\nKey files:\n- releases/2025.2/extra-packages.yaml (restructure schema)\n- .github/workflows/build-images.yaml (add step + build-args, lines ~96-189)\n- images/keystone/Dockerfile (ARGs + variable substitution)\n- tests/container-images/verify_release_config.sh (update + remove tests)\n- tests/lib/assertions.sh (existing assertion helpers — use assert_eq)\n- docs/reference/container-images.md (update schema docs)\n- docs/reference/build-images-workflow.md (add step docs)\n\nRisks:\n- If yq is not available in the CI runner, the 'Resolve extra packages' step will fail. Mitigation: yq is already used by the existing 'Resolve source ref' step, so it is guaranteed to be available.\n- If PIP_EXTRAS is empty and no null check fires, the Dockerfile would install keystone[] which may cause uv to error. Mitigation: REQ-004 requires fail-fast on empty/null pip_extras.\n- The unquoted ${EXTRA_APT_PACKAGES} in apt-get install relies on shell word splitting. If a package name contained spaces (invalid for dpkg), it would break. Mitigation: REQ-007 validates package names match dpkg naming conventions (no spaces).</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_valid_yaml_structure (pip_extras field exists)\" requirement-id=\"REQ-001\">yq '.keystone.pip_extras | length' returns >= 1, asserting pip_extras exists and is non-empty</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_valid_yaml_structure (pip_packages field exists)\" requirement-id=\"REQ-001\">yq '.keystone.pip_packages' returns a valid (possibly empty) sequence, not null</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_valid_yaml_structure (apt_packages field exists and non-empty)\" requirement-id=\"REQ-001\">yq '.keystone.apt_packages | length' returns >= 1, asserting apt_packages exists and is non-empty</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_valid_yaml_structure (pip_extras are bare names)\" requirement-id=\"REQ-001\">Each pip_extras entry matches ^[a-z][a-z0-9_]*$ — no brackets, no service name prefix</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_valid_yaml_structure (apt_packages valid dpkg names)\" requirement-id=\"REQ-002\">Each apt_packages entry matches ^[a-z0-9][a-z0-9.+-]+$ — valid dpkg package naming</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_valid_yaml_structure (libldap2 present)\" requirement-id=\"REQ-002\">keystone.apt_packages contains libldap2 (not libldap-2.5-0)</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test_extra_packages_match_dockerfile removed\" requirement-id=\"REQ-008\">Function test_extra_packages_match_dockerfile does not exist in the file; grep returns no match</spec>\n    <spec test-file=\"tests/container-images/verify_release_config.sh\" test-function=\"test script runs successfully with 2 test functions (source-refs + extra-packages schema)\" requirement-id=\"REQ-008\">Script exits 0 with all assertions passing; no reference to match_dockerfile in output</spec>\n    <spec test-file=\".github/workflows/build-images.yaml\" test-function=\"Resolve extra packages step (manual/CI verification)\" requirement-id=\"REQ-003\">Step uses yq to extract pip_extras (joined by comma), pip_packages (joined by space), apt_packages (joined by space) and writes to $GITHUB_OUTPUT</spec>\n    <spec test-file=\".github/workflows/build-images.yaml\" test-function=\"Resolve extra packages step null check (manual/CI verification)\" requirement-id=\"REQ-004\">Step exits 1 with ::error:: if pip_extras or apt_packages are empty or null</spec>\n    <spec test-file=\".github/workflows/build-images.yaml\" test-function=\"Build service image step has build-args (manual/CI verification)\" requirement-id=\"REQ-005\">docker/build-push-action includes build-args: PIP_EXTRAS, PIP_PACKAGES, EXTRA_APT_PACKAGES referencing extra-packages step outputs</spec>\n    <spec test-file=\"images/keystone/Dockerfile\" test-function=\"Dockerfile declares ARGs (manual verification)\" requirement-id=\"REQ-006\">Dockerfile contains ARG PIP_EXTRAS, ARG PIP_PACKAGES in build stage and ARG EXTRA_APT_PACKAGES in runtime stage</spec>\n    <spec test-file=\"images/keystone/Dockerfile\" test-function=\"Dockerfile uses ARG variables (manual verification)\" requirement-id=\"REQ-006\">uv pip install uses [${PIP_EXTRAS}] and ${PIP_PACKAGES}; apt-get uses ${EXTRA_APT_PACKAGES}; no hardcoded package names remain</spec>\n    <spec test-file=\"docs/reference/container-images.md\" test-function=\"Documentation shows new schema (manual verification)\" requirement-id=\"REQ-009\">extra-packages.yaml section shows pip_extras/pip_packages/apt_packages fields with libldap2 and bare extra names</spec>\n    <spec test-file=\"docs/reference/build-images-workflow.md\" test-function=\"Documentation shows new CI step (manual verification)\" requirement-id=\"REQ-009\">build-service-images steps table includes 'Resolve extra packages' step and build-args documentation</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"CI pipeline consumes extra-packages.yaml during service image builds\">As a CI engineer, I want the build-images workflow to read pip_extras, pip_packages, and apt_packages from extra-packages.yaml and pass them as Docker build arguments, so that extra-packages.yaml is the single source of truth for per-service packages, eliminating silent drift between YAML and Dockerfile</story>\n    <story title=\"Dockerfile accepts package lists via build arguments instead of hardcoding them\">As a CI engineer, I want the keystone Dockerfile to declare ARG PIP_EXTRAS, ARG PIP_PACKAGES, and ARG EXTRA_APT_PACKAGES and use them in the uv pip install and apt-get install commands, so that the Dockerfile is release-independent and package lists are injected by the CI pipeline</story>\n    <story title=\"Release engineer manages per-service packages by editing only extra-packages.yaml\">As a release engineer, I want extra-packages.yaml to use a clear schema with pip_extras (bare extra names), pip_packages (standalone packages), and apt_packages (runtime system packages), so that I can add, remove, or modify service packages in one place without touching Dockerfiles or workflow files</story>\n    <story title=\"CI tests validate the restructured extra-packages.yaml schema\">As a developer, I want verify_release_config.sh to validate the new pip_extras/pip_packages/apt_packages fields and remove the obsolete Dockerfile-matching test, so that malformed configurations are caught before builds and the test suite reflects the new architecture</story>\n    <story title=\"Reference documentation reflects the new extra-packages.yaml schema and workflow step\">As a developer, I want docs/reference/container-images.md and docs/reference/build-images-workflow.md to describe the new schema and CI integration, so that contributors understand the current format and know that extra-packages.yaml is consumed by the build pipeline</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">releases/2025.2/extra-packages.yaml SHALL use a restructured schema with pip_extras (bare extra names list), pip_packages (standalone packages list, may be empty), and apt_packages (runtime system packages list) per service</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">apt_packages in extra-packages.yaml SHALL contain valid Ubuntu Noble package names, correcting libldap-2.5-0 to libldap2</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The CI workflow SHALL add a 'Resolve extra packages' step in build-service-images (after 'Resolve source ref') that extracts pip_extras, pip_packages, and apt_packages from extra-packages.yaml via yq and writes them to $GITHUB_OUTPUT</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The CI workflow SHALL fail with a descriptive ::error:: message if pip_extras or apt_packages are missing or null for a matrix service in extra-packages.yaml</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The CI workflow SHALL pass PIP_EXTRAS, PIP_PACKAGES, and EXTRA_APT_PACKAGES as --build-arg values to docker/build-push-action for service image builds</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The keystone Dockerfile SHALL declare ARG PIP_EXTRAS, ARG PIP_PACKAGES, ARG EXTRA_APT_PACKAGES and use them in place of all hardcoded package lists</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">test_extra_packages_valid_yaml_structure in verify_release_config.sh SHALL validate the new field names (pip_extras, pip_packages, apt_packages), non-empty pip_extras and apt_packages, and valid package name patterns</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The obsolete test_extra_packages_match_dockerfile function SHALL be removed from verify_release_config.sh</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">Reference documentation SHALL be updated to describe the new extra-packages.yaml schema and the 'Resolve extra packages' CI workflow step</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17954,
  "caller": "run_claude_cli"
}