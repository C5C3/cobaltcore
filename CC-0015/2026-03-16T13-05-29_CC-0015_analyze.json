{
  "timestamp": "2026-03-16T13:05:29.544746+00:00",
  "feature_id": "CC-0015",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0015",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0015\">\n  <title>G001: Implement Keystone dependency details</title>\n  <description>**Size:** 🔨 medium\n**Category:** backend\n**Priority:** high\n**Feature ID:** CC-0015 (S015)\n\n## Summary\n\nS015 completes the detailed dependency interactions for the Keystone reconciler. The core logic — DB connection string assembly (managed/brownfield), memcached server list discovery, Fernet rotation CronJob with writable Secret mount and maxActiveKeys enforcement, and bootstrap Job with admin credentials — was implemented in CC-0013. The remaining gaps are: (1) **Deployment rolling restart mechanism after Fernet key rotation** via a hash-based pod template annotation, (2) **standalone table-driven unit tests** for the pure helper functions (`resolveDatabaseHost`, `dbPort`, `resolveCacheServers`), and (3) **envtest integration tests** verifying CronJob and Job detailed specs.\n\n## Scope\n\n**Included:**\n- Fernet keys hash annotation on Deployment pod template (`keystone.c5c3.io/fernet-keys-hash`) — computed from the `{name}-fernet-keys` Secret data, set on `spec.template.metadata.annotations` in `buildKeystoneDeployment()` (currently has no annotations at line 87-89 of `reconcile_deployment.go`). The existing `secretToKeystoneMapper` watch already triggers reconcile on fernet-keys Secret changes (via UID ownership check at `keystone_controller.go:160-182`).\n- Table-driven unit tests for `resolveDatabaseHost()` — managed mode (`{clusterRef.name}.{ns}.svc:{port}`), brownfield (`{host}:{port}`), port fallback to 3306\n- Table-driven unit tests for `dbPort()` — explicit port, zero/default fallback to 3306\n- Table-driven unit tests for `resolveCacheServers()` — managed mode pod DNS pattern (`memcached-{i}.{name}:11211`), brownfield explicit list join, replicas=0 fallback to 3, nil ClusterRef + empty Servers returning `\"\"`\n- Envtest integration test verifying CronJob detailed spec: schedule, init container `copy-keys`, emptyDir volume, `fernet-keys-src` Secret volume, ServiceAccount `{name}-fernet-rotate`, `OS_fernet_tokens__max_active_keys` env var, image tag\n- Envtest integration test verifying bootstrap Job detailed spec: `keystone-manage bootstrap` command/args, admin-credentials env from SecretKeyRef, `backoffLimit: 4`, `ttlSecondsAfterFinished: 300`, config volume mount\n\n**Excluded:**\n- Refactoring existing sub-reconcilers (complete from CC-0013, YAGNI)\n- Fernet key rotation E2E tests (covered by CC-0016 Chainsaw suite)\n- Credential-keys volume mount management (noted as deferred in `reconcile_deployment.go:160`, separate feature)\n- MariaDB CR status field resolution (current implementation uses service DNS `{name}.{ns}.svc`, which is the correct K8s pattern)\n- ConfigMap hash annotation (already handled by content-hash ConfigMap naming in `reconcileConfig`)\n\n## Visualization\n\n```mermaid\nflowchart TD\n    subgraph FernetRotation[\"Fernet Key Rotation Flow\"]\n        CJ[\"CronJob: fernet-rotate\"]\n        IC[\"Init Container: copy keys to emptyDir\"]\n        MC[\"Main Container: keystone-manage fernet_rotate\"]\n        PY[\"Python script: PATCH Secret via K8s API\"]\n        SEC[\"Secret: fernet-keys\"]\n        WATCH[\"secretToKeystoneMapper triggers reconcile\"]\n        REC[\"Reconciler: compute fernet-keys hash\"]\n        ANN[\"Deployment pod template annotation update\"]\n        ROLL[\"Rolling restart of Keystone pods\"]\n    end\n\n    CJ --> IC\n    IC --> MC\n    MC --> PY\n    PY -->|\"PATCH\"| SEC\n    SEC -->|\"ownership UID match\"| WATCH\n    WATCH --> REC\n    REC -->|\"hash changed\"| ANN\n    ANN --> ROLL\n\n    subgraph DBResolution[\"DB Connection String Assembly\"]\n        MANAGED_DB[\"Managed: ClusterRef set\"]\n        BROWN_DB[\"Brownfield: Host set\"]\n        DNS[\"Service DNS: name.ns.svc:port\"]\n        EXPLICIT[\"Explicit: host:port\"]\n        CONN[\"mysql+pymysql://user:pass@host:port/db\"]\n    end\n\n    MANAGED_DB --> DNS --> CONN\n    BROWN_DB --> EXPLICIT --> CONN\n\n    subgraph CacheResolution[\"Memcached Server Discovery\"]\n        MANAGED_MC[\"Managed: ClusterRef set\"]\n        BROWN_MC[\"Brownfield: Servers list\"]\n        POD_DNS[\"Pod DNS: memcached-i.name:11211\"]\n        EXPLICIT_MC[\"Explicit server list\"]\n        SERVERS[\"Comma-joined server string\"]\n    end\n\n    MANAGED_MC --> POD_DNS --> SERVERS\n    BROWN_MC --> EXPLICIT_MC --> SERVERS\n```\n\n```mermaid\nsequenceDiagram\n    participant CJ as Fernet CronJob\n    participant K8S as K8s API\n    participant SEC as fernet-keys Secret\n    participant REC as Keystone Reconciler\n    participant DEP as Keystone Deployment\n\n    CJ->>CJ: Init: copy Secret keys to emptyDir\n    CJ->>CJ: keystone-manage fernet_rotate\n    CJ->>K8S: PATCH Secret with rotated keys\n    K8S->>SEC: Update Secret data\n    SEC-->>REC: secretToKeystoneMapper watch fires\n    REC->>SEC: Read Secret data, compute SHA-256 hash\n    REC->>DEP: Set pod template annotation with new hash\n    DEP-->>DEP: Rolling restart with new Fernet keys\n```\n\n## Key Components\n\n- **`resolveDatabaseHost()`** (`reconcile_config.go:177`): Pure function resolving DB host — managed mode constructs `{clusterRef.name}.{ns}.svc:{port}`, brownfield uses explicit `{host}:{port}`. Implemented; needs standalone table-driven unit tests.\n- **`dbPort()`** (`reconcile_config.go:188`): Pure function returning port with 3306 default fallback. Implemented; needs edge case unit tests (zero value, explicit value).\n- **`resolveCacheServers()`** (`reconcile_config.go:155`): Pure function constructing memcached endpoint list — managed mode generates `memcached-{i}.{name}:11211` for `replicas` count (default 3), brownfield joins explicit `Servers` list. Implemented; needs standalone table-driven unit tests.\n- **Fernet keys hash annotation** (NEW in `reconcile_deployment.go`): Compute SHA-256 of the `{name}-fernet-keys` Secret data, set as `keystone.c5c3.io/fernet-keys-hash` annotation on `spec.template.metadata.annotations` in `buildKeystoneDeployment()`. Requires `reconcileDeployment()` to read the fernet-keys Secret and pass the hash. This is the missing piece — `buildKeystoneDeployment()` currently sets no pod template annotations (line 87-89).\n- **`fernetRotationCronJob()`** (`reconcile_fernet.go:218`): Complete CronJob spec with init container, emptyDir, `OS_fernet_tokens__max_active_keys` env var, ServiceAccount. Implemented; needs envtest test verifying detailed spec fields.\n- **`buildBootstrapJob()`** (`reconcile_bootstrap.go:69`): Complete Job spec with `backoffLimit: 4`, `ttlSecondsAfterFinished: 300`, admin-credentials env from SecretKeyRef, config ConfigMap volume mount. Implemented; needs envtest test verifying detailed spec fields.</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add fernet-keys hash annotation to buildKeystoneDeployment() (REQ-001, REQ-002, REQ-008)</task>\n    <task status=\"pending\">1.2 Add table-driven unit tests for resolveDatabaseHost() and dbPort() (REQ-003, REQ-004)</task>\n    <task status=\"pending\">1.3 Add table-driven unit tests for resolveCacheServers() (REQ-005)</task>\n    <task status=\"pending\">2.1 Add envtest integration test for CronJob detailed spec (REQ-006)</task>\n    <task status=\"pending\">2.2 Add envtest integration test for bootstrap Job detailed spec (REQ-007)</task>\n    <task status=\"pending\">3.1 Update reference documentation for fernet-keys hash annotation (REQ-001)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThe fernet-keys hash annotation closes the loop between the Fernet rotation CronJob and the Keystone Deployment:\n\n```\nCronJob PATCH → Secret update → secretToKeystoneMapper watch → Reconcile()\n→ reconcileDeployment() reads Secret → computes hash → sets annotation\n→ K8s rolling restart\n```\n\n## Key Decisions\n\n1. **Hash computation function location**: Add `fernetKeysHash()` as a private method on `KeystoneReconciler` in `reconcile_deployment.go`. It reads the `{name}-fernet-keys` Secret, JSON-marshals the `.Data` map, and returns `hex.EncodeToString(sha256.Sum256(data)[:])`. This follows the pattern in `internal/common/job/job.go:PodSpecHash()` but operates on Secret data instead of PodSpec.\n\n2. **buildKeystoneDeployment() signature change**: Add `fernetKeysHash string` parameter. All callers (reconcileDeployment and test helpers readyDeployment/notReadyDeployment) must be updated.\n\n3. **Secret not found handling**: If the fernet-keys Secret doesn't exist yet (first reconcile before reconcileFernetKeys runs), return empty string. This is safe because reconcileFernetKeys runs before reconcileDeployment in the sequential reconciliation flow, but we handle it defensively.\n\n4. **Deterministic hashing**: `json.Marshal` on `map[string][]byte` sorts keys alphabetically, ensuring deterministic output. No additional sorting needed.\n\n## Files Modified\n\n- `operators/keystone/internal/controller/reconcile_deployment.go` — Add fernetKeysHash(), modify buildKeystoneDeployment() signature, modify reconcileDeployment() to read Secret\n- `operators/keystone/internal/controller/reconcile_deployment_test.go` — Update existing tests for new signature, add annotation and hash tests\n- `operators/keystone/internal/controller/reconcile_config_test.go` — Add TestResolveDatabaseHost, TestDBPort, TestResolveCacheServers\n- `operators/keystone/internal/controller/integration_test.go` — Add TestIntegration_CronJobDetailedSpec, TestIntegration_BootstrapJobDetailedSpec\n- `docs/reference/keystone-reconciler.md` — Document annotation in reconcileDeployment section\n\n## Risks\n\n- **Import cycle**: `reconcile_deployment.go` needs `crypto/sha256`, `encoding/hex`, `encoding/json` — all stdlib, no risk.\n- **Existing test breakage**: `buildKeystoneDeployment()` signature change breaks all callers. Must update `readyDeployment()`, `notReadyDeployment()`, and all `TestReconcileDeployment_*` tests in one task.\n- **Envtest flakiness**: Integration tests depend on timing. Use existing `eventuallyTimeout` and `pollInterval` constants for consistency.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_FernetKeysHashAnnotation\" requirement-id=\"REQ-001\">Deployment spec.template.metadata.annotations should contain keystone.c5c3.io/fernet-keys-hash with the provided hash string</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_FernetKeysHashAnnotation_EmptyHash\" requirement-id=\"REQ-002\">When fernetKeysHash is empty string, annotation is still set (empty value) or omitted gracefully</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_FernetKeysHashFromSecret\" requirement-id=\"REQ-002\">reconcileDeployment reads fernet-keys Secret, computes hash, and the created Deployment has the annotation</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestResolveDatabaseHost\" requirement-id=\"REQ-003\">Table-driven test covering managed mode (ClusterRef set) and brownfield mode (Host set) with port variations</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestDBPort\" requirement-id=\"REQ-004\">Table-driven test covering explicit port, zero port default to 3306</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestResolveCacheServers\" requirement-id=\"REQ-005\">Table-driven test covering managed mode (with default and custom replicas), brownfield mode, and neither-set case</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestResolveCacheServers_ManagedDefaultReplicas\" requirement-id=\"REQ-005\">When Replicas=0 and ClusterRef set, defaults to 3 replicas generating 3 endpoints</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestResolveCacheServers_NeitherSet\" requirement-id=\"REQ-005\">When ClusterRef is nil and Servers is empty, returns empty string</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_CronJobDetailedSpec\" requirement-id=\"REQ-006\">CronJob schedule, init container, main container, env vars, volumes, and ServiceAccount verified after full reconciliation</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_CronJobDetailedSpec_ImageTag\" requirement-id=\"REQ-006\">Both init container and main container images match spec.image.repository:spec.image.tag</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_BootstrapJobDetailedSpec\" requirement-id=\"REQ-007\">Job command, args, env SecretKeyRef, backoffLimit, ttlSecondsAfterFinished, and config volume verified</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_BootstrapJobDetailedSpec_AdminPasswordSecretKeyRef\" requirement-id=\"REQ-007\">BOOTSTRAP_PASSWORD env uses SecretKeyRef with correct Secret name and key=password</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestFernetKeysHash_Deterministic\" requirement-id=\"REQ-008\">Same Secret data produces same hash; different data produces different hash</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestFernetKeysHash_SecretNotFound\" requirement-id=\"REQ-002\">When fernet-keys Secret does not exist, returns empty string without error</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Keystone pods restart automatically after Fernet key rotation\">As a operator, I want Keystone pods to perform a rolling restart when the fernet-keys Secret is updated by the rotation CronJob, so that newly rotated Fernet keys are picked up by all Keystone API pods without manual intervention</story>\n    <story title=\"Pure helper functions have standalone unit test coverage\">As a developer, I want table-driven unit tests for resolveDatabaseHost(), dbPort(), and resolveCacheServers() that test each function in isolation, so that regressions in connection string assembly are caught without running full reconciliation tests</story>\n    <story title=\"CronJob detailed spec verified by envtest integration test\">As a developer, I want an envtest integration test that verifies the Fernet rotation CronJob spec fields after full reconciliation, so that CronJob spec correctness is validated in a real API server environment, not just unit tests with fake clients</story>\n    <story title=\"Bootstrap Job detailed spec verified by envtest integration test\">As a developer, I want an envtest integration test that verifies the bootstrap Job spec fields after full reconciliation, so that bootstrap Job spec correctness is validated in a real API server environment</story>\n    <story title=\"Fernet hash annotation does not break existing Deployment tests\">As a developer, I want existing Deployment unit tests to continue passing after the fernet-keys hash annotation is added, so that the new feature does not introduce regressions in existing reconciliation behavior</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">buildKeystoneDeployment() SHALL set a keystone.c5c3.io/fernet-keys-hash annotation on spec.template.metadata.annotations, computed as the SHA-256 hex digest of the fernet-keys Secret data</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">reconcileDeployment() SHALL read the fernet-keys Secret and compute the SHA-256 hash to pass to buildKeystoneDeployment()</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">resolveDatabaseHost() SHALL return {clusterRef.name}.{namespace}.svc:{port} in managed mode and {host}:{port} in brownfield mode</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">dbPort() SHALL return the explicit port when > 0, or 3306 as default</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">resolveCacheServers() SHALL construct memcached endpoint list in managed mode and join explicit servers in brownfield mode</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">An envtest integration test SHALL verify the Fernet rotation CronJob detailed spec fields after full reconciliation</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">An envtest integration test SHALL verify the bootstrap Job detailed spec fields after full reconciliation</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The fernet-keys hash computation SHALL produce a deterministic SHA-256 hex digest from the Secret data map</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 18878,
  "caller": "run_claude_cli"
}