{
  "timestamp": "2026-04-21T20:50:29.594796+00:00",
  "feature_id": "CC-0079",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0079",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0079\">\n  <title>Add finalizer for OpenBao PushSecret cleanup</title>\n  <description># GitHub Issue #193: Add finalizer for OpenBao PushSecret cleanup on CR deletion\n\n**Category**: improvement | **Scope**: Medium\n\n**Description**: Implement a finalizer on the Keystone CR that cleans up PushSecret data written to OpenBao KV-v2 when the CR is deleted. While owned Kubernetes resources (Deployments, Services, Jobs) are garbage-collected via ownerReferences, OpenBao secrets persist indefinitely after CR deletion. The finalizer should delete the relevant KV-v2 paths and then remove itself.\n\n**Motivation**: Deleting a Keystone CR leaves stale secrets in OpenBao including database credentials, fernet keys, and credential keys. These orphaned secrets are a security risk (stale credentials that should no longer exist) and an operational burden (manual cleanup required). This is the standard Kubernetes pattern for external resource lifecycle management.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/keystone_controller.go`\n- `operators/keystone/internal/controller/reconcile_secrets.go`\n\n**Acceptance Criteria**:\n- [ ] Finalizer added to Keystone CR on first reconcile\n- [ ] On CR deletion, PushSecret-managed paths in OpenBao are cleaned up\n- [ ] Finalizer is removed after successful cleanup\n- [ ] If OpenBao is unavailable during deletion, the finalizer blocks deletion with a clear status message\n- [ ] E2E test verifies OpenBao paths are empty after CR deletion\n\n---\nSource: https://github.com/C5C3/forge/issues/193</description>\n  <tasks>\n    <task status=\"pending\">1.1 Define package-level constant keystoneOpenBaoFinalizer = \"keystone.openstack.c5c3.io/openbao-finalizer\" in operators/keystone/internal/controller/reconcile_secrets.go and add unit test TestKeystoneOpenBaoFinalizerConstant_HasExpectedValue in keystone_controller_test.go (REQ-005, CC-0079)</task>\n    <task status=\"pending\">1.2 Set Spec.DeletionPolicy = esov1alpha1.PushSecretDeletionPolicyDelete in fernetKeysPushSecret in operators/keystone/internal/controller/reconcile_fernet.go and add unit test TestFernetKeysPushSecret_HasDeletionPolicyDelete in reconcile_secrets_test.go (REQ-008, CC-0079)</task>\n    <task status=\"pending\">1.3 Set Spec.DeletionPolicy = esov1alpha1.PushSecretDeletionPolicyDelete in credentialKeysPushSecret in operators/keystone/internal/controller/reconcile_credential.go and add unit test TestCredentialKeysPushSecret_HasDeletionPolicyDelete in reconcile_secrets_test.go (REQ-008, CC-0079)</task>\n    <task status=\"pending\">1.4 Extend reconcile_fernet_test.go with TestReconcileFernetKeys_PushSecretDeletionPolicyDelete and reconcile_credential_test.go with TestReconcileCredentialKeys_PushSecretDeletionPolicyDelete: run the sub-reconciler, Get the stored PushSecret from fake client, assert DeletionPolicy=Delete (REQ-008, CC-0079)</task>\n    <task status=\"pending\">2.1 Implement finalizeOpenBaoSecrets(ctx, keystone) in operators/keystone/internal/controller/reconcile_secrets.go: issue client.Delete(IgnoreNotFound) for {name}-fernet-keys-backup and {name}-credential-keys-backup PushSecrets; Get each afterwards; return (done bool, stuckName string, err error) where done=true only when both are confirmed NotFound; tolerate apierrors.IsNotFound on Delete and Get (REQ-002, REQ-003, REQ-004, CC-0079)</task>\n    <task status=\"pending\">2.2 Add unit tests in reconcile_secrets_test.go for finalizeOpenBaoSecrets: TestFinalizeOpenBaoSecrets_DeletesBothPushSecrets, TestFinalizeOpenBaoSecrets_NotFoundIsTolerated, TestFinalizeOpenBaoSecrets_IsIdempotent, TestFinalizeOpenBaoSecrets_RequeuesWhilePushSecretTerminating; use fake.NewClientBuilder with esov1alpha1 scheme; use fake client interceptor to count Delete/Get calls for idempotency assertion (REQ-002, REQ-003, REQ-004, CC-0079)</task>\n    <task status=\"pending\">2.3 Extend finalizeOpenBaoSecrets (or add setOpenBaoFinalizerBlockedCondition helper) to set SecretsReady=False, reason=\"OpenBaoFinalizerBlocked\", message naming the stuck PushSecret; write only when done=false; covered by TestFinalizeOpenBaoSecrets_SetsBlockedConditionOnStall (REQ-009, CC-0079)</task>\n    <task status=\"pending\">3.1 In Reconcile in operators/keystone/internal/controller/keystone_controller.go, after the CR Get, branch on !keystone.DeletionTimestamp.IsZero(): if finalizer present, emit Normal \"FinalizingOpenBaoSecrets\" event exactly once (dedupe by checking that at least one PushSecret still exists), call finalizeOpenBaoSecrets; on done=false return ctrl.Result{RequeueAfter: RequeueSecretPolling} via updateStatus; on done=true emit Normal \"OpenBaoSecretsFinalized\", RemoveFinalizer, Update; if finalizer absent, return empty result (REQ-002, REQ-006, REQ-007, CC-0079)</task>\n    <task status=\"pending\">3.2 In the same Reconcile function, add a pre-sub-reconciler block (live CR path) that calls controllerutil.AddFinalizer(keystone, keystoneOpenBaoFinalizer) when missing and returns ctrl.Result{Requeue: true} after a successful client.Update so the next pass runs with the finalizer persisted; leave the existing sub-reconciler pipeline unchanged when the finalizer is already present (REQ-001, REQ-006, CC-0079)</task>\n    <task status=\"pending\">3.3 Add unit tests in keystone_controller_test.go for finalizer add/remove flow: TestReconcile_AddsOpenBaoFinalizerOnFirstReconcile, TestReconcile_OpenBaoFinalizerAlreadyPresent_NoExtraUpdate, TestReconcile_OpenBaoFinalizerUpdateConflict_ReturnsError (inject Conflict via interceptor), TestReconcile_TerminatingCR_SkipsSubReconcilers, TestReconcile_TerminatingCR_RequeuesWhilePushSecretsExist, TestReconcile_TerminatingCR_WithoutOpenBaoFinalizer_NoOp (REQ-001, REQ-002, REQ-004, REQ-006, CC-0079)</task>\n    <task status=\"pending\">3.4 Add unit tests in keystone_controller_test.go for event emission: TestReconcile_TerminatingCR_EmitsFinalizingOpenBaoSecretsEvent, TestReconcile_TerminatingCR_EmitsOpenBaoSecretsFinalizedEvent, TestReconcile_TerminatingCR_NoDuplicateStartEventOnRequeue; use record.NewFakeRecorder(10) and drain the channel to assert reason strings (REQ-007, CC-0079)</task>\n    <task status=\"pending\">4.1 Add integration test TestIntegration_OpenBaoFinalizerLifecycle_AddAndRemove in operators/keystone/internal/controller/integration_test.go using envtest: create a managed Keystone CR; Eventually assert keystoneOpenBaoFinalizer present; verify both backup PushSecrets exist with DeletionPolicy=Delete; delete the CR; simulate ESO clearing its finalizer on each PushSecret so the API server garbage-collects them; Eventually assert PushSecrets are NotFound and the Keystone CR itself is NotFound within a bounded timeout (REQ-002, REQ-008, CC-0079)</task>\n    <task status=\"pending\">4.2 Add integration test TestIntegration_OpenBaoFinalizer_BlockedWhenPushSecretStuck in integration_test.go: create Keystone CR, delete it, leave the ESO finalizer on fernet-keys-backup (do not clear); Eventually assert status.conditions contains SecretsReady=False with reason OpenBaoFinalizerBlocked and message naming fernet-keys-backup; then clear the ESO finalizer; Eventually assert the Keystone CR is NotFound (REQ-004, REQ-009, CC-0079)</task>\n    <task status=\"pending\">5.1 Extend tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml: (a) in the Ready=True step add an assertion that metadata.finalizers contains \"keystone.openstack.c5c3.io/openbao-finalizer\"; (b) keep the existing PushSecret error assertion for fernet-keys-backup and ADD an error assertion for {name}-credential-keys-backup after deletion; (c) ensure delete step uses a <=2m bounded timeout (REQ-010, CC-0079)</task>\n    <task status=\"pending\">5.2 Add a final script step to tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml that uses kubectl exec against the openbao pod (namespace openbao, label app.kubernetes.io/name=openbao) to run `bao kv get kv-v2/openstack/keystone/fernet-keys` and `bao kv get kv-v2/openstack/keystone/credential-keys`; script asserts both commands exit non-zero (path not found) and prints the output for debugging (REQ-010, CC-0079)</task>\n    <task status=\"pending\">6.1 Update docs/reference/keystone-reconciler.md: add an \"OpenBao Finalizer\" subsection with (a) the finalizer constant name and source file, (b) the two backup PushSecrets and their KV-v2 paths, (c) the DeletionPolicy=Delete wiring that flows through ESO, (d) reconcile branching on DeletionTimestamp and event emission, (e) NotFound tolerance and idempotency, (f) the SecretsReady=False/OpenBaoFinalizerBlocked failure mode (REQ-011, CC-0079)</task>\n\n  </tasks>\n  <implementation-notes>ARCHITECTURAL DECISIONS (CC-0079):\n\n1. Finalizer name: \"keystone.openstack.c5c3.io/openbao-finalizer\". Dedicated to OpenBao/PushSecret cleanup, kept separate from any MariaDB finalizer (CC-0078) so the two cleanup domains remove independently. Kubernetes permits multiple finalizers; each is stripped when its own cleanup completes. Constant lives in reconcile_secrets.go (package-level, lower-case, package-private) next to openBaoClusterStoreName.\n\n2. Cleanup strategy: delegate the remote KV-v2 delete to ESO via PushSecret Spec.DeletionPolicy=Delete. The Keystone operator has no OpenBao credentials; ESO is the only component with the provider client. The finalizer therefore (a) issues client.Delete on the two backup PushSecrets, (b) re-Gets each to observe ESO's finalizer dance, (c) removes its own finalizer only once both PushSecrets are fully NotFound. This ensures the user-visible \"Keystone CR gone\" state implies the remote keys have actually been purged (or that ESO has surfaced the failure via its own status).\n\n3. Why not delete the KV-v2 path directly from the operator? Would require embedding OpenBao credentials in the operator ServiceAccount, duplicating ESO's backend abstraction, and breaks the single-responsibility of who talks to OpenBao. The ESO-mediated path reuses the existing ClusterSecretStore and its auth.\n\n4. Reconcile branching: finalizer handling runs immediately after the CR Get and before reconcileSecrets. A Terminating CR must not re-enter the sub-reconciler pipeline because reconcileFernetKeys/reconcileCredentialKeys would re-create the PushSecrets the finalizer just deleted — classic infinite loop.\n\n5. done=true vs done=false: finalizeOpenBaoSecrets returns done=true only after a fresh Get confirms NotFound on both PushSecrets. Getting back a DeletionTimestamped object (ESO finalizer still present) still counts as \"present\" → return done=false. This guarantees we do not strand a Keystone CR in deleting state after the Keystone finalizer is stripped while ESO's finalizer is still blocking its own object.\n\n6. Event deduplication: the \"FinalizingOpenBaoSecrets\" Event is emitted only when at least one PushSecret is still present on entry to the finalizer handler. Once both are already NotFound on entry (re-runs, pre-deleted PushSecrets), the start Event is skipped and only the completion Event fires. This avoids Event spam under requeue loops while still giving operators a kubectl-describe trail.\n\n7. Status surfacing: while the finalizer is blocked on ESO, set SecretsReady=False with reason=\"OpenBaoFinalizerBlocked\" and a message naming the stuck PushSecret. Reusing the existing SecretsReady condition (CC-0013, CC-0047) keeps the status surface coherent — any secret-store lifecycle concern appears under the same condition type.\n\n8. Log correlation: every finalizer-related log line includes keystone.name / keystone.namespace via log.FromContext(ctx).WithValues(\"keystone\", client.ObjectKeyFromObject(keystone).String()) per the established operator logging pattern.\n\n9. Shared KV-v2 paths (known limitation, out of scope): both backup PushSecrets target cluster-global paths (kv-v2/data/openstack/keystone/fernet-keys, /credential-keys) rather than {name}-scoped paths. In multi-Keystone-per-cluster deployments, deleting one CR would cleanup the shared path used by all. Deferred to a separate design discussion (see deferred_items). The current deploy model is single-Keystone-per-cluster for the openstack namespace, so this is not observable in CI.\n\n10. Interaction with CC-0078 (MariaDB finalizer): if CC-0078 has merged, both finalizers are added on the live-CR path and removed independently. Reconcile's DeletionTimestamp branch invokes both finalizer handlers in sequence (openbao first, then database — order does not matter for correctness). If CC-0078 has not merged, the openbao finalizer stands alone. No coupling between the two code paths other than sharing the same Reconcile entry point.\n\n11. No schema changes. metadata.finalizers is a built-in array on every object; Spec.DeletionPolicy is an existing field on PushSecret.\n\nDATA MODEL:\n- Constant: keystoneOpenBaoFinalizer string = \"keystone.openstack.c5c3.io/openbao-finalizer\" (reconcile_secrets.go)\n- PushSecret field: Spec.DeletionPolicy = esov1alpha1.PushSecretDeletionPolicyDelete on both backups\n- Condition: SecretsReady=False, reason=\"OpenBaoFinalizerBlocked\", message=\"PushSecret <name> is still Terminating; ESO may be unable to reach OpenBao\"\n\nARCHITECTURE DIAGRAM:\n\n  +------------------+       +---------------------------+\n  |  kubectl delete  | ----> |  Keystone CR              |\n  |  keystone <name> |       |  (DeletionTimestamp set)  |\n  +------------------+       +---------------------------+\n                                          |\n                                          v\n                             +---------------------------+\n                             | KeystoneReconciler.       |\n                             | Reconcile()               |\n                             |  └─ if DeletionTS != 0 && |\n                             |     openbao finalizer set  |\n                             +---------------------------+\n                                          |\n                                          v\n                             +---------------------------+\n                             | finalizeOpenBaoSecrets    |\n                             |  ─ Delete(fernet-backup) ─+─> ESO finalizer held\n                             |  ─ Delete(cred-backup)   ─+─> ESO deletes KV-v2 path\n                             |  ─ re-Get each           |      │\n                             +---------------------------+      v\n                                          |            OpenBao /kv-v2/openstack/keystone/*\n                               both NotFound?                   wiped\n                                  yes /          \\ no\n                                     v            v\n                     RemoveFinalizer +Update    Set SecretsReady=False/OpenBaoFinalizerBlocked;\n                                     |          Requeue RequeueSecretPolling\n                                     v\n                             API server deletes Keystone CR\n\nDATA FLOW:\n  1. User issues kubectl delete keystone <name>.\n  2. API server sets DeletionTimestamp; Keystone CR stays in etcd because openbao-finalizer is present.\n  3. Watch triggers Reconcile.\n  4. Reconcile Gets the CR, sees DeletionTimestamp != 0, skips sub-reconcilers.\n  5. On first pass where at least one PushSecret is still present: emit \"FinalizingOpenBaoSecrets\" Event.\n  6. Call finalizeOpenBaoSecrets: Delete(IgnoreNotFound) on each backup PushSecret.\n  7. Each PushSecret has DeletionPolicy=Delete; ESO picks up the DeletionTimestamp, purges the KV-v2 path in OpenBao, removes its own finalizer, API server deletes the PushSecret.\n  8. Re-Get each PushSecret: if any still present, set SecretsReady=False/OpenBaoFinalizerBlocked and requeue after RequeueSecretPolling.\n  9. Next reconcile: re-run finalizeOpenBaoSecrets; when both NotFound, emit \"OpenBaoSecretsFinalized\", RemoveFinalizer, Update.\n 10. API server observes empty openbao finalizer and garbage-collects the Keystone CR (once any other finalizers are also cleared).\n\nPITFALLS TO AVOID:\n- Do NOT RemoveFinalizer before confirming NotFound on both PushSecrets (would leave OpenBao paths orphaned if ESO has not yet finished).\n- Do NOT talk to OpenBao directly from the operator (credentials, lifecycle, and abstraction all live in ESO).\n- Do NOT emit \"FinalizingOpenBaoSecrets\" on every requeue (noise under stuck deletions); check \"at least one PushSecret present\" sentinel on entry.\n- Do NOT run sub-reconcilers on a Terminating CR (re-creates PushSecrets, infinite loop).\n- Do NOT add Update-conflict error Events (controller-runtime retry backoff handles it; Events would be noise).\n- Keep the finalizer constant package-private (lower-case initial); no cross-package consumer exists.\n- Do NOT forget DeletionPolicy=Delete on the builder — without it, the PushSecret is deleted locally but the remote KV-v2 path stays, defeating the feature.\n\nPATTERNS TO FOLLOW:\n- Reconcile structure and updateStatus usage already defined in keystone_controller.go:79-185.\n- Package-level constants live alongside related helpers in reconcile_secrets.go (see openBaoClusterStoreName:26).\n- Fake client unit tests with secretsTestScheme(), secretsTestKeystone(), record.NewFakeRecorder(); interceptor.Funcs for Conflict/error injection (see reconcile_secrets_test.go).\n- Integration tests follow the envtest pattern in integration_test.go (Gomega Eventually, testutil.SetupKeystoneEnvTestWithController, simulators package).\n- Chainsaw step structure in tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml (apply, assert, delete, error + final script step for remote verification).\n- Condition writing via conditions.SetCondition with ObservedGeneration — pattern sub-reconciler-condition-contract-with-observedgeneration.md.\n- Event emission via r.Recorder.Event/Eventf paired with condition updates — pattern kubernetes-event-emission-alongside-condition-setting-with-fakerecorder-test-ass.md.\n\nDEFERRED ITEMS:\n- Per-CR-scoped KV-v2 paths (e.g. kv-v2/data/openstack/keystone/<name>/fernet-keys). Current shared-path design works for single-Keystone-per-namespace deploys; revisit if multi-Keystone-per-namespace lands.\n- OpenBao-side policy verification (e.g. confirm the ESO policy actually grants delete on kv-v2/*). Currently implicit via deploy/openbao/policies/eso-management.hcl.\n- Grace-period / timeout escalation when ESO is permanently wedged: today the reconciler retries forever with SecretsReady=False. Consider a DeletionTimestamp-age heuristic in a follow-up if operator teams report stuck deletions.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_AddsOpenBaoFinalizerOnFirstReconcile\" requirement-id=\"REQ-001\">[unit] Reconcile on a Keystone CR without the openbao finalizer results in metadata.finalizers containing keystoneOpenBaoFinalizer and returns ctrl.Result{Requeue: true}</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_OpenBaoFinalizerAlreadyPresent_NoExtraUpdate\" requirement-id=\"REQ-001\">[unit] Reconcile on a CR that already has the openbao finalizer does not issue an extra Update for finalizer maintenance and proceeds into reconcileSecrets</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_OpenBaoFinalizerUpdateConflict_ReturnsError\" requirement-id=\"REQ-001\">[unit] When a fake client interceptor injects Conflict on the finalizer Update, Reconcile returns the error unchanged so controller-runtime retries</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestFinalizeOpenBaoSecrets_DeletesBothPushSecrets\" requirement-id=\"REQ-002\">[unit] finalizeOpenBaoSecrets issues Delete for both backup PushSecrets, returns (done=false, nil) while either exists, and (done=true, nil) when both are NotFound on re-Get</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestFinalizeOpenBaoSecrets_NotFoundIsTolerated\" requirement-id=\"REQ-003\">[unit] When either backup PushSecret is already NotFound at entry, finalizeOpenBaoSecrets returns done=true without error and without surfacing NotFound as a failure</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestFinalizeOpenBaoSecrets_IsIdempotent\" requirement-id=\"REQ-003\">[unit] Calling finalizeOpenBaoSecrets twice in a row with no PushSecrets present returns (done=true, nil) both times and issues no extra Delete calls on the second invocation (verified via interceptor call counter)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestFinalizeOpenBaoSecrets_RequeuesWhilePushSecretTerminating\" requirement-id=\"REQ-004\">[unit] When fernet-keys-backup remains in the fake client store with DeletionTimestamp set (ESO finalizer held), finalizeOpenBaoSecrets returns done=false and the caller requeues with RequeueSecretPolling</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestFinalizeOpenBaoSecrets_SetsBlockedConditionOnStall\" requirement-id=\"REQ-009\">[unit] When a backup PushSecret is still Terminating, finalizeOpenBaoSecrets (or its Reconcile caller) sets SecretsReady=False with reason=OpenBaoFinalizerBlocked and a message naming the stuck PushSecret</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestFernetKeysPushSecret_HasDeletionPolicyDelete\" requirement-id=\"REQ-008\">[unit] fernetKeysPushSecret(keystone).Spec.DeletionPolicy equals esov1alpha1.PushSecretDeletionPolicyDelete</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_secrets_test.go\" test-function=\"TestCredentialKeysPushSecret_HasDeletionPolicyDelete\" requirement-id=\"REQ-008\">[unit] credentialKeysPushSecret(keystone).Spec.DeletionPolicy equals esov1alpha1.PushSecretDeletionPolicyDelete</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestReconcileFernetKeys_PushSecretDeletionPolicyDelete\" requirement-id=\"REQ-008\">[unit] After reconcileFernetKeys runs, the stored PushSecret object has Spec.DeletionPolicy == Delete (verified via fake client Get)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_PushSecretDeletionPolicyDelete\" requirement-id=\"REQ-008\">[unit] After reconcileCredentialKeys runs, the stored PushSecret object has Spec.DeletionPolicy == Delete</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestKeystoneOpenBaoFinalizerConstant_HasExpectedValue\" requirement-id=\"REQ-005\">[unit] keystoneOpenBaoFinalizer == \"keystone.openstack.c5c3.io/openbao-finalizer\" and is the only Go definition of that string in the operators/keystone module</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_TerminatingCR_SkipsSubReconcilers\" requirement-id=\"REQ-006\">[unit] Reconcile on a Terminating CR never invokes reconcileFernetKeys/reconcileCredentialKeys/reconcileDeployment; asserted by observing that no Deployment, Secret, or PushSecret is created during the call</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_TerminatingCR_RequeuesWhilePushSecretsExist\" requirement-id=\"REQ-004\">[unit] Reconcile on a Terminating CR with at least one backup PushSecret still present returns ctrl.Result{RequeueAfter: RequeueSecretPolling} and does NOT remove the finalizer</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_TerminatingCR_WithoutOpenBaoFinalizer_NoOp\" requirement-id=\"REQ-002\">[unit] A Terminating CR that never had the openbao finalizer returns ctrl.Result{}, nil without issuing any Delete calls or emitting Events</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_TerminatingCR_EmitsFinalizingOpenBaoSecretsEvent\" requirement-id=\"REQ-007\">[unit] On the first reconcile observing DeletionTimestamp, a Normal Event with reason \"FinalizingOpenBaoSecrets\" is recorded via record.FakeRecorder</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_TerminatingCR_EmitsOpenBaoSecretsFinalizedEvent\" requirement-id=\"REQ-007\">[unit] When cleanup completes, exactly one Normal Event with reason \"OpenBaoSecretsFinalized\" is recorded</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestReconcile_TerminatingCR_NoDuplicateStartEventOnRequeue\" requirement-id=\"REQ-007\">[unit] After two consecutive terminating reconciles with PushSecrets still Terminating, only one FinalizingOpenBaoSecrets Event is emitted across both passes</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_OpenBaoFinalizerLifecycle_AddAndRemove\" requirement-id=\"REQ-002\">[integration] Under envtest with ESO simulator: create Keystone CR; Eventually assert openbao finalizer present; simulate ESO clearing its PushSecret finalizer after Delete; delete Keystone CR; Eventually assert PushSecrets NotFound, Keystone CR NotFound</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_OpenBaoFinalizer_BlockedWhenPushSecretStuck\" requirement-id=\"REQ-004\">[integration] Under envtest: create Keystone CR; delete it; simulate ESO holding its finalizer on fernet-keys-backup; Eventually assert Keystone CR remains Terminating with SecretsReady=False/OpenBaoFinalizerBlocked in status.conditions; clear ESO finalizer; Eventually assert CR is NotFound</spec>\n    <spec test-file=\"tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml\" test-function=\"keystone-deletion-cleanup (step: Assert openbao finalizer present)\" requirement-id=\"REQ-010\">[e2e] After Ready=True, the test asserts metadata.finalizers contains keystone.openstack.c5c3.io/openbao-finalizer</spec>\n    <spec test-file=\"tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml\" test-function=\"keystone-deletion-cleanup (step: Assert backup PushSecrets removed)\" requirement-id=\"REQ-010\">[e2e] After CR deletion, error assertions confirm {name}-fernet-keys-backup and {name}-credential-keys-backup PushSecrets are NotFound within 2 minutes</spec>\n    <spec test-file=\"tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml\" test-function=\"keystone-deletion-cleanup (step: Verify OpenBao KV-v2 paths empty)\" requirement-id=\"REQ-010\">[e2e] A script step runs kubectl exec against the openbao pod; bao kv get kv-v2/openstack/keystone/fernet-keys and bao kv get kv-v2/openstack/keystone/credential-keys both exit non-zero (path absent)</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Cluster admin deletes a Keystone CR without leaving OpenBao secrets behind\">As a cluster admin, I want deleting a Keystone CR to automatically purge the KV-v2 paths written by its fernet-keys-backup and credential-keys-backup PushSecrets, so that stale cryptographic material does not linger in OpenBao after teardown (CC-0079)</story>\n    <story title=\"Operator developer can trace OpenBao finalizer lifecycle via conditions and events\">As a operator developer, I want the controller to add the OpenBao finalizer on first reconcile and emit Events when cleanup starts and completes, so that finalizer state is observable via kubectl describe without reading controller source (CC-0079)</story>\n    <story title=\"SRE sees a clear status message when OpenBao is unavailable during deletion\">As a SRE, I want the Keystone CR's conditions to surface a descriptive message while ESO is blocked cleaning up OpenBao, so that a stuck deletion can be diagnosed from kubectl get keystone -o yaml without attaching a debugger (CC-0079)</story>\n    <story title=\"Finalizer handler tolerates already-deleted PushSecrets\">As a system, I want cleanup to succeed when fernet-keys-backup or credential-keys-backup PushSecrets have already been removed by another actor, so that re-running the finalizer after operator restart, retry, or manual intervention never blocks CR removal (CC-0079)</story>\n    <story title=\"Existing Keystone CRs without the OpenBao finalizer are upgraded transparently\">As a cluster admin, I want Keystone CRs created before this change to gain the finalizer on their next reconcile, so that upgrading the operator does not require manual migration of existing CRs (CC-0079)</story>\n    <story title=\"Terminating CRs skip sub-reconcilers so PushSecrets are not recreated during cleanup\">As a operator developer, I want the Reconcile entry point to short-circuit to the finalizer handler when DeletionTimestamp is non-zero, so that reconcileFernetKeys and reconcileCredentialKeys do not re-create the PushSecrets the finalizer is deleting (CC-0079)</story>\n    <story title=\"E2E chainsaw test validates end-to-end deletion of OpenBao material\">As a E2E test author, I want tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml to assert finalizer presence and KV-v2 path absence after deletion, so that regressions in finalizer behaviour are caught in CI before release (CC-0079)</story>\n    <story title=\"Documentation reader understands the OpenBao finalizer contract\">As a operator developer, I want docs/reference/keystone-reconciler.md to describe the finalizer name, lifecycle, cleanup scope, and failure modes, so that future contributors and SREs can reason about OpenBao teardown without reading controller source (CC-0079)</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The KeystoneReconciler SHALL add a finalizer named \"keystone.openstack.c5c3.io/openbao-finalizer\" to every Keystone CR on its first reconcile when the CR is not being deleted (CC-0079)</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">When a Keystone CR is being deleted (metadata.DeletionTimestamp non-zero) and the openbao finalizer is present, the controller SHALL delete the {name}-fernet-keys-backup and {name}-credential-keys-backup PushSecret CRs before removing the finalizer (CC-0079)</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The finalizer handler SHALL treat apierrors.IsNotFound responses from client.Delete and client.Get on backup PushSecrets as success and SHALL NOT propagate them as reconciler errors (CC-0079)</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">While either backup PushSecret still exists after the finalizer has issued Delete, the finalizer handler SHALL requeue using RequeueSecretPolling and SHALL NOT remove the openbao finalizer (CC-0079)</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The openbao finalizer constant SHALL be defined as a package-level string in operators/keystone/internal/controller/reconcile_secrets.go with the value \"keystone.openstack.c5c3.io/openbao-finalizer\" (CC-0079)</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The existing Reconcile entry point SHALL be updated so that finalizer handling executes after the CR fetch and before any sub-reconciler runs, and so that a Terminating CR never invokes sub-reconcilers (CC-0079)</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The controller SHALL emit Kubernetes Events on finalizer lifecycle transitions: Normal \"FinalizingOpenBaoSecrets\" when cleanup starts, Normal \"OpenBaoSecretsFinalized\" when it completes (CC-0079)</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The fernetKeysPushSecret and credentialKeysPushSecret builders SHALL set Spec.DeletionPolicy to esov1alpha1.PushSecretDeletionPolicyDelete so that deleting the PushSecret CR causes ESO to purge the corresponding KV-v2 path in OpenBao (CC-0079)</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">While the openbao finalizer is blocked on ESO, the controller SHALL set the SecretsReady condition to False with reason \"OpenBaoFinalizerBlocked\" and a message identifying the PushSecret that is still Terminating (CC-0079)</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The E2E chainsaw test tests/e2e/keystone/deletion-cleanup/chainsaw-test.yaml SHALL assert finalizer presence after Ready=True, backup PushSecret NotFound after CR deletion, and absence of both KV-v2 paths in OpenBao within a 2-minute bounded timeout (CC-0079)</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The reference documentation (docs/reference/keystone-reconciler.md) SHALL describe the OpenBao finalizer: name, lifecycle, PushSecret DeletionPolicy integration, failure modes, and idempotency (CC-0079)</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 35026,
  "caller": "run_claude_cli"
}