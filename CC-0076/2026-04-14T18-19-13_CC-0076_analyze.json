{
  "timestamp": "2026-04-14T18:19:13.549604+00:00",
  "feature_id": "CC-0076",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0076",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0076\">\n  <title>Fix race condition in memcached-pod-kill chaos test</title>\n  <description># GitHub Issue #214: Fix race condition in memcached-pod-kill chaos test that causes flaky failures\n\n**Category**: bug | **Scope**: Small\n\n**Description**: The `keystone-memcached-pod-kill` chaos test in `tests/e2e-chaos/memcached-pod-kill/chainsaw-test.yaml` races with Chaos Mesh pod deletion and fails non-deterministically. In step-4 the test uses `wait` with a label selector to confirm the killed pod becomes `Ready=false` before asserting Keystone stays `Ready=True`. chainsaw/kubectl `wait` resolves the label selector once up front, grabs the doomed pod (e.g. `openstack-memcached-75845cbcd5-ht4h9`), and watches that specific object. When Chaos Mesh deletes the pod mid-wait, the watch errors with `Error from server (NotFound): pods \"openstack-memcached-75845cbcd5-ht4h9\" not found` instead of re-resolving the selector onto the replacement pod the ReplicaSet immediately creates. Observed in run https://github.com/C5C3/forge/actions/runs/24412091069/job/71314792352: the replacement pod `openstack-memcached-75845cbcd5-ptznn` was already `Scheduled`/`Created`/`Started` within the same second (visible in the event dump), the memcached-operator Deployment was healthy, and the Keystone CR ended the test with `Ready=True/AllReady` — confirming this is a test-only race, not an operator regression. The `NOTE` block at lines 50-56 of `chainsaw-test.yaml` already anticipates this race, but the documented mitigation (\"pod-kill with gracePeriod:0 plus controller restart takes multiple seconds\") does not hold in practice — the Pending→Scheduled→Created transition for the replacement pod happens at the same timestamp as the delete.\n\n**Motivation**: The `keystone-mariadb-pod-kill` and `keystone-openbao-pod-kill` tests in the same suite pass reliably because they assert on Keystone CR sub-conditions (e.g. `DatabaseReady=False`), which chainsaw retries internally until they match. The memcached test cannot use that pattern by design — cache outages are treated as performance degradation only and must not flip any Keystone condition (CC-0047, SC-CHAOS-002). The current workaround (pod-level `Ready=false` wait) introduces a structural race that produces false negatives in CI, blocks the chaos test suite (issue #165), and hides real regressions in the noise.\n\n**Affected Areas**:\n- `tests/e2e-chaos/memcached-pod-kill/chainsaw-test.yaml`\n\n**Acceptance Criteria**:\n- [ ] `keystone-memcached-pod-kill` no longer relies on `wait` with `condition: Ready=false` against a label selector that matches the pod being deleted\n- [ ] The chaos effect is verified via a mechanism that is resilient to pod replacement, e.g. asserting on `Deployment.status.unavailableReplicas > 0` or on a `Pod` UID/`resourceVersion` change, both of which chainsaw retries\n- [ ] Alternatively, the `Ready=false` wait is dropped entirely and the test relies only on the subsequent `Ready=true` wait plus the Keystone `AllReady` assertion — which is sufficient to validate the test's actual goal (memcached failures do not degrade Keystone)\n- [ ] The `NOTE` block at lines 50-56 is removed or rewritten to reflect the new approach\n- [ ] The test passes on three consecutive CI runs of `.github/workflows/ci.yaml` (e2e-chaos job)\n- [ ] No change is required in the memcached-operator repository — this is a pure test fix\n\n**Labels:** bug\n\n---\nSource: https://github.com/C5C3/forge/issues/214</description>\n  <tasks>\n    <task status=\"pending\">1.1 Replace step-4 wait operations with Deployment-level readyReplicas polling script in chainsaw-test.yaml (REQ-001, REQ-002, REQ-003, REQ-004, REQ-005)</task>\n    <task status=\"pending\">1.2 Update docs/reference/chaos-e2e-tests.md memcached-pod-kill section to reflect new step-4 approach (REQ-006)</task>\n\n  </tasks>\n  <implementation-notes>Architecture decision: Replace Pod-level chainsaw wait (which resolves label selectors to specific pod objects at query time) with Deployment-level readyReplicas polling (which watches the persistent Deployment object). This is the same pattern used by operator-pod-crash/chainsaw-test.yaml:54-85 and api-pod-kill-pdb/chainsaw-test.yaml:72-98.\n\nData flow after fix:\n1. Step 3: PodChaos applied -> Chaos Mesh kills one memcached pod\n2. Step 4, Phase 1: Script polls Deployment.status.readyReplicas -> detects drop (chaos confirmed)\n3. Step 4, Phase 2: ReplicaSet creates replacement -> replacement passes readiness -> readyReplicas returns\n4. Step 4, Assert: All 6 Keystone conditions asserted True (no-regression pattern)\n5. Step 5: PodChaos deleted\n6. Step 6: Final Keystone AllReady assertion\n\nPotential pitfalls:\n- Do NOT use set -euo pipefail — ${READY:-0} pattern fails under set -e when kubectl returns empty output (documented in operator-pod-crash comment block)\n- Use the Deployment name openstack-memcached (from pod name openstack-memcached-75845cbcd5-ht4h9 and cache.clusterRef.name in 00-keystone-cr.yaml)\n- Script timeout (120s) must cover both Phase 1 (30 iterations * 2s = 60s max) and Phase 2 (60 iterations * 2s = 120s max)\n- Keep the assert sub-step and catch block completely unchanged — only the wait operations and comments change\n\nDeferred items:\n- Investigate if any other chaos tests have similar wait-on-Pod races (operator-pod-crash already uses Deployment polling, so no issue there)\n- Upstream contribution to chainsaw/kubectl to support label-selector re-resolution in wait command\n- Consider adding a CI retry mechanism for chaos tests as an additional reliability layer</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/e2e-chaos/memcached-pod-kill/chainsaw-test.yaml\" test-function=\"step-4 script Phase 1: readyReplicas drop detection\" requirement-id=\"REQ-002\">[e2e] Script polls memcached Deployment readyReplicas via kubectl jsonpath and detects the drop below desired within 60s (30 iterations * 2s). Exits with ERROR if readyReplicas never drops.</spec>\n    <spec test-file=\"tests/e2e-chaos/memcached-pod-kill/chainsaw-test.yaml\" test-function=\"step-4 script Phase 2: readyReplicas recovery detection\" requirement-id=\"REQ-003\">[e2e] Script polls memcached Deployment readyReplicas and detects recovery to desired within 120s (60 iterations * 2s). Exits 0 on recovery, exits 1 with ERROR on timeout.</spec>\n    <spec test-file=\"tests/e2e-chaos/memcached-pod-kill/chainsaw-test.yaml\" test-function=\"step-4 assert: all 6 Keystone conditions True\" requirement-id=\"REQ-004\">[e2e] Chainsaw assert retries until all 6 Keystone conditions (SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady, Ready=AllReady) match True, within the 5m assert timeout. Identical to original test assertions.</spec>\n    <spec test-file=\"tests/e2e-chaos/memcached-pod-kill/chainsaw-test.yaml\" test-function=\"step-4 full flow: no NotFound error during pod replacement race\" requirement-id=\"REQ-001\">[e2e] When the replacement memcached pod is created within the same second as the original pod deletion, the test passes without any 'pods not found' errors — because the script watches the Deployment object (which persists), not individual Pod objects.</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"CI engineer gets reliable memcached-pod-kill chaos test results\">As a CI engineer, I want the keystone-memcached-pod-kill chaos test to pass deterministically without flaky NotFound errors from kubectl wait racing with pod deletion, so that CI results accurately reflect operator behavior and flaky failures do not block the chaos test suite or hide real regressions</story>\n    <story title=\"CI engineer can verify chaos injection took effect before operator assertions\">As a CI engineer, I want the test to confirm that Chaos Mesh actually killed a memcached pod before asserting Keystone operator conditions, so that a Chaos Mesh misconfiguration (selector not matching any pods) does not produce a false-positive test pass</story>\n    <story title=\"Test maintainer finds documentation consistent with test implementation\">As a test maintainer, I want the chaos-e2e-tests reference documentation to accurately describe the memcached-pod-kill step-4 mechanics after the race condition fix, so that developers modifying or debugging the test can trust the documentation</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">Step-4 SHALL NOT use chainsaw wait with condition=Ready=false on Pods matching a label selector that targets the pod being deleted by Chaos Mesh</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">Step-4 SHALL verify chaos took effect via Deployment-level readyReplicas polling, resilient to pod replacement</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">Step-4 SHALL verify memcached Deployment recovery (readyReplicas returns to desired) before asserting Keystone conditions</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">Step-4 SHALL assert all 6 Keystone conditions remain True (SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady, Ready=AllReady) after memcached pod kill — unchanged from the original test</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The NOTE block (original lines 49-53) SHALL be removed and the step-4 comment SHALL describe the Deployment-level polling approach with CC-0076 reference</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">docs/reference/chaos-e2e-tests.md SHALL reflect the updated step-4 mechanics for the memcached-pod-kill test suite</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 11661,
  "caller": "run_claude_cli"
}