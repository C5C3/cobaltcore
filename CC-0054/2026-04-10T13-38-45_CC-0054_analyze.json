{
  "timestamp": "2026-04-10T13:38:45.745359+00:00",
  "feature_id": "CC-0054",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0054",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0054\">\n  <title>Add chaos E2E tests to CI pipeline</title>\n  <description># GitHub Issue #165: Add chaos E2E tests to CI pipeline\n\n## Summary\n\nThe chaos test framework introduced in CC-0047 (12a8c92) provides Chainsaw-based chaos E2E tests using Chaos Mesh `PodChaos` CRDs (MariaDB, Memcached, OpenBao pod-kill scenarios). These tests are currently only runnable locally via `make e2e-chaos` but are **not integrated into the CI pipeline**.\n\nAs more chaos test scenarios are planned, CI integration is needed to ensure operator resilience is continuously validated.\n\n## Current state\n\n- Three chaos scenarios exist under `tests/e2e-chaos/`:\n  - `mariadb-pod-kill` (SC-CHAOS-001)\n  - `memcached-pod-kill` (SC-CHAOS-002)\n  - `openbao-pod-kill` (SC-CHAOS-003)\n- A dedicated Chainsaw config exists at `tests/e2e-chaos/chainsaw-config.yaml` with serial execution and extended timeouts (5m assert, 2m cleanup)\n- A `make e2e-chaos` target exists in the Makefile\n- No reference to chaos tests exists in `.github/workflows/ci.yaml`\n\n## Requirements\n\n- [ ] Add a `e2e-chaos` job to the CI workflow (`.github/workflows/ci.yaml`)\n- [ ] The job needs a kind cluster with Chaos Mesh installed (in addition to the standard E2E infrastructure)\n- [ ] Wire up change detection so chaos tests run when `tests/e2e-chaos/**` or operator/shared code changes\n- [ ] Chaos tests must run **after** the regular E2E tests pass (they depend on a working baseline)\n- [ ] Collect JUnit reports and diagnostics artifacts on failure (as done for other E2E jobs)\n- [ ] Consider whether chaos tests should be required to pass for merge or run as informational/non-blocking initially\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/165</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add `e2e_chaos` path filter and `e2e-chaos` output to ci.yaml changes job (REQ-002, CC-0054)</task>\n    <task status=\"pending\">1.2 Wire `e2e-chaos` output through `hack/ci-resolve-changes.sh` (REQ-002, CC-0054)</task>\n    <task status=\"pending\">2.1 Add `e2e-chaos` job to ci.yaml (REQ-001, REQ-003, REQ-004, REQ-005, REQ-006, REQ-007, REQ-008, CC-0054)</task>\n    <task status=\"pending\">3.1 Update `docs/reference/ci-workflow.md` with e2e-chaos job documentation (REQ-001, CC-0054)</task>\n    <task status=\"pending\">3.2 Update `docs/reference/chaos-e2e-tests.md` CI Trigger Policy section (REQ-001, REQ-002, CC-0054)</task>\n\n  </tasks>\n  <implementation-notes>## Architectural Decisions\n\n1. **Independent cluster per job**: The `e2e-chaos` job creates its own kind cluster rather than sharing with `e2e-operator`. This follows the existing pattern where each E2E job is isolated. Trade-off: longer total CI time but simpler and more reliable.\n\n2. **Non-blocking via `continue-on-error: true`**: Job-level (not step-level) continue-on-error. This means the entire chaos job is informational. The GitHub UI shows a yellow warning icon for failures but the workflow succeeds. Revisit when chaos test stability is proven over 2-4 weeks of CI runs.\n\n3. **Change detection via `go_changed` flag**: Rather than duplicating operator path filters, the chaos job leverages the existing `go_changed` boolean which is true when ANY Go code (shared or per-operator) changes. This means chaos tests run whenever regular E2E tests would run, plus when chaos test definitions change independently.\n\n4. **Keystone-only (no matrix)**: All 3 chaos scenarios test keystone operator resilience. A matrix strategy is unnecessary. When chaos tests are added for other operators, the job can be converted to a matrix.\n\n5. **No 2026.1 or upgrade images**: Chaos tests use `image.tag: 2025.2` (default release). The 2026.1 build and upgrade tag steps from e2e-operator are omitted to save ~2 min CI time.\n\n## Job Dependency Graph\n\n```\nchanges ──┬── lint ────────────┐\n          ├── shellcheck ──────┤\n          ├── test ────────────┤\n          ├── test-integration ┤\n          ├── verify-codegen ──┤\n          │                    ├──> e2e-operator ──┬──> build-and-push ──> merge-images\n          │                    │                   ├──> helm-push\n          │                    │                   │\n          │                    └──> e2e-chaos ─────┘ (non-blocking, no downstream deps)\n          ├── e2e-infra\n          ├── test-race\n          ├── helm-validate\n          └── docs\n```\n\n## Data Flow: Change Detection\n\n```\n1. dorny/paths-filter evaluates e2e_chaos filter against PR diff\n2. ci-resolve-changes.sh reads FILTER_e2e_chaos env var\n3. Script emits e2e-chaos=true when:\n   - FILTER_e2e_chaos=true (chaos test files changed), OR\n   - go_changed=true (operator/shared code changed OR tag push)\n4. e2e-chaos job evaluates: needs.changes.outputs.e2e-chaos == 'true'\n```\n\n## Pitfalls\n\n- **GitHub Actions skipped-job behavior**: When `e2e-operator` is skipped (no operator changes), its `result` is `skipped` (not `failure`). The `!contains(needs.*.result, 'failure')` check correctly allows the chaos job to proceed. Do NOT use `success()` or `needs.e2e-operator.result == 'success'` as the condition — this would block chaos tests when e2e-operator is skipped.\n\n- **Chaos Mesh already in deploy-infra**: The `deploy-infra.sh` script deploys Chaos Mesh as a HelmRelease. No additional Chaos Mesh installation step is needed. The `setup-e2e-infra` composite action handles everything.\n\n- **Serial execution timing**: With `parallel: 1` in the chaos config and 5m assert timeouts, worst-case test execution is ~30 min (3 scenarios × ~10 min each). The 60-min job timeout provides adequate buffer for cluster setup.\n\n## Key Files\n\n- `.github/workflows/ci.yaml` — Main CI workflow (lines 39-109 for change detection, lines 336-411 for e2e-operator pattern)\n- `hack/ci-resolve-changes.sh` — Change detection script (emit e2e-chaos output after line 73)\n- `.github/actions/setup-e2e-infra/action.yaml` — Composite action reused by e2e-chaos job\n- `hack/ci-deploy-operator.sh` — Operator deployment reused by e2e-chaos job\n- `hack/ci-dump-diagnostics.sh` — Diagnostic script reused by e2e-chaos job\n- `hack/ci-build-service-image.sh` — Service image build reused by e2e-chaos job\n- `tests/e2e-chaos/chainsaw-config.yaml` — Chaos-specific Chainsaw config (serial, extended timeouts)\n- `docs/reference/ci-workflow.md` — CI docs to update\n- `docs/reference/chaos-e2e-tests.md` — Chaos test docs to update</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos job exists with correct structure\" requirement-id=\"REQ-001\">The `e2e-chaos` job is defined with kind cluster setup, operator build/deploy, chaos test execution, artifact upload, and diagnostics dump</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos job runs correct chainsaw command\" requirement-id=\"REQ-001\">The test step runs `chainsaw test --config tests/e2e-chaos/chainsaw-config.yaml tests/e2e-chaos/` in a directory with `_output/reports/` pre-created</spec>\n    <spec test-file=\"hack/ci-resolve-changes.sh\" test-function=\"[unit] e2e-chaos output true when FILTER_e2e_chaos is true\" requirement-id=\"REQ-002\">Running the script with `FILTER_e2e_chaos=true` and `FILTER_go_common=false` produces `e2e-chaos=true` in GITHUB_OUTPUT</spec>\n    <spec test-file=\"hack/ci-resolve-changes.sh\" test-function=\"[unit] e2e-chaos output true when go_changed is true\" requirement-id=\"REQ-002\">Running the script with `FILTER_e2e_chaos=false` and `FILTER_go_common=true` produces `e2e-chaos=true` in GITHUB_OUTPUT</spec>\n    <spec test-file=\"hack/ci-resolve-changes.sh\" test-function=\"[unit] e2e-chaos output true on tag push\" requirement-id=\"REQ-002\">Running the script with `GITHUB_REF=refs/tags/v1.0.0` produces `e2e-chaos=true` in GITHUB_OUTPUT</spec>\n    <spec test-file=\"hack/ci-resolve-changes.sh\" test-function=\"[unit] e2e-chaos output false when only docs change\" requirement-id=\"REQ-002\">Running the script with `FILTER_e2e_chaos=false`, `FILTER_go_common=false`, and no operator filters produces `e2e-chaos=false`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos depends on e2e-operator and pre-flight checks\" requirement-id=\"REQ-003\">The `e2e-chaos` job `needs` includes `changes`, `lint`, `shellcheck`, `test`, `test-integration`, `verify-codegen`, and `e2e-operator`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos if condition blocks on failure\" requirement-id=\"REQ-003\">The `if` condition includes `!contains(needs.*.result, 'failure')` and `!contains(needs.*.result, 'cancelled')` to block when any dependency fails</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] JUnit report uploaded as e2e-chaos-junit-report\" requirement-id=\"REQ-004\">The upload-artifact step uses `name: e2e-chaos-junit-report`, `path: _output/reports/`, `retention-days: 14`, and `if: always()`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] JUnit report uploaded regardless of test outcome\" requirement-id=\"REQ-004\">The upload-artifact step has `if: always()` so reports are uploaded even when chaos tests fail</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] diagnostics dumped on failure with OPERATOR=keystone\" requirement-id=\"REQ-005\">The diagnostics step runs `hack/ci-dump-diagnostics.sh` with `env: OPERATOR: keystone` and `if: always()`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] diagnostics step runs always\" requirement-id=\"REQ-005\">The diagnostics step has `if: always()` so it runs even on timeout or test failures</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] continue-on-error true on e2e-chaos job\" requirement-id=\"REQ-006\">The `e2e-chaos` job has `continue-on-error: true` at the job level</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] publish jobs do not depend on e2e-chaos\" requirement-id=\"REQ-006\">The `build-and-push`, `helm-push`, `merge-operator-images`, and `github-release` jobs do NOT list `e2e-chaos` in their `needs`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos creates kind cluster and deploys infra\" requirement-id=\"REQ-007\">The job creates a kind cluster via `helm/kind-action@v1.14.0` with `config: hack/kind-config.yaml` and `cluster_name: forge-e2e`, then runs the `setup-e2e-infra` composite action</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos builds and deploys keystone operator\" requirement-id=\"REQ-007\">The job builds the keystone operator image, builds the 2025.2 service image, loads both into kind, and deploys via `hack/ci-deploy-operator.sh`</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[e2e] e2e-chaos job timeout is 60 minutes\" requirement-id=\"REQ-008\">The `e2e-chaos` job has `timeout-minutes: 60`</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"CI pipeline runs chaos E2E tests on relevant code changes\">As a CI engineer, I want chaos E2E tests to run automatically in the CI pipeline when operator code or chaos test definitions change, so that operator resilience is continuously validated without manual intervention</story>\n    <story title=\"Chaos tests run after regular E2E tests verify baseline\">As a developer, I want chaos tests to run only after the regular E2E operator tests pass or are skipped, so that chaos tests do not waste CI resources testing a broken operator baseline</story>\n    <story title=\"Chaos test failures are diagnosable via CI artifacts\">As a developer, I want JUnit reports and diagnostic information to be collected when chaos tests run, so that I can diagnose failures without reproducing them locally</story>\n    <story title=\"Chaos test failures do not block PR merges initially\">As a team lead, I want chaos tests to run as non-blocking (informational) initially, so that newly integrated chaos tests do not block development while their stability is being proven in CI</story>\n    <story title=\"CI documentation reflects chaos test integration\">As a developer, I want the CI workflow and chaos test reference docs to accurately describe the chaos CI job, so that I can understand when and how chaos tests run in CI without reading the workflow YAML</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The CI workflow SHALL include an `e2e-chaos` job that runs all Chainsaw chaos E2E tests</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The change detection SHALL trigger chaos tests when `tests/e2e-chaos/**` files or Go operator/shared code changes</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The `e2e-chaos` job SHALL depend on pre-flight checks and the `e2e-operator` job, blocking when either fails</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The `e2e-chaos` job SHALL upload JUnit test reports as workflow artifacts</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The `e2e-chaos` job SHALL dump diagnostic information on failure using the shared diagnostic script</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The `e2e-chaos` job SHALL use `continue-on-error: true` for non-blocking behavior initially</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The `e2e-chaos` job SHALL deploy a kind cluster with the full infrastructure stack including Chaos Mesh</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The `e2e-chaos` job SHALL have a timeout of 60 minutes to accommodate serial chaos test execution</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 15709,
  "caller": "run_claude_cli"
}