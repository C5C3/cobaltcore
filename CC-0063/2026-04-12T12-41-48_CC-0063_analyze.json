{
  "timestamp": "2026-04-12T12:41:48.098508+00:00",
  "feature_id": "CC-0063",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0063",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0063\">\n  <title>Add graceful shutdown with preStop hook and startup probe</title>\n  <description># GitHub Issue #160: Add graceful shutdown with preStop hook and startup probe\n\n**Category**: improvement | **Scope**: Small\n\n**Description**: The Keystone API Deployment currently has no `preStop` lifecycle hook, no explicit `terminationGracePeriodSeconds`, and no startup probe. This means that during rolling updates or node drains, in-flight requests can be dropped because the pod is removed from the Service endpoints and terminated simultaneously. Additionally, after a cold start (e.g. following a config change or db_sync), the liveness probe with its 15-second initial delay may kill the container before Keystone has finished initializing.\n\nAdd the following to the `keystone-api` container in `buildKeystoneDeployment`:\n\n1. **PreStop hook** with a short sleep to allow kube-proxy/iptables to converge before the process receives SIGTERM:\n   ```yaml\n   lifecycle:\n     preStop:\n       exec:\n         command: [\"/bin/sh\", \"-c\", \"sleep 5\"]\n   ```\n\n2. **`terminationGracePeriodSeconds: 30`** on the PodSpec to give uWSGI enough time to finish in-flight requests after SIGTERM.\n\n3. **Startup probe** with a generous failure threshold so slow first-boot scenarios (large DB, cold caches) do not trigger a liveness-kill loop:\n   ```yaml\n   startupProbe:\n     httpGet:\n       path: /v3\n       port: 5000\n     failureThreshold: 30\n     periodSeconds: 10\n   ```\n   While the startup probe is active, kubelet skips both the liveness and readiness probes, preventing premature restarts.\n\n**Motivation**: Without a preStop hook, there is a race condition between endpoint removal and process termination: kube-proxy may still route traffic to a pod that has already received SIGTERM. The 5-second sleep gives the cluster networking stack time to propagate the endpoint change. Without a startup probe, the liveness probe (15s initial delay, 20s period) can restart the container if Keystone takes longer than ~35 seconds to start — a realistic scenario after a schema migration or with cold Memcached caches. Together, these changes eliminate two classes of transient errors during rollouts.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/reconcile_deployment.go` (`buildKeystoneDeployment`)\n- `operators/keystone/internal/controller/reconcile_deployment_test.go`\n\n**Acceptance Criteria**:\n- [ ] `keystone-api` container has a `preStop` lifecycle hook executing `sleep 5`\n- [ ] PodSpec sets `terminationGracePeriodSeconds: 30`\n- [ ] `keystone-api` container has a startup probe on `/v3` port 5000 with `failureThreshold: 30` and `periodSeconds: 10`\n- [ ] Existing liveness and readiness probes remain unchanged\n- [ ] Unit tests verify the presence and values of the lifecycle hook, termination grace period, and startup probe\n- [ ] Rolling update with `replicas >= 2` completes without dropping requests (verified manually or via integration test)\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/160</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add preStop lifecycle hook, terminationGracePeriodSeconds, and startup probe to buildKeystoneDeployment (REQ-001, REQ-002, REQ-003, REQ-004)</task>\n    <task status=\"pending\">1.2 Add unit tests for preStop hook, terminationGracePeriodSeconds, and startup probe (REQ-005)</task>\n    <task status=\"pending\">2.1 Add integration test for graceful shutdown deployment spec (REQ-006)</task>\n\n  </tasks>\n  <implementation-notes>This is a small, focused change to a single function (buildKeystoneDeployment) in reconcile_deployment.go. The three additions are:\n\n1. **Lifecycle.PreStop** on the container: `corev1.Lifecycle{PreStop: &corev1.LifecycleHandler{Exec: &corev1.ExecAction{Command: []string{\"/bin/sh\", \"-c\", \"sleep 5\"}}}}`\n\n2. **TerminationGracePeriodSeconds** on PodSpec: `TerminationGracePeriodSeconds: ptr.To(int64(30))` — note this uses k8s.io/utils/ptr which is already a project dependency (used in integration_test.go).\n\n3. **StartupProbe** on the container: follows the same httpGet pattern as existing probes but with failureThreshold and periodSeconds instead of initialDelaySeconds.\n\nNo new imports are needed in the production code beyond possibly `k8s.io/utils/ptr`. The test file already imports everything needed.\n\n**Pitfalls to avoid:**\n- Do NOT modify liveness or readiness probes — they must remain exactly as-is\n- terminationGracePeriodSeconds must be a *int64 pointer, not a bare int64\n- The preStop exec command must be a string slice, not a single concatenated string\n- startupProbe should NOT have initialDelaySeconds set (defaults to 0, which is correct since the probe itself handles the startup window)</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (extended: preStop lifecycle hook assertions)\" requirement-id=\"REQ-001\">container.Lifecycle is non-nil, container.Lifecycle.PreStop.Exec.Command equals [\"/bin/sh\", \"-c\", \"sleep 5\"]</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (extended: terminationGracePeriodSeconds assertions)\" requirement-id=\"REQ-002\">deploy.Spec.Template.Spec.TerminationGracePeriodSeconds is non-nil and equals int64(30)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (extended: startup probe assertions)\" requirement-id=\"REQ-003\">container.StartupProbe is non-nil, uses httpGet /v3 port 5000, failureThreshold 30, periodSeconds 10</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (existing: liveness probe assertions unchanged)\" requirement-id=\"REQ-004\">Existing liveness probe assertions (httpGet /v3 port 5000, initialDelaySeconds 15, periodSeconds 20) pass without modification</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (existing: readiness probe assertions unchanged)\" requirement-id=\"REQ-004\">Existing readiness probe assertions (httpGet /v3 port 5000, initialDelaySeconds 5, periodSeconds 10) pass without modification</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_GracefulShutdownSpec\" requirement-id=\"REQ-006\">After full reconciliation via envtest, resulting Deployment has preStop hook, terminationGracePeriodSeconds 30, and startup probe with correct values</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator ensures graceful shutdown during rolling updates\">As a platform operator, I want the Keystone API pods to drain in-flight requests before terminating during rolling updates, so that end users do not experience dropped requests or 502 errors during deployments</story>\n    <story title=\"Operator ensures Keystone survives slow cold starts\">As a platform operator, I want the Keystone API container to have a startup probe with a generous failure threshold, so that slow first-boot scenarios (large DB migrations, cold caches) do not trigger liveness-kill loops</story>\n    <story title=\"Existing probe behavior remains unchanged\">As a platform operator, I want the existing liveness and readiness probes to remain unchanged, so that steady-state health checking behavior is not regressed</story>\n    <story title=\"Unit tests verify all graceful shutdown configuration\">As a developer, I want comprehensive unit tests covering the lifecycle hook, termination grace period, and startup probe, so that regressions to graceful shutdown behavior are caught before merge</story>\n    <story title=\"Integration test verifies graceful shutdown config in full reconciliation\">As a developer, I want integration tests to verify the graceful shutdown fields survive a full reconciliation cycle, so that the deployment spec is correctly persisted to the API server with all lifecycle fields</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The keystone-api container SHALL have a preStop lifecycle hook that executes sleep 5 via /bin/sh -c</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The PodSpec SHALL set terminationGracePeriodSeconds to 30</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The keystone-api container SHALL have a startup probe on /v3 port 5000 with failureThreshold 30 and periodSeconds 10</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">Existing liveness and readiness probes SHALL remain unchanged</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">Unit tests SHALL verify the presence and values of the lifecycle hook, termination grace period, and startup probe</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHOULD\">Integration tests SHOULD verify graceful shutdown fields survive full reconciliation</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 11228,
  "caller": "run_claude_cli"
}