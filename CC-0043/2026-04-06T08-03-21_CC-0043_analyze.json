{
  "timestamp": "2026-04-06T08:03:21.099088+00:00",
  "feature_id": "CC-0043",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0043",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0043\">\n  <title>Add namespace-scoped RBAC and namespace flag</title>\n  <description>**Size:** 📦 medium\n**Category:** infrastructure\n**Priority:** high\n**Source:** GitHub Issue [#123](https://github.com/C5C3/forge/issues/123)\n\n1. Add `--namespace` string flag to `internal/common/bootstrap/manager.go` alongside existing flags (lines 66-84); when set, configure `cache.Options{DefaultNamespaces: map[string]cache.Config{ns: {}}}` at line 91.\n2. Add `rbac.namespaceScoped: false` toggle to `operators/keystone/helm/keystone-operator/values.yaml`.\n3. Create `templates/role.yaml` (new) mirroring the 12 apiGroup rule blocks from `clusterrole.yaml`, guarded by `{{- if .Values.rbac.namespaceScoped }}`.\n4. Create `templates/rolebinding.yaml` (new) binding ServiceAccount to Role, same conditional.\n5. Wrap existing `templates/clusterrole.yaml` and `templates/clusterrolebinding.yaml` in `{{- if not .Values.rbac.namespaceScoped }}`.\n6. Update `templates/deployment.yaml` to conditionally add `--namespace={{ .Release.Namespace }}` container arg when `rbac.namespaceScoped: true` (follow existing `--leader-elect` conditional pattern).\n7. Add multi-tenant deployment documentation describing when to use namespace-scoped mode, webhook limitation caveat (recommend `webhook.enabled: false`), and CRD installation remains cluster-admin concern.\n\nVerification: `helm template` with defaults produces ClusterRole (no Role); with `rbac.namespaceScoped=true` produces Role (no ClusterRole) and `--namespace` flag. `helm lint` passes both configs. Bootstrap tests verify flag populates cache options.\n\n**Rationale:** ClusterRole grants operator permissions across all namespaces, violating least-privilege in multi-tenant deployments. A compromised operator with ClusterRole can access resources in every namespace. Namespace-scoped RBAC limits blast radius to a single namespace, enabling secure multi-tenant patterns where each tenant gets an isolated operator instance.\n\n**Affected Areas:**\n- internal/common/bootstrap/manager.go\n- operators/keystone/helm/keystone-operator/values.yaml\n- operators/keystone/helm/keystone-operator/templates/clusterrole.yaml\n- operators/keystone/helm/keystone-operator/templates/clusterrolebinding.yaml\n- operators/keystone/helm/keystone-operator/templates/role.yaml\n- operators/keystone/helm/keystone-operator/templates/rolebinding.yaml\n- operators/keystone/helm/keystone-operator/templates/deployment.yaml\n- docs/</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add --namespace flag to bootstrap manager.go with cache restriction logic and unit tests (REQ-001, REQ-002)</task>\n    <task status=\"pending\">2.1 Add rbac.namespaceScoped to values.yaml and wrap clusterrole/clusterrolebinding in conditional guards (REQ-003, REQ-006)</task>\n    <task status=\"pending\">2.2 Create templates/role.yaml with all 14 rule blocks and tests/role_test.yaml (REQ-004)</task>\n    <task status=\"pending\">2.3 Create templates/rolebinding.yaml and tests/rolebinding_test.yaml (REQ-005)</task>\n    <task status=\"pending\">2.4 Update deployment.yaml with conditional --namespace arg and update deployment_test.yaml (REQ-007)</task>\n    <task status=\"pending\">3.1 Add namespace-scoped helm template scenario to CI helm-validate job (REQ-003, REQ-006)</task>\n    <task status=\"pending\">3.2 Add multi-tenant deployment documentation with webhook caveat (REQ-008)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\n```\n                     values.yaml\n                  rbac.namespaceScoped\n                         |\n          +--------------+--------------+\n          | false (default)             | true\n          v                             v\n  clusterrole.yaml              role.yaml\n  clusterrolebinding.yaml       rolebinding.yaml\n                         |\n                         v\n                  deployment.yaml\n                  --namespace={{ .Release.Namespace }}\n                  (only when true)\n                         |\n                         v\n              bootstrap/manager.go\n              --namespace flag\n              cache.Options.DefaultNamespaces\n```\n\n## Data Flow\n1. Helm values `rbac.namespaceScoped` controls which RBAC templates render and whether `--namespace` arg is injected into the Deployment\n2. The container arg `--namespace=<ns>` is parsed by `bootstrap.Run()` via Go's `flag` package\n3. When the namespace flag is non-empty, `cacheOptions()` sets `cache.Options.DefaultNamespaces` to restrict the controller-runtime informer cache\n4. The manager only watches resources in the specified namespace, enforcing namespace isolation at the runtime level in addition to RBAC\n\n## Key Patterns to Follow\n- **Conditional rendering**: Follow the `{{- if .Values.webhook.enabled }}` pattern used in deployment.yaml for webhook volumes. Apply same pattern for `rbac.namespaceScoped`.\n- **Flag registration**: Follow existing `flag.StringVar` / `flag.BoolVar` / `flag.DurationVar` pattern at lines 66-80 of manager.go.\n- **Helm test structure**: Follow existing test pattern in `tests/clusterrole_test.yaml` with suite name, template reference, and assertion style.\n- **Feature ID comments**: All new Go code and Helm templates should include `Feature: CC-0043` or `CC-0043` comments.\n\n## Pitfalls\n- **Rule sync**: role.yaml rules MUST exactly mirror clusterrole.yaml. Any future RBAC changes must update both files. The review pattern `sync-rbac-markers-with-helm-clusterrole-templates.md` should be extended post-implementation.\n- **Webhook incompatibility**: MutatingWebhookConfiguration and ValidatingWebhookConfiguration are cluster-scoped Kubernetes resources. In namespace-scoped mode, the operator cannot create/manage them with only namespace-level RBAC. Documentation must clearly recommend `webhook.enabled: false`.\n- **CRDs remain cluster-scoped**: CRD installation is always a cluster-admin task. The operator does not install CRDs itself, so this is not a functional issue, but documentation must clarify this.\n- **Flag global state**: `flag.Parse()` uses global state. The `cacheOptions` helper is extracted as a pure function to enable unit testing without flag side effects.\n- **Existing test count**: clusterrole_test.yaml asserts `count: 14` for rules. The new role_test.yaml must assert the same count.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"internal/common/bootstrap/manager_test.go\" test-function=\"TestCacheOptions_withNamespace\" requirement-id=\"REQ-001\">cacheOptions with namespace='tenant-a' returns cache.Options with DefaultNamespaces map containing 'tenant-a' key</spec>\n    <spec test-file=\"internal/common/bootstrap/manager_test.go\" test-function=\"TestCacheOptions_withoutNamespace\" requirement-id=\"REQ-001\">cacheOptions with empty namespace returns cache.Options with nil DefaultNamespaces</spec>\n    <spec test-file=\"internal/common/bootstrap/manager_test.go\" test-function=\"TestCacheOptions_syncPeriodPreserved\" requirement-id=\"REQ-002\">cacheOptions always sets SyncPeriod regardless of namespace value</spec>\n    <spec test-file=\"internal/common/bootstrap/manager_test.go\" test-function=\"TestCacheOptions_singleNamespaceEntry\" requirement-id=\"REQ-002\">DefaultNamespaces map has exactly one entry when namespace is set, with empty cache.Config value</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/role_test.yaml\" test-function=\"should render Role when rbac.namespaceScoped is true\" requirement-id=\"REQ-004\">Role resource is rendered with kind: Role, correct metadata labels, and fullname</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/role_test.yaml\" test-function=\"should not render Role when rbac.namespaceScoped is false\" requirement-id=\"REQ-004\">No Role document is produced with default values</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/role_test.yaml\" test-function=\"should have 14 rules matching ClusterRole\" requirement-id=\"REQ-004\">Role rules array has exactly 14 entries with all apiGroup rules from ClusterRole</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/rolebinding_test.yaml\" test-function=\"should render RoleBinding referencing Role when namespaceScoped is true\" requirement-id=\"REQ-005\">RoleBinding has roleRef.kind=Role, roleRef.name=fullname, correct ServiceAccount subject</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/rolebinding_test.yaml\" test-function=\"should not render RoleBinding when namespaceScoped is false\" requirement-id=\"REQ-005\">No RoleBinding document is produced with default values</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/clusterrole_test.yaml\" test-function=\"should not render ClusterRole when rbac.namespaceScoped is true\" requirement-id=\"REQ-006\">ClusterRole template produces zero documents when rbac.namespaceScoped=true</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/clusterrolebinding_test.yaml\" test-function=\"should not render ClusterRoleBinding when rbac.namespaceScoped is true\" requirement-id=\"REQ-006\">ClusterRoleBinding template produces zero documents when rbac.namespaceScoped=true</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should include --namespace arg when rbac.namespaceScoped is true\" requirement-id=\"REQ-007\">Container args include --namespace=NAMESPACE alongside --leader-elect and other flags</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should not include --namespace arg when rbac.namespaceScoped is false\" requirement-id=\"REQ-007\">Container args remain unchanged from current defaults (no --namespace)</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should include both --leader-elect and --namespace when both enabled\" requirement-id=\"REQ-007\">Container args include --leader-elect, --namespace=NAMESPACE, --metrics-bind-address, --health-probe-bind-address</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Cluster admin deploys operator with default cluster-wide RBAC\">As a cluster admin, I want the operator to deploy with ClusterRole and ClusterRoleBinding by default, so that existing deployments continue working without configuration changes and the operator can manage resources across all namespaces</story>\n    <story title=\"Platform engineer deploys operator with namespace-scoped RBAC\">As a platform engineer, I want to deploy the operator scoped to a single namespace using rbac.namespaceScoped=true, so that the operator only has permissions within its namespace, enabling secure multi-tenant isolation</story>\n    <story title=\"Developer uses --namespace flag in bootstrap to restrict cache\">As a developer, I want the shared bootstrap package to accept a --namespace flag that restricts the controller-runtime cache to a single namespace, so that both keystone and c5c3 operators can run in namespace-scoped mode without code changes to their main.go</story>\n    <story title=\"Platform operator follows multi-tenant deployment documentation\">As a platform operator, I want documentation explaining when and how to use namespace-scoped mode with known caveats, so that I can correctly configure the operator for multi-tenant environments without hitting known pitfalls</story>\n    <story title=\"CI validates both RBAC modes produce valid manifests\">As a developer, I want the CI pipeline to validate helm template rendering for both cluster-wide and namespace-scoped configurations, so that regressions in either RBAC mode are caught before merge</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The bootstrap package SHALL accept a --namespace string flag alongside existing flags (--metrics-bind-address, --health-probe-bind-address, --leader-elect, --sync-period)</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">When --namespace is set, the controller-runtime cache SHALL be restricted to that single namespace via cache.Options.DefaultNamespaces</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The Helm chart SHALL provide an rbac.namespaceScoped toggle in values.yaml, defaulting to false</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">When rbac.namespaceScoped is true, the chart SHALL render a namespace-scoped Role with identical rules to the existing ClusterRole (all 14 rule blocks)</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">When rbac.namespaceScoped is true, the chart SHALL render a RoleBinding binding the ServiceAccount to the namespace-scoped Role</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">When rbac.namespaceScoped is false (default), the chart SHALL render ClusterRole and ClusterRoleBinding; when true, they SHALL NOT be rendered</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">When rbac.namespaceScoped is true, the Deployment SHALL include --namespace={{ .Release.Namespace }} as a container arg</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The project SHALL include documentation describing the multi-tenant deployment pattern with namespace-scoped RBAC, including the webhook limitation caveat</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 15385,
  "caller": "run_claude_cli"
}