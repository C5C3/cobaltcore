{
  "timestamp": "2026-04-15T09:58:24.308108+00:00",
  "feature_id": "CC-0075",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0075",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0075\">\n  <title>Add topologySpreadConstraints and PriorityClass support</title>\n  <description># GitHub Issue #207: Add topologySpreadConstraints and PriorityClass support to the Keystone Deployment\n\n**Category**: improvement | **Scope**: Small\n\n**Description**: `buildKeystoneDeployment` (operators/keystone/internal/controller/reconcile_deployment.go:150) currently produces a Deployment whose pod template has neither `topologySpreadConstraints`, nor `affinity`, nor `priorityClassName`. With the default `replicas: 3` plus the `minAvailable: 1` PDB (CC-0037, reconcile_deployment.go:284-307), nothing prevents the scheduler from coalescing all three Keystone pods onto the same node, so a single node reboot, kernel panic, or kubelet restart still produces a total Keystone outage despite the apparent 3-way HA. Add (1) sensible default `TopologySpreadConstraints` to the generated pod template — `maxSkew: 1` across `topology.kubernetes.io/zone` and `kubernetes.io/hostname`, both with `whenUnsatisfiable: ScheduleAnyway` so that the defaults never deadlock single-node dev clusters — and (2) an optional `spec.priorityClassName` field on the Keystone CR that is wired through to the pod template when set. Both should be overridable via new optional fields on the CRD: `spec.topologySpreadConstraints` and `spec.priorityClassName`.\n\n**Motivation**: Keystone is the gating dependency for every other OpenStack service, so an unavailable Keystone takes down the entire control plane. The current setup looks HA on paper (3 replicas + PDB) but provides no anti-coalescing guarantees: a freshly-scheduled CR on a small cluster regularly lands all three pods on the same node, and even on multi-node clusters the scheduler has no signal to spread them. PriorityClass support is needed so operators can mark Keystone with a high-priority class on saturated control-plane nodes, ensuring it wins eviction races against tenant workloads. The change is intentionally small (~30 lines of generation code plus a CRD field and webhook validation) and unlocks a meaningful step up in production stability.\n\n**Affected Areas**:\n- `operators/keystone/api/v1alpha1/keystone_types.go`\n- `operators/keystone/api/v1alpha1/keystone_webhook.go`\n- `operators/keystone/internal/controller/reconcile_deployment.go`\n- `operators/keystone/internal/controller/reconcile_deployment_test.go`\n- `operators/keystone/helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml`\n- `operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml`\n- `architecture/docs/04-architecture/04-high-availability.md`\n\n**Acceptance Criteria**:\n- [ ] `KeystoneSpec` gains an optional `priorityClassName string` field\n- [ ] `KeystoneSpec` gains an optional `topologySpreadConstraints []corev1.TopologySpreadConstraint` field\n- [ ] `buildKeystoneDeployment` sets default `TopologySpreadConstraints` of `maxSkew: 1` over `topology.kubernetes.io/zone` and `kubernetes.io/hostname`, both `whenUnsatisfiable: ScheduleAnyway`, with `LabelSelector` matching the Deployment's selector labels\n- [ ] When `spec.topologySpreadConstraints` is set, it fully replaces the defaults (no merging surprises)\n- [ ] When `spec.priorityClassName` is set, it is applied to the pod template; otherwise the field is left empty so admission control does not reject pods outside `kube-system`\n- [ ] The validating webhook rejects a `priorityClassName` that does not exist in the cluster (lookup against `scheduling.k8s.io/v1 PriorityClass`) and rejects topology constraints whose label selector does not match the Deployment selector\n- [ ] Unit tests in `reconcile_deployment_test.go` cover: defaults applied, override replaces defaults, `priorityClassName` set/unset, webhook validation paths\n- [ ] CRD manifests in both `helm/.../crds/` and `config/crd/bases/` are regenerated and committed\n- [ ] `architecture/docs/04-architecture/04-high-availability.md` documents the new defaults and the override fields, including a recommended `PriorityClass` definition operators should ship alongside Keystone for production\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/207</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add priorityClassName and topologySpreadConstraints fields to KeystoneSpec, and wire them into buildKeystoneDeployment with default TSCs (REQ-001, REQ-002, REQ-003)</task>\n    <task status=\"pending\">1.2 Add unit tests for deployment builder: default TSCs, custom TSCs override, priorityClassName set/unset (REQ-001, REQ-002, REQ-003)</task>\n    <task status=\"pending\">2.1 Inject client.Reader into KeystoneWebhook, add PriorityClass and TSC label selector validation (REQ-004, REQ-005, REQ-006)</task>\n    <task status=\"pending\">2.2 Update main.go and integration test helpers to pass client to KeystoneWebhook (REQ-006)</task>\n    <task status=\"pending\">2.3 Add webhook unit tests for PriorityClass validation and TSC label selector validation (REQ-004, REQ-005)</task>\n    <task status=\"pending\">3.1 Regenerate CRD manifests and sync Helm CRDs (REQ-007)</task>\n    <task status=\"pending\">4.1 Update HA architecture documentation with topology spread defaults and PriorityClass recommendation (REQ-008)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\n```\nKeystoneSpec (types.go)\n├── priorityClassName: string         ← NEW\n└── topologySpreadConstraints: []TSC  ← NEW\n        │\n        ▼\nbuildKeystoneDeployment (reconcile_deployment.go)\n├── defaultTopologySpreadConstraints() ← NEW helper\n├── PodSpec.TopologySpreadConstraints  ← wired from spec or defaults\n└── PodSpec.PriorityClassName          ← wired from spec\n        │\n        ▼\nKeystoneWebhook.validate() (keystone_webhook.go)\n├── Client client.Reader               ← NEW field for PriorityClass lookup\n├── validate(ctx, k)                   ← ctx added for API calls\n├── PriorityClass existence check      ← NEW validation\n└── TSC LabelSelector match check      ← NEW validation\n```\n\n## Data Flow\n\n1. User submits Keystone CR with optional priorityClassName and topologySpreadConstraints\n2. Mutating webhook defaults (no changes needed — no defaulting for these fields)\n3. Validating webhook checks: PriorityClass exists (API lookup), TSC label selectors match deployment selector\n4. Reconciler builds Deployment: if spec.topologySpreadConstraints is nil, apply defaults; otherwise use spec values. Wire priorityClassName directly.\n5. Deployment applied to cluster; scheduler uses TSCs and PriorityClass for placement decisions.\n\n## Key Decisions\n\n- **ScheduleAnyway not DoNotSchedule**: Default TSCs use ScheduleAnyway so single-node dev clusters are not deadlocked. Production operators who need strict spreading can override.\n- **Full replacement, no merging**: When spec.topologySpreadConstraints is set, defaults are fully replaced. This avoids surprises where users cannot remove a default.\n- **client.Reader not client.Client**: The webhook only needs read access (Get). Using the Reader interface minimizes the surface area.\n- **No nil guard on Client**: If priorityClassName is set but Client is nil (test misconfiguration), the code panics — this is intentional fail-fast behavior.\n- **Label selector validation in webhook**: The webhook computes expected selector labels directly ({app.kubernetes.io/name: keystone, app.kubernetes.io/instance: k.Name}) rather than importing selectorLabels() from the controller package, maintaining package boundaries.\n\n## Pitfalls\n\n- All 3 webhook instantiation sites (main.go, 2 integration tests) must be updated to inject the client. Missing any one causes nil panic when PriorityClass validation runs.\n- The validate() signature change (adding ctx) requires updating both ValidateCreate and ValidateUpdate method bodies.\n- CRD regeneration (make manifests) must happen AFTER types.go changes are complete, or the YAML will be stale.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_DefaultTopologySpreadConstraints\" requirement-id=\"REQ-001\">[unit] When spec.TopologySpreadConstraints is nil, the built Deployment should have exactly 2 TSCs: zone (maxSkew=1, ScheduleAnyway) and hostname (maxSkew=1, ScheduleAnyway), each with LabelSelector matching selectorLabels</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_DefaultTopologySpreadConstraints_LabelSelector\" requirement-id=\"REQ-001\">[unit] Default TSC LabelSelector.MatchLabels must equal {app.kubernetes.io/name: keystone, app.kubernetes.io/instance: test-keystone}</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_CustomTopologySpreadConstraintsOverrideDefaults\" requirement-id=\"REQ-002\">[unit] When spec.TopologySpreadConstraints is set to a single custom TSC, the Deployment should contain only that TSC and no defaults</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_EmptyTopologySpreadConstraintsDisablesDefaults\" requirement-id=\"REQ-002\">[unit] When spec.TopologySpreadConstraints is an empty non-nil slice, the Deployment should have no TopologySpreadConstraints</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PriorityClassNameSet\" requirement-id=\"REQ-003\">[unit] When spec.PriorityClassName is 'system-cluster-critical', pod template PriorityClassName equals 'system-cluster-critical'</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PriorityClassNameEmpty\" requirement-id=\"REQ-003\">[unit] When spec.PriorityClassName is empty, pod template PriorityClassName is empty string</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_PriorityClassNameNotFoundRejected\" requirement-id=\"REQ-004\">[unit] Webhook rejects spec.priorityClassName referencing a non-existent PriorityClass, error contains 'priorityClassName'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_PriorityClassNameExistsAccepted\" requirement-id=\"REQ-004\">[unit] Webhook accepts spec.priorityClassName when the referenced PriorityClass exists in the fake client</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_PriorityClassNameEmptyAccepted\" requirement-id=\"REQ-004\">[unit] Webhook accepts empty spec.priorityClassName without performing a PriorityClass lookup</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_TopologySpreadConstraintCorrectLabelsAccepted\" requirement-id=\"REQ-005\">[unit] Webhook accepts custom TSC whose LabelSelector.MatchLabels matches the expected deployment selector</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_TopologySpreadConstraintWrongLabelsRejected\" requirement-id=\"REQ-005\">[unit] Webhook rejects custom TSC whose LabelSelector.MatchLabels does not match expected labels, error contains 'topologySpreadConstraints'</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_TopologySpreadConstraintNilSelectorRejected\" requirement-id=\"REQ-005\">[unit] Webhook rejects custom TSC with nil LabelSelector, error contains 'labelSelector'</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator deploys Keystone with default HA topology spreading\">As a platform operator, I want Keystone pods to automatically spread across zones and nodes when I deploy a Keystone CR without specifying topology constraints, so that a single node or zone failure does not take down all Keystone pods, providing genuine HA beyond the 3-replica + PDB paper guarantee</story>\n    <story title=\"Operator overrides topology spread constraints with custom values\">As a platform operator, I want to replace the default topology spread constraints with my own set when I specify spec.topologySpreadConstraints on the Keystone CR, so that I can adapt spreading behavior to my cluster topology (e.g., single-zone, rack-aware) without merging surprises</story>\n    <story title=\"Operator assigns PriorityClass to Keystone pods\">As a platform operator, I want to set spec.priorityClassName on the Keystone CR so that Keystone pods are assigned a PriorityClass, so that Keystone pods win eviction races against lower-priority workloads on saturated control-plane nodes</story>\n    <story title=\"Operator receives clear validation errors for invalid scheduling configuration\">As a platform operator, I want the validating webhook to reject invalid priorityClassName references and mismatched label selectors with clear field-path error messages, so that I catch configuration errors at admission time before they reach the scheduler</story>\n    <story title=\"Operator reads HA documentation for topology and priority configuration\">As a platform operator, I want the HA architecture documentation to describe the default topology spread behavior, the override mechanism, and a recommended PriorityClass definition, so that I can make informed decisions about production scheduling configuration</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The system SHALL apply default TopologySpreadConstraints (maxSkew=1 over topology.kubernetes.io/zone and kubernetes.io/hostname, both ScheduleAnyway) to the Keystone Deployment pod template when spec.topologySpreadConstraints is nil</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The system SHALL fully replace default TopologySpreadConstraints with user-provided values when spec.topologySpreadConstraints is set (non-nil), with no merging of defaults</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The system SHALL set the pod template PriorityClassName to spec.priorityClassName when that field is non-empty</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The validating webhook SHALL reject a spec.priorityClassName that does not correspond to an existing scheduling.k8s.io/v1 PriorityClass in the cluster</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The validating webhook SHALL reject custom TopologySpreadConstraints whose LabelSelector does not match the Deployment's selector labels</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The KeystoneWebhook struct SHALL accept a client.Reader for cluster-scoped resource lookups, and validate() SHALL accept a context.Context parameter</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The system SHALL regenerate CRD manifests in both config/crd/bases/ and helm/.../crds/ after adding the new fields</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The HA architecture documentation SHALL document the default TopologySpreadConstraints, the override mechanism, and a recommended PriorityClass definition</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17369,
  "caller": "run_claude_cli"
}