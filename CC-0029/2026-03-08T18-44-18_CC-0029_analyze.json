{
  "timestamp": "2026-03-08T18:44:18.037650+00:00",
  "feature_id": "CC-0029",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0029",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0029\">\n  <title>Add CycloneDX SBOM and Sigstore attestation to CI</title>\n  <description>**Size:** 📦 medium\n**Category:** infrastructure\n**Priority:** high\n**Source:** Proposed for 'From the start, SBOMs with CycloneDX should be generated for all'\n\nIntegrate SBOM generation and attestation into `.github/workflows/build-images.yaml` for all container images:\n\n1. **`build-base-images` job**: Add `id-token: write` and `attestations: write` permissions. After `build-python-base` and `build-venv-builder` push steps, add two new step pairs each:\n   - `anchore/sbom-action` (SHA-pinned) scanning the pushed image by digest (`steps.build-python-base.outputs.digest` / `steps.build-venv-builder.outputs.digest`), outputting CycloneDX 1.5 JSON (`format: cyclonedx-json`)\n   - `actions/attest-sbom@v2` (SHA-pinned) attesting the SBOM via Sigstore keyless OIDC and pushing attestation to GHCR (`push-to-registry: true`)\n   - Both steps guarded by `if: github.event_name != 'pull_request'`\n\n2. **`build-service-images` job**: Same permissions additions. After the `build-service` step, add one SBOM generation + attestation step pair referencing `steps.build-service.outputs.digest` and `matrix.service` for naming. Same PR-skip guard.\n\n3. **SHA-pinning**: Both new actions must be SHA-pinned with version comments per existing convention enforced by `verify_build_images_workflow.sh`.\n\n4. **Verification tests**: Update `tests/container-images/verify_build_images_workflow.sh` with new test cases validating: SBOM generation steps exist with `anchore/sbom-action` and correct `format: cyclonedx-json` parameter; attestation steps exist with `actions/attest-sbom` and `push-to-registry: true`; `id-token: write` and `attestations: write` permissions on both build jobs; `if: github.event_name != 'pull_request'` guard on all SBOM/attestation steps. Use existing assertion helpers from `tests/lib/assertions.sh`.\n\n**Rationale:** Container images published to GHCR currently lack any software bill of materials or cryptographic attestation, leaving consumers unable to verify image contents or provenance. The architecture doc (`architecture/docs/08-container-images/04-sbom.md`) mandates CycloneDX SBOMs with Sigstore attestation. Without this, the project cannot meet supply chain security requirements, and downstream consumers have no tamper-evident way to audit image dependencies. This is a prerequisite for any future vulnerability scanning or compliance reporting from SBOMs.\n\n**Affected Areas:**\n- .github/workflows/build-images.yaml\n- tests/container-images/verify_build_images_workflow.sh\n- architecture/docs/08-container-images/04-sbom.md</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add SBOM permissions and generation/attestation steps for python-base and venv-builder in build-base-images job (REQ-010, REQ-011, REQ-012, REQ-013, REQ-014, REQ-015, REQ-016)</task>\n    <task status=\"pending\">1.2 Add SBOM permissions and generation/attestation steps for service images in build-service-images job, including image output in tags step (REQ-010, REQ-011, REQ-012, REQ-013, REQ-014, REQ-015, REQ-016)</task>\n    <task status=\"pending\">2.1 Add verification tests for SBOM/attestation job permissions (id-token: write, attestations: write) on both build jobs in verify_build_images_workflow.sh (REQ-012, REQ-017)</task>\n    <task status=\"pending\">2.2 Add verification tests for SBOM generation steps (anchore/sbom-action existence, format: cyclonedx-json, digest references) in verify_build_images_workflow.sh (REQ-010, REQ-015, REQ-017)</task>\n    <task status=\"pending\">2.3 Add verification tests for attestation steps (actions/attest-sbom existence, push-to-registry: true) and PR-skip guard on all SBOM/attestation steps in verify_build_images_workflow.sh (REQ-011, REQ-013, REQ-016, REQ-017)</task>\n    <task status=\"pending\">3.1 Update docs/reference/build-images-workflow.md with SBOM generation, attestation steps, required permissions, and PR behavior documentation (REQ-010, REQ-011, REQ-012)</task>\n\n  </tasks>\n  <implementation-notes>Architectural decisions:\n\n1. **Step placement**: SBOM generation steps go immediately after each build-push-action step, followed by the corresponding attestation step. In build-base-images: python-base SBOM+attest after build-python-base, venv-builder SBOM+attest after build-venv-builder. In build-service-images: service SBOM+attest after build-service (before the inline PR verification step).\n\n2. **Image owner for service images**: build-service-images lacks a normalized owner output (unlike build-base-images which has steps.meta.outputs.owner). Extend the existing 'Derive tags' step (id: tags) to also output `image` — the `ghcr.io/${IMAGE_OWNER}/${MATRIX_SERVICE}` path already computed as the `IMAGE` variable. Add `echo \"image=${IMAGE}\" >> \"$GITHUB_OUTPUT\"` and reference as `steps.tags.outputs.image` in SBOM/attestation `with:` parameters.\n\n3. **Output file naming**: Use distinct filenames to avoid collisions within a job: `sbom-python-base.cyclonedx.json` and `sbom-venv-builder.cyclonedx.json` for base images; `sbom-${{ matrix.service }}.cyclonedx.json` for service images. Using `matrix.service` in `with:` context is safe (not a shell injection vector).\n\n4. **SHA pins**: Look up the latest release commit SHAs at implementation time:\n   - anchore/sbom-action: check https://github.com/anchore/sbom-action/tags for latest v0.x release tag, get full commit SHA\n   - actions/attest-sbom: check https://github.com/actions/attest-sbom/tags for latest v2.x release tag, get full commit SHA\n   Pin format: `uses: <org>/<action>@<40-char-sha> # v<version>` (must match test_actions_pinned_to_sha and test_actions_have_version_comments expectations)\n\n5. **Expression injection safety**: The new SBOM/attestation steps use `with:` parameters (YAML values in GitHub Actions expression context), not `run:` blocks, so shell injection is not a concern. The only `run:` block change is adding an `echo` to the existing tags step, which already uses env vars per the expression injection defense pattern.\n\n6. **Test structure**: Follow the existing pattern in verify_build_images_workflow.sh — one bash function per test concern, using yq_raw() for YAML queries and assert_* helpers for assertions. Add CC-0029 reference in test function echo headers. Add test function calls to the 'Run all tests' section at the bottom of the file.\n\n7. **Permissions scope**: id-token: write and attestations: write are added only to the two build jobs (build-base-images, build-service-images). Verification jobs (verify-base-images, verify-service-images) retain their minimal permissions. The test should verify this boundary.\n\nPotential pitfalls:\n- Forgetting to add new test function calls to the run-all-tests section at the bottom of verify_build_images_workflow.sh\n- Using an anchore/sbom-action parameter name that differs from the pinned version's action.yml\n- Not adding the image output to the tags step in build-service-images, leaving SBOM/attestation steps without a lowercase image name for GHCR</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_permissions_on_build_base_images\" requirement-id=\"REQ-012\">build-base-images job has id-token: write and attestations: write permissions</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_permissions_on_build_service_images\" requirement-id=\"REQ-012\">build-service-images job has id-token: write and attestations: write permissions</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_verify_jobs_no_sbom_permissions\" requirement-id=\"REQ-012\">verify-base-images and verify-service-images jobs do NOT have id-token or attestations permissions (least privilege)</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_generation_steps_exist\" requirement-id=\"REQ-010\">SBOM generation steps using anchore/sbom-action exist in both build-base-images (2 steps for python-base and venv-builder) and build-service-images (1 step)</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_format_cyclonedx_json\" requirement-id=\"REQ-010\">All SBOM generation steps specify format: cyclonedx-json</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_generation_references_digest\" requirement-id=\"REQ-015\">SBOM generation steps reference the correct digest output (build-python-base.outputs.digest, build-venv-builder.outputs.digest, build-service.outputs.digest)</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_attestation_steps_exist\" requirement-id=\"REQ-011\">Attestation steps using actions/attest-sbom exist in both build-base-images and build-service-images jobs</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_attestation_push_to_registry\" requirement-id=\"REQ-016\">All attestation steps have push-to-registry: true parameter</spec>\n    <spec test-file=\"tests/container-images/verify_build_images_workflow.sh\" test-function=\"test_sbom_steps_pr_skip_guard\" requirement-id=\"REQ-013\">All SBOM generation and attestation steps in both build jobs have if condition containing github.event_name != 'pull_request'</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Image consumer verifies image provenance via SBOM attestation\">As a image consumer, I want to verify the cryptographic attestation of a container image's SBOM before deployment, so that I can trust that the image contents have not been tampered with since build time</story>\n    <story title=\"Image consumer inspects software components via CycloneDX SBOM\">As a image consumer, I want to obtain a CycloneDX 1.5 JSON SBOM listing all software components in a container image, so that I can audit dependencies, check for known vulnerabilities, and meet compliance requirements</story>\n    <story title=\"CI workflow generates SBOMs for all pushed container images\">As a CI/CD maintainer, I want SBOM generation and attestation to be integrated into the existing build-images workflow, so that every image pushed to GHCR automatically receives a signed SBOM without manual intervention</story>\n    <story title=\"PR builds remain unaffected by SBOM generation\">As a developer, I want pull request builds to skip SBOM generation and attestation entirely, so that PR CI time is not increased and no unnecessary attestations are created for ephemeral builds</story>\n    <story title=\"Workflow maintainer validates SBOM configuration via verification tests\">As a workflow maintainer, I want verify_build_images_workflow.sh to validate all SBOM/attestation configuration, so that misconfiguration of SBOM generation or attestation is caught before merge</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The workflow SHALL generate CycloneDX 1.5 JSON SBOMs using anchore/sbom-action for every image pushed to GHCR</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The workflow SHALL attest each SBOM using actions/attest-sbom@v2 with Sigstore keyless OIDC signing</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">The build-base-images and build-service-images jobs SHALL have id-token: write and attestations: write permissions</requirement>\n    <requirement id=\"REQ-013\" priority=\"SHALL\">All SBOM generation and attestation steps SHALL be guarded by if: github.event_name != 'pull_request'</requirement>\n    <requirement id=\"REQ-014\" priority=\"SHALL\">The anchore/sbom-action and actions/attest-sbom actions SHALL be SHA-pinned with version comments</requirement>\n    <requirement id=\"REQ-015\" priority=\"SHALL\">The workflow SHALL generate SBOMs for all three image types: python-base, venv-builder, and service images</requirement>\n    <requirement id=\"REQ-016\" priority=\"SHALL\">The attestation SHALL be pushed to GHCR as an OCI referrer artifact via push-to-registry: true</requirement>\n    <requirement id=\"REQ-017\" priority=\"SHALL\">The verify_build_images_workflow.sh SHALL include test cases validating all SBOM/attestation configuration</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 14453,
  "caller": "run_claude_cli"
}