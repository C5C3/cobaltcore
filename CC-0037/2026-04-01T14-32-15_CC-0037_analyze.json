{
  "timestamp": "2026-04-01T14:32:15.906572+00:00",
  "feature_id": "CC-0037",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0037",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0037\">\n  <title>Add PodDisruptionBudget to Keystone operator</title>\n  <description>**Size:** 🔧 small\n**Category:** backend\n**Priority:** high\n**Source:** Proposed for 'Implement PodDisruptionBudget\n  - In reconcileDeplo'\n\nImplement PDB support in the Keystone operator:\n\n1. **`buildPodDisruptionBudget()`** in `operators/keystone/internal/controller/reconcile_deployment.go`: Construct `policyv1.PodDisruptionBudget` named `{keystone.Name}-api`, using `commonLabels()` on ObjectMeta and `selectorLabels()` on `Spec.Selector.MatchLabels`. Use `minAvailable: 1` when `replicas > 1`, `maxUnavailable: 1` when `replicas == 1` to prevent drain deadlocks.\n\n2. **`EnsurePDB()`** in `internal/common/deployment/deployment.go`: Reusable get-or-create/update helper following the existing `EnsureDeployment`/`EnsureService` contract — set owner reference via `SetControllerReference` on create, patch spec on update, return error only (no readiness bool). Use symmetric DeepCopy-then-normalize before spec comparison per codebase patterns.\n\n3. **RBAC marker** on `Reconcile()` in `operators/keystone/internal/controller/keystone_controller.go`: Add `+kubebuilder:rbac:groups=policy,resources=poddisruptionbudgets,verbs=get;list;watch;create;update;patch;delete` alongside existing markers.\n\n4. **`SetupWithManager` watch** in `keystone_controller.go`: Add `Owns(&policyv1.PodDisruptionBudget{})` so PDB changes trigger reconciliation.\n\n5. **Import** `policyv1 \"k8s.io/api/policy/v1\"` in both controller and deployment helper files.\n\n6. **Call site** in `reconcileDeployment`: Invoke `buildPodDisruptionBudget()` + `EnsurePDB()` after `EnsureService`, before setting the `DeploymentReady` condition.\n\n7. **Tests** in `operators/keystone/internal/controller/reconcile_deployment_test.go`: Add `policyv1` to `deployTestScheme()`. Test PDB creation (owner ref, labels, selector matches Deployment pod selector), correct `minAvailable`/`maxUnavailable` for replica counts >1 and ==1, and update-on-replica-change behavior. Follow existing Gomega BDD patterns with `NewGomegaWithT`.\n\n**Rationale:** Keystone currently has no PodDisruptionBudget. During voluntary disruptions (kubectl drain, node upgrades, cluster maintenance), all Keystone API pods can be evicted simultaneously, causing a complete API outage. With a PDB, Kubernetes guarantees at least one pod remains available during rolling disruptions, providing zero-downtime resilience for the identity service that all other OpenStack components depend on.\n\n**Affected Areas:**\n- operators/keystone/internal/controller/reconcile_deployment.go\n- operators/keystone/internal/controller/reconcile_deployment_test.go\n- operators/keystone/internal/controller/keystone_controller.go\n- internal/common/deployment/deployment.go</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add EnsurePDB helper and unit tests in internal/common/deployment (REQ-004, REQ-006, REQ-007)</task>\n    <task status=\"pending\">2.1 Add buildPodDisruptionBudget function and policyv1 import in reconcile_deployment.go (REQ-002, REQ-003, REQ-006)</task>\n    <task status=\"pending\">2.2 Add PDB call site in reconcileDeployment (REQ-001)</task>\n    <task status=\"pending\">2.3 Add RBAC marker, policyv1 import, and Owns watch in keystone_controller.go (REQ-005, REQ-006)</task>\n    <task status=\"pending\">3.1 Add PDB tests in reconcile_deployment_test.go (REQ-008)</task>\n\n  </tasks>\n  <implementation-notes>Architecture: This feature adds PodDisruptionBudget management to the existing reconcileDeployment sub-reconciler. No new sub-reconciler or condition type is needed — PDB is an operational resource managed alongside the Deployment and Service.\n\nData flow: Keystone CR spec.replicas → buildPodDisruptionBudget() constructs PDB object → EnsurePDB() creates or updates in cluster → Kubernetes scheduler respects PDB during voluntary disruptions.\n\nKey design decisions:\n1. EnsurePDB returns error only (not bool), matching EnsureService — PDB has no readiness concept.\n2. PDB spec is always replaced entirely (existing.Spec = pdb.Spec) matching the codebase pattern in EnsureCronJob. No server-assigned fields to preserve (unlike Service ClusterIP).\n3. minAvailable vs maxUnavailable logic prevents drain deadlock: a PDB with minAvailable=1 on a 1-replica deployment would block all evictions since Kubernetes cannot guarantee availability while evicting the only pod.\n4. Feature comment: // Feature: CC-0037 at the top of new functions.\n\nFiles modified:\n- internal/common/deployment/deployment.go — Add policyv1 import + EnsurePDB function (follows EnsureCronJob pattern from internal/common/job/job.go:110-138)\n- internal/common/deployment/deployment_test.go — Add policyv1 scheme registration + EnsurePDB unit tests (follows TestEnsureService_* pattern)\n- operators/keystone/internal/controller/reconcile_deployment.go — Add policyv1 import + buildPodDisruptionBudget function + call site in reconcileDeployment\n- operators/keystone/internal/controller/keystone_controller.go — Add policyv1 import + RBAC marker + Owns watch\n- operators/keystone/internal/controller/reconcile_deployment_test.go — Add policyv1 scheme registration + PDB test cases\n\nExisting patterns to follow:\n- Builder: buildKeystoneService() at reconcile_deployment.go:245-261 — returns *Type without creating\n- Ensure: EnsureCronJob() at job/job.go:110-138 — simplest error-only ensure pattern (no server-assigned field preservation)\n- Tests: TestEnsureService_creates at deployment_test.go:231-249 — fake client, owner ref verification\n- Controller tests: TestReconcileDeployment_DeploymentAndServiceCreated at reconcile_deployment_test.go:106-128</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"internal/common/deployment/deployment_test.go\" test-function=\"TestEnsurePDB_creates\" requirement-id=\"REQ-007\">When no PDB exists, EnsurePDB creates it with OwnerReferences[0].Name == owner name</spec>\n    <spec test-file=\"internal/common/deployment/deployment_test.go\" test-function=\"TestEnsurePDB_updatesExisting\" requirement-id=\"REQ-007\">When PDB exists with different spec, EnsurePDB updates the spec to match desired</spec>\n    <spec test-file=\"internal/common/deployment/deployment_test.go\" test-function=\"TestEnsurePDB_idempotent\" requirement-id=\"REQ-007\">Calling EnsurePDB twice with same input succeeds with exactly one PDB in namespace</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_PDBCreated\" requirement-id=\"REQ-008\">After reconcileDeployment, PDB test-keystone-api exists with correct OwnerReference to test-keystone</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_PDBLabelsAndSelector\" requirement-id=\"REQ-008\">PDB labels match commonLabels, PDB selector matches Deployment selector (selectorLabels)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_PDBMinAvailableForMultipleReplicas\" requirement-id=\"REQ-008\">With replicas=3, PDB.Spec.MinAvailable == intstr.FromInt32(1) and MaxUnavailable is nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_PDBMaxUnavailableForSingleReplica\" requirement-id=\"REQ-008\">With replicas=1, PDB.Spec.MaxUnavailable == intstr.FromInt32(1) and MinAvailable is nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_PDBUpdatedOnReplicaChange\" requirement-id=\"REQ-008\">Changing replicas from 3 to 1 and re-running reconcileDeployment switches PDB from minAvailable=1 to maxUnavailable=1</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_PDBSelectorMatchesDeployment\" requirement-id=\"REQ-003\">PDB Spec.Selector.MatchLabels is identical to Deployment Spec.Selector.MatchLabels</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildPodDisruptionBudget_BoundaryReplicas2\" requirement-id=\"REQ-002\">With replicas=2, PDB uses minAvailable=1 (boundary: smallest multi-replica value)</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator ensures PDB exists for Keystone API pods during reconciliation\">As a cluster operator, I want the Keystone operator to automatically create and manage a PodDisruptionBudget for the Keystone API deployment, so that voluntary disruptions (node drains, upgrades) cannot evict all API pods simultaneously, preserving identity service availability</story>\n    <story title=\"PDB uses minAvailable when replicas > 1 and maxUnavailable when replicas == 1\">As a cluster operator, I want the PDB to use the correct disruption strategy based on replica count, so that single-replica deployments are not drain-deadlocked (minAvailable=1 blocks all evictions) and multi-replica deployments guarantee at least one pod remains available</story>\n    <story title=\"PDB is updated when Keystone spec changes\">As a cluster operator, I want the operator to reconcile the PDB when the Keystone CR or PDB is modified externally, so that the PDB always reflects the current desired state and manual edits are corrected</story>\n    <story title=\"EnsurePDB is a reusable helper following the existing Ensure* contract\">As a developer, I want a generic EnsurePDB helper in internal/common/deployment that other operators can reuse, so that PDB management is consistent across all operators and follows the established get-or-create/update pattern</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">reconcileDeployment SHALL build and ensure a PodDisruptionBudget named {keystone.Name}-api after ensuring the Service and before evaluating the DeploymentReady condition</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">buildPodDisruptionBudget SHALL set minAvailable=1 when replicas > 1 and maxUnavailable=1 when replicas == 1</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">buildPodDisruptionBudget SHALL use commonLabels() on ObjectMeta.Labels and selectorLabels() on Spec.Selector.MatchLabels</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">EnsurePDB SHALL follow the get-or-create/update contract: SetControllerReference on create, replace Spec on update, return error only</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">keystone_controller.go SHALL include an RBAC marker for policy/poddisruptionbudgets and an Owns watch for PodDisruptionBudget</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">policyv1 \"k8s.io/api/policy/v1\" SHALL be imported in reconcile_deployment.go, keystone_controller.go, and deployment.go</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">EnsurePDB unit tests SHALL cover create with owner ref, update, idempotency, and error scenarios in deployment_test.go</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">Controller-level tests SHALL verify PDB creation, labels, selector, replica-based strategy selection, and update-on-replica-change</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 13350,
  "caller": "run_claude_cli"
}