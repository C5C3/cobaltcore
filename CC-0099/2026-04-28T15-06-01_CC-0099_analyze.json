{
  "timestamp": "2026-04-28T15:06:01.958693+00:00",
  "feature_id": "CC-0099",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0099",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0099\">\n  <title>Restrict mode on mounted Fernet key Secret volumes</title>\n  <description># GitHub Issue #300: Restrict mode on mounted Fernet/credential key Secret volumes to silence Keystone's world-readable key_repository warning\n\n**Category**: bug | **Scope**: Small\n\n**Description**: Every Keystone pod reconciled by the operator emits the following warning on startup:\n\n```\n2026-04-28 14:32:43.466 12 WARNING keystone.common.fernet_utils [None req-f046276d-3ae1-43e5-9729-054a91337e86 - - - - - -] key_repository is world readable: /etc/keystone/fernet-keys\n```\n\nThe check originates upstream in `keystone.common.fernet_utils.FernetUtils._check_key_repository` and fires whenever the directory hosting the Fernet repository carries the world-read bit (`stat.S_IROTH`). The exact same `FernetUtils` class is reused by the credential provider against `/etc/keystone/credential-keys` (`keystone.credential.providers.fernet.Provider`), so the credential repository will emit the analogous warning under identical conditions — the fernet line just lands first in the log on cold start. The defect is symmetric and both repositories must be fixed together.\n\nRoot cause is the volume layout in `operators/keystone/internal/controller/reconcile_deployment.go:246-261`. Both `SecretVolumeSource` blocks are constructed without a `DefaultMode`, so Kubernetes falls back to the volume default — `0644` for projected files and `0755` for the projected directory, both world-readable. Combined with the absence of `PodSecurityContext.FSGroup` on the pod (`reconcile_deployment.go:166-167` sets only `TerminationGracePeriodSeconds`, `TopologySpreadConstraints`, `PriorityClassName`; the container-level `restrictedSecurityContext()` at `security_context.go:21-35` neither does nor can set `fsGroup`), kubelet leaves both projected directories at `0755`.\n\nThe same construction is reused by the rotation CronJob templates:\n\n- `operators/keystone/internal/controller/reconcile_fernet.go:357-378` — `fernet-keys-src` Secret volume with no `DefaultMode`; the init container `cp /fernet-keys-src/* /etc/keystone/fernet-keys/` (`reconcile_fernet.go:314`) targets an `EmptyDir` working copy that inherits a world-readable mode.\n- `operators/keystone/internal/controller/reconcile_credential.go` — symmetric credential rotation CronJob with the same defect.\n\nOperationally this is a noise + defence-in-depth issue. The keystone process runs as UID/GID `42424` (`security_context.go:16,25-26`) and `ReadOnlyRootFilesystem=true` plus the PSS Restricted profile (CC-0045) bound the in-cluster blast radius. But:\n\n- The warning trains operators to ignore `WARNING` lines from `keystone.common.fernet_utils`, masking real issues at the same severity (e.g. owner-mismatch warnings emitted by the same function).\n- A future log-based alert or an upstream promotion of the check to `ERROR` would page on every pod start across the fleet.\n- Mounted Fernet keys are active token-signing material and credential keys protect every persisted EC2/application credential at rest in the Keystone DB; there is no reason any sidecar, ephemeral debug container, or future init container co-scheduled into the pod's mount namespace should be able to read them.\n\n**Motivation**: Reproduced on a single-replica Keystone pod against current `main`. The warning is deterministic, fires on every cold start before `--master` worker fork, and is currently unblocked only by ignoring it. The fix is mechanical:\n\n1. Set `PodSecurityContext.FSGroup = openstackUID` (42424) on the Keystone pod and on both rotation CronJob pods. With FSGroup set, kubelet projects the Secret volume directory at mode `0750` (owner `root`, group `42424`, group-readable but not world-readable) and applies group-read on files via the standard `defaultMode | 050` projection — the openstack process can still read its own keys via the group bit.\n2. Add `DefaultMode: ptr.To(int32(0o400))` on every `fernet-keys` / `credential-keys` `SecretVolumeSource` (effective file mode becomes `0450` after the FSGroup `| 050` projection — owner-read + group-read, no world bits).\n3. Update the rotation init container `cp` invocation to `install -m 0400 /fernet-keys-src/* /etc/keystone/fernet-keys/` (or `cp -p` followed by `chmod 0400`) so the EmptyDir working copy is non-world-readable after the rotation pod hands off to keystone-manage.\n\nBoth Fernet and credential paths share identical builder shape (`reconcile_fernet.go` ↔ `reconcile_credential.go`), so one symmetric fix covers both.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/reconcile_deployment.go` (`buildKeystoneDeployment`, PodSpec at L166-167, Secret volumes at L246-261)\n- `operators/keystone/internal/controller/reconcile_fernet.go` (`fernetRotateCronJob` Pod template L357-378, init container at L314)\n- `operators/keystone/internal/controller/reconcile_credential.go` (symmetric credential rotation CronJob)\n- `operators/keystone/internal/controller/reconcile_deployment_test.go` (currently asserts volume/mount existence at L280-291, L303-315 but no mode assertions)\n- `operators/keystone/internal/controller/reconcile_fernet_test.go`, `reconcile_credential_test.go` (rotation CronJob mode assertions)\n- `docs/reference/keystone/keystone-reconciler.md` (volume table currently lists `defaultMode: 0555` for the rotation scripts ConfigMap but no mode for either key Secret volume)\n\n**Acceptance Criteria**:\n- [ ] `buildKeystoneDeployment` sets `Spec.Template.Spec.SecurityContext.FSGroup = ptr.To(openstackUID)` (reusing the constant from `security_context.go:16`) and `DefaultMode = ptr.To(int32(0o400))` on both the `fernet-keys` and `credential-keys` `SecretVolumeSource`.\n- [ ] Fernet and credential rotation CronJob templates apply the same `FSGroup` on the Pod and the same `DefaultMode` on every `*-keys-src` Secret volume.\n- [ ] The rotation init container preserves a non-world-readable mode on the EmptyDir working copy (`install -m 0400 ...` or `cp -p` + `chmod 0400`), so post-rotation the keystone container still mounts `/etc/keystone/fernet-keys` non-world-readable.\n- [ ] `reconcile_deployment_test.go` asserts `Spec.Template.Spec.SecurityContext.FSGroup == 42424` and `DefaultMode == 0o400` on both Secret volumes in the rendered Deployment.\n- [ ] `reconcile_fernet_test.go` and `reconcile_credential_test.go` assert the same on the rotation CronJob and assert the init-container command argv contains the non-world-readable copy/chmod step.\n- [ ] An integration or chainsaw test scrapes the keystone container log for 30 s after pod-Ready and asserts the `key_repository is world readable` substring is absent for *both* `fernet-keys` and `credential-keys` paths (negative assertion for each, since the fernet line currently masks the credential line).\n- [ ] No regression in `chainsaw/keystone-fernet-rotation` or `chainsaw/keystone-credential-rotation` — the keystone-manage rotation step still reads/writes the working copy under the new permissions.\n- [ ] `docs/reference/keystone/keystone-reconciler.md` documents the chosen mode for both Secret volumes alongside the existing `0555` note for the rotation script ConfigMap, with a one-line rationale referencing Keystone's `_check_key_repository` check.\n\n**Refs**: CC-0045 (PSS Restricted profile / `restrictedSecurityContext`), upstream `keystone/common/fernet_utils.py::_check_key_repository`.\n\n**Labels:** bug\n\n**Labels:** bug\n\n---\nSource: https://github.com/C5C3/forge/issues/300</description>\n  <tasks>\n    <task status=\"pending\">1.1 [RED] Add unit-test assertions in reconcile_deployment_test.go for Pod-level FSGroup=42424 and DefaultMode=0o400 on both `fernet-keys` and `credential-keys` Secret volumes (REQ-001, REQ-002, REQ-008, CC-0099). Tests must fail against the current implementation. WHAT: extend the existing `TestKeystoneController_BuildDeployment*`-style block at L270-315 with new sub-tests `TestBuildKeystoneDeployment_PodSecurityContextSetsFSGroup`, `TestBuildKeystoneDeployment_FernetAndCredentialVolumesSetDefaultMode0400`, `TestBuildKeystoneDeployment_ContainerSecurityContextUnchangedByCC0099`, `TestBuildKeystoneDeployment_NoFSGroupChangePolicyOrUnsupportedFields`. WHERE: operators/keystone/internal/controller/reconcile_deployment_test.go. HOW: reuse the existing reconciler-runner helpers; reference openstackUID via the constant.</task>\n    <task status=\"pending\">1.2 [GREEN] In reconcile_deployment.go buildKeystoneDeployment, add `Spec.Template.Spec.SecurityContext = &corev1.PodSecurityContext{FSGroup: ptr.To(openstackUID)}` and add `DefaultMode: ptr.To(int32(0o400))` to the Secret volume sources for `fernet-keys` (L246-253) and `credential-keys` (L254-261) (REQ-001, REQ-002, CC-0099). Reuse the openstackUID const from security_context.go — do not redeclare. WHAT: minimal edit; do not touch container-level SecurityContext or any other Pod-level field. WHERE: operators/keystone/internal/controller/reconcile_deployment.go (PodSpec at L166, Volumes at L246-261). VERIFY: tests from 1.1 pass.</task>\n    <task status=\"pending\">2.1 [RED] Add unit-test assertions in reconcile_fernet_test.go for Pod-level FSGroup=42424 on the fernet rotation Pod template, DefaultMode=0o400 on `fernet-keys-src` and the read-only `credential-keys` Secret volume, init-container argv contains `0400` + `install`/`chmod`, and rotate-container/init-container SecurityContext bits unchanged (REQ-003, REQ-005, REQ-008, CC-0099). WHERE: operators/keystone/internal/controller/reconcile_fernet_test.go (extend the existing `TestFernetRotationCronJob_*` block around L600-697). New sub-tests: `TestFernetRotationCronJob_PodSecurityContextSetsFSGroup`, `TestFernetRotationCronJob_KeySecretVolumesSetDefaultMode0400`, `TestFernetRotationCronJob_CopyKeysInitContainerPreservesNonWorldReadableMode`, `TestFernetRotationCronJob_RotateContainerVolumeMountsUnchanged`, `TestFernetRotationCronJob_RotationContainerSecurityContextUnchanged`. Tests must fail against the current implementation.</task>\n    <task status=\"pending\">2.2 [GREEN] In reconcile_fernet.go fernetRotationCronJob (L286-403), set `Spec.SecurityContext = &corev1.PodSecurityContext{FSGroup: ptr.To(openstackUID)}` on the Pod template, add `DefaultMode: ptr.To(int32(0o400))` to the `fernet-keys-src` SecretVolumeSource at L361 and to the read-only `credential-keys` SecretVolumeSource at L376, and change the init-container Command at L314 from `cp /fernet-keys-src/* /etc/keystone/fernet-keys/` to `install -m 0400 /fernet-keys-src/* /etc/keystone/fernet-keys/` (REQ-003, REQ-005, CC-0099). VERIFY: tests from 2.1 pass.</task>\n    <task status=\"pending\">3.1 [RED] Add symmetric unit-test assertions in reconcile_credential_test.go for credentialRotationCronJob (REQ-004, REQ-005, REQ-008, REQ-009, CC-0099). New sub-tests: `TestCredentialRotationCronJob_PodSecurityContextSetsFSGroup`, `TestCredentialRotationCronJob_KeySecretVolumesSetDefaultMode0400`, `TestCredentialRotationCronJob_CopyKeysInitContainerPreservesNonWorldReadableMode`, `TestCredentialRotationCronJob_RotateContainerVolumeMountsUnchanged`, `TestCredentialRotationCronJob_RotationContainerSecurityContextUnchanged`. Mirror the structure of the fernet tests added in 2.1; assert against `credential-keys-src`, the read-only `fernet-keys` mount, and `/credential-keys-src` paths.</task>\n    <task status=\"pending\">3.2 [GREEN] In reconcile_credential.go credentialRotationCronJob (L289-407), set `Spec.SecurityContext = &corev1.PodSecurityContext{FSGroup: ptr.To(openstackUID)}` on the Pod template, add `DefaultMode: ptr.To(int32(0o400))` to the `credential-keys-src` SecretVolumeSource at L365 and to the read-only `fernet-keys` SecretVolumeSource at L380, and change the init-container Command at L317 from `cp /credential-keys-src/* /etc/keystone/credential-keys/` to `install -m 0400 /credential-keys-src/* /etc/keystone/credential-keys/` (REQ-004, REQ-005, CC-0099). VERIFY: tests from 3.1 pass.</task>\n    <task status=\"pending\">4.1 [E2E test scaffold] Create chainsaw test directory `tests/e2e/keystone/key-repository-mode/` with `00-keystone-cr.yaml` (minimal Keystone CR cloned from tests/e2e/keystone/basic-deployment/00-keystone-cr.yaml, instance name `keystone-keymode`, namespace `openstack`) (REQ-006, CC-0099). The CR shape must match other tests in tests/e2e/keystone so the existing chainsaw config picks it up.</task>\n    <task status=\"pending\">4.2 [E2E test body] Add `chainsaw-test.yaml` to `tests/e2e/keystone/key-repository-mode/` with steps: (1) apply CR, (2) wait for Ready=True (catch block prints pod logs and events), (3) scrape keystone container log via `kubectl logs <pod> -c keystone --tail=2000 --since=120s`, (4) assert zero matches for `key_repository is world readable: /etc/keystone/fernet-keys` AND zero matches for `key_repository is world readable: /etc/keystone/credential-keys` — separate grep invocations so a regression on either path fails independently with a CC-0099-tagged error message (REQ-006, CC-0099). Mirror the catch/diagnostics shape of tests/e2e/keystone/fernet-rotation/chainsaw-test.yaml.</task>\n    <task status=\"pending\">5.1 Update docs/reference/keystone/keystone-reconciler.md (REQ-007, CC-0099): (a) append `, CC-0099` to the front-matter `feature:` list at L4; (b) in the Deployment Volumes section, add `defaultMode: 0400` rows for `fernet-keys` and `credential-keys` Secret volumes plus a one-liner `Pod fsGroup: 42424` note in the Pod-level table; (c) in the Fernet Rotation CronJob volume table at L1474-1478, append `defaultMode: 0400` for `fernet-keys-src` and the read-only `credential-keys` mount, and add a `fsGroup: 42424` row; (d) repeat (c) for the Credential Rotation CronJob table around L1647; (e) one-line rationale row referencing upstream Keystone's `_check_key_repository`.</task>\n    <task status=\"pending\">6.1 Run `go test ./operators/keystone/internal/controller/...` and confirm REFACTOR pass: all unit tests added in 1.1, 2.1, 3.1 pass; expectRestrictedSecurityContext()-based assertions still hold; no other tests changed (REQ-001..REQ-005, REQ-008, REQ-009, CC-0099). Capture the test count delta in the PR description.</task>\n    <task status=\"pending\">6.2 Verify chainsaw schema lint passes for the new `tests/e2e/keystone/key-repository-mode/chainsaw-test.yaml` via the project's chainsaw-lint job (CI job added in #299). Locally validate via the documented chainsaw CLI invocation referenced in tests/e2e/chainsaw-config.yaml. Fix any schema errors before opening the PR (REQ-006, CC-0099).</task>\n\n  </tasks>\n  <implementation-notes>Architecture: this is a one-field, three-call-site hardening. PodSecurityContext lives at PodSpec level (corev1.PodSpec.SecurityContext, type *corev1.PodSecurityContext) and is orthogonal to the existing container-level SecurityContext (corev1.Container.SecurityContext, type *corev1.SecurityContext) returned by restrictedSecurityContext(). They co-exist; the CC-0045 PSS Restricted contract is enforced at the container level and is not affected by setting Pod-level FSGroup.\n\nData flow (cold start): operator builds Deployment → kubelet schedules pod → projects fernet-keys Secret with DefaultMode=0o400 + FSGroup=42424 chgrp → effective directory 0o750, files 0o450 → keystone process (UID/GID 42424) reads files via the group bit → `_check_key_repository` no longer sees S_IROTH → no WARNING line.\n\nData flow (rotation): operator builds CronJob → kubelet schedules rotation pod with FSGroup=42424 → init container `install -m 0400 /fernet-keys-src/* /etc/keystone/fernet-keys/` writes 0o400-mode files into the EmptyDir owned by openstack:openstack → rotation container runs `keystone-manage fernet_rotate` against the writable EmptyDir → script PATCHes the staging Secret → operator promotes onto production Secret on the watch event → kubelet re-projects production Secret onto the live API pod with new keys at 0o450, no rollout.\n\nKey invariant: FSGroup and DefaultMode must land together. A partial fix that sets DefaultMode=0o400 without FSGroup would block keystone startup because the openstack process would lose group-read access (file would be 0o400 owned by root). The plan binds them in the same task pair (1.1/1.2, 2.1/2.2, 3.1/3.2) to prevent this.\n\nWhy `install -m 0400` over `cp -p && chmod`: single syscall path, fails atomically, no race window during which the EmptyDir is world-readable. BusyBox and coreutils both implement `install` with -m, and the keystone base image ships coreutils via images/python-base/Dockerfile (verified during research). If a future image swap drops `install`, the test asserts the substring `0400` AND either `install` or `chmod` so the fallback `cp + chmod 0400` form would still pass.\n\nWhy a new chainsaw test (key-repository-mode) and not bolting onto fernet-rotation: separation of concerns — the rotation tests verify rotation correctness; the new test verifies the absence of a specific log line. Mixing them entangles failure modes and makes diagnostics harder. The new test is also faster: no rotation step, just apply→Ready→grep.\n\nFollow these existing files for the established style: reconcile_fernet.go:392 for `ptr.To(int32(0o555))` literal; reconcile_fernet_test.go:696-697 for the mode-assertion shape; reconcile_deployment_test.go:280-291 for the volume-by-name matching pattern.\n\nASCII flow:\n\n```\n  Keystone CR -> buildKeystoneDeployment -> Deployment(PodSpec.SecurityContext.FSGroup=42424,\n                                                       Volumes[fernet-keys].Secret.DefaultMode=0o400,\n                                                       Volumes[credential-keys].Secret.DefaultMode=0o400)\n                                              |\n                                              v\n                                            kubelet projects:\n                                              /etc/keystone/fernet-keys      drwxr-x---  root:42424\n                                              /etc/keystone/fernet-keys/0   -r--r-----  root:42424\n                                              (similar for credential-keys)\n                                              |\n                                              v\n                                            keystone-manage runs as 42424:42424\n                                              -> _check_key_repository: stat.S_IROTH not set\n                                              -> no WARNING emitted   (REQ-006 satisfied)\n```\n\nRotation flow:\n\n```\n  CronJob trigger -> Pod scheduled with FSGroup=42424\n      InitContainer copy-keys (UID 42424):\n          install -m 0400 /fernet-keys-src/* /etc/keystone/fernet-keys/\n              -> EmptyDir files: -r-------- 42424:42424\n      Container fernet-rotate (UID 42424):\n          keystone-manage fernet_rotate against /etc/keystone/fernet-keys (writable)\n          PATCH staging Secret with new key set + completed-at annotation\n      Operator watch -> applyRotationOutput -> production Secret updated\n      Kubelet re-projects production Secret onto API pod -> new keys live, no rollout\n```\n\nDeferred items: see deferred_items section.\n\nFeature ID: every new test message, doc reference, and PR/commit reference must include CC-0099.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_PodSecurityContextSetsFSGroup\" requirement-id=\"REQ-001\">deploy.Spec.Template.Spec.SecurityContext != nil and *deploy.Spec.Template.Spec.SecurityContext.FSGroup == int64(42424); all other PodSecurityContext fields nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_FernetAndCredentialVolumesSetDefaultMode0400\" requirement-id=\"REQ-002\">Both `fernet-keys` and `credential-keys` Volume entries have Secret.DefaultMode set to int32(0o400); the unrelated `config` ConfigMap volume has DefaultMode==nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_ContainerSecurityContextUnchangedByCC0099\" requirement-id=\"REQ-008\">expectRestrictedSecurityContext(g, &container) still passes — container-level SecurityContext bits identical to pre-CC-0099 expectations (CC-0045 contract preserved)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetRotationCronJob_PodSecurityContextSetsFSGroup\" requirement-id=\"REQ-003\">podSpec.SecurityContext != nil and *podSpec.SecurityContext.FSGroup == int64(42424); other Pod-level fields nil</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetRotationCronJob_KeySecretVolumesSetDefaultMode0400\" requirement-id=\"REQ-003\">`fernet-keys-src` and the read-only `credential-keys` Secret volumes both carry DefaultMode==int32(0o400); the EmptyDir working copy `fernet-keys` has EmptyDir!=nil and Secret==nil (unchanged)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetRotationCronJob_CopyKeysInitContainerPreservesNonWorldReadableMode\" requirement-id=\"REQ-005\">initContainer.Command is `[]string{\"sh\",\"-c\",<script>}` and <script> contains the substring `0400` AND either `install -m 0400` or `chmod 0400`</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialRotationCronJob_PodSecurityContextSetsFSGroup\" requirement-id=\"REQ-004\">podSpec.SecurityContext != nil and *podSpec.SecurityContext.FSGroup == int64(42424)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialRotationCronJob_KeySecretVolumesSetDefaultMode0400\" requirement-id=\"REQ-004\">`credential-keys-src` and the read-only `fernet-keys` Secret volumes both carry DefaultMode==int32(0o400); EmptyDir `credential-keys` is unchanged</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialRotationCronJob_CopyKeysInitContainerPreservesNonWorldReadableMode\" requirement-id=\"REQ-005\">Init container Command shape `[\"sh\",\"-c\",<script>]` and the script contains 0400 + install/chmod against /credential-keys-src and /etc/keystone/credential-keys</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetRotationCronJob_RotationContainerSecurityContextUnchanged\" requirement-id=\"REQ-008\">expectRestrictedSecurityContext on both the `fernet-rotate` container and the `copy-keys` init container still passes (CC-0045)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialRotationCronJob_RotationContainerSecurityContextUnchanged\" requirement-id=\"REQ-008\">expectRestrictedSecurityContext on both `credential-rotate` and `copy-keys` containers passes</spec>\n    <spec test-file=\"tests/e2e/keystone/key-repository-mode/chainsaw-test.yaml\" test-function=\"step: assert no `key_repository is world readable` for fernet path\" requirement-id=\"REQ-006\">After Ready=True, the keystone container log scraped for 30 s contains zero matches for `key_repository is world readable: /etc/keystone/fernet-keys`; otherwise the step exits non-zero with the offending line and CC-0099 in the message</spec>\n    <spec test-file=\"tests/e2e/keystone/key-repository-mode/chainsaw-test.yaml\" test-function=\"step: assert no `key_repository is world readable` for credential path\" requirement-id=\"REQ-006\">Same scrape contains zero matches for `key_repository is world readable: /etc/keystone/credential-keys`</spec>\n    <spec test-file=\"tests/e2e/keystone/fernet-rotation/chainsaw-test.yaml\" test-function=\"step: existing rotation flow still passes under stricter mode\" requirement-id=\"REQ-009\">Manifest unchanged; the rotation Job completes, Secret hash changes, pod UIDs unchanged, pre-rotation token validates — assertion is the unmodified existing test passing on a build with CC-0099 applied (no test additions, regression guard via no-op)</spec>\n    <spec test-file=\"tests/e2e/keystone/credential-rotation/chainsaw-test.yaml\" test-function=\"step: existing credential rotation flow still passes under stricter mode\" requirement-id=\"REQ-009\">Manifest unchanged; existing credential-rotation chainsaw test still passes</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_NoFSGroupChangePolicyOrUnsupportedFields\" requirement-id=\"REQ-001\">deploy.Spec.Template.Spec.SecurityContext.FSGroupChangePolicy == nil (kubelet default `Always` is intentional — keys are small, the chgrp is cheap, and `OnRootMismatch` would skip projection on rotation when the directory mode is already 0750)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_fernet_test.go\" test-function=\"TestFernetRotationCronJob_RotateContainerVolumeMountsUnchanged\" requirement-id=\"REQ-009\">Number, names, mount paths, and ReadOnly flags of the rotate container's VolumeMounts match the pre-CC-0099 baseline (regression guard for scope-creep onto VolumeMounts)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestCredentialRotationCronJob_RotateContainerVolumeMountsUnchanged\" requirement-id=\"REQ-009\">Number, names, mount paths, ReadOnly flags of credential-rotate VolumeMounts match the baseline</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator silences the world-readable key_repository warning on Keystone pod startup (CC-0099)\">As a operator (oncall), I want the keystone container logs to be free of the recurring `key_repository is world readable` WARNING for both /etc/keystone/fernet-keys and /etc/keystone/credential-keys, so that real WARNING-level signals from keystone.common.fernet_utils stay actionable and a future log-based alert does not page on every pod start</story>\n    <story title=\"Cluster security reviewer can verify Fernet/credential key files are not world-readable on the rendered Pod spec (CC-0099)\">As a security reviewer, I want the rendered Keystone Deployment and rotation CronJob PodSpecs to set a FSGroup of 42424 and DefaultMode 0o400 on every Secret volume that carries Fernet or credential keys, so that I can audit the cluster for defence-in-depth on token-signing and credential-encryption material without reaching into a live pod's mount namespace</story>\n    <story title=\"Operator preserves Fernet/credential key rotation correctness under the stricter file mode (CC-0099)\">As a operator running scheduled rotations, I want the fernet-rotate and credential-rotate CronJobs to keep producing valid rotated keys when the working-copy EmptyDir is non-world-readable, so that the security hardening does not break the existing rotation path or keystone-manage's ability to read/write the working copy</story>\n    <story title=\"Future maintainer reads in the reconciler reference docs which mode applies to which key Secret volume and why (CC-0099)\">As a future maintainer, I want the keystone-reconciler reference doc to document the chosen `fsGroup` and `defaultMode` values for both Fernet and credential key volumes alongside the existing `0555` note for the rotation script ConfigMap, so that I do not regress these values in a future refactor and I understand the upstream Keystone check (`_check_key_repository`) that motivates them</story>\n    <story title=\"Developer onboarding to the rotation path can read the test asserting the install/chmod step in the init container argv (CC-0099)\">As a developer onboarding to the rotation path, I want the existing rotation CronJob unit tests to spell out the exact init-container shell argv that preserves the non-world-readable mode on the EmptyDir working copy, so that a future change to `cp` semantics or a switch to a different copy mechanism cannot silently regress the file mode</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">buildKeystoneDeployment SHALL set Spec.Template.Spec.SecurityContext.FSGroup to ptr.To(openstackUID) (42424) so kubelet projects every Secret volume directory at mode 0750 instead of 0755 (CC-0099)</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">buildKeystoneDeployment SHALL set DefaultMode to ptr.To(int32(0o400)) on both the `fernet-keys` and `credential-keys` SecretVolumeSource entries (CC-0099)</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">fernetRotationCronJob SHALL set FSGroup=42424 on the Pod template and DefaultMode=0o400 on the `fernet-keys-src` Secret volume; the read-only `credential-keys` Secret volume mounted by the fernet rotate Pod SHALL also carry DefaultMode=0o400 (CC-0099)</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">credentialRotationCronJob SHALL set FSGroup=42424 on the Pod template and DefaultMode=0o400 on the `credential-keys-src` Secret volume; the read-only `fernet-keys` Secret volume mounted by the credential rotate Pod SHALL also carry DefaultMode=0o400 (CC-0099)</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">Both rotation CronJobs' `copy-keys` init container SHALL preserve a non-world-readable mode (0o400) on the EmptyDir working copy after the cp step (CC-0099)</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">A chainsaw E2E test SHALL assert that the keystone container log emits zero matches for the substring `key_repository is world readable` against both the fernet and credential key paths within 30 s after pod-Ready (CC-0099)</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The Keystone reconciler reference doc SHALL document `fsGroup: 42424` on the Pod and `defaultMode: 0400` on every Fernet/credential key Secret volume (CC-0099)</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The change SHALL preserve the existing PSS Restricted contract: container-level SecurityContext returned by restrictedSecurityContext() remains unchanged for the keystone API container, the fernet-rotate container, the credential-rotate container, and both `copy-keys` init containers (CC-0099, CC-0045)</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHOULD\">Existing fernet and credential rotation chainsaw E2E tests SHOULD continue to pass without modification under the new file modes (CC-0099)</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 32606,
  "caller": "run_claude_cli"
}