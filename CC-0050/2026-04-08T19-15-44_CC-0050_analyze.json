{
  "timestamp": "2026-04-08T19:15:44.103217+00:00",
  "feature_id": "CC-0050",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0050",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0050\">\n  <title>Refactor CI workflow into reusable scripts and actions</title>\n  <description># GitHub Issue #143: Refactor CI workflow: extract inline scripts into reusable shell scripts and composite actions\n\n**Category**: improvement | **Scope**: Medium\n\n**Description**: `.github/workflows/ci.yaml` has grown to ~885 lines and contains large inline shell blocks that are duplicated across jobs. This makes the workflow hard to read, test locally, and maintain as new operators are added. The inline scripts should be extracted into standalone shell scripts under `hack/` (or a new `scripts/ci/` directory) and repeated step sequences should be consolidated into composite actions under `.github/actions/`.\n\nKey areas of duplication and complexity:\n\n1. **Diagnostic dump steps** — The `kubectl get pods`, `kubectl get events`, `kubectl logs` blocks are copy-pasted across `e2e-infra` (lines 353-366), `e2e-operator` (lines 472-502), and `tempest` (lines 680-696) with minor variations. Extract into a single `hack/ci-dump-diagnostics.sh` script.\n\n2. **Service image build logic** — The sequence of resolving source refs, cloning upstream, applying constraint overrides, and building the python-base → venv-builder → service image chain is duplicated between `e2e-operator` (lines 404-432) and `tempest` (lines 537-557). Extract into a reusable `hack/ci-build-service-image.sh` script.\n\n3. **Infrastructure setup sequence** — The kind cluster + Flux + test deps + deploy-infra pattern is repeated in `e2e-infra`, `e2e-operator`, and `tempest`. Consider a composite action (e.g. `.github/actions/setup-e2e-cluster`) that encapsulates this.\n\n4. **Operator deploy sequence** — CRD install + `kubectl wait` + `helm install` is repeated in `e2e-operator` (lines 456-466) and `tempest` (lines 587-597). Extract into a composite action or script.\n\n5. **Tempest run block** — The Tempest execution step (lines 604-671) is a ~70-line inline script with sed templating, port-forward setup, and a nested `docker run bash -c` block. This should be a standalone script that is easier to test and debug locally.\n\n**Motivation**: As more operators are added (glance, nova, etc.), each new operator will require copy-pasting these inline blocks, increasing the risk of drift between jobs. Standalone scripts can be tested locally with shellcheck and run outside of CI for debugging. The workflow file itself becomes a declarative orchestration layer rather than a container for business logic.\n\n**Affected Areas**:\n- `.github/workflows/ci.yaml`\n- `hack/` (new scripts)\n- `.github/actions/` (new or extended composite actions)\n\n**Acceptance Criteria**:\n- [ ] Diagnostic dump logic is a single shared script called from all E2E jobs\n- [ ] Service image build logic is extracted into a reusable script with parameters for operator name and release\n- [ ] Infrastructure + operator deployment steps are consolidated (composite action or script)\n- [ ] Tempest run block is extracted into a standalone script\n- [ ] CI workflow file is reduced to < 500 lines (orchestration only, no multi-line inline scripts beyond ~5 lines)\n- [ ] All extracted scripts pass shellcheck\n- [ ] No functional change to CI behavior — all existing jobs produce identical results\n- [ ] Extracted scripts are usable for local debugging (e.g. `hack/ci-dump-diagnostics.sh` works against any kubeconfig)\n\n**Labels:** enhancement\n\n---\nSource: https://github.com/C5C3/forge/issues/143</description>\n  <tasks>\n    <task status=\"pending\">1.1 Create hack/ci-dump-diagnostics.sh — shared diagnostic dump script (REQ-001)</task>\n    <task status=\"pending\">1.2 Create hack/ci-build-service-image.sh — reusable service image build script (REQ-002)</task>\n    <task status=\"pending\">1.3 Create hack/ci-deploy-operator.sh — reusable operator deployment script (REQ-003)</task>\n    <task status=\"pending\">1.4 Create hack/ci-run-tempest.sh — CI-specific Tempest wrapper script (REQ-004)</task>\n    <task status=\"pending\">1.5 Create .github/actions/setup-e2e-infra composite action (REQ-005)</task>\n    <task status=\"pending\">2.1 Refactor e2e-infra job in ci.yaml to use extracted scripts and composite action (REQ-001, REQ-005, REQ-006)</task>\n    <task status=\"pending\">2.2 Refactor e2e-operator job in ci.yaml to use extracted scripts and composite action (REQ-001, REQ-002, REQ-003, REQ-005, REQ-006)</task>\n    <task status=\"pending\">2.3 Refactor tempest job in ci.yaml to use extracted scripts and composite action (REQ-001, REQ-002, REQ-003, REQ-004, REQ-005, REQ-006)</task>\n    <task status=\"pending\">3.1 Verify ci.yaml line count and no large inline scripts remain (REQ-006)</task>\n    <task status=\"pending\">3.2 Run shellcheck on all new and existing scripts (REQ-007)</task>\n    <task status=\"pending\">3.3 Update docs/reference/ci-workflow.md to reflect refactored structure (REQ-009)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThis is a pure refactoring of `.github/workflows/ci.yaml` — no behavioral changes.\n\n### Component Relationships\n\n```\n.github/workflows/ci.yaml (orchestration layer, <500 lines)\n  ├── .github/actions/setup-e2e-infra/   (composite: Flux + test-deps + deploy-infra)\n  ├── .github/actions/derive-service-tags/ (existing composite)\n  ├── .github/actions/platform-pair/       (existing composite)\n  ├── hack/ci-dump-diagnostics.sh         (shared diagnostic dump)\n  ├── hack/ci-build-service-image.sh      (service image build chain)\n  ├── hack/ci-deploy-operator.sh          (CRD install + helm deploy)\n  ├── hack/ci-run-tempest.sh              (CI Tempest execution)\n  ├── hack/deploy-infra.sh                (existing, called by composite action)\n  ├── hack/install-test-deps.sh           (existing, called by composite action)\n  ├── hack/run-tempest.sh                 (existing local Tempest script)\n  └── hack/resolve-test-ref.sh            (existing, called by ci-run-tempest.sh)\n```\n\n### Data Flow\n\n1. ci.yaml calls composite action/scripts with env vars (OPERATOR, RELEASE, IMAGE_PREFIX)\n2. Scripts read from releases/ and tests/ directories for configuration\n3. Scripts interact with docker (image builds), kubectl (cluster operations), helm (deployments)\n4. Scripts produce output artifacts in _output/ directory\n5. ci.yaml uploads artifacts via actions/upload-artifact\n\n### Key Decisions\n\n- **hack/ over scripts/ci/**: Use hack/ directory (existing convention) rather than a new scripts/ci/ directory. The issue mentions both; hack/ is where all existing scripts live.\n- **CI-specific Tempest wrapper**: Create hack/ci-run-tempest.sh rather than modifying hack/run-tempest.sh. The local script (run-tempest.sh) has macOS support, timeout handling, and different Docker networking. The CI script matches the current inline block exactly for zero-risk refactoring.\n- **Composite action for infra setup**: The Flux CLI install uses a GitHub Action (fluxcd/flux2/action), which can only be used in a composite action, not in a shell script. This is why setup-e2e-infra is a composite action rather than a script.\n- **Environment variables over positional arguments**: Scripts use env vars (OPERATOR, RELEASE, IMAGE_PREFIX) to match the GitHub Actions convention where env: blocks are natural. This also makes scripts self-documenting when read in ci.yaml.\n\n### Pitfalls\n\n- **Flux action in composite**: The fluxcd/flux2/action must be used as a step within a composite action; it cannot be called from a shell script.\n- **GITHUB_WORKSPACE**: The CI Tempest wrapper uses GITHUB_WORKSPACE for volume mounts. When called from ci.yaml, this is available. When testing locally, users should set it.\n- **shellcheck glob**: The CI shellcheck job uses `hack/*.sh` which automatically includes new scripts. No change needed.\n- **changes job path filter**: hack/ is already in the go_common and e2e_infra path filters, so new hack/*.sh scripts are covered.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"hack/ci-dump-diagnostics.sh\" test-function=\"shellcheck --severity=warning hack/ci-dump-diagnostics.sh [unit]\" requirement-id=\"REQ-001\">shellcheck exits with code 0, no warnings</spec>\n    <spec test-file=\"hack/ci-dump-diagnostics.sh\" test-function=\"bash -n hack/ci-dump-diagnostics.sh (syntax check) [unit]\" requirement-id=\"REQ-001\">Script has valid bash syntax</spec>\n    <spec test-file=\"hack/ci-build-service-image.sh\" test-function=\"shellcheck --severity=warning hack/ci-build-service-image.sh [unit]\" requirement-id=\"REQ-002\">shellcheck exits with code 0, no warnings</spec>\n    <spec test-file=\"hack/ci-build-service-image.sh\" test-function=\"bash -n hack/ci-build-service-image.sh (syntax check) [unit]\" requirement-id=\"REQ-002\">Script has valid bash syntax</spec>\n    <spec test-file=\"hack/ci-build-service-image.sh\" test-function=\"Script exits with error when OPERATOR is unset [unit]\" requirement-id=\"REQ-002\">Running without OPERATOR set results in non-zero exit and error message</spec>\n    <spec test-file=\"hack/ci-deploy-operator.sh\" test-function=\"shellcheck --severity=warning hack/ci-deploy-operator.sh [unit]\" requirement-id=\"REQ-003\">shellcheck exits with code 0, no warnings</spec>\n    <spec test-file=\"hack/ci-deploy-operator.sh\" test-function=\"bash -n hack/ci-deploy-operator.sh (syntax check) [unit]\" requirement-id=\"REQ-003\">Script has valid bash syntax</spec>\n    <spec test-file=\"hack/ci-run-tempest.sh\" test-function=\"shellcheck --severity=warning hack/ci-run-tempest.sh [unit]\" requirement-id=\"REQ-004\">shellcheck exits with code 0, no warnings</spec>\n    <spec test-file=\"hack/ci-run-tempest.sh\" test-function=\"bash -n hack/ci-run-tempest.sh (syntax check) [unit]\" requirement-id=\"REQ-004\">Script has valid bash syntax</spec>\n    <spec test-file=\".github/actions/setup-e2e-infra/action.yaml\" test-function=\"YAML syntax validation [unit]\" requirement-id=\"REQ-005\">action.yaml is valid YAML with required 'name', 'runs' keys and composite type</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"wc -l ci.yaml < 500 [integration]\" requirement-id=\"REQ-006\">ci.yaml has fewer than 500 lines</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"grep for multi-line run blocks > 5 lines [integration]\" requirement-id=\"REQ-006\">No inline run: block exceeds ~5 lines</spec>\n    <spec test-file=\"hack/*.sh\" test-function=\"shellcheck --severity=warning hack/*.sh [integration]\" requirement-id=\"REQ-007\">All scripts in hack/ pass shellcheck</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"Verify job dependency graph unchanged [integration]\" requirement-id=\"REQ-008\">All needs:, if:, and matrix: blocks are identical to pre-refactoring</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"Verify artifact upload names and paths unchanged [integration]\" requirement-id=\"REQ-008\">All upload-artifact steps have same name: and path: values</spec>\n    <spec test-file=\"docs/reference/ci-workflow.md\" test-function=\"Documentation references CC-0050 and new scripts [unit]\" requirement-id=\"REQ-009\">ci-workflow.md mentions CC-0050, hack/ci-dump-diagnostics.sh, hack/ci-build-service-image.sh, hack/ci-deploy-operator.sh, hack/ci-run-tempest.sh, and setup-e2e-infra</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"CI maintainer can read the workflow as a declarative orchestration layer\">As a CI maintainer, I want the ci.yaml workflow file to contain only orchestration logic (job definitions, conditions, matrix strategies) with inline scripts no longer than ~5 lines, so that I can understand the CI pipeline structure at a glance without wading through hundreds of lines of shell code</story>\n    <story title=\"CI maintainer can diagnose E2E failures using a shared diagnostic script\">As a CI maintainer, I want a single hack/ci-dump-diagnostics.sh script called from all E2E jobs (e2e-infra, e2e-operator, tempest), so that diagnostic output is consistent across jobs and I can run the same script locally against any kubeconfig</story>\n    <story title=\"CI maintainer can build service images via a reusable script\">As a CI maintainer, I want the service image build logic (source-ref resolution, upstream clone, constraint overrides, docker build chain) extracted into hack/ci-build-service-image.sh, so that adding a new operator does not require copy-pasting the build sequence and I can test builds locally</story>\n    <story title=\"CI maintainer can deploy an operator via a reusable script\">As a CI maintainer, I want the operator CRD install + kubectl wait + helm install sequence extracted into hack/ci-deploy-operator.sh, so that deploying an operator in E2E tests is a single script call that is consistent between e2e-operator and tempest</story>\n    <story title=\"CI maintainer can run Tempest via the CI workflow using the existing hack/run-tempest.sh\">As a CI maintainer, I want the ~70-line inline Tempest execution block in the tempest job replaced with a call to hack/run-tempest.sh (which already exists), so that Tempest execution logic is maintained in one place and can be tested locally</story>\n    <story title=\"CI maintainer can set up E2E cluster infrastructure via composite action\">As a CI maintainer, I want the repeated sequence of Flux CLI install + test deps install + deploy-infra consolidated into a composite action .github/actions/setup-e2e-infra, so that each E2E job uses a single 'uses: ./.github/actions/setup-e2e-infra' step instead of 3 separate steps</story>\n    <story title=\"All extracted scripts pass shellcheck and CI behavior is unchanged\">As a CI maintainer, I want zero functional changes to CI behavior after the refactoring, so that existing CI jobs produce identical results and nothing breaks during the transition</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The system SHALL extract the diagnostic dump logic from e2e-infra, e2e-operator, and tempest into a single hack/ci-dump-diagnostics.sh script</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The system SHALL extract service image build logic into hack/ci-build-service-image.sh with parameters for operator name, release, and image prefix</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The system SHALL extract operator deployment logic into hack/ci-deploy-operator.sh with parameters for operator name, image repository, and image tag</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The system SHALL replace the inline Tempest execution block in ci.yaml with a call to hack/run-tempest.sh or a CI-specific thin wrapper</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The system SHALL create a composite action .github/actions/setup-e2e-infra that encapsulates the Flux CLI + test deps + deploy-infra sequence</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The CI workflow file SHALL be reduced to under 500 lines containing only orchestration logic</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">All extracted shell scripts SHALL pass shellcheck --severity=warning</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The refactoring SHALL NOT change CI behavior — all existing jobs SHALL produce identical results</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The system SHALL update the ci-workflow.md reference documentation to reflect the refactored structure</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 17440,
  "caller": "run_claude_cli"
}