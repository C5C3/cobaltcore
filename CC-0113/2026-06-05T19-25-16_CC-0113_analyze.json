{
  "timestamp": "2026-06-05T19:25:16.253887+00:00",
  "feature_id": "CC-0113",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0113",
  "model": "claude-opus-4-8",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0113\">\n  <title>Fix TTL-driven re-creation loop for keystone Jobs</title>\n  <description># GitHub Issue #415: fix(keystone-operator): stop the TTL-driven re-creation loop for the bootstrap and schema-check Jobs\n\n## Summary\n\nThe `bootstrap` and `schema-check` Jobs are re-created and re-run roughly every 5 minutes for the lifetime of every Keystone CR, even in steady state. On a quickstart ControlPlane this produces continuous Pod churn (~2 Jobs/Pods every 5 min), event spam, and a periodic burst of reconciles (~40–49/min vs. 3–9/min at rest) that in turn drives a wave of `Status().Update` conflicts (see #361 for the broader reconcile-loop-performance context).\n\nThe bootstrap Job runs `keystone-manage bootstrap`, so each cycle issues real (idempotent) DB writes against the admin user; the schema-check Job issues a DB query each cycle.\n\n## Root cause\n\n`job.RunJob` (`internal/common/job/job.go:63`) is a \"create-if-absent, report-completion\" helper. Its not-found branch (`job.go:67-69`) re-creates the Job whenever it is missing.\n\nBoth Job builders stamp a 5-minute cleanup TTL:\n\n- `buildBootstrapJob` — `TTLSecondsAfterFinished: 300` (`operators/keystone/internal/controller/reconcile_bootstrap.go:139,220`)\n- `buildSchemaCheckJob` — `TTLSecondsAfterFinished: 300` (`operators/keystone/internal/controller/reconcile_database.go:892-894`)\n\nThe loop is then: Job completes → after 300 s the Kubernetes TTL controller deletes it → the `Owns(&batchv1.Job{})` watch (`operators/keystone/internal/controller/keystone_controller.go:715`) delivers the delete event → reconcile → `RunJob` sees `IsNotFound` → re-create. Repeat every ~5 min.\n\n`buildDBSyncJob` (`reconcile_database.go:845`) sets **no** TTL, so its Job lingers, `RunJob` finds it `Complete`, and it is **not** re-created. db-sync is the correct model; bootstrap and schema-check diverge from it.\n\nThis is a systematic pattern, not two isolated cases: every `RunJob` caller that sets a TTL loops. That also includes the policy-validation Job (`reconcile_policyvalidation.go:159`), which only avoids the loop here because no policy overrides are configured — it would loop the moment they are.\n\n## Proposed fix (Option A)\n\nRemove `TTLSecondsAfterFinished` from `buildBootstrapJob` and `buildSchemaCheckJob` (and, consistently, from `buildPolicyValidationJob`), matching the db-sync behaviour. The completed Job then lingers and acts as the state record `RunJob` already relies on; the TTL-driven re-create trigger disappears.\n\nCost: one completed Pod per Job type lingers — negligible; it is GC'd via the owner reference on CR deletion, and replaced on the next genuine re-run.\n\n### Why this does not break admin-password rotation\n\nThis is the critical constraint: `keystone-manage bootstrap` is also how the admin user's DB password is (re)set, so rotation must keep working. It does, because rotation rides a **different** re-create trigger than the TTL:\n\n- `reconcileBootstrap` hashes the admin Secret's `password` and stamps it as a pod-template annotation `forge.c5c3.io/admin-password-hash` (`reconcile_bootstrap.go:38-43,89,223-224`).\n- `PodSpecHash` hashes the full `PodTemplateSpec` incl. annotations (`job.go:44`), so a rotated password changes `forge.c5c3.io/pod-spec-hash`.\n- The rotation re-create lives in the hash-gate at `job.go:78-104`: an existing `Complete` Job whose stored hash differs from the desired hash is deleted and re-created with the new password.\n\nThat gate requires the existing completed Job as the record of the last-applied hash — exactly the record the TTL destroys today. Removing the TTL preserves it. In steady state the TTL only ever re-created the Job with the *same* hash, so the two triggers are cleanly separable:\n\n| Trigger | Re-creates Job | After fix |\n| --- | --- | --- |\n| TTL delete (`job.go:67`) | every 5 min, same hash | **gone** |\n| Hash mismatch / rotation (`job.go:85`) | on real password change | **kept** |\n\nRotation latency is unaffected: a password change still enters via the Secret watch (`keystone_controller.go:731`), not via the TTL.\n\n## Affected tests\n\nThese pin `TTLSecondsAfterFinished == 300` and must be updated to assert the TTL is unset:\n\n- `operators/keystone/internal/controller/reconcile_bootstrap_test.go:421-423`\n- `operators/keystone/internal/controller/reconcile_database_test.go:1581-1582`\n- `operators/keystone/internal/controller/reconcile_policyvalidation_test.go:497-498`\n\n## Design decision (resolved)\n\nNo periodic schema-drift check is wanted. schema-check re-running only on an image/config (hash) change — not every 5 min — is the accepted behaviour. The fix is therefore a plain TTL removal with **no** added `RequeueAfter` drift interval. Intent confirmed; implementation can proceed.\n\n## Out of scope (tracked separately)\n\nThe `Status().Update` conflicts this loop amplifies are a related but distinct hardening item (wrap `updateStatus` in `RetryOnConflict` / optimistic patch). Filing only the loop fix here; happy to split or fold in per maintainer preference.\n\nRelated: #361\n\n\n**Labels:** bug\n\n---\nSource: https://github.com/C5C3/forge/issues/415</description>\n  <tasks>\n    <task status=\"pending\">1.1 Remove TTL from buildBootstrapJob and flip its unit TTL assertion (REQ-001, REQ-006, REQ-007). In operators/keystone/internal/controller/reconcile_bootstrap.go, delete `ttl := int32(300)` (~line 139) and the `TTLSecondsAfterFinished: &ttl,` field (~line 220), keeping BackoffLimit/RestartPolicy/env/volumes intact. In reconcile_bootstrap_test.go TestReconcileBootstrap_JobSpec_TTLAndBackoff (lines 421-423), first change the assertion to expect Spec.TTLSecondsAfterFinished to be nil (RED), then make the source change (GREEN). Keep the backoffLimit==4 and ownerRef assertions.</task>\n    <task status=\"pending\">1.2 Remove TTL from buildSchemaCheckJob, update godoc/comment, and flip its unit TTL assertion (REQ-002, REQ-007, REQ-008). In reconcile_database.go buildSchemaCheckJob, delete `ttl := int32(300)` (~line 892) and `j.Spec.TTLSecondsAfterFinished = &ttl` (~line 894); retain `j.Spec.BackoffLimit = &backoffLimit`. Update the godoc (~line 879) and the inline 'auto-cleanup after 5 min' comment (~line 890) to drop the TTL claim and note the Job lingers as the RunJob state record (CC-0113, #415). In reconcile_database_test.go TestBuildSchemaCheckJob_TTL (lines 1573-1583), flip to assert nil and update its godoc; leave TestBuildSchemaCheckJob_BackoffLimit (==2) intact.</task>\n    <task status=\"pending\">1.3 Remove TTL from buildPolicyValidationJob, update godoc, and flip its unit TTL assertion (REQ-003, REQ-007, REQ-008). In reconcile_policyvalidation.go buildPolicyValidationJob, delete `ttl := int32(300)` (~line 150) and the `TTLSecondsAfterFinished: &ttl,` field (~line 159); keep BackoffLimit==2 and RestartPolicy Never. Update the godoc (~line 145) to drop 'TTL=300'. In reconcile_policyvalidation_test.go TestBuildPolicyValidationJob_BackoffAndTTL (lines 486-501), flip the TTL assertion to nil, keep backoff==2 and RestartPolicy Never, and update the test godoc (~line 487).</task>\n    <task status=\"pending\">2.1 Flip the integration TTL assertion the issue missed (REQ-001, REQ-007). In operators/keystone/internal/controller/integration_test.go TestIntegration_BootstrapJobDetailedSpec (lines 1002-1004), change the assertion to expect bootstrapJob.Spec.TTLSecondsAfterFinished to be nil; keep the backoffLimit==4 and container/image assertions. This site is NOT in the issue's affected-tests list but pins TTL=300 and would otherwise fail the build.</task>\n    <task status=\"pending\">2.2 Confirm and lock admin-password rotation after TTL removal (REQ-004). Verify TestReconcileBootstrap_PasswordChangeRecreatesJob (reconcile_bootstrap_test.go ~line 803) still passes, then extend it (or add TestReconcileBootstrap_RecreatedRotationJob_HasNoTTL) to assert the re-created bootstrap Job has Spec.TTLSecondsAfterFinished == nil and carries the current admin-password-hash — proving rotation rides the pod-spec-hash gate, not the deleted TTL.</task>\n    <task status=\"pending\">2.3 Add steady-state no-recreation regression tests (REQ-005). Add a unit test (e.g. TestReconcileDatabase_CompletedSchemaCheck_SameHash_NotRecreated and a bootstrap counterpart) that seeds a completed Job carrying job.PodSpecHashAnnotation == PodSpecHash(desired template) (mirroring testCompletedSchemaCheckJob), runs reconcile, and asserts RunJob returns done, the Job is not deleted (UID stable), and TTL stays nil. This locks the core promise that steady state no longer churns.</task>\n    <task status=\"pending\">3.1 Update the schema-drift reference doc (REQ-008). In docs/reference/keystone/keystone-schema-drift-detection.md: replace the 'enables ttlSecondsAfterFinished for automatic cleanup' rationale (line ~32) with the lingering-Job/no-loop rationale (cite CC-0113, #415); change the TTLSecondsAfterFinished row (line ~79) and the db-sync-vs-schema-check comparison row (line ~90) to 'not set'; and remove the 'TTL controller will also automatically clean up the Job after 5 minutes' troubleshooting note (line ~280).</task>\n    <task status=\"pending\">3.2 Update the keystone reconciler reference doc (REQ-008). In docs/reference/keystone/keystone-reconciler.md: change the policy-validation Job TTL row (line ~1955) and the bootstrap Job TTL row (line ~2479) to 'not set'; update the bootstrap test-coverage description (line ~3079) so it no longer says 'TTL/backoff'; add a one-line note that completed Jobs linger as the RunJob pod-spec-hash record and are GC'd via ownerReference on CR deletion (CC-0113, #415). Do NOT touch the pre-existing RestartPolicy row discrepancy (see deferred items).</task>\n    <task status=\"pending\">4.1 Verification sweep (REQ-006, REQ-007). Run `go test ./operators/keystone/internal/controller/...` (unit + envtest) and confirm green. Grep operators/keystone for `TTLSecondsAfterFinished` and `int32(300)` and confirm no TTL declaration or assertion remains for the bootstrap, schema-check, or policy-validation Jobs (only buildDBSyncJob's already-TTL-free model remains). Honest blocker note: the production TTL re-creation loop is NOT reproducible in envtest because the TTLAfterFinished controller lives in kube-controller-manager (absent in envtest), so the 'TTL unset' unit/integration assertions plus the steady-state regression test are the regression guard — not a live loop test.</task>\n\n  </tasks>\n  <implementation-notes>ARCHITECTURE DECISION (Option A, resolved): Remove TTLSecondsAfterFinished from the bootstrap, schema-check, and policy-validation Job builders so each completed Job lingers and serves as the state record RunJob already relies on. This deletes the only steady-state re-create trigger (the K8s TTL controller deleting the Job, which the Owns(&batchv1.Job{}) watch turns into a reconcile that hits RunJob's IsNotFound branch). buildDBSyncJob/buildDBJob already set no TTL and are the reference model. No periodic schema-drift RequeueAfter is added (confirmed design decision). The Status().Update conflict hardening is out of scope (#361).\n\nTWO RE-CREATE TRIGGERS (cleanly separable):\n| Trigger | Re-creates Job (today) | After fix |\n| TTL delete (job.go:67 IsNotFound branch) | every ~5 min, SAME hash | gone |\n| Hash mismatch / rotation (job.go:78-104) | only on real image/config/password change | kept |\nRemoving the TTL preserves the completed Job as the last-applied-hash record that the hash gate (and rotation) requires — exactly the record the TTL used to destroy.\n\nTHE LOOP (before) vs FIX (after):\n  Job completes --> [TTL controller deletes after 300s] --> Owns(Job) watch delivers delete\n        ^                                                              |\n        |                                                              v\n  RunJob.IsNotFound re-creates  <-------------------------------  reconcile\n  After fix: no TTL -> no delete -> watch quiet -> RunJob sees Complete (matching hash) -> returns done. No churn.\n\nEXACT EDIT SITES (verified):\n  - reconcile_bootstrap.go: remove `ttl := int32(300)` (~139) and `TTLSecondsAfterFinished: &ttl,` (~220).\n  - reconcile_database.go (buildSchemaCheckJob): remove `ttl := int32(300)` (~892) and `j.Spec.TTLSecondsAfterFinished = &ttl` (~894); keep backoffLimit; update godoc (~879) + inline comment (~890).\n  - reconcile_policyvalidation.go (buildPolicyValidationJob): remove `ttl := int32(300)` (~150) and `TTLSecondsAfterFinished: &ttl,` (~159); keep backoffLimit; update godoc (~145).\n  - Tests to flip to nil: reconcile_bootstrap_test.go:421-423, reconcile_database_test.go:1573-1583, reconcile_policyvalidation_test.go:486-501, AND integration_test.go:1002-1004 (the 4th site, NOT in the issue's list).\n\nPITFALLS:\n  1) Do not remove the backoffLimit lines alongside the TTL lines in buildSchemaCheckJob/buildPolicyValidationJob — `ttl` and `backoffLimit` are adjacent; only the TTL ones go.\n  2) integration_test.go:1003-1004 will fail the build if missed (the issue's affected-tests list is incomplete).\n  3) envtest does NOT run the TTLAfterFinished controller, so the loop is not reproducible there; the unit/integration 'TTL unset' assertions + the steady-state regression test are the guard. State this honestly rather than claiming a live-loop test.\n  4) Keep buildDBSyncJob/buildDBJob untouched — they are already correct.\n  5) Architecture docs under architecture/docs are read-only reference; do not edit them (only docs/ is user-facing/editable).\n\nFEATURE ID: CC-0113 (GitHub issue #415). Use 'CC-0113' (and reference #415) in any new comments, godoc, doc edits, and the eventual commit message per project convention.\n\nTASK-COUNT NOTE: This is a surgical, single-layer (operator Go) fix; 9 well-scoped tasks fully cover it. The 12-task floor was intentionally NOT padded per the 'never invent tasks to hit numbers' rule — every task maps to a real edit or verification.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_JobSpec_TTLAndBackoff\" requirement-id=\"REQ-001\">[unit] After reconcileBootstrap, the created bootstrap Job has Spec.TTLSecondsAfterFinished == nil (BeNil) while BackoffLimit remains 4 and the owner reference is still set.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_BootstrapJobDetailedSpec\" requirement-id=\"REQ-001\">[integration] In envtest, the reconciled bootstrap Job has Spec.TTLSecondsAfterFinished == nil while backoffLimit stays 4 and the container/image assertions still pass.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_TTL\" requirement-id=\"REQ-002\">[unit] buildSchemaCheckJob returns a Job with Spec.TTLSecondsAfterFinished == nil (BeNil); test godoc updated to reflect 'TTL unset'.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_BackoffLimit\" requirement-id=\"REQ-002\">[unit] buildSchemaCheckJob still sets *BackoffLimit == 2 (regression guard that only the TTL was removed).</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_policyvalidation_test.go\" test-function=\"TestBuildPolicyValidationJob_BackoffAndTTL\" requirement-id=\"REQ-003\">[unit] buildPolicyValidationJob returns Spec.TTLSecondsAfterFinished == nil (BeNil) while *BackoffLimit == 2 and RestartPolicy == Never; test godoc updated.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_policyvalidation_test.go\" test-function=\"TestReconcilePolicyValidation_NoOverrides_DeletesJob_Unchanged\" requirement-id=\"REQ-003\">[unit] With nil policyOverrides, reconcilePolicyValidation still deletes any existing validation Job and sets PolicyValidReady=True/PolicyValidationNotRequired (existing behaviour unaffected by the TTL removal; reuse/extend existing nil-overrides test).</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_PasswordChangeRecreatesJob\" requirement-id=\"REQ-004\">[unit] A completed bootstrap Job stamped with the OLD password hash is detected stale, deleted, and re-created with the current admin-password-hash; reconcile requeues with RequeueBootstrapWait. Still passes post-TTL-removal.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_RecreatedRotationJob_HasNoTTL\" requirement-id=\"REQ-004\">[unit] After the rotation re-create path runs, the new bootstrap Job has Spec.TTLSecondsAfterFinished == nil (consistency with REQ-001). Add as a focused assertion or extend the rotation test.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestReconcileDatabase_CompletedSchemaCheck_SameHash_NotRecreated\" requirement-id=\"REQ-005\">[unit] Seed a completed schema-check Job with a matching PodSpecHash annotation; after reconcile, RunJob returns done, the Job is NOT deleted (same UID), and TTLSecondsAfterFinished stays nil.</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_CompletedSameHash_NotRecreated\" requirement-id=\"REQ-005\">[unit] Seed a completed bootstrap Job with a matching hash and the current admin-password-hash; reconcile returns BootstrapReady=True without deleting/re-creating the Job (steady-state regression guard).</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_bootstrap_test.go\" test-function=\"TestReconcileBootstrap_JobSpec_TTLAndBackoff\" requirement-id=\"REQ-006\">[unit] The created bootstrap Job still has exactly one owner reference pointing to the Keystone CR (ownerReference GC path unaffected by TTL removal).</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_database_test.go\" test-function=\"TestBuildSchemaCheckJob_Command\" requirement-id=\"REQ-006\">[unit] schema-check container command remains `/bin/sh -eu -c` with keystone-manage db_sync --check (no collateral change beyond TTL).</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_BootstrapJobDetailedSpec\" requirement-id=\"REQ-007\">[integration] The full envtest reconcile still reaches a Ready Keystone with the bootstrap Job present and TTL unset (no behavioural regression).</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_policyvalidation_test.go\" test-function=\"TestBuildPolicyValidationJob_BackoffAndTTL\" requirement-id=\"REQ-007\">[unit] Confirms no remaining To(Equal(int32(300))) TTL assertion for the policy-validation Job after the flip.</spec>\n    <spec test-file=\"docs/reference/keystone/keystone-schema-drift-detection.md\" test-function=\"doc-verify-no-ttl-claim\" requirement-id=\"REQ-008\">[doc] Manual/grep verification: the schema-check TTLSecondsAfterFinished rows read 'not set', the 'enables ttlSecondsAfterFinished for automatic cleanup' rationale is replaced with the lingering-Job/no-loop rationale, and the troubleshooting note no longer claims a 5-minute TTL cleanup.</spec>\n    <spec test-file=\"docs/reference/keystone/keystone-reconciler.md\" test-function=\"doc-verify-no-ttl-claim\" requirement-id=\"REQ-008\">[doc] Manual/grep verification: the policy-validation (line ~1955) and bootstrap (line ~2479) TTL rows read 'not set' and the bootstrap test-coverage note (line ~3079) no longer says 'TTL/backoff'.</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Cluster operator: steady-state ControlPlane stops churning Jobs/Pods\">As a cluster operator / SRE, I want the bootstrap and schema-check Jobs to stop being re-created and re-run roughly every 5 minutes once a Keystone CR reaches steady state, so that a quickstart ControlPlane stops producing continuous Pod churn (~2 Jobs/Pods per 5 min), event spam, and the periodic reconcile burst (~40-49/min) that amplifies Status().Update conflicts (CC-0113, #415, #361)</story>\n    <story title=\"Operator developer: Job builders follow the consistent no-TTL db-sync model\">As a keystone operator developer, I want the bootstrap, schema-check, and policy-validation Job builders to omit TTLSecondsAfterFinished exactly like buildDBSyncJob/buildDBJob, so that every RunJob caller uses the same 'completed Job lingers as the state record' model and no caller silently re-creates a TTL-deleted Job (CC-0113, #415)</story>\n    <story title=\"Security owner: admin-password rotation still re-runs bootstrap\">As a security / credential owner, I want removing the TTL to NOT change admin-password rotation behaviour, so that rotating the admin Secret's password still forces keystone-manage bootstrap to re-run via the pod-spec-hash gate, not via the deleted TTL trigger (CC-0113, #415, CC-0108)</story>\n    <story title=\"Operator: completed Jobs are still garbage-collected on CR deletion\">As a cluster operator, I want the now-lingering completed Jobs to be cleaned up when the Keystone CR is deleted, so that no orphaned completed Jobs/Pods accumulate beyond one per Job type per CR (CC-0113, #415)</story>\n    <story title=\"Documentation reader: keystone reference docs match the new behaviour\">As a documentation reader / operator, I want the user-facing keystone reference docs and source godoc to stop claiming the Jobs are TTL-cleaned after 5 minutes, so that the documented Job lifecycle matches the implementation and explains the lingering-Job/no-loop rationale (CC-0113, #415)</story>\n    <story title=\"Reviewer: tests pin the new no-TTL contract so the loop cannot regress\">As a code reviewer, I want every test that previously asserted TTL == 300 updated to assert TTL is unset, across all four assertion sites, so that a future change that re-introduces a TTL (and therefore the re-creation loop) fails CI (CC-0113, #415)</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">buildBootstrapJob SHALL NOT set TTLSecondsAfterFinished; the returned Job's Spec.TTLSecondsAfterFinished SHALL be nil.</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">buildSchemaCheckJob SHALL NOT set TTLSecondsAfterFinished (nil) while continuing to set BackoffLimit == 2.</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">buildPolicyValidationJob SHALL NOT set TTLSecondsAfterFinished (nil) while continuing to set BackoffLimit == 2 and RestartPolicy == Never.</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">Admin-password rotation SHALL continue to re-run the bootstrap Job via the pod-spec-hash gate after the TTL is removed.</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">In steady state, a completed bootstrap, schema-check, or policy-validation Job whose stored pod-spec hash matches the desired template SHALL NOT be deleted or re-created on subsequent reconciles.</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The change SHALL be surgical: BackoffLimit, RestartPolicy, container command, env, volumes/volumeMounts, and owner references of every affected Job SHALL remain unchanged, and the owner-reference garbage-collection path SHALL be unaffected.</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">All four existing tests that pin TTLSecondsAfterFinished == 300 for these Jobs SHALL be updated to assert the TTL is unset, and the keystone controller test suite (unit + envtest) SHALL pass.</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">Source godoc/comments and user-facing docs under docs/reference/keystone SHALL be updated to remove the ttlSecondsAfterFinished=300 / automatic-cleanup claims for these three Jobs and to describe the lingering-Job model.</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 25953,
  "caller": "run_claude_cli"
}