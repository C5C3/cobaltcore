{
  "timestamp": "2026-04-04T15:25:04.033110+00:00",
  "feature_id": "CC-0041",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0041",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0041\">\n  <title>Add helm-validate CI job with lint and template tests</title>\n  <description>**Size:** 📦 medium\n**Category:** infrastructure\n**Priority:** high\n**Source:** GitHub Issue [#121](https://github.com/C5C3/forge/issues/121)\n\nAdd a `helm-validate` job to `.github/workflows/ci.yaml` gated by a new `helm` path filter (`operators/keystone/helm/**`) in the existing `dorny/paths-filter` config. The job: (1) sets up Helm via `azure/setup-helm`, (2) installs helm-unittest plugin with pinned version via `helm plugin install`, (3) runs `helm lint` on the keystone-operator chart, (4) runs `helm template` with 4 value override scenarios (defaults, webhook.enabled=false, serviceAccount.create=false+serviceAccount.name=existing-sa, minimal resource overrides), (5) runs `helm unittest`. Create unittest files under `operators/keystone/helm/keystone-operator/tests/` for deployment (image, replicas, resources, securityContext, probes, leader-elect arg, webhook volume mount), clusterrole (RBAC rules coverage), clusterrolebinding (role ref + subject), service (metrics port, conditional webhook port), and webhook-configuration (present when enabled, absent when disabled).\n\n**Rationale:** The CI pipeline validates Go code but has zero Helm chart validation. Template errors, broken value wiring, and invalid YAML are only caught at deploy time. This has caused silent breakage when chart values were renamed or conditionals were misconfigured. Adding lint+template+unittest catches these issues at PR time and prevents broken releases.\n\n**Affected Areas:**\n- .github/workflows/ci.yaml\n- operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\n- operators/keystone/helm/keystone-operator/tests/clusterrole_test.yaml\n- operators/keystone/helm/keystone-operator/tests/clusterrolebinding_test.yaml\n- operators/keystone/helm/keystone-operator/tests/service_test.yaml\n- operators/keystone/helm/keystone-operator/tests/webhook_test.yaml</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add helm path filter and helm-validate job to CI workflow (CC-0041, REQ-001, REQ-002, REQ-003, REQ-004, REQ-005)</task>\n    <task status=\"pending\">2.1 Create deployment_test.yaml helm-unittest file (CC-0041, REQ-006)</task>\n    <task status=\"pending\">2.2 Create clusterrole_test.yaml helm-unittest file (CC-0041, REQ-007)</task>\n    <task status=\"pending\">2.3 Create clusterrolebinding_test.yaml helm-unittest file (CC-0041, REQ-008)</task>\n    <task status=\"pending\">2.4 Create service_test.yaml helm-unittest file (CC-0041, REQ-009)</task>\n    <task status=\"pending\">2.5 Create webhook_test.yaml helm-unittest file (CC-0041, REQ-010)</task>\n    <task status=\"pending\">3.1 Update CI workflow reference documentation for helm-validate job (CC-0041, REQ-011)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThis feature adds a single new CI job (`helm-validate`) to the existing GitHub Actions workflow and creates 5 helm-unittest test files. No existing jobs are modified.\n\n```\n┌──────────────────────────────────────────────────────┐\n│ .github/workflows/ci.yaml                            │\n│                                                      │\n│  changes job                                         │\n│  ├─ dorny/paths-filter (+ helm filter)               │\n│  └─ Resolve effective changes (+ helm output)        │\n│                                                      │\n│  helm-validate job  [NEW]                            │\n│  ├─ actions/checkout                                 │\n│  ├─ azure/setup-helm                                 │\n│  ├─ helm plugin install (helm-unittest, pinned)      │\n│  ├─ helm lint                                        │\n│  ├─ helm template (4 scenarios)                      │\n│  └─ helm unittest                                    │\n└──────────────────────────────────────────────────────┘\n                         │\n               reads test files from\n                         ▼\n┌──────────────────────────────────────────────────────┐\n│ operators/keystone/helm/keystone-operator/tests/      │\n│  ├─ deployment_test.yaml                             │\n│  ├─ clusterrole_test.yaml                            │\n│  ├─ clusterrolebinding_test.yaml                     │\n│  ├─ service_test.yaml                                │\n│  └─ webhook_test.yaml                                │\n└──────────────────────────────────────────────────────┘\n```\n\n## Data Flow\n\n1. PR is opened → GitHub triggers CI workflow\n2. `changes` job runs dorny/paths-filter → determines `helm=true/false`\n3. If `helm=true`: `helm-validate` job starts\n4. Job sets up Helm CLI + installs helm-unittest plugin\n5. `helm lint` validates chart structure and syntax\n6. `helm template` renders chart with 4 value combinations → validates rendering\n7. `helm unittest` runs test files from `tests/` → validates template output assertions\n8. Job result blocks merge via branch protection if any step fails\n\n## Key Patterns to Follow\n\n- **Action pinning**: Use full SHA hash with `# v<version>` comment (e.g., `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6`)\n- **Path filter**: Follow existing `dorny/paths-filter` pattern; add `helm` alongside `go_common`, `keystone`, `docs`, `e2e_infra`\n- **Output resolution**: Mirror the docs/e2e-infra pattern in the 'Resolve effective changes' step; force `helm=true` on tag pushes\n- **Job structure**: Match existing job layout — `needs`, `if`, `runs-on`, `timeout-minutes`, then steps\n- **helm-unittest syntax**: Use `suite`, `templates`, `tests` top-level keys; `it`, `set`, `asserts` per test case; `documentIndex` for multi-document templates\n\n## Potential Pitfalls\n\n1. **helm-unittest plugin install**: The `helm plugin install` command clones from GitHub; network issues could cause flaky CI. Consider caching the plugin in the future (deferred).\n2. **Multi-document templates**: webhook-configuration.yaml renders 2 documents (MutatingWebhookConfiguration + ValidatingWebhookConfiguration). Use `documentIndex: 0` and `documentIndex: 1` in test assertions to target each document.\n3. **Template helper evaluation**: helm-unittest evaluates template helpers, so fullname will resolve to `RELEASE-NAME-keystone-operator` in test assertions (default release name is RELEASE-NAME).\n4. **helm template --set with dots**: Values like `resources.limits.cpu` use dots as path separators in `--set` flags. This is standard Helm behavior and works correctly.\n5. **Empty template output**: When `webhook.enabled=false`, the webhook-configuration.yaml template produces no output. Use `hasDocuments` with `count: 0` to assert this.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should render with default image and replicas\" requirement-id=\"REQ-006\">Deployment has replicas=2, container image=ghcr.io/c5c3/keystone-operator:0.1.0, imagePullPolicy=IfNotPresent [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should set resource limits and requests from values\" requirement-id=\"REQ-006\">Container resources: limits.cpu=500m, limits.memory=128Mi, requests.cpu=10m, requests.memory=64Mi [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should configure pod security context with non-root user 65532\" requirement-id=\"REQ-006\">Pod securityContext: runAsNonRoot=true, runAsUser=65532, runAsGroup=65532, fsGroup=65532, seccompProfile.type=RuntimeDefault [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should configure container security context with no privilege escalation\" requirement-id=\"REQ-006\">Container securityContext: allowPrivilegeEscalation=false, capabilities.drop=[ALL], readOnlyRootFilesystem=true, seccompProfile.type=RuntimeDefault [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should configure liveness and readiness probes on port 8081\" requirement-id=\"REQ-006\">livenessProbe.httpGet: path=/healthz port=8081; readinessProbe.httpGet: path=/readyz port=8081 [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should include leader-elect arg when leader election enabled\" requirement-id=\"REQ-006\">Container args contain --leader-elect, --metrics-bind-address=:8080, --health-probe-bind-address=:8081 [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should include webhook volume mount when webhook enabled\" requirement-id=\"REQ-006\">Container has volumeMount name=webhook-certs, mountPath=/tmp/k8s-webhook-server/serving-certs, readOnly=true; pod has matching volume [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should not include webhook volume mount when webhook disabled\" requirement-id=\"REQ-006\">With webhook.enabled=false: container has no volumeMounts, pod has no volumes, no webhook port (9443) [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/deployment_test.yaml\" test-function=\"should include webhook container port when webhook enabled\" requirement-id=\"REQ-006\">Container ports include metrics (8080), health (8081), and webhook (9443) when webhook.enabled=true [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/clusterrole_test.yaml\" test-function=\"should include all expected RBAC rules\" requirement-id=\"REQ-007\">Rules cover all 12 API groups: keystone.openstack.c5c3.io (keystones, status, finalizers), apps (deployments), core (services, configmaps, secrets, serviceaccounts, events), batch (jobs, cronjobs), k8s.mariadb.com (databases, users, grants), external-secrets.io, rbac.authorization.k8s.io, policy, autoscaling, networking.k8s.io, coordination.k8s.io [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/clusterrole_test.yaml\" test-function=\"should restrict verbs correctly for events and externalsecrets\" requirement-id=\"REQ-007\">Events rule has only [create, patch] verbs; externalsecrets rule has [get, list, watch, create, update, patch] without delete [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/clusterrolebinding_test.yaml\" test-function=\"should reference the correct ClusterRole\" requirement-id=\"REQ-008\">roleRef: apiGroup=rbac.authorization.k8s.io, kind=ClusterRole, name=RELEASE-NAME-keystone-operator [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/clusterrolebinding_test.yaml\" test-function=\"should bind to the operator ServiceAccount in release namespace\" requirement-id=\"REQ-008\">subjects[0]: kind=ServiceAccount, name=RELEASE-NAME-keystone-operator, namespace=NAMESPACE [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/service_test.yaml\" test-function=\"should expose metrics port from values\" requirement-id=\"REQ-009\">Service type=ClusterIP, ports include name=metrics port=8080 targetPort=8080 protocol=TCP [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/service_test.yaml\" test-function=\"should include webhook port when webhook enabled\" requirement-id=\"REQ-009\">With webhook.enabled=true: ports include name=webhook port=443 targetPort=9443 protocol=TCP [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/service_test.yaml\" test-function=\"should not include webhook port when webhook disabled\" requirement-id=\"REQ-009\">With webhook.enabled=false: ports do not contain name=webhook; only metrics port present [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/webhook_test.yaml\" test-function=\"should render MutatingWebhookConfiguration when webhook enabled\" requirement-id=\"REQ-010\">With webhook.enabled=true: MutatingWebhookConfiguration rendered with name ending -mutating, webhook path /mutate-keystone-openstack-c5c3-io-v1alpha1-keystone [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/webhook_test.yaml\" test-function=\"should render ValidatingWebhookConfiguration when webhook enabled\" requirement-id=\"REQ-010\">With webhook.enabled=true: ValidatingWebhookConfiguration rendered with name ending -validating, webhook path /validate-keystone-openstack-c5c3-io-v1alpha1-keystone [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/webhook_test.yaml\" test-function=\"should not render webhook resources when webhook disabled\" requirement-id=\"REQ-010\">With webhook.enabled=false: template produces no documents (hasDocuments count 0) [unit]</spec>\n    <spec test-file=\"operators/keystone/helm/keystone-operator/tests/webhook_test.yaml\" test-function=\"should include cert-manager inject-ca-from annotation\" requirement-id=\"REQ-010\">Both webhook configs have annotation cert-manager.io/inject-ca-from: NAMESPACE/RELEASE-NAME-keystone-operator-webhook [unit]</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[manual-verification] helm lint passes on current chart\" requirement-id=\"REQ-003\">Running 'helm lint operators/keystone/helm/keystone-operator/' locally exits with code 0 [integration]</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[manual-verification] helm template default scenario renders\" requirement-id=\"REQ-004\">Running 'helm template test operators/keystone/helm/keystone-operator/' locally produces valid YAML with all expected resources [integration]</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[manual-verification] helm template webhook-disabled scenario renders\" requirement-id=\"REQ-004\">Running 'helm template test operators/keystone/helm/keystone-operator/ --set webhook.enabled=false' produces valid YAML excluding webhook resources [integration]</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[manual-verification] helm template existing-sa scenario renders\" requirement-id=\"REQ-004\">Running 'helm template test operators/keystone/helm/keystone-operator/ --set serviceAccount.create=false --set serviceAccount.name=existing-sa' produces valid YAML without ServiceAccount resource [integration]</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[manual-verification] helm template resource-overrides scenario renders\" requirement-id=\"REQ-004\">Running 'helm template test operators/keystone/helm/keystone-operator/ --set resources.limits.cpu=100m --set resources.limits.memory=64Mi' produces valid YAML with overridden resources [integration]</spec>\n    <spec test-file=\".github/workflows/ci.yaml\" test-function=\"[manual-verification] helm unittest passes on current chart\" requirement-id=\"REQ-005\">Running 'helm unittest operators/keystone/helm/keystone-operator/' locally exits with code 0 and all test suites pass [integration]</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"CI Maintainer validates Helm chart at PR time to catch template errors before merge\">As a CI maintainer, I want a helm-validate CI job that runs helm lint, helm template, and helm-unittest on every PR touching Helm chart files, so that template errors, broken value wiring, and invalid YAML are caught at PR time instead of at deploy time</story>\n    <story title=\"CI Maintainer avoids unnecessary CI cost by gating helm-validate on path filter\">As a CI maintainer, I want the helm-validate job to run only when Helm chart files change, not on every PR, so that CI resources are not wasted validating unchanged charts (e.g. docs-only or Go-only changes)</story>\n    <story title=\"CI Maintainer catches broken value wiring via template rendering scenarios\">As a CI maintainer, I want helm template to render the chart with 4 value override scenarios covering all conditional branches, so that renamed values, broken conditionals, and invalid YAML in conditional blocks are detected</story>\n    <story title=\"CI Maintainer catches deployment template regressions via unit tests\">As a CI maintainer, I want helm-unittest tests for the deployment template covering image, replicas, resources, securityContext, probes, args, and webhook volume mount, so that changes to the deployment template that break security hardening, health checks, or conditional logic are caught automatically</story>\n    <story title=\"CI Maintainer catches RBAC, service, and webhook regressions via unit tests\">As a CI maintainer, I want helm-unittest tests for clusterrole, clusterrolebinding, service, and webhook-configuration templates, so that accidental removal of RBAC rules, broken port configuration, and webhook conditional rendering errors are caught</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The changes job SHALL include a 'helm' path filter matching 'operators/keystone/helm/**' and expose it as a job output</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The helm-validate job SHALL set up Helm via azure/setup-helm and install helm-unittest plugin with a pinned version</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The helm-validate job SHALL run helm lint on the keystone-operator chart and fail on errors</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The helm-validate job SHALL run helm template with 4 value override scenarios and fail on render errors</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The helm-validate job SHALL run helm unittest against test files in the chart's tests/ directory and fail on test failures</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">deployment_test.yaml SHALL assert container image, replicas, resources, securityContext, probes, leader-elect arg, and conditional webhook volume mount</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">clusterrole_test.yaml SHALL assert all 12 RBAC rule blocks covering all managed API groups and resources</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">clusterrolebinding_test.yaml SHALL assert the roleRef references the correct ClusterRole and the subject references the correct ServiceAccount</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">service_test.yaml SHALL assert metrics port configuration and conditional webhook port</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">webhook_test.yaml SHALL assert webhook configurations render when enabled and are absent when disabled</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The CI workflow reference documentation SHALL be updated to include the helm-validate job</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 20897,
  "caller": "run_claude_cli"
}