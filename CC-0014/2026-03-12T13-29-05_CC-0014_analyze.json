{
  "timestamp": "2026-03-12T13:29:05.746354+00:00",
  "feature_id": "CC-0014",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0014",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<previous-feature feature-id=\"CC-0013\" status=\"completed\">\n  <title>F001: Implement Keystone reconciler and sub-reconcilers</title>\n  <tasks hint=\"showing first 5\">\n    <task>1.1 Create KeystoneReconciler struct, Reconcile() method, and SetupWithManager with RBAC markers (REQ-001, REQ-008, REQ-010, REQ-011, REQ-013)</task>\n    <task>1.2 Implement reconcileSecrets sub-reconciler (REQ-002)</task>\n    <task>1.3 Implement reconcileDatabase sub-reconciler for managed and brownfield modes (REQ-003)</task>\n    <task>1.4 Implement reconcileFernetKeys sub-reconciler with key generation, CronJob, and PushSecret (REQ-004)</task>\n    <task>1.5 Implement reconcileConfig sub-reconciler with config generation pipeline (REQ-005)</task>\n  </tasks>\n</previous-feature>\n\n<feature feature-id=\"CC-0014\">\n  <title>F002: Add Keystone reconciler integration tests</title>\n  <description>**Size:** 🏗️ large\n**Category:** backend\n**Priority:** high\n**Feature ID:** CC-0014 (S014 in PLAN.md)\n\n## Summary\n\nAdd envtest integration tests (CC-0014) for the Keystone reconciler exercising the full reconciliation loop against a real API server with etcd. Tests create prerequisite Secrets simulating ESO sync, create a Keystone CR, then drive condition progression through SecretsReady → FernetKeysReady → (ConfigMap created) → DatabaseReady → DeploymentReady → BootstrapReady → Ready by simulating external dependencies (MariaDB readiness, Job completion, Deployment availability). They verify correct creation of all child resources (Deployment, Service, ConfigMap, Jobs, CronJob, Secret, RBAC, PushSecret) and that `status.endpoint` and `status.conditions` (including ObservedGeneration) are populated correctly. Both brownfield and managed database modes are covered.\n\n## Scope\n\n**Included:**\n- New integration test file `operators/keystone/internal/controller/integration_test.go` with `//go:build integration` tag\n- Full reconciliation loop test (brownfield mode): create prereqs → create CR → manager-driven reconcile → verify all 6 conditions reach True\n- Full reconciliation loop test (managed mode): same flow but with `spec.database.clusterRef` + MariaDB CR simulation via `SimulateDatabaseReady`, `SimulateUserReady`, `SimulateGrantReady`\n- Condition progression test: verify each sub-condition transitions False→True in reconciliation order, later conditions remain absent/False until predecessors are satisfied\n- Resource creation verification: assert Deployment (`{name}-api`), Service (`{name}-api`), ConfigMap (immutable, `{name}-config-{hash}`), Jobs (`{name}-db-sync`, `{name}-bootstrap`), CronJob (`{name}-fernet-rotate`), Secret (`{name}-fernet-keys`), ServiceAccount (`{name}-fernet-rotate`), Role (`{name}-fernet-rotate`), RoleBinding (`{name}-fernet-rotate`), PushSecret (`{name}-fernet-keys-backup`) exist after full reconciliation\n- Status field verification: `status.endpoint` matches `http://{name}-api.{namespace}.svc.cluster.local:5000/v3`, `status.conditions` ObservedGeneration matches CR Generation\n- `SimulateDeploymentReady` function in shared simulators package (`internal/common/testutil/simulators/simulators.go`)\n- New `SetupKeystoneEnvTestWithController` helper in `operators/keystone/internal/testutil/envtest_setup.go` that registers the `KeystoneReconciler` via `SetupWithManager` alongside CRDs and webhooks; must also install fake CRDs for MariaDB, ESO, and cert-manager types from the common envtest `fake_crds/` directory and register them in the scheme (via `SharedScheme()` or equivalent)\n- Build tag `integration` consistent with existing CC-0012 tests\n\n**Excluded:**\n- Webhook defaulting/validation testing (already covered by CC-0012 — YAGNI)\n- Error injection / fault tolerance tests (separate feature, not requested)\n- Chainsaw E2E tests (deferred to Phase 7 per PLAN.md)\n- Federation, middleware, plugin, or policyOverrides paths (not part of core reconciliation loop, no current requirement)\n- Performance / stress testing (YAGNI)\n- Deletion / finalizer testing (not implemented in the reconciler, YAGNI)\n- `test-integration` Makefile target wiring (currently a stub, separate infrastructure concern)\n\n## Visualization\n\n```mermaid\nsequenceDiagram\n    participant T as Test\n    participant API as envtest API Server\n    participant R as Keystone Reconciler\n    participant S as Simulators\n\n    T->>API: Create Namespace\n    T->>API: Create ExternalSecrets\n    T->>API: Create Secrets (DB creds, admin creds)\n    S->>API: SimulateExternalSecretSync\n    T->>API: Create Keystone CR\n    Note over R: Manager triggers reconcile\n    R->>API: Check ExternalSecret + Secret\n    R->>API: Set SecretsReady=True\n    R->>API: Create Fernet Keys Secret\n    R->>API: Create SA, Role, RoleBinding\n    R->>API: Create CronJob, PushSecret\n    R->>API: Set FernetKeysReady=True\n    R->>API: Create ConfigMap (immutable, hashed)\n    R->>API: Create db-sync Job\n    R->>API: Set DatabaseReady=False (DBSyncInProgress)\n    S->>API: SimulateJobComplete (db-sync)\n    Note over R: Requeue triggers reconcile\n    R->>API: Set DatabaseReady=True\n    R->>API: Create Deployment + Service\n    R->>API: Set DeploymentReady=False\n    S->>API: SimulateDeploymentReady\n    Note over R: Requeue triggers reconcile\n    R->>API: Set DeploymentReady=True, status.endpoint\n    R->>API: Create bootstrap Job\n    R->>API: Set BootstrapReady=False\n    S->>API: SimulateJobComplete (bootstrap)\n    Note over R: Requeue triggers reconcile\n    R->>API: Set BootstrapReady=True\n    R->>API: Set Ready=True (AllReady)\n    T->>API: Verify all conditions, resources, status\n```\n\n```mermaid\nstateDiagram-v2\n    [*] --> SecretsReady: ESO Secrets exist\n    SecretsReady --> FernetKeysReady: Fernet Secret + RBAC + CronJob + PushSecret created\n    FernetKeysReady --> DatabaseReady: ConfigMap created, db-sync Job completes\n    DatabaseReady --> DeploymentReady: Deployment + Service created, pods available\n    DeploymentReady --> BootstrapReady: Bootstrap Job completes\n    BootstrapReady --> Ready: AllTrue aggregation\n    Ready --> [*]\n```\n\n```mermaid\nflowchart TD\n    subgraph TestFile[\"integration_test.go\"]\n        TFL[\"TestIntegration_FullReconcile_Brownfield\"]\n        TFM[\"TestIntegration_FullReconcile_Managed\"]\n        TCP[\"TestIntegration_ConditionProgression\"]\n        TRC[\"TestIntegration_ResourceCreation\"]\n        TSE[\"TestIntegration_StatusEndpoint\"]\n        TOG[\"TestIntegration_ObservedGeneration\"]\n    end\n    subgraph Helpers[\"testutil/envtest_setup.go\"]\n        SK[\"SetupKeystoneEnvTestWithController (new)\"]\n        SE2[\"SetupKeystoneEnvTest (unchanged)\"]\n    end\n    subgraph Simulators[\"simulators/simulators.go\"]\n        SD[\"SimulateDeploymentReady (new)\"]\n        SE[\"SimulateExternalSecretSync\"]\n        SJ[\"SimulateJobComplete\"]\n        SDB[\"SimulateDatabaseReady\"]\n        SU[\"SimulateUserReady\"]\n        SG[\"SimulateGrantReady\"]\n    end\n    TestFile --> SK\n    TestFile --> Simulators\n```\n\n## Key Components\n\n- **`operators/keystone/internal/controller/integration_test.go`** (new): Main test file with `//go:build integration` tag. Contains 6 test functions using `testing.T` + gomega (dot-imported), consistent with CC-0012 patterns. Each test calls `testutil.SkipIfEnvTestUnavailable(t)`, creates an isolated namespace, uses `gomega.Eventually` for polling condition progression. Brownfield test uses `spec.database.host`; managed test uses `spec.database.clusterRef` and exercises MariaDB CR creation + simulation.\n- **`operators/keystone/internal/testutil/envtest_setup.go`** (modified): New `SetupKeystoneEnvTestWithController(t, addToScheme, registerWebhooks, registerController)` function that extends envtest setup to also register the `KeystoneReconciler` via `SetupWithManager`. Must install additional fake CRDs (MariaDB Database/User/Grant, ESO ExternalSecret/PushSecret) alongside the Keystone CRD, and register their types in the scheme. Existing `SetupKeystoneEnvTest` remains unchanged — CC-0012 webhook tests are unaffected.\n- **`internal/common/testutil/simulators/simulators.go`** (modified): New `SimulateDeploymentReady(ctx, c, key, replicas)` function that sets `status.conditions[Available]=True`, `status.readyReplicas`, and `status.observedGeneration` on an existing Deployment. Follows the established typed-resource simulator pattern (like `SimulateMariaDBReady`).\n- **Existing simulators** (unchanged, consumed): `SimulateExternalSecretSync`, `SimulateJobComplete`, `SimulateDatabaseReady`, `SimulateUserReady`, `SimulateGrantReady` — simulate external controller behavior in envtest where those controllers don't run.\n- **Existing `builders.SecretBuilder`** (unchanged, consumed): Used to construct prerequisite DB credentials Secret (keys: `username`, `password`) and admin credentials Secret (key: `password`).</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add SimulateDeploymentReady to shared simulators package with unit test (REQ-001)</task>\n    <task status=\"pending\">1.2 Add SetupKeystoneEnvTestWithController to envtest_setup.go (REQ-002, REQ-009)</task>\n    <task status=\"pending\">2.1 Create integration_test.go with shared helpers and TestIntegration_FullReconcile_Brownfield (REQ-003, REQ-005, REQ-006, REQ-007)</task>\n    <task status=\"pending\">2.2 Add TestIntegration_ConditionProgression test (REQ-008)</task>\n    <task status=\"pending\">2.3 Add TestIntegration_ResourceCreation, TestIntegration_StatusEndpoint, TestIntegration_ObservedGeneration tests (REQ-005, REQ-006, REQ-007)</task>\n    <task status=\"pending\">2.4 Add TestIntegration_FullReconcile_Managed test (REQ-004)</task>\n\n  </tasks>\n  <implementation-notes>ARCHITECTURE: The integration tests exercise the full reconciliation loop by running the KeystoneReconciler inside a controller-runtime Manager against envtest's real API server with etcd. External controllers (ESO operator, MariaDB operator, Kubernetes deployment controller) don't run in envtest, so their behavior is simulated using the shared simulator functions.\n\nKEY PATTERN - ENVTEST WITH CONTROLLER: The new SetupKeystoneEnvTestWithController extends the existing SetupKeystoneEnvTest pattern (operators/keystone/internal/testutil/envtest_setup.go) by adding: (1) fake CRD installation from internal/common/testutil/fake_crds/, (2) registerController callback for SetupWithManager. It reuses the same manager lifecycle (goroutine, cleanup via t.Cleanup) and direct-client creation. Fake CRD paths are resolved relative to the source file using runtime.Caller(0) — navigate up to the repo root, then into internal/common/testutil/fake_crds/.\n\nKEY PATTERN - SIMULATOR: SimulateDeploymentReady follows the typed-resource pattern (like SimulateJobComplete): Get the existing object, modify status fields, call c.Status().Update(). It must set three things to satisfy IsDeploymentReady: (a) status.conditions with DeploymentAvailable=True, (b) status.readyReplicas >= spec.replicas, (c) status.observedGeneration >= metadata.generation.\n\nKEY PATTERN - TEST STRUCTURE: Each test follows: (1) SkipIfEnvTestUnavailable, (2) create unique namespace with GenerateName, (3) create prerequisites (ExternalSecrets, Secrets, simulate sync), (4) create Keystone CR, (5) poll for conditions using EventuallyCondition, (6) simulate external dependencies at appropriate points. The driveFullReconciliation helper encapsulates the simulation loop.\n\nPITFALLS: (1) ConfigMap names include a content hash — use client.List with label selector or prefix matching to find them. (2) The reconciler creates fernet keys on first reconcile then requeues — tests must account for this extra reconcile cycle. (3) Jobs use PodSpecHash annotation for spec-change detection — simulator doesn't need to set this as the integration test uses real reconciler-created Jobs. (4) The managed-mode test must create the Keystone CR with clusterRef and without host (CEL validation enforces mutual exclusivity).\n\nFILES TO CREATE: operators/keystone/internal/controller/integration_test.go (new, ~400 lines).\nFILES TO MODIFY: internal/common/testutil/simulators/simulators.go (add ~25 lines), internal/common/testutil/simulators/simulators_test.go (add ~40 lines), operators/keystone/internal/testutil/envtest_setup.go (add ~80 lines).\nFILES UNCHANGED: operators/keystone/api/v1alpha1/integration_test.go (CC-0012), all sub-reconciler files, all existing simulators.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"internal/common/testutil/simulators/simulators_test.go\" test-function=\"TestSimulateDeploymentReady_SetsAvailableCondition\" requirement-id=\"REQ-001\">After calling SimulateDeploymentReady, the Deployment should have Available=True condition, readyReplicas matching input, and observedGeneration matching generation</spec>\n    <spec test-file=\"internal/common/testutil/simulators/simulators_test.go\" test-function=\"TestSimulateDeploymentReady_NotFound\" requirement-id=\"REQ-001\">Calling SimulateDeploymentReady with a non-existent Deployment key should return a wrapped NotFound error</spec>\n    <spec test-file=\"internal/common/testutil/simulators/simulators_test.go\" test-function=\"TestSimulateDeploymentReady_IsDeploymentReadyReturnsTrue\" requirement-id=\"REQ-001\">After calling SimulateDeploymentReady, deployment.IsDeploymentReady should return true for the updated Deployment</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_FullReconcile_Brownfield\" requirement-id=\"REQ-003\">All 6 conditions reach True (SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady, Ready) after simulating external dependencies in brownfield mode</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_FullReconcile_Brownfield\" requirement-id=\"REQ-006\">status.endpoint equals http://{name}-api.{namespace}.svc.cluster.local:5000/v3 after full reconciliation</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_FullReconcile_Managed\" requirement-id=\"REQ-004\">MariaDB Database, User, and Grant CRs are created; after simulating their readiness and other dependencies, all 6 conditions reach True</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_FullReconcile_Managed\" requirement-id=\"REQ-004\">DatabaseReady=False with reason WaitingForDatabase until MariaDB CRs are simulated as ready</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_ConditionProgression\" requirement-id=\"REQ-008\">Conditions transition sequentially: SecretsReady first, then FernetKeysReady, then DatabaseReady, then DeploymentReady, then BootstrapReady, then Ready; later conditions are absent or False until predecessors are True</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_ConditionProgression\" requirement-id=\"REQ-008\">SecretsReady=False blocks all subsequent conditions from being set</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_ResourceCreation\" requirement-id=\"REQ-005\">All 12 child resources exist after full reconciliation: Deployment, Service, ConfigMap, 2 Jobs, CronJob, Secret, ServiceAccount, Role, RoleBinding, PushSecret</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_StatusEndpoint\" requirement-id=\"REQ-006\">status.endpoint is set to http://{name}-api.{namespace}.svc.cluster.local:5000/v3 after DeploymentReady=True</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_ObservedGeneration\" requirement-id=\"REQ-007\">All conditions have ObservedGeneration equal to the Keystone CR's metadata.generation after full reconciliation</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_ConditionProgression\" requirement-id=\"REQ-008\">Ready=False with reason NotAllReady when BootstrapReady is not yet True; transitions to Ready=True (AllReady) only after all sub-conditions are True</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Platform engineer verifies brownfield Keystone reconciliation loop\">As a platform engineer, I want to run integration tests that exercise the full Keystone reconciliation loop in brownfield mode (external database host), so that I have confidence that the reconciler creates all expected child resources and progresses through all conditions to Ready=True when using a pre-existing database</story>\n    <story title=\"Platform engineer verifies managed-database Keystone reconciliation loop\">As a platform engineer, I want to run integration tests that exercise the full Keystone reconciliation loop in managed mode (spec.database.clusterRef), so that I have confidence that the reconciler correctly creates MariaDB Database/User/Grant CRs and waits for their readiness before proceeding</story>\n    <story title=\"Platform engineer verifies condition progression order\">As a platform engineer, I want to verify that Keystone conditions transition in the correct order and later conditions remain absent or False until predecessors are satisfied, so that I can trust that the reconciler enforces sequential dependency ordering and doesn't skip ahead</story>\n    <story title=\"Platform engineer verifies all child resources are created\">As a platform engineer, I want to verify that all expected child resources exist after a full reconciliation, so that I can trust that the reconciler creates the complete set of Kubernetes resources needed to run Keystone</story>\n    <story title=\"Test infrastructure provides SimulateDeploymentReady for envtest\">As a test author, I want a SimulateDeploymentReady simulator function that makes a Deployment appear ready in envtest, so that integration tests for any operator can simulate Deployment readiness without running the real deployment controller</story>\n    <story title=\"Test infrastructure provides SetupKeystoneEnvTestWithController\">As a test author, I want a SetupKeystoneEnvTestWithController helper that starts an envtest environment with the KeystoneReconciler registered, so that integration tests can exercise the full reconciliation loop with a real API server instead of unit-testing individual sub-reconcilers</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">SimulateDeploymentReady SHALL set status.conditions with Available=True, status.readyReplicas, and status.observedGeneration on an existing Deployment</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">SetupKeystoneEnvTestWithController SHALL start an envtest environment with the KeystoneReconciler registered via SetupWithManager, installing Keystone CRD, webhook configs, and all required fake CRDs</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The brownfield full-reconcile integration test SHALL verify that all 6 conditions (SecretsReady, FernetKeysReady, DatabaseReady, DeploymentReady, BootstrapReady, Ready) reach True after simulating all external dependencies</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The managed-mode full-reconcile integration test SHALL verify that MariaDB Database/User/Grant CRs are created and that all 6 conditions reach True after simulating MariaDB readiness</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The resource creation integration test SHALL verify that all expected child resources exist after a full reconciliation</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The status endpoint integration test SHALL verify that status.endpoint is set to the correct URL format after DeploymentReady=True</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The ObservedGeneration integration test SHALL verify that all conditions have ObservedGeneration matching the Keystone CR's metadata.generation</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The condition progression integration test SHALL verify that conditions transition in the defined order: SecretsReady → FernetKeysReady → DatabaseReady → DeploymentReady → BootstrapReady → Ready</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">SetupKeystoneEnvTestWithController SHALL NOT modify the existing SetupKeystoneEnvTest function or its behavior</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Check if previous feature already implemented any of our pending tasks</rule>\n  <rule>Run: git log --oneline -5 to see what the previous feature committed</rule>\n  <rule>Mark tasks as removed_tasks if they were done by the previous feature</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 22837,
  "caller": "run_claude_cli"
}