{
  "timestamp": "2026-03-30T21:41:19.332651+00:00",
  "feature_id": "CC-0036",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0036",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0036\">\n  <title>Implement credential key management sub-reconciler</title>\n  <description>**Size:** 📦 medium\n**Category:** backend\n**Priority:** high\n**Source:** Proposed for 'In the Keystone Operator, in addition to the Fernet keys'\n\nAdd full credential key lifecycle management mirroring the existing Fernet key implementation. This includes:\n\n1. **CRD type** (`api/v1alpha1/keystone_types.go`): Add `CredentialKeysSpec` with `rotationSchedule` (default `\"0 0 * * 0\"`) and `maxActiveKeys` (default `3`, min `3`), as `spec.credentialKeys` on `KeystoneSpec`.\n\n2. **Sub-reconciler** (`internal/controller/reconcile_credential.go`): New file following the 5-step pattern from `reconcile_fernet.go` — Secret creation with `generateFernetKey()` reuse → RBAC (SA + Role + RoleBinding named `{name}-credential-rotate`) → CronJob (`{name}-credential-rotate` running `credential_rotate` + `credential_migrate` in sequence) → PushSecret (`{name}-credential-keys-backup` targeting `kv-v2/data/openstack/keystone/credential-keys`) → `CredentialKeysReady` condition.\n\n3. **Controller updates** (`internal/controller/keystone_controller.go`): Add `\"CredentialKeysReady\"` to `subConditionTypes`, insert `reconcileCredentialKeys()` between Fernet and Database sub-reconcilers.\n\n4. **Config update** (`internal/controller/reconcile_config.go`): Add `[credential]` section to keystone.conf with `key_repository = /etc/keystone/credential-keys` and `max_active_keys`.\n\n5. **Deployment updates** (`internal/controller/reconcile_deployment.go`): Add `credentialKeysHash()` SHA-256 function, set `keystone.c5c3.io/credential-keys-hash` pod annotation for rolling restarts, remove `Optional: ptr.To(true)` and `TODO(CC-0013)` from credential-keys volume.\n\n6. **Unit tests** (`internal/controller/reconcile_credential_test.go`): 11 test cases mirroring Fernet tests — Secret creation + requeue, idempotency, schedule update, key validity, schedule propagation, PushSecret references, key generation validity, key uniqueness, min-active-keys floor, condition messages, CronJob spec.\n\n7. **E2E test** (`tests/e2e/keystone/credential-rotation/chainsaw-test.yaml`): Chainsaw test covering CR apply → Ready → CronJob schedule assertion → manual rotation trigger → Secret hash change → annotation propagation → Ready stability.\n\n**Rationale:** Credential keys are required by OpenStack Keystone to encrypt credentials stored in the database (EC2 credentials, application credentials, etc.). Without this implementation, the Keystone operator cannot securely manage credential encryption at rest, which is a core Keystone security feature. The credential key rotation (including the mandatory `credential_migrate` step) ensures encrypted credentials remain accessible after key rotation — a critical operational requirement that distinguishes this from Fernet key rotation.\n\n**Affected Areas:**\n- operators/keystone/api/v1alpha1/keystone_types.go\n- operators/keystone/internal/controller/reconcile_credential.go\n- operators/keystone/internal/controller/reconcile_credential_test.go\n- operators/keystone/internal/controller/keystone_controller.go\n- operators/keystone/internal/controller/reconcile_config.go\n- operators/keystone/internal/controller/reconcile_deployment.go\n- tests/e2e/keystone/credential-rotation/chainsaw-test.yaml\n- tests/e2e/keystone/credential-rotation/00-keystone-cr.yaml</description>\n  <tasks>\n    <task status=\"pending\">1.1 Add CredentialKeysSpec type to CRD, update webhook defaults and validation, regenerate manifests (CC-0036, REQ-009, REQ-011)</task>\n    <task status=\"pending\">1.2 Add [credential] section to keystone.conf defaults and update config test (CC-0036, REQ-011)</task>\n    <task status=\"pending\">2.1 Create reconcile_credential.go sub-reconciler with Secret, RBAC, CronJob, PushSecret, and condition (CC-0036, REQ-001 through REQ-009)</task>\n    <task status=\"pending\">2.2 Create reconcile_credential_test.go with 11 unit tests mirroring Fernet tests (CC-0036, REQ-001 through REQ-009)</task>\n    <task status=\"pending\">3.1 Update keystone_controller.go to add CredentialKeysReady condition and insert reconcile call (CC-0036, REQ-007)</task>\n    <task status=\"pending\">3.2 Update reconcile_deployment.go — add credentialKeysHash, credential-keys-hash annotation, remove Optional — and update deployment tests (CC-0036, REQ-010, REQ-012)</task>\n    <task status=\"pending\">4.1 Create E2E Chainsaw test for credential key rotation lifecycle (CC-0036, REQ-001 through REQ-012)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\n```\n┌─────────────────────────────────────────────────────────┐\n│                  Reconcile Loop                          │\n│                                                          │\n│  reconcileSecrets                                        │\n│       ↓                                                  │\n│  reconcileConfig ──→ ConfigMap (keystone.conf)           │\n│       ↓               includes [credential] section      │\n│  reconcileFernetKeys                                     │\n│       ↓                                                  │\n│  reconcileCredentialKeys (NEW)                           │\n│       │  1. Ensure Secret ──→ {name}-credential-keys     │\n│       │  2. Ensure RBAC   ──→ SA/Role/RoleBinding        │\n│       │  3. Ensure CronJob ──→ {name}-credential-rotate  │\n│       │  4. Ensure PushSecret ──→ OpenBao backup         │\n│       │  5. Set CredentialKeysReady condition             │\n│       ↓                                                  │\n│  reconcileDatabase                                       │\n│       ↓                                                  │\n│  reconcileDeployment ──→ Deployment with hash annotation │\n│       ↓                                                  │\n│  reconcileBootstrap                                      │\n└─────────────────────────────────────────────────────────┘\n```\n\n## Data Flow\n\n```\ncrypto/rand (32 bytes) → base64url → Secret data keys\n                                          │\n                    ┌─────────────────────┼──────────────────┐\n                    ↓                     ↓                  ↓\n           Deployment volume     CronJob rotation     PushSecret\n           (read-only mount)     (rotate+migrate→     (→ OpenBao)\n                    │             patch Secret)\n                    ↓\n            SHA-256 hash → pod annotation\n                           (rolling restart)\n```\n\n## Key Design Decisions\n\n1. **Reuse `generateFernetKey()`**: Credential keys use the same format as Fernet keys (32 bytes, base64url). No new key generation function needed.\n\n2. **Rotation script difference**: The `credentialRotateScript` MUST run `credential_migrate` after `credential_rotate`. This is the critical distinction from Fernet — without it, existing encrypted credentials become inaccessible after rotation.\n\n3. **CronJob env var**: Uses `OS_credential__max_active_keys` (oslo.config override for [credential] section) instead of `OS_fernet_tokens__max_active_keys`.\n\n4. **Deployment function signature change**: `buildKeystoneDeployment` gains a 4th parameter for credentialKeysHash. All callers and test helpers must be updated.\n\n5. **Condition ordering**: `CredentialKeysReady` inserted between `FernetKeysReady` and `DatabaseReady` in subConditionTypes — this matches the reconciliation call order.\n\n## Patterns to Follow\n\n- **Sub-reconciler file**: Follow `reconcile_fernet.go` exactly (same imports, same structure, same error wrapping)\n- **Test file**: Follow `reconcile_fernet_test.go` exactly (same test helpers pattern, same Gomega assertions)\n- **Labels**: Use `commonLabels(keystone)` on all created resources\n- **Owner references**: Use `controllerutil.SetControllerReference` on all created resources\n- **RBAC immutability**: `RoleRef` only set when `rb.RoleRef.Name == \"\"` (immutable after creation)\n- **CronJob updates**: Use `job.EnsureCronJob` from common library (handles create vs update)\n- **PushSecret updates**: Use `secrets.EnsurePushSecret` from common library\n\n## Potential Pitfalls\n\n1. **Forgetting credential_migrate**: The rotation script MUST include both commands. Missing credential_migrate is a data-loss scenario.\n2. **Wrong oslo.config env var prefix**: Must be `OS_credential__max_active_keys` (double underscore), not `OS_credential_max_active_keys`.\n3. **buildKeystoneDeployment callers**: ALL callers (production + tests) must be updated for the new 4th parameter.\n4. **ptr import removal**: After removing `Optional: ptr.To(true)`, verify if `k8s.io/utils/ptr` import is still needed.\n5. **CRD sync**: After `make manifests`, `make sync-crds` must be run to update Helm chart CRDs.</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_NoSecret_CreatesSecretAndRequeues\" requirement-id=\"REQ-001\">Secret created with 3 keys, ownerRef, labels; Requeue=true returned; CredentialKeysReady=False/GeneratingKeys</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_SecretAlreadyExists\" requirement-id=\"REQ-002\">Existing Secret data preserved; CronJob and PushSecret created; CredentialKeysReady=True</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_MinActiveKeysFloor\" requirement-id=\"REQ-009\">Even with MaxActiveKeys=1, exactly 3 keys are generated (floor of 3)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_GeneratedKeysAreValid\" requirement-id=\"REQ-008\">5 keys generated (MaxActiveKeys=5), each valid base64url decoding to 32 bytes</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_CronJobSpec\" requirement-id=\"REQ-004\">CronJob has correct labels, SA, init container (copy-keys), main container (credential-rotate with credentialRotateScript), env vars (SECRET_NAME, SECRET_NAMESPACE, OS_credential__max_active_keys), volumes (credential-keys-src, credential-keys, config)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_CronJobScheduleMatchesSpec\" requirement-id=\"REQ-005\">CronJob schedule equals the custom spec value (e.g., '30 2 * * 1')</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_CronJobScheduleUpdated\" requirement-id=\"REQ-005\">Existing CronJob with old schedule is updated to new schedule from spec</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_PushSecretReferencesCorrectSecret\" requirement-id=\"REQ-006\">PushSecret selector points to {name}-credential-keys, uses ClusterSecretStore openbao-cluster-store, remote key is kv-v2/data/openstack/keystone/credential-keys</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_credential_test.go\" test-function=\"TestReconcileCredentialKeys_ConditionMessages\" requirement-id=\"REQ-007\">CredentialKeysReady=True, reason=CredentialKeysAvailable, message='Credential keys Secret exists and rotation CronJob is configured', ObservedGeneration matches</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestBuildKeystoneDeployment_CredentialKeysHashAnnotation\" requirement-id=\"REQ-010\">Pod annotation keystone.c5c3.io/credential-keys-hash is set to the provided hash value</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestCredentialKeysHash_Deterministic\" requirement-id=\"REQ-010\">Same Secret data produces same hash; different data produces different hash; hash is 64-char hex string</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestCredentialKeysHash_SecretNotFound\" requirement-id=\"REQ-010\">Not-found error returned when credential-keys Secret does not exist</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_deployment_test.go\" test-function=\"TestReconcileDeployment_DeploymentSpec (updated assertion)\" requirement-id=\"REQ-012\">credential-keys volume Secret.Optional is nil (not set), replacing previous assertion that Optional=true</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_CredentialKeysSectionPresent\" requirement-id=\"REQ-011\">Rendered keystone.conf contains [credential] section with key_repository=/etc/keystone/credential-keys and max_active_keys=3</spec>\n    <spec test-file=\"operators/keystone/internal/controller/reconcile_config_test.go\" test-function=\"TestReconcileConfig_BasicManagedDatabaseAndCache (updated assertion)\" requirement-id=\"REQ-011\">Existing test additionally asserts keystoneConf contains [credential] section</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestDefault_SetsZeroValueDefaults (updated assertion)\" requirement-id=\"REQ-009\">Existing test additionally asserts CredentialKeys.MaxActiveKeys defaults to 3</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_webhook_test.go\" test-function=\"TestValidate_CredentialKeysMaxActiveKeysBelowMinimumRejected\" requirement-id=\"REQ-009\">Validation rejects CredentialKeys.MaxActiveKeys=2 with field error on spec.credentialKeys.maxActiveKeys</spec>\n    <spec test-file=\"operators/keystone/api/v1alpha1/keystone_types_test.go\" test-function=\"TestCredentialKeysSpecFields\" requirement-id=\"REQ-009\">Zero-value CredentialKeysSpec has MaxActiveKeys=0 and RotationSchedule=''</spec>\n    <spec test-file=\"tests/e2e/keystone/credential-rotation/chainsaw-test.yaml\" test-function=\"Step 3: Assert CronJob schedule\" requirement-id=\"REQ-005\">CronJob keystone-credential-credential-rotate exists with schedule '0 0 * * 0'</spec>\n    <spec test-file=\"tests/e2e/keystone/credential-rotation/chainsaw-test.yaml\" test-function=\"Step 4: Trigger rotation and verify Secret + Deployment changed\" requirement-id=\"REQ-004\">Manual job from CronJob completes; Secret data hash changes; Deployment credential-keys-hash annotation changes within 120s</spec>\n    <spec test-file=\"tests/e2e/keystone/credential-rotation/chainsaw-test.yaml\" test-function=\"Step 5: Assert Ready=True maintained\" requirement-id=\"REQ-007\">Keystone CR status conditions[Ready].status='True' and reason='AllReady' after rotation</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Operator auto-generates credential keys on first reconcile\">As a Keystone operator controller, I want to automatically generate a credential-keys Secret with cryptographically secure keys when a Keystone CR is created, so that Keystone can encrypt credentials stored in the database without manual intervention</story>\n    <story title=\"Platform operator gets credential keys rotated on schedule with mandatory migrate step\">As a platform operator, I want credential keys to be rotated automatically via a CronJob that runs both credential_rotate and credential_migrate, so that credential encryption stays secure and existing encrypted credentials remain accessible after rotation</story>\n    <story title=\"Platform operator gets credential keys backed up to OpenBao\">As a platform operator, I want credential keys to be backed up to OpenBao via PushSecret, so that keys survive cluster loss and can be restored</story>\n    <story title=\"Keystone pods restart when credential keys change\">As a Keystone service, I want pods to be restarted via rolling update when credential keys are rotated, so that running Keystone instances use the latest credential encryption keys</story>\n    <story title=\"Platform operator monitors credential key status via conditions\">As a platform operator, I want to see CredentialKeysReady condition on the Keystone CR status, so that I can monitor credential key management health and debug issues</story>\n    <story title=\"Platform operator configures credential key rotation parameters via CRD\">As a platform operator, I want to configure rotation schedule and max active keys via spec.credentialKeys, so that I can customize credential key rotation to match my security requirements</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The system SHALL create a credential-keys Secret with cryptographically secure keys when none exists</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The system SHALL requeue after initial Secret creation to confirm availability before proceeding</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The system SHALL create dedicated RBAC resources for the credential rotation CronJob</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The system SHALL create a CronJob that runs credential_rotate followed by credential_migrate in sequence</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The system SHALL update the CronJob schedule when spec.credentialKeys.rotationSchedule changes</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The system SHALL create a PushSecret to back up credential keys to OpenBao</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The system SHALL set the CredentialKeysReady condition to reflect the reconciliation state</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The system SHALL generate credential keys as 32-byte cryptographically random values with base64url encoding</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The system SHALL enforce a minimum floor of 3 active keys regardless of spec value</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The system SHALL compute a SHA-256 hash of credential-keys Secret data and set it as a pod annotation for rolling restarts</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The system SHALL add a [credential] section to keystone.conf with key_repository and max_active_keys</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">The system SHALL remove the Optional flag from the credential-keys volume in the Deployment</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 20547,
  "caller": "run_claude_cli"
}