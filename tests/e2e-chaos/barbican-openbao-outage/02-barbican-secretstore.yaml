# SPDX-FileCopyrightText: Copyright 2026 SAP SE or an SAP affiliate company
#
# SPDX-License-Identifier: Apache-2.0

# Default secret store for the OpenBao outage chaos test, in managed mode.
#
# instanceRef points at the proving OpenBaoCluster openbao-instance
# (deploy/kind/infrastructure/openbao-instance.yaml) in this namespace, and
# everything else is derived from that name: the server URL, the CA Secret
# openbao-instance-tls-ca, and the provisioner ServiceAccount the operator
# authenticates as to mint the store's AppRole credentials. That instance is
# also the one 03-networkchaos.yaml partitions away from the API pods, so the
# outage this suite injects is the outage of the store the secrets go to.
#
# isDefault is what makes the parent Barbican reach Ready: a Barbican with no
# credential-ready default store reports SecretStoresReady=False with reason
# NoDefaultSecretStore and never renders a config. The single-default rule is
# scoped to spec.barbicanRef, so this store does not collide with the default
# stores the other suites attach to their own Barbicans.
apiVersion: barbican.openstack.c5c3.io/v1alpha1
kind: BarbicanSecretStore
metadata:
  name: barbican-bao-outage-store
  namespace: openstack
spec:
  barbicanRef:
    name: barbican-bao-outage
  type: OpenBao
  isDefault: true
  openBao:
    instanceRef:
      name: openbao-instance
