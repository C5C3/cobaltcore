# SPDX-FileCopyrightText: Copyright 2026 SAP SE or an SAP affiliate company
#
# SPDX-License-Identifier: Apache-2.0

# Tempest exclude list for Barbican key-manager API tests.
#
# One regex pattern per line. Lines starting with # are comments.
# Blank lines are ignored. Patterns are matched against test IDs.
# Follows the same format as releases/2025.2/test-excludes/keystone.txt.
#
# Every group of patterns added here carries a tracked-by/re-evaluate-on pair
# directly above it so an exclusion does not silently outlive its cause:
#   tracked-by:      link to the issue that owns the exclusion
#   re-evaluate-on:  the condition that retires it, e.g. barbican-tempest-plugin
#                    > <the version the mismatch was observed on>
# Re-check every group on each plugin bump and drop the patterns upstream has
# fixed.

# tracked-by: https://github.com/C5C3/cobaltcore/issues/991
# re-evaluate-on: barbican-tempest-plugin > 4.5.0
# ProjectQuotasTest drives the project-quota API with a credential that holds
# only key-manager:service-admin (its client credentials in
# barbican-tempest-plugin 4.5.0); under the new policy defaults
# project_quotas:get/put/delete require role:admin, so the one test gets 403.
# 04-policy-check-job.yaml asserts that 403 directly. Drop the pattern once the
# plugin runs the test as project_admin.
barbican_tempest_plugin\.tests\.api\.test_quotas\.ProjectQuotasTest\.test_manage_project_quotas
