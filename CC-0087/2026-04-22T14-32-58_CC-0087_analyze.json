{
  "timestamp": "2026-04-22T14:32:58.968440+00:00",
  "feature_id": "CC-0087",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0087",
  "model": "claude-opus-4-7",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<feature feature-id=\"CC-0087\">\n  <title>Add field indexer for Secret-to-Keystone mapper to reduce AP</title>\n  <description># GitHub Issue #194: Add field indexer for Secret-to-Keystone mapper to reduce API server load\n\n**Category**: performance | **Scope**: Small\n\n**Description**: Replace the current secretToKeystoneMapper implementation (keystone_controller.go:213) which lists all Keystone CRs on every Secret event with a field indexer registered via mgr.GetFieldIndexer(). Index Keystone CRs by their referenced Secret names so the mapper can perform an O(1) indexed lookup instead of an O(n) list operation.\n\n**Motivation**: In namespaces with many Secrets (common with ExternalSecretOperator), every Secret create/update/delete triggers a full list of all Keystone CRs against the API server. This generates unnecessary API server load that scales linearly with Secret count. Field indexers are the standard controller-runtime pattern for efficient reverse lookups.\n\n**Affected Areas**:\n- `operators/keystone/internal/controller/keystone_controller.go`\n\n**Acceptance Criteria**:\n- [ ] Field indexer registered for Secret name references on Keystone CRs\n- [ ] secretToKeystoneMapper uses indexed lookup instead of List\n- [ ] API server request count for Keystone CR listing reduced to zero during Secret-only events\n- [ ] Unit test verifies mapper returns correct reconcile requests via index\n- [ ] No behavioral change in reconcile triggering\n\n---\nSource: https://github.com/C5C3/forge/issues/194</description>\n  <tasks>\n    <task status=\"pending\">1.1 Declare exported const KeystoneSecretNameIndexKey and a package-level extractor func keystoneSecretNameExtractor(obj client.Object) []string in keystone_controller.go; extractor returns deduplicated, non-empty Secret names from spec.database.secretRef.name and spec.bootstrap.adminPasswordSecretRef.name (REQ-001, REQ-006, CC-0087)</task>\n    <task status=\"pending\">1.2 Write unit tests TestKeystoneSecretIndexer_ExtractsBothReferencedSecretNames, TestKeystoneSecretIndexer_DeduplicatesIdenticalNames, TestKeystoneSecretIndexer_SkipsEmptyNames in keystone_controller_test.go verifying the extractor contract (REQ-001, CC-0087)</task>\n    <task status=\"pending\">2.1 Add registerSecretNameIndex(ctx context.Context, indexer client.FieldIndexer) error helper in keystone_controller.go that calls indexer.IndexField(ctx, &keystonev1alpha1.Keystone{}, KeystoneSecretNameIndexKey, keystoneSecretNameExtractor) and wraps errors with the index key; wire it into SetupWithManager before the Watches chain, returning any error (REQ-001, REQ-006, CC-0087)</task>\n    <task status=\"pending\">2.2 Refactor secretToKeystoneMapper to: (a) perform a namespace-scoped List with client.MatchingFields{KeystoneSecretNameIndexKey: obj.GetName()}; (b) iterate obj.GetOwnerReferences() for Kind=Keystone and apiVersion matching keystonev1alpha1.GroupVersion.String() and enqueue by ownerRef.Name; (c) deduplicate resulting reconcile.Request values by types.NamespacedName; (d) log and swallow List errors identically to the current implementation; remove the old KeystoneList iteration and the existing isOwnedBy helper if it becomes unused (REQ-002, REQ-003, REQ-005, REQ-007, CC-0087)</task>\n    <task status=\"pending\">2.3 Write unit tests TestSecretToKeystoneMapper_UsesIndexedLookup, TestSecretToKeystoneMapper_IndexedLookupScopedToNamespace, TestSecretToKeystoneMapper_IndexedLookupErrorLoggedAndSwallowed, TestSecretToKeystoneMapper_NoUnfilteredListCall in keystone_controller_test.go using fake.NewClientBuilder().WithIndex(&keystonev1alpha1.Keystone{}, KeystoneSecretNameIndexKey, keystoneSecretNameExtractor) and interceptor.Funcs to observe List call shapes (REQ-002, REQ-007, CC-0087)</task>\n    <task status=\"pending\">2.4 Write unit tests TestSecretToKeystoneMapper_OwnerRefPathDoesNotList and TestSecretToKeystoneMapper_DeduplicatesIndexAndOwnerPaths and TestSecretToKeystoneMapper_MultipleReferencingKeystonesAllEnqueued in keystone_controller_test.go, pinning the owner-ref enqueue path and the deduplication invariant (REQ-003, REQ-005, CC-0087)</task>\n    <task status=\"pending\">2.5 Update existing tests TestSecretToKeystoneMapper_ReferencedSecrets, TestSecretToKeystoneMapper_OwnedSecrets, TestSecretToKeystoneMapper_EnqueuesOnStagingSecretUpdate, TestSecretToKeystoneMapper_OrphanStagingSecretNotEnqueued to build the fake client with WithIndex so the refactored mapper has an index to query, and assert they still pass without behavioral change (REQ-004, CC-0087)</task>\n    <task status=\"pending\">2.6 Add unit test TestKeystoneSecretIndexerKey_IsDeclaredAsConst asserting the exported const exists and matches the string used at registration; prevents silent drift to a literal (REQ-006, CC-0087)</task>\n    <task status=\"pending\">3.1 Update operators/keystone/internal/controller/integration_test.go:setupEnvTestWithController to invoke registerSecretNameIndex(ctx, mgr.GetFieldIndexer()) before Complete(r), returning any error so the test fails fast on registration failure (REQ-008, CC-0087)</task>\n    <task status=\"pending\">3.2 Add integration test TestIntegration_SecretEventTriggersReconcileViaIndexer in integration_test.go: create Keystone referencing Secret \"keystone-db\", create that Secret, then Eventually assert the Keystone's ObservedGeneration or status condition updates within eventuallyTimeout (REQ-008, CC-0087)</task>\n    <task status=\"pending\">3.3 Add integration test TestIntegration_UnrelatedSecretDoesNotTriggerReconcile in integration_test.go: snapshot Keystone ResourceVersion, create an unrelated Secret in the same namespace, then Consistently assert no new reconcile-driven status change occurs within a short window (REQ-008, CC-0087)</task>\n    <task status=\"pending\">3.4 Add integration test TestIntegration_IndexerRegistrationFailsManagerStartCleanly in integration_test.go: call registerSecretNameIndex twice with the same key against the manager's FieldIndexer and assert the second call returns an error whose message mentions KeystoneSecretNameIndexKey (REQ-001, CC-0087)</task>\n    <task status=\"pending\">4.1 Update docs/reference/keystone-reconciler.md: in the Watches section, document the new field indexer — index key name (KeystoneSecretNameIndexKey), indexed fields (spec.database.secretRef.name, spec.bootstrap.adminPasswordSecretRef.name), registration site (SetupWithManager), lookup site (secretToKeystoneMapper), and the owner-ref fallback path for rotation staging Secrets; add a short note explaining the API-server-load motivation and link back to CC-0087 (REQ-006, CC-0087)</task>\n\n  </tasks>\n  <implementation-notes>ARCHITECTURE (CC-0087)\n\nThe change is local to operators/keystone/internal/controller/keystone_controller.go and its tests, plus one docs page and the integration-test setup helper. No API type changes, no CRD changes, no RBAC changes (List on Keystone is already granted; a field indexer uses the cache, not new API verbs).\n\nKey decisions:\n\n1. Single-key indexer (REQ-001, REQ-006). We index the UNION of referenced Secret names under one key, 'spec.secretRefs.name'. A Keystone CR whose two SecretRef fields hold distinct names contributes two index entries; a CR where they are equal contributes one (the extractor deduplicates). An empty string is omitted so missing/optional fields do not pollute the index.\n\n2. Exported const KeystoneSecretNameIndexKey. Tests, docs, and the mapper all reference the same constant. This prevents silent drift between register and lookup sites. The recommended value is 'spec.secretRefs.name'.\n\n3. Split mapper paths (REQ-002, REQ-003). The refactored mapper has two independent enqueue paths:\n   a. Referenced-name path: c.List(ctx, &KeystoneList{}, client.InNamespace(obj.GetNamespace()), client.MatchingFields{KeystoneSecretNameIndexKey: obj.GetName()}).\n   b. Owner-ref path: iterate obj.GetOwnerReferences(); for each ref whose Kind==\"Keystone\" and APIVersion==keystonev1alpha1.GroupVersion.String(), enqueue {Namespace: obj.GetNamespace(), Name: ref.Name}. This replaces the old isOwnedBy-scanning-the-list logic with a direct, List-free lookup that preserves CC-0081 behavior for rotation staging Secrets.\n   c. Union & deduplicate by types.NamespacedName before returning (REQ-005).\n\n4. Error handling (REQ-002). The indexed List can still fail (cache not synced, context cancelled). We preserve the existing behavior: log via log.FromContext(ctx).Error and return nil — controller-runtime handler.MapFunc contract forbids returning errors.\n\n5. Integration-path parity (REQ-008). setupEnvTestWithController currently wires the reconciler inline with ctrl.NewControllerManagedBy(mgr).For(...).Watches(..., secretToKeystoneMapper(mgr.GetClient())); we add registerSecretNameIndex(ctx, mgr.GetFieldIndexer()) before Complete(r) so the integration test uses the same registration helper as production and fails fast if registration breaks.\n\n6. Fake client test setup. Unit tests that instantiate the mapper must build the fake client with WithIndex(&keystonev1alpha1.Keystone{}, KeystoneSecretNameIndexKey, keystoneSecretNameExtractor) so MatchingFields queries resolve. Use interceptor.Funcs{List: ...} to observe and assert shape of List calls (presence of field selector). controller-runtime's fake client supports both.\n\nARCHITECTURE DIAGRAM\n\n  Secret event\n      |\n      v\n  secretToKeystoneMapper(ctx, secretObj)\n      |\n      +-- path A: List Keystones WHERE spec.secretRefs.name == secretObj.Name (namespaced)\n      |       -> append {Namespace, Name} for each match\n      |\n      +-- path B: for each ownerRef in secretObj.OwnerReferences\n      |       if Kind==\"Keystone\" and APIVersion matches\n      |          -> append {Namespace: secretObj.Namespace, Name: ownerRef.Name}\n      |\n      v\n  dedupe by types.NamespacedName -> []reconcile.Request\n\nDATA FLOW\n\n  At manager startup:\n    SetupWithManager(mgr) -> registerSecretNameIndex(ctx, mgr.GetFieldIndexer())\n        -> mgr.GetFieldIndexer().IndexField(ctx, &Keystone{}, KeystoneSecretNameIndexKey, extractor)\n    Cache informer populates the index from Keystone objects.\n\n  At steady state:\n    kube-apiserver -> informer -> Secret event -> mapper -> cache lookup (no apiserver round-trip) -> reconcile.Request\n\nPITFALLS\n\n  * Forgetting to register the index in envtest tests -> MatchingFields returns no hits; add the registration to the shared helper (task 3.1).\n  * APIVersion check on ownerRef. keystonev1alpha1.GroupVersion.String() is authoritative; avoid hardcoding 'keystone.openstack.c5c3.io/v1alpha1' as a string (REQ-003).\n  * Owner-ref path must NOT dereference a Keystone by Get; that would add a round-trip per Secret event and defeat the purpose. Just construct the NamespacedName.\n  * The old isOwnedBy helper becomes unused if not called elsewhere; grep first and delete if orphaned. If still referenced, leave it.\n  * Empty-string extraction: if extractor returns [\"\"], controller-runtime will index under the empty key; ensure the filter drops empties (REQ-001).\n  * handler.MapFunc contract: never return an error; log and return nil on List failure (REQ-002).\n\nFILES TO MODIFY\n\n  operators/keystone/internal/controller/keystone_controller.go          (SetupWithManager, secretToKeystoneMapper, new extractor + const + helper)\n  operators/keystone/internal/controller/keystone_controller_test.go     (new + updated unit tests)\n  operators/keystone/internal/controller/integration_test.go             (setupEnvTestWithController + three new tests)\n  docs/reference/keystone-reconciler.md                                  (document indexer)\n\nFILES TO READ FOR CONTEXT (do not modify)\n\n  operators/keystone/api/v1alpha1/keystone_types.go                      (SecretRefSpec fields)\n  operators/keystone/main.go                                             (controller wiring unchanged)\n  internal/common/testutil/envtest/setup.go                              (envtest entry points)</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestKeystoneSecretIndexer_ExtractsBothReferencedSecretNames\" requirement-id=\"REQ-001\">[unit] The indexer extractor returns a slice containing both spec.database.secretRef.name and spec.bootstrap.adminPasswordSecretRef.name for a Keystone CR with distinct values</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestKeystoneSecretIndexer_DeduplicatesIdenticalNames\" requirement-id=\"REQ-001\">[unit] When both referenced fields hold the same name, the extractor returns that name only once (no duplicate index entries)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestKeystoneSecretIndexer_SkipsEmptyNames\" requirement-id=\"REQ-001\">[unit] The extractor omits empty strings so unset fields do not pollute the index</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_ReferencedSecrets\" requirement-id=\"REQ-004\">[unit] Existing test continues to pass after refactor: Secret matching spec.database.secretRef.name or spec.bootstrap.adminPasswordSecretRef.name enqueues the referencing Keystone</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_UsesIndexedLookup\" requirement-id=\"REQ-002\">[unit] Using a fake client built with WithIndex for the Keystone indexed field, the mapper returns the correct Keystone without performing any unfiltered List (assert via interceptor counting List calls with no field selector)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_IndexedLookupScopedToNamespace\" requirement-id=\"REQ-002\">[unit] A Secret in namespace A does not enqueue a matching Keystone in namespace B even when both reference the same Secret name</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_IndexedLookupErrorLoggedAndSwallowed\" requirement-id=\"REQ-002\">[unit] When the indexed List returns an error, the mapper logs it and returns nil rather than panicking or returning an error</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_OwnedSecrets\" requirement-id=\"REQ-003\">[unit] Existing test continues to pass: a Secret carrying an ownerReference matching the Keystone UID enqueues a reconcile for that Keystone</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_OwnerRefPathDoesNotList\" requirement-id=\"REQ-003\">[unit] The owner-ref enqueue path does not invoke c.List at all; only ownerRef iteration occurs (assert via interceptor)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_EnqueuesOnStagingSecretUpdate\" requirement-id=\"REQ-003\">[unit] Existing test continues to pass: rotation staging Secrets owned by the Keystone CR enqueue reconcile via the owner-ref path (CC-0081)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_OrphanStagingSecretNotEnqueued\" requirement-id=\"REQ-003\">[unit] Existing test continues to pass: a Secret with the staging label but no owner reference and no name match enqueues nothing</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_DeduplicatesIndexAndOwnerPaths\" requirement-id=\"REQ-005\">[unit] When a Secret is both name-referenced and owner-referenced to the same Keystone, the mapper returns exactly one reconcile.Request</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_MultipleReferencingKeystonesAllEnqueued\" requirement-id=\"REQ-005\">[unit] When two Keystone CRs in the same namespace reference the same Secret name, both are enqueued (one request each)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestKeystoneSecretIndexerKey_IsDeclaredAsConst\" requirement-id=\"REQ-006\">[unit] The index key is reachable as an exported package-level constant and equals the value used at both register and lookup sites (grep-assertion via reflection or identity check)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/keystone_controller_test.go\" test-function=\"TestSecretToKeystoneMapper_NoUnfilteredListCall\" requirement-id=\"REQ-007\">[unit] An interceptor-instrumented client asserts that any c.List call from the mapper carries a non-empty client.MatchingFields option (no unfiltered list)</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_SecretEventTriggersReconcileViaIndexer\" requirement-id=\"REQ-008\">[integration] With envtest, register the indexer during controller setup, create a Keystone CR referencing Secret 'keystone-db', then create/update that Secret and assert the Keystone is reconciled (condition change or RequeueAfter observed) within eventuallyTimeout</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_UnrelatedSecretDoesNotTriggerReconcile\" requirement-id=\"REQ-008\">[integration] With envtest and a Keystone CR with a known ResourceVersion, create an unrelated Secret in the same namespace and assert the Keystone's status conditions observe no additional reconcile attributable to the Secret event</spec>\n    <spec test-file=\"operators/keystone/internal/controller/integration_test.go\" test-function=\"TestIntegration_IndexerRegistrationFailsManagerStartCleanly\" requirement-id=\"REQ-001\">[integration] Injecting a duplicate indexer registration (or a mock that returns an error from IndexField) causes setupEnvTestWithController to fail fast with an error mentioning the index key</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Cluster operator sees reduced API server load from Secret events\">As a cluster operator, I want the Keystone controller to avoid listing all Keystone CRs on every Secret event, so that namespaces with many ESO-managed Secrets do not generate linearly scaling load on the kube-apiserver</story>\n    <story title=\"Keystone operator developer retains existing reconcile-trigger behavior\">As a Keystone operator developer, I want the refactored mapper to enqueue reconcile requests for every Secret that previously did, so that no behavioral regression is introduced when replacing List with an indexed lookup (CC-0087)</story>\n    <story title=\"Keystone operator developer finds the indexer setup documented\">As a Keystone operator developer, I want the field indexer and mapper flow documented in the reconciler reference, so that future changes to Secret references add new indexed fields correctly (CC-0087)</story>\n    <story title=\"Reviewer confirms no behavioral regression in end-to-end reconcile triggering\">As a reviewer, I want unit and integration tests to exercise the indexed mapper end-to-end against envtest, so that I can sign off on CC-0087 with confidence that no Secret event is silently dropped</story>\n    <story title=\"Cluster operator diagnoses a broken indexer without silent reconcile failures\">As a cluster operator, I want the controller to fail startup loudly when the indexer cannot be registered, so that a silently unindexed mapper does not pass review unnoticed (CC-0087)</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">The Keystone controller SHALL register a field indexer on Keystone CRs that indexes the union of referenced Secret names (spec.database.secretRef.name and spec.bootstrap.adminPasswordSecretRef.name) under a single, stable index key (CC-0087).</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The secretToKeystoneMapper SHALL perform a namespaced List with client.MatchingFields against the registered index to find Keystone CRs referencing the incoming Secret by name, replacing the prior unfiltered List (CC-0087).</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The secretToKeystoneMapper SHALL additionally enqueue any Keystone CRs referenced by the incoming Secret's ownerReferences (Kind=Keystone, matching apiVersion), using a direct per-ref lookup rather than a List (CC-0087, CC-0081).</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">The refactor SHALL NOT change the observable set of reconcile.Request values emitted by the mapper for any Secret input already covered by existing unit tests (CC-0087).</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The mapper SHALL deduplicate reconcile.Request values so a single Secret event never enqueues the same Keystone CR twice when it is reachable via both the indexed field path and an owner reference (CC-0087).</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The indexer key name SHALL be exported or declared as a package-level const so tests and documentation can reference the exact string used at registration and lookup sites (CC-0087).</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHOULD\">The mapper SHOULD remain O(k) in the number of matching Keystone CRs k rather than O(n) in total Keystone CRs in the namespace (CC-0087).</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The integration test SHALL register the field indexer via the manager's GetFieldIndexer in setupEnvTestWithController so that the integration path mirrors production (CC-0087).</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 23977,
  "caller": "run_claude_cli"
}