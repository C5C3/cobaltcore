{
  "timestamp": "2026-03-23T10:55:33.217385+00:00",
  "feature_id": "CC-0018",
  "task_type": "analyze",
  "session_key": "reanalyze-CC-0018",
  "model": "opus",
  "prompt": "Output in English only - all code, comments, commits, docs.\n\nQuick validation of feature tasks before implementation.\n\n<previous-feature feature-id=\"CC-0017\" status=\"completed\">\n  <title>H001: Implement Keystone Operator packaging</title>\n  <tasks hint=\"showing first 5\">\n    <task>1.1 Create operators/keystone/Dockerfile with multi-stage build: golang:1.25 builder with CGO_ENABLED=0, workspace root context copying go.work + all modules, gcr.io/distroless/static:nonroot runtime with USER 65532:65532 (CC-0017, REQ-001, REQ-002)</task>\n    <task>2.1 Create Chart.yaml (apiVersion v2, name keystone-operator, version 0.1.0, appVersion 0.1.0, type application) and values.yaml with all configurable parameters: image (repo/tag/pullPolicy), replicas (default 2), resources (requests/limits), leaderElection.enabled (default true), webhook.enabled (default true), metrics.port (default 8080), serviceAccount.create/name (CC-0017, REQ-003, REQ-004)</task>\n    <task>2.2 Create _helpers.tpl with standard Helm template helpers: keystone-operator.name, keystone-operator.fullname, keystone-operator.chart, keystone-operator.labels (helm.sh/chart, app.kubernetes.io/name, instance, version, managed-by), keystone-operator.selectorLabels, keystone-operator.serviceAccountName (CC-0017, REQ-003)</task>\n    <task>2.3 Copy CRD from operators/keystone/config/crd/bases/keystone.openstack.c5c3.io_keystones.yaml to operators/keystone/helm/keystone-operator/crds/keystone.openstack.c5c3.io_keystones.yaml — exact copy with no modifications (CC-0017, REQ-009)</task>\n    <task>3.1 Create RBAC templates: serviceaccount.yaml (conditional on serviceAccount.create), clusterrole.yaml with all rules from kubebuilder RBAC markers in keystone_controller.go (7 API groups with exact verbs), clusterrolebinding.yaml binding ClusterRole to ServiceAccount in release namespace (CC-0017, REQ-003, REQ-005)</task>\n  </tasks>\n</previous-feature>\n\n<feature feature-id=\"CC-0018\">\n  <title>H002: Complete CI pipeline and coverage setup</title>\n  <description>**Size:** 🏗️ large\n**Category:** infrastructure\n**Priority:** high\n\n## Open Questions\n\nAll questions from the initial elaboration have been answered by the user. No remaining ambiguities.\n\n## Summary\n\nExtend the existing `.github/workflows/ci.yaml` (S018) into a full job matrix covering lint, unit tests, integration tests (per operator), E2E tests (kind + Chainsaw), operator Docker image build-and-push to GHCR, and Helm chart OCI push. Add `v*` tag triggers so tagged commits produce versioned images (SemVer stripped from git tag), charts, and a GitHub Release. Create a `.codecov.yml` with component-level coverage thresholds (80% `internal/common/`, 70% operator controllers, 90% webhooks). Use `codecov/codecov-action` for coverage upload.\n\n## Scope\n\n**Included:**\n- Extend `ci.yaml` `on.push` trigger to include `tags: [\"v*\"]`\n- **Lint job**: retain existing `lint` (golangci-lint v2.11.3) + `shellcheck` jobs unchanged\n- **Test job (unit)**: refactor existing single `test` job into a matrix strategy over `[common, keystone, c5c3]` with `-coverprofile` output per module\n- **Test job (integration)**: un-stub `test-integration` Makefile target; add per-operator matrix job with `setup-envtest` binary\n- **Coverage upload**: `codecov/codecov-action` with flag-based component separation in both `test` and `test-integration` jobs\n- **E2E infra job**: retain existing `e2e-infra` job unchanged\n- **E2E keystone job**: new job — build operator Docker image, `kind load docker-image`, deploy via Helm chart, run Chainsaw keystone test suites from `tests/e2e/keystone/`\n- **Build-and-push job**: build `operators/keystone/Dockerfile`, push to GHCR; image tag = SemVer from git tag (strip `v` prefix) or `main`-branch SHA; conditional on `main` push or `v*` tag; uses `docker/metadata-action` for tag derivation\n- **Helm-push job**: `helm package` on `operators/keystone/helm/keystone-operator/`, `helm push` to `oci://ghcr.io/<owner>/charts/`; conditional on `main` push or `v*` tag\n- **GitHub Release job**: on `v*` tags only, `gh release create` attaching Helm chart tarball; uses GitHub auto-generated release notes\n- **Verify-codegen job**: new gate job running `make manifests && make generate && git diff --exit-code` + `make verify-crd-sync` (from S025)\n- **Implement `docker-build` Makefile target**: currently stubbed with error; must support `OPERATOR=<op>` parameter\n- **Implement `helm-package` Makefile target**: currently stubbed with error; must support `OPERATOR=<op>` parameter\n- **Implement `test-integration` Makefile target**: currently stubbed; wire envtest-based integration tests\n- **`.codecov.yml`**: new file with component thresholds — 80% `internal/common/`, 70% `operators/*/internal/controller/`, 90% `operators/*/api/`\n\n**Excluded:**\n- Modifying `build-images.yaml` (handles OpenStack service container images — separate concern, different tag strategy with SBOM/signing)\n- Modifying `cleanup-images.yaml`, `verify-container-images.yaml`, `deploy-docs.yaml` (unrelated workflows)\n- SBOM generation / cosign signing for operator images (not requested; can follow `build-images.yaml` patterns later)\n- c5c3-operator Dockerfile (doesn't exist yet; S022 creates it — matrix prepared to accommodate it)\n- Release notes generation logic beyond GitHub auto-generated notes\n- Branch protection rules or required status checks configuration\n- Changes to existing `e2e-infra` job logic\n- Changes to `.golangci.yml` lint configuration\n\n## Visualization\n\n```mermaid\nflowchart TD\n    trigger[\"on.push: main / PR / tags v*\"]\n\n    subgraph gates[\"Gate Jobs #40;always#41;\"]\n        lint[\"lint #40;golangci-lint#41;\"]\n        shellcheck[\"shellcheck #40;hack/*.sh#41;\"]\n        verify[\"verify-codegen #40;manifests + generate + crd-sync#41;\"]\n        test[\"test #40;matrix: common, keystone, c5c3#41;\"]\n        testint[\"test-integration #40;matrix: keystone, c5c3#41;\"]\n    end\n\n    subgraph e2e_group[\"E2E Jobs\"]\n        e2einfra[\"e2e-infra #40;existing: kind + Chainsaw infra#41;\"]\n        e2eop[\"e2e-keystone #40;build image + kind load + Helm deploy + Chainsaw#41;\"]\n    end\n\n    subgraph publish[\"Publish Jobs #40;main / v* tags only#41;\"]\n        buildpush[\"build-and-push #40;keystone operator image to GHCR#41;\"]\n        helmpush[\"helm-push #40;OCI push to GHCR#41;\"]\n    end\n\n    subgraph release_group[\"Release Job #40;v* tags only#41;\"]\n        ghrelease[\"github-release #40;gh release create + chart tarball#41;\"]\n    end\n\n    codecov[\"Upload coverage to Codecov\"]\n\n    trigger --> gates\n    test --> codecov\n    testint --> codecov\n    gates --> e2e_group\n    gates --> publish\n    publish --> release_group\n```\n\n```mermaid\nsequenceDiagram\n    participant Tag as Git Tag v*\n    participant CI as ci.yaml\n    participant GHCR as ghcr.io\n    participant GH as GitHub Releases\n\n    Tag->>CI: Trigger full pipeline\n    CI->>CI: Gate jobs #40;lint, test, verify, E2E#41;\n    CI->>GHCR: docker push operator:0.1.0\n    CI->>GHCR: helm push oci://charts/keystone-operator:0.1.0\n    CI->>GH: gh release create v0.1.0 + chart tarball\n```\n\n```mermaid\nflowchart LR\n    subgraph codecov_cfg[\".codecov.yml Thresholds\"]\n        common[\"internal/common/ >= 80%\"]\n        controllers[\"operators/*/internal/controller/ >= 70%\"]\n        webhooks[\"operators/*/api/ >= 90%\"]\n    end\n```\n\n## Key Components\n\n- **`ci.yaml` trigger extension**: Add `tags: [\"v*\"]` to `on.push.branches` sibling, enabling tag-triggered full pipeline runs\n- **`verify-codegen` job**: New gate job — `make manifests && make generate && git diff --exit-code` + `make verify-crd-sync`; catches uncommitted generated code before merge\n- **`test` job (matrix)**: Refactor existing single `test` job into matrix strategy over `[common, keystone, c5c3]`; each entry runs with `-coverprofile` and uploads to Codecov with component flags\n- **`test-integration` job**: Un-stub `test-integration` Makefile target; new CI job with per-operator matrix, `setup-envtest` for kubebuilder assets, `-coverprofile` output, Codecov upload\n- **Codecov upload step**: `codecov/codecov-action` in `test` and `test-integration` jobs; uses `flags` parameter for component-level threshold matching in `.codecov.yml`\n- **`e2e-keystone` job**: New job — `docker build` operator image, `kind load docker-image`, `helm install` from `operators/keystone/helm/keystone-operator/`, run Chainsaw tests from `tests/e2e/keystone/` (12 existing suites); depends on gate jobs passing\n- **`build-and-push` job**: `docker/build-push-action` building `operators/keystone/Dockerfile`; `docker/metadata-action` derives tags (SemVer from `v*` tag, `main` branch SHA); pushes to GHCR; conditional on `github.ref == 'refs/heads/main'` or `startsWith(github.ref, 'refs/tags/v')`\n- **`helm-push` job**: `helm package operators/keystone/helm/keystone-operator/` → `helm push` to `oci://ghcr.io/<owner>/charts/`; chart version set from git tag; same push conditions as `build-and-push`\n- **`github-release` job**: On `v*` tags only; `gh release create ${{ github.ref_name }} --generate-notes` attaching packaged Helm chart tarball as release asset\n- **`docker-build` Makefile target**: Replace current stub with `docker build -t $(IMG) -f operators/$(OPERATOR)/Dockerfile .` supporting `OPERATOR` and `IMG` variables\n- **`helm-package` Makefile target**: Replace current stub with `helm package operators/$(OPERATOR)/helm/$(OPERATOR)-operator/` supporting `OPERATOR` variable\n- **`test-integration` Makefile target**: Replace current stub with envtest-based test runner, filtering to `*/internal/controller/...` packages with build tag `integration`\n- **`.codecov.yml`**: New file with `coverage.status.project` and `coverage.status.patch` settings; component definitions mapping `internal/common/**` → 80%, `operators/*/internal/controller/**` → 70%, `operators/*/api/**` → 90%</description>\n  <tasks>\n    <task status=\"pending\">1.1 Implement docker-build Makefile target (REQ-010)</task>\n    <task status=\"pending\">1.2 Implement helm-package Makefile target (REQ-011)</task>\n    <task status=\"pending\">1.3 Implement test-integration Makefile target (REQ-003)</task>\n    <task status=\"pending\">2.1 Add v* tag trigger and permissions to ci.yaml (REQ-001)</task>\n    <task status=\"pending\">2.2 Refactor test job to matrix strategy with coverage (REQ-002, REQ-004)</task>\n    <task status=\"pending\">2.3 Add test-integration CI job with matrix and coverage upload (REQ-003, REQ-004)</task>\n    <task status=\"pending\">2.4 Add verify-codegen gate job to ci.yaml (REQ-009)</task>\n    <task status=\"pending\">3.1 Add e2e-keystone job to ci.yaml (REQ-005)</task>\n    <task status=\"pending\">3.2 Add build-and-push job to ci.yaml (REQ-006)</task>\n    <task status=\"pending\">3.3 Add helm-push job to ci.yaml (REQ-007)</task>\n    <task status=\"pending\">3.4 Add github-release job to ci.yaml (REQ-008)</task>\n    <task status=\"pending\">4.1 Create .codecov.yml with component thresholds (REQ-012)</task>\n    <task status=\"pending\">5.1 Update docs/reference/ci-workflow.md to reflect new CI pipeline (REQ-001 through REQ-012)</task>\n\n  </tasks>\n  <implementation-notes>## Architecture\n\nThis feature extends the existing CI pipeline (ci.yaml) from 5 jobs to 11 jobs organized in a DAG:\n\n```\nGate Jobs (always):\n  lint ─────────────┐\n  shellcheck ───────┤\n  verify-codegen ───┤\n  test (matrix) ────┼──> E2E Jobs + Publish Jobs\n  test-integration ─┘\n\nE2E Jobs (depends on gates):\n  e2e-infra (existing, unchanged)\n  e2e-keystone (new)\n\nPublish Jobs (main/tags, depends on gates):\n  build-and-push ───┐\n  helm-push ────────┼──> Release Job\n                    │\nRelease Job (v* tags only):\n  github-release ───┘\n\nIndependent:\n  docs (existing, unchanged)\n```\n\n## Key Decisions\n\n1. **Permissions model**: Top-level permissions stay minimal (contents:read). Jobs needing packages:write (publish) or contents:write (release) declare per-job permissions blocks. This follows least-privilege principle from existing ci.yaml.\n\n2. **Matrix strategy for tests**: Uses include-style matrix with module name and test path rather than a simple list, enabling different go test paths for common (./internal/common/...) vs operators (./operators/<op>/...).\n\n3. **setup-envtest in CI**: The test-integration job installs setup-envtest via `go install` rather than downloading a binary, because the Go toolchain is already set up and this ensures version compatibility.\n\n4. **Image tag for E2E**: Uses `ci-test` as the image tag for e2e-keystone (not `:latest`) and sets `imagePullPolicy: Never` in Helm values to ensure the kind-loaded image is used.\n\n5. **Chart version derivation**: The helm-push and github-release jobs derive chart version from the git tag by stripping the `v` prefix. On main pushes, the default version from Chart.yaml is used.\n\n6. **Codecov fail_ci_if_error: false**: Fork PRs don't have access to CODECOV_TOKEN. Setting fail_ci_if_error: false prevents CI from failing due to upload issues on forks.\n\n## Files Modified\n\n- `.github/workflows/ci.yaml` — Major extension (trigger, 6 new jobs, 1 refactored job)\n- `Makefile` — Replace 3 stubs (docker-build, helm-package, test-integration)\n- `.codecov.yml` — New file\n- `docs/reference/ci-workflow.md` — Updated documentation\n\n## Patterns to Follow\n\n- **Action pinning**: Pin all GitHub Actions by SHA hash with version comment, as done in existing ci.yaml (e.g. `actions/checkout@de0fac2...  # v6`)\n- **SPDX headers**: All files start with `# SPDX-FileCopyrightText: Copyright 2026 SAP SE` \n- **Makefile conventions**: Use `@` prefix for silent execution, `$(error ...)` for intentional failures, `$(OPERATORS)` variable for iteration\n- **GHCR login pattern**: From build-images.yaml — docker/login-action with registry: ghcr.io, username: github.actor, password: secrets.GITHUB_TOKEN\n- **Owner normalization**: From build-images.yaml — `echo \"owner=${GITHUB_REPOSITORY_OWNER,,}\" >> \"$GITHUB_OUTPUT\"` for lowercase owner</implementation-notes>\n</feature>\n\n<existing-specifications hint='read-only reference — NEVER remove these'>\n  <test-specifications>\n    <spec test-file=\"Makefile (manual verification)\" test-function=\"make docker-build OPERATOR=keystone\" requirement-id=\"REQ-010\">Docker image is built from operators/keystone/Dockerfile and tagged with default IMG value</spec>\n    <spec test-file=\"Makefile (manual verification)\" test-function=\"make docker-build OPERATOR=keystone IMG=test:latest\" requirement-id=\"REQ-010\">Docker image is tagged as test:latest</spec>\n    <spec test-file=\"Makefile (manual verification)\" test-function=\"make helm-package OPERATOR=keystone\" requirement-id=\"REQ-011\">Helm chart .tgz is produced in the current directory</spec>\n    <spec test-file=\"Makefile (manual verification)\" test-function=\"make helm-package OPERATOR=keystone CHART_VERSION=1.0.0\" requirement-id=\"REQ-011\">Helm chart .tgz has version 1.0.0 in filename and Chart.yaml</spec>\n    <spec test-file=\"Makefile (manual verification)\" test-function=\"make test-integration OPERATOR=keystone\" requirement-id=\"REQ-003\">envtest-based tests run (or zero tests if none exist yet) with setup-envtest providing KUBEBUILDER_ASSETS</spec>\n    <spec test-file=\"Makefile (manual verification)\" test-function=\"make test-integration (all operators)\" requirement-id=\"REQ-003\">Integration tests run for all operators in OPERATORS list, producing cover-integration-*.out files</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"Trigger on v* tag push\" requirement-id=\"REQ-001\">All CI jobs trigger on v* tag push including publish and release jobs</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"Existing triggers unchanged\" requirement-id=\"REQ-001\">Push to main and PR targeting main still trigger only gate and e2e jobs (not release)</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"Test job matrix produces 3 entries\" requirement-id=\"REQ-002\">Matrix strategy creates parallel jobs for common, keystone, c5c3 with distinct coverprofiles</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"Codecov upload with correct flags\" requirement-id=\"REQ-004\">codecov-action uploads with flags matching module name (e.g. common-unit, keystone-integration)</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"test-integration job runs envtest\" requirement-id=\"REQ-003\">test-integration job installs setup-envtest and runs make test-integration with OPERATOR matrix</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"verify-codegen catches stale generated code\" requirement-id=\"REQ-009\">verify-codegen job fails when make manifests/generate produces uncommitted diffs</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"verify-codegen catches CRD sync drift\" requirement-id=\"REQ-009\">verify-codegen job fails when make verify-crd-sync detects Helm CRD divergence</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"e2e-keystone builds and deploys operator\" requirement-id=\"REQ-005\">e2e-keystone job builds image, loads into kind, deploys via Helm, runs Chainsaw tests</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"e2e-keystone depends on gate jobs\" requirement-id=\"REQ-005\">e2e-keystone has needs: [lint, shellcheck, test, test-integration, verify-codegen]</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"build-and-push on v* tag produces SemVer image\" requirement-id=\"REQ-006\">docker/metadata-action produces tag 0.1.0 from v0.1.0 tag, image pushed to GHCR</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"build-and-push skipped on PR\" requirement-id=\"REQ-006\">build-and-push job if condition evaluates false for pull_request events</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"helm-push on v* tag pushes versioned chart\" requirement-id=\"REQ-007\">Chart packaged with SemVer from tag and pushed to oci://ghcr.io/<owner>/charts/</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"helm-push skipped on PR\" requirement-id=\"REQ-007\">helm-push job if condition evaluates false for pull_request events</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"github-release creates release on v* tag\" requirement-id=\"REQ-008\">gh release create called with --generate-notes and chart tarball on v* tag</spec>\n    <spec test-file=\".github/workflows/ci.yaml (CI validation)\" test-function=\"github-release skipped on main push\" requirement-id=\"REQ-008\">github-release job if condition evaluates false for non-tag pushes</spec>\n    <spec test-file=\".codecov.yml (Codecov validation)\" test-function=\"Common component threshold set to 80%\" requirement-id=\"REQ-012\">internal/common/** component has target: 80% in .codecov.yml</spec>\n    <spec test-file=\".codecov.yml (Codecov validation)\" test-function=\"Controller component threshold set to 70%\" requirement-id=\"REQ-012\">operators/*/internal/controller/** component has target: 70% in .codecov.yml</spec>\n    <spec test-file=\".codecov.yml (Codecov validation)\" test-function=\"Webhook component threshold set to 90%\" requirement-id=\"REQ-012\">operators/*/api/** component has target: 90% in .codecov.yml</spec>\n  </test-specifications>\n  <stories>\n    <story title=\"Developer gets per-module coverage feedback on every PR\">As a developer, I want unit tests to run as a matrix over [common, keystone, c5c3] with per-module coverage profiles uploaded to Codecov, so that I can see which module a coverage regression originates from and address it before merge</story>\n    <story title=\"Developer gets integration test results in CI\">As a developer, I want the test-integration Makefile target to run real envtest-based tests and the CI workflow to include an integration test job, so that controller logic is validated against a real API server before merge</story>\n    <story title=\"Developer sees codegen drift caught before merge\">As a developer, I want a verify-codegen CI gate that runs make manifests, make generate, and make verify-crd-sync and fails if any files changed, so that generated code drift is caught automatically instead of discovered in production</story>\n    <story title=\"Release engineer gets versioned operator images from git tags\">As a release engineer, I want pushing a v* git tag to trigger building and pushing the operator Docker image to GHCR with the SemVer version as the image tag, so that tagged releases produce immutable, versioned container images for production deployment</story>\n    <story title=\"Release engineer gets versioned Helm charts from git tags\">As a release engineer, I want pushing a v* git tag to trigger packaging and pushing the Helm chart to GHCR OCI registry, so that FluxCD HelmReleases can reference versioned chart OCI artifacts</story>\n    <story title=\"Release engineer gets GitHub Releases with artifacts on tagged commits\">As a release engineer, I want a v* tag to automatically create a GitHub Release with auto-generated notes and the Helm chart tarball attached, so that stakeholders can track releases and download artifacts from a single location</story>\n    <story title=\"Developer gets E2E validation of the Keystone operator in CI\">As a developer, I want a CI job that builds the operator image, loads it into a kind cluster, deploys via Helm, and runs Chainsaw E2E tests, so that operator functionality is validated end-to-end before merge</story>\n    <story title=\"Maintainer enforces component-level coverage thresholds\">As a maintainer, I want a .codecov.yml file with component-level thresholds: 80% for internal/common/, 70% for operator controllers, 90% for webhooks/API, so that coverage regressions are caught per-component rather than only at the project level</story>\n    <story title=\"Developer can build operator Docker images locally via Makefile\">As a developer, I want make docker-build OPERATOR=keystone to build the operator Docker image locally, so that I can test container builds without pushing to CI</story>\n    <story title=\"Developer can package Helm charts locally via Makefile\">As a developer, I want make helm-package OPERATOR=keystone to package the operator Helm chart, so that I can verify chart packaging locally before CI runs</story>\n  </stories>\n  <requirements>\n    <requirement id=\"REQ-001\" priority=\"SHALL\">ci.yaml SHALL trigger on push to tags matching 'v*' in addition to existing branch triggers</requirement>\n    <requirement id=\"REQ-002\" priority=\"SHALL\">The test job SHALL use a matrix strategy over [common, keystone, c5c3] with -coverprofile output per matrix entry</requirement>\n    <requirement id=\"REQ-003\" priority=\"SHALL\">The test-integration Makefile target SHALL run envtest-based integration tests with setup-envtest for kubebuilder assets</requirement>\n    <requirement id=\"REQ-004\" priority=\"SHALL\">codecov/codecov-action SHALL upload coverage with flag-based component separation in both test and test-integration jobs</requirement>\n    <requirement id=\"REQ-005\" priority=\"SHALL\">The e2e-keystone job SHALL build operator image, load into kind, deploy via Helm, and run Chainsaw keystone tests</requirement>\n    <requirement id=\"REQ-006\" priority=\"SHALL\">The build-and-push job SHALL build operator image and push to GHCR with SemVer or SHA tags, conditional on main push or v* tag</requirement>\n    <requirement id=\"REQ-007\" priority=\"SHALL\">The helm-push job SHALL package and push Helm chart to oci://ghcr.io/<owner>/charts/, conditional on main push or v* tag</requirement>\n    <requirement id=\"REQ-008\" priority=\"SHALL\">The github-release job SHALL create GitHub Release with auto-generated notes and Helm chart tarball on v* tags only</requirement>\n    <requirement id=\"REQ-009\" priority=\"SHALL\">The verify-codegen job SHALL run make manifests, make generate, git diff --exit-code, and make verify-crd-sync as a gate job</requirement>\n    <requirement id=\"REQ-010\" priority=\"SHALL\">The docker-build Makefile target SHALL build Docker image from operators/$(OPERATOR)/Dockerfile with configurable OPERATOR and IMG variables</requirement>\n    <requirement id=\"REQ-011\" priority=\"SHALL\">The helm-package Makefile target SHALL run helm package on operators/$(OPERATOR)/helm/$(OPERATOR)-operator/ with configurable OPERATOR variable</requirement>\n    <requirement id=\"REQ-012\" priority=\"SHALL\">The .codecov.yml SHALL define component thresholds: 80% internal/common/, 70% operator controllers, 90% operator API/webhooks</requirement>\n  </requirements>\n</existing-specifications>\n\n<validation-rules>\n  <rule>Check if task descriptions are clear and actionable</rule>\n  <rule>Check if tasks are ordered logically (dependencies first)</rule>\n  <rule>Remove any tasks that duplicate tool-execution (mypy, ruff, eslint, pytest - these run automatically)</rule>\n  <rule>Remove any \"Run test suite\", \"Run full tests\", \"Execute tests\" tasks - testing happens automatically during review phase</rule>\n  <rule>Check if previous feature already implemented any of our pending tasks</rule>\n  <rule>Run: git log --oneline -5 to see what the previous feature committed</rule>\n  <rule>Mark tasks as removed_tasks if they were done by the previous feature</rule>\n  <rule>Do NOT add new tasks - only validate existing ones</rule>\n</validation-rules>\n\n<enrichment-rules hint=\"append-only — existing items must NEVER be removed\">\n  <rule>Review existing test_specifications, stories, and requirements (listed above)</rule>\n  <rule>If the current codebase reveals additional test cases, stories, or requirements that are clearly needed for this feature, add them via added_test_specifications, added_stories, or added_requirements</rule>\n  <rule>NEVER remove or modify existing test_specifications, stories, or requirements — only append new ones</rule>\n  <rule>Only add items that are clearly justified by the codebase context — no speculative or random additions</rule>\n  <rule>New test_specifications must not duplicate existing ones (same test_file + test_function)</rule>\n  <rule>New stories must not duplicate existing ones (same title)</rule>\n  <rule>New requirements must not duplicate existing ones (same id)</rule>\n  <rule>Prefer NOT adding anything over adding something of unclear value</rule>\n</enrichment-rules>\n<response-rules>\n  <rule>Set needs_update=false unless tasks have obvious issues or new specs/stories/requirements are added</rule>\n  <rule>Only modify tasks if they are unclear, duplicated, or out of order</rule>\n  <rule>Be conservative - prefer no changes over unnecessary changes</rule>\n</response-rules>\n\nOutput JSON matching the schema.\n",
  "prompt_length": 26794,
  "caller": "run_claude_cli"
}